import base64
import zlib
import json
import re
import os
import time as _time
import threading
import sqlite3

from java import cast, dynamic_proxy, jint, jclass, jfloat, jarray, jlong, jbyte
from java.lang import Boolean, Integer, Long, Math, Runnable, Float, Object, String, CharSequence
from java.util import ArrayList
from android.text import SpannableStringBuilder, Spanned
from android.text.style import ForegroundColorSpan, StyleSpan
from android.graphics import Typeface
from android.view import View, ViewGroup
from android.util import SparseArray, Base64
from android.widget import ScrollView, LinearLayout, TextView, EditText
from android.text import InputType
from androidx.core.content import ContextCompat

from org.telegram.messenger import (ApplicationLoader, MessageObject,
                                     MessagesController, MessagesStorage,
                                     UserConfig, Utilities, NotificationCenter,
                                     LocaleController, ChatObject, AndroidUtilities,
                                     ContactsController, SendMessagesHelper,
                                     AccountInstance, FileLoader, ImageLocation,
                                     VideoEditedInfo)
from org.telegram.tgnet import TLRPC, TLObject, ConnectionsManager, NativeByteBuffer
from org.telegram.tgnet.tl import TL_account
from org.telegram.ui.ActionBar import Theme, AlertDialog
from org.telegram.ui.Cells import ChatMessageCell
from org.telegram.ui import ChatActivity, LaunchActivity
from org.telegram.ui.Components import BulletinFactory, ColoredImageSpan

from android_utils import log, run_on_ui_thread
from base_plugin import (BasePlugin, HookResult, HookStrategy, MenuItemData,
                         MenuItemType, MethodHook, MethodReplacement, XposedHook)
from client_utils import (get_messages_controller, get_messages_storage,
                          run_on_queue, send_request, get_last_fragment, RequestCallback,
                          send_document)
from file_utils import write_file, read_file, ensure_dir_exists
from hook_utils import find_class, get_private_field, set_private_field
from ui.bulletin import BulletinHelper
from ui.settings import Header, Switch, Selector, Text, Divider, Input, Custom, SimpleSettingFactory
from ui.alert import AlertDialogBuilder
from org.telegram.messenger import MediaDataController
from org.telegram.ui.Components import BackupImageView, LayoutHelper
from android.view import Gravity
from android.widget import LinearLayout as JLinearLayout, FrameLayout as JFrameLayout

__id__ = "max_reextera"
__name__ = "MAX re:extera"
__description__ = "Ghost mode, spy features, message filters and more! Fork of famous plugin re:extera"
__author__ = "@itNotMax - @MaxExteraPlugins"
__version__ = "1.3.54"
__icon__ = "MaxExteraPluginsStickers/22"
__min_version__ = "12.8.1"
__app_version__ = ">=12.8.1"
__sdk_version__ = ">=1.4.3.3"

ENTRY_CLASS = "com.maxplugins.sample.Main"
DEX_BEGIN = "__DEX_BEGIN__"
DEX_END = "__DEX_END__"
PREFS_NAME = "max_reextera_prefs"
PRESENCE_KEY = "presence_data"
PREMIUM_CACHE_KEY = "premium_cache"
FLAG_DELETED = 1 << 16
UPDATE_JSON_URL = "https://raw.githubusercontent.com/yaksflash/MAX_ReExtera/main/update.json"

READ_REQS = frozenset(["TL_messages_readHistory", "TL_messages_readEncryptedHistory",
             "TL_messages_readDiscussion", "TL_messages_readMessageContents",
             "TL_channels_readHistory", "TL_channels_readMessageContents",
             "TL_messages_markDialogUnread"])
TYPING_REQS = frozenset(["TL_messages_setTyping", "TL_messages_setEncryptedTyping"])
STORIES_REQS = frozenset(["TL_stories_readStories", "TL_stories_incrementStoryViews"])
SEND_REQS = frozenset(["TL_messages_sendMessage", "TL_messages_sendMedia",
             "TL_messages_sendMultiMedia", "TL_messages_forwardMessages"])
ONLINE_REQS = SEND_REQS | READ_REQS | frozenset(["TL_messages_editMessage",
              "TL_stories_sendStory", "TL_stories_sendReaction"])
BLOCKED_AD_REQS = frozenset(["TL_help_getPromoData"])
DELETE_REQS = frozenset(["TL_messages_deleteMessages", "TL_channels_deleteMessages"])
PRESENCE_UPD = frozenset(["TL_updateUserStatus", "TL_updateUserTyping", "TL_updateChatUserTyping",
                "TL_updateChannelUserTyping", "TL_updateReadHistoryOutbox",
                "TL_updateNewMessage", "TL_updateNewChannelMessage"])

ONE_YEAR_SECONDS = 365 * 24 * 60 * 60

_OPT_REVISIONS = 7171
_OPT_MARK_READ = 7272
_OPT_VIEW_DELETED = 7373


class L:
    _ru = {
        "ghost": "Призрак", "spy": "Шпион", "filters": "Фильтры", "hacker": "Хакер",
        "settings_misc": "Настройки и прочее",
        "ghost_header": "Режим призрака", "ghost_toggle": "Режим призрака",
        "ghost_toggle_sub": "Скрыть онлайн-статус, набор текста и прочтение",
        "ghost_settings": "Настройки призрака",
        "no_online": "Не отправлять онлайн", "instant_offline": "Немедленный оффлайн",
        "no_typing": "Не отправлять печатает",
        "no_read": "Не читать сообщения", "no_read_sub": "Собеседник не увидит галочки прочтения",
        "no_read_stories": "Не читать истории",
        "read_on_action": "Читать при действиях", "read_on_action_sub": "Прочтение отправится при ответе или реакции",
        "use_scheduled": "Использовать отложку",
        "silent_send": "Отправлять без звука", "silent_never": "Никогда", "silent_always": "Всегда", "silent_ghost": "Только с призраком",
        "outside_ghost": "Вне призрака",
        "read_whitelist": "Белый список прочтения", "read_whitelist_sub": "Эти люди будут видеть ваше прочтение даже в призраке",
        "read_wl_users": "Пользователи", "read_wl_empty": "Список пуст",
        "story_prompt": "Спрашивать для историй", "story_prompt_sub": "Предложит включить призрака перед просмотром",
        "drawer_btn": "Кнопка Призрак в шторке",
        "ghost_on": "Призрак вкл", "ghost_off": "Призрак выкл",
        "story_dlg_title": "Режим призрака", "story_dlg_msg": "Включить режим призрака для просмотра историй?",
        "yes": "Да", "no": "Нет",
        "max_vigilance": "Максимальная бдительность", "keep_alive": "Оставлять клиент активным", "keep_alive_sub": "Работа в фоне даже после закрытия (тратит батарею)",
        "spy_mode": "Режим шпиона",
        "save_deleted": "Сохранять удалённые", "save_deleted_sub": "Удалённые сообщения останутся в чате",
        "save_edits": "Сохранять историю правок", "save_edits_sub": "Все версии отредактированных сообщений",
        "save_bots": "Сохранять в чатах с ботами",
        "save_online": "Сохранять последний онлайн", "save_online_sub": "Запоминать время онлайна всех пользователей",
        "save_own_del": "Не сохранять свои удаления",
        "appearance": "Оформление", "show_mark": "Показывать метку",
        "use_trash_icon": "Использовать значок корзины", "use_trash_icon_sub": "Иконка вместо текстовой метки",
        "del_text": "Текст удалённого", "del_color": "Цвет подписи HEX (без #)",
        "transparent_del": "Прозрачные удалёнки", "transparent_del_sub": "Удалённые сообщения становятся полупрозрачными",
        "opacity_level": "Уровень прозрачности",
        "storage": "Хранилище", "save_attachments": "Сохранять вложения",
        "max_folder": "Макс. размер папки вложений", "auto_cleanup": "Автоочистка медиа", "auto_cleanup_sub": "Автоматически удалять старые файлы",
        "attach_folder": "Папка вложений", "attach_folder_sub": "Выбрать папку для сохранения",
        "attach_folder_default": "По умолчанию (Downloads)", "attach_folder_reset": "Сбросить",
        "folder_set": "Папка установлена", "folder_reset": "Папка сброшена",
        "wipe_storage": "Очистить хранилище",
        "wipe_confirm": "Удалить все сохранённые сообщения и историю правок?",
        "delete": "Удалить", "cancel": "Отмена", "cleared": "Очищено",
        "tracker": "Трекер", "only_hidden": "Только скрытый",
        "decay": "Затухание", "tracked_users": "Отслеживаемые",
        "journal": "Журнал", "wipe_tracker": "Очистить трекер",
        "hide_ads": "Скрыть рекламу", "hide_feed_ads": "Скрыть рекламу в ленте", "hide_feed_ads_sub": "Убирает рекламные сообщения из ленты",
        "no_fwd_limit": "Снять лимит пересылок", "no_fwd_limit_sub": "Пересылать более 100 сообщений за раз",
        "local_premium": "Локальный Premium", "local_premium_sub": "Премиум-функции видны только вам",
        "once_media": "Одноразовые медиа", "once_media_sub": "Одноразовые фото/видео не исчезают",
        "bypass": "Обход", "screenshots": "Скриншоты", "screenshots_sub": "Разрешить скриншоты в защищённых чатах",
        "noforwards": "Noforwards", "noforwards_sub": "Копировать и пересылать защищённый контент",
        "fwd_mode": "Режим пересылки", "fwd_copy": "Копия (от своего лица)", "fwd_try": "Пересылка (может быть ошибка)",
        "hidden_gifts": "Скрытые подарки", "hidden_gifts_sub": "Вернуть удалённые подарки в каталог",
        "stories_bypass": "Сторис", "stories_bypass_sub": "Сохранять сторис с запретом на скачивание",
        "info": "Информация", "author_channel": "Канал автора", "credits": "Благодарности",
        "credits_empty": "Пока пусто", "params": "Параметры",
        "language": "Язык плагина", "lang_auto": "Авто", "lang_ru": "Русский", "lang_en": "English",
        "revisions": "История правок", "no_revisions": "Нет истории правок",
        "original": "Оригинал", "version": "Версия", "view": "Просмотр",
        "close": "Закрыть", "back": "Назад", "saved": "Сохранено",
        "view_deleted": "Посмотреть удалёнки", "no_deleted": "Нет удалённых сообщений",
        "deleted_list": "Удалённые сообщения", "msg_deleted": "удалено",
        "add": "Добавить", "remove": "Удалить?", "removed": "Удалён",
        "added": "Добавлен", "not_found": "Не найден", "error": "Ошибка",
        "invalid_id": "Неверный ID", "list_empty": "Список пуст",
        "online_journal": "Журнал онлайна", "no_records": "Нет записей",
        "patterns": "Шаблоны", "add_pattern": "Добавить шаблон",
        "invalid_regex": "Невалидный regex",
        "enable_filters": "Включить фильтры", "enable_filters_sub": "Скрывать сообщения по шаблонам",
        "regex_patterns": "Regex-шаблоны (SQLite)", "add_filter": "Добавить фильтр",
        "new_filter": "Новый фильтр", "editor": "Редактор",
        "regex_expr": "Regex выражение", "enabled": "Включён",
        "case_insensitive": "Без учёта регистра", "inverted": "Обратный", "inverted_sub": "НЕ содержащие",
        "save": "Сохранить", "enter_expr": "Введите выражение!",
        "mark_read": "Прочитать", "edit_history": "История изменений",
        "forward": "Переслать", "save_media": "Сохранить",
        "media_changed": "[медиа изменено]", "no_text": "[без текста]",
        "go_to_msg": "Перейти к сообщению", "download_file": "Скачать файл",
        "has_photo": "[фото]", "has_video": "[видео]", "has_doc": "[файл]",
        "has_sticker": "[стикер]", "has_voice": "[голосовое]", "has_round": "[кружочек]",
        "saved_to": "Сохранено",
        "browse_attachments": "Просмотр вложений", "browse_attachments_sub": "Сохранённые файлы по чатам",
        "no_attachments": "Нет сохранённых вложений", "files_count": "файлов",
        "updates": "Обновления", "check_updates": "Проверять обновления при старте",
        "check_updates_sub": "Автопроверка новых версий плагина",
        "update_available": "Доступно обновление", "whats_new": "Что нового",
        "download_verified": "Скачать с проверенного канала",
        "view_author_channel": "Посмотреть в канале автора",
        "remind_later": "Напомнить позже",
        "author_warning": "Вы хотите перейти на канал автора этого плагина. Хоть я и ручаюсь за свои плагины, но все же у меня нет метки проверенного источника.",
        "dont_show_again": "Больше не показывать",
        "author_warn_toggle": "Предупреждение о канале автора",
        "author_warn_toggle_sub": "Показывать предупреждение при переходе",
        "no_updates": "Обновлений нет",
        "backup": "Резервная копия", "export_settings": "Экспорт настроек",
        "export_to_saved": "Экспорт в Избранное", "export_to_saved_sub": "Отправить .mre_backup файл в Избранное",
        "export_done": "Настройки экспортированы в Избранное",
        "export_error": "Ошибка экспорта",
        "import_title": "Импорт настроек", "import_confirm": "Восстановить все настройки MAX re:extera из этого бэкапа?",
        "import_done": "Настройки импортированы! Перезагрузите плагин",
        "import_error": "Ошибка импорта",
        "username_or_id": "@username или id",
        "cr_extera": "Без экстеры ничего бы не было",
        "cr_bleizix": "Фундамент re:extera",
        "cr_shiawasez": "Обновление re:extera (v2)",
        "cr_thisisgame": "Мотивация и поддержка",
        "cr_summering": "Тестирование и развитие",
        "cr_testing": "Тестирование и развитие",
        "cr_kvuco": "AdBlock, Mirror, RFS",
        "cr_binbash": "Deleted Gift Sender",
        "cr_ai_title": "ИИ", "cr_ai": "Реализация :)))",


    }
    _en = {
        "ghost": "Ghost", "spy": "Spy", "filters": "Filters", "hacker": "Hacker",
        "settings_misc": "Settings & more",
        "ghost_header": "Ghost mode", "ghost_toggle": "Ghost mode",
        "ghost_toggle_sub": "Hide online status, typing and read receipts",
        "ghost_settings": "Ghost settings",
        "no_online": "Don't send online", "instant_offline": "Instant offline",
        "no_typing": "Don't send typing",
        "no_read": "Don't read messages", "no_read_sub": "Others won't see read checkmarks",
        "no_read_stories": "Don't read stories",
        "read_on_action": "Read on action", "read_on_action_sub": "Send read receipt on reply or reaction",
        "use_scheduled": "Use scheduled", "silent_send": "Silent send",
        "silent_never": "Never", "silent_always": "Always", "silent_ghost": "Ghost only",
        "outside_ghost": "Outside ghost",
        "read_whitelist": "Read whitelist", "read_whitelist_sub": "These people will see your read receipts even in ghost",
        "read_wl_users": "Users", "read_wl_empty": "List is empty",
        "story_prompt": "Ask for stories", "story_prompt_sub": "Offer to enable ghost before viewing",
        "drawer_btn": "Ghost button in drawer",
        "ghost_on": "Ghost ON", "ghost_off": "Ghost OFF",
        "story_dlg_title": "Ghost mode", "story_dlg_msg": "Enable ghost mode for viewing stories?",
        "yes": "Yes", "no": "No",
        "max_vigilance": "Maximum vigilance", "keep_alive": "Keep client active", "keep_alive_sub": "Stay active in background (uses more battery)",
        "spy_mode": "Spy mode",
        "save_deleted": "Save deleted", "save_deleted_sub": "Deleted messages stay visible in chat",
        "save_edits": "Save edit history", "save_edits_sub": "Keep all versions of edited messages",
        "save_bots": "Save in bot chats",
        "save_online": "Save last online", "save_online_sub": "Remember online time for all users",
        "save_own_del": "Don't save own deletions",
        "appearance": "Appearance", "show_mark": "Show mark",
        "use_trash_icon": "Use trash icon", "use_trash_icon_sub": "Icon instead of text label",
        "del_text": "Deleted text", "del_color": "Label color HEX (no #)",
        "transparent_del": "Transparent deleted", "transparent_del_sub": "Deleted messages become semi-transparent",
        "opacity_level": "Opacity level",
        "storage": "Storage", "save_attachments": "Save attachments",
        "max_folder": "Max attachment folder size", "auto_cleanup": "Auto-cleanup media", "auto_cleanup_sub": "Automatically delete old files",
        "attach_folder": "Attachments folder", "attach_folder_sub": "Choose folder for saving",
        "attach_folder_default": "Default (Downloads)", "attach_folder_reset": "Reset",
        "folder_set": "Folder set", "folder_reset": "Folder reset",
        "wipe_storage": "Wipe storage",
        "wipe_confirm": "Delete all saved messages and edit history?",
        "delete": "Delete", "cancel": "Cancel", "cleared": "Cleared",
        "tracker": "Tracker", "only_hidden": "Hidden only",
        "decay": "Decay", "tracked_users": "Tracked users",
        "journal": "Journal", "wipe_tracker": "Wipe tracker",
        "hide_ads": "Hide ads", "hide_feed_ads": "Hide ads in feed", "hide_feed_ads_sub": "Removes ad messages from the feed",
        "no_fwd_limit": "Remove forward limit", "no_fwd_limit_sub": "Forward more than 100 messages at once",
        "local_premium": "Local Premium", "local_premium_sub": "Premium features visible only to you",
        "once_media": "Once media", "once_media_sub": "View-once photos/videos won't disappear",
        "bypass": "Bypass", "screenshots": "Screenshots", "screenshots_sub": "Allow screenshots in protected chats",
        "noforwards": "Noforwards", "noforwards_sub": "Copy and forward protected content",
        "fwd_mode": "Forward mode", "fwd_copy": "Copy (as your own)", "fwd_try": "Forward (may error)",
        "hidden_gifts": "Hidden gifts", "hidden_gifts_sub": "Bring removed gifts back to the catalog",
        "stories_bypass": "Stories", "stories_bypass_sub": "Save stories with download restrictions",
        "info": "Information", "author_channel": "Author's channel", "credits": "Credits",
        "credits_empty": "Nothing here yet", "params": "Parameters",
        "language": "Plugin language", "lang_auto": "Auto", "lang_ru": "Русский", "lang_en": "English",
        "revisions": "Edit history", "no_revisions": "No edit history",
        "original": "Original", "version": "Version", "view": "Preview",
        "close": "Close", "back": "Back", "saved": "Saved",
        "view_deleted": "View deleted", "no_deleted": "No deleted messages",
        "deleted_list": "Deleted messages", "msg_deleted": "deleted",
        "add": "Add", "remove": "Remove?", "removed": "Removed",
        "added": "Added", "not_found": "Not found", "error": "Error",
        "invalid_id": "Invalid ID", "list_empty": "List is empty",
        "online_journal": "Online journal", "no_records": "No records",
        "patterns": "Patterns", "add_pattern": "Add pattern",
        "invalid_regex": "Invalid regex",
        "enable_filters": "Enable filters", "enable_filters_sub": "Hide messages by patterns",
        "regex_patterns": "Regex patterns (SQLite)", "add_filter": "Add filter",
        "new_filter": "New filter", "editor": "Editor",
        "regex_expr": "Regex expression", "enabled": "Enabled",
        "case_insensitive": "Case insensitive", "inverted": "Inverted", "inverted_sub": "NOT matching",
        "save": "Save", "enter_expr": "Enter expression!",
        "mark_read": "Mark read", "edit_history": "Edit history",
        "forward": "Forward", "save_media": "Save",
        "media_changed": "[media changed]", "no_text": "[no text]",
        "go_to_msg": "Go to message", "download_file": "Download file",
        "has_photo": "[photo]", "has_video": "[video]", "has_doc": "[file]",
        "has_sticker": "[sticker]", "has_voice": "[voice]", "has_round": "[round]",
        "saved_to": "Saved",
        "browse_attachments": "Browse attachments", "browse_attachments_sub": "Saved files by chat",
        "no_attachments": "No saved attachments", "files_count": "files",
        "updates": "Updates", "check_updates": "Check updates on start",
        "check_updates_sub": "Auto-check for new plugin versions",
        "update_available": "Update available", "whats_new": "What's new",
        "download_verified": "Download from verified channel",
        "view_author_channel": "View in author's channel",
        "remind_later": "Remind later",
        "author_warning": "You are about to visit the author's channel. Although I vouch for my plugins, I don't have a verified source badge.",
        "dont_show_again": "Don't show again",
        "author_warn_toggle": "Author channel warning",
        "author_warn_toggle_sub": "Show warning when navigating",
        "no_updates": "No updates available",
        "backup": "Backup", "export_settings": "Export settings",
        "export_to_saved": "Export to Saved Messages", "export_to_saved_sub": "Send .mre_backup file to Saved Messages",
        "export_done": "Settings exported to Saved Messages",
        "export_error": "Export error",
        "import_title": "Import settings", "import_confirm": "Restore all MAX re:extera settings from this backup?",
        "import_done": "Settings imported! Reload plugin",
        "import_error": "Import error",
        "username_or_id": "@username or ID",
        "cr_extera": "Nothing would exist without extera",
        "cr_bleizix": "Foundation of re:extera",
        "cr_shiawasez": "re:extera update (v2)",
        "cr_thisisgame": "Motivation and support",
        "cr_summering": "Testing and development",
        "cr_testing": "Testing and development",
        "cr_kvuco": "AdBlock, Mirror, RFS",
        "cr_binbash": "Deleted Gift Sender",
        "cr_ai_title": "AI", "cr_ai": "Implementation :)))",


    }
    _prefs_key = "max_lang"
    _lang_cache = [None, 0]
    @classmethod
    def _lang(cls):
        now = _time.time()
        if cls._lang_cache[0] is not None and now - cls._lang_cache[1] < 10.0:
            return cls._lang_cache[0]
        try:
            v = ApplicationLoader.applicationContext.getSharedPreferences(PREFS_NAME, 0).getString(cls._prefs_key, "auto")
        except Exception:
            v = "auto"
        if v != "ru" and v != "en":
            v = cls._detect_tg_lang()
        cls._lang_cache = [v, now]
        return v
    @classmethod
    def _detect_tg_lang(cls):
        try:
            lang = LocaleController.getInstance().getCurrentLocale().getLanguage()
            return "ru" if lang == "ru" else "en"
        except Exception:
            return "en"
    @classmethod
    def g(cls, key):
        return (cls._ru if cls._lang() == "ru" else cls._en).get(key, key)

REPLY_QUOTE_FROM = None
try:
    REPLY_QUOTE_FROM = getattr(ChatActivity.ReplyQuote, "from")
except Exception:
    pass


def _sl(val):
    try:
        return str(val).strip() or "0"
    except Exception:
        return "0"

def _int(v, d=0):
    try:
        return int(v)
    except Exception:
        return d

def _fm(cls, name, pc=None):
    try:
        for m in cls.getDeclaredMethods():
            if str(m.getName()) == name:
                if pc is None or len(m.getParameterTypes()) == pc:
                    m.setAccessible(True)
                    return m
    except Exception:
        pass
    return None

def _ff(cls, name):
    try:
        f = cls.getDeclaredField(name)
        f.setAccessible(True)
        return f
    except Exception:
        pass
    return None

def _al_to_list(arr):
    return [] if arr is None else [arr.get(i) for i in range(arr.size())]

def _to_al(it):
    r = ArrayList()
    for x in it:
        r.add(x)
    return r

def _jlist(jl):
    out = []
    if jl is None:
        return out
    try:
        it = jl.iterator()
        while it.hasNext():
            out.append(it.next())
    except Exception:
        try:
            for i in range(jl.size()):
                out.append(jl.get(i))
        except Exception:
            pass
    return out

def _peer_did(msg):
    try:
        p = msg.peer_id
        if isinstance(p, TLRPC.TL_peerUser):
            return int(p.user_id)
        if isinstance(p, TLRPC.TL_peerChat):
            return -int(p.chat_id)
        if isinstance(p, TLRPC.TL_peerChannel):
            return -int(p.channel_id)
    except Exception:
        pass
    return 0

def _encode_msg(msg):
    buf = None
    try:
        buf = NativeByteBuffer(msg.getObjectSize())
        msg.serializeToStream(buf)
        data = buf.toByteArray()
        buf.reuse()
        buf = None
        return Base64.encodeToString(data, Base64.NO_WRAP)
    except Exception:
        if buf:
            try:
                buf.reuse()
            except Exception:
                pass
        return None

def _decode_msg(raw):
    buf = None
    try:
        data = Base64.decode(raw, Base64.NO_WRAP)
        buf = NativeByteBuffer(len(data))
        buf.writeBytes(data)
        buf.position(0)
        msg = TLRPC.Message.TLdeserialize(buf, buf.readInt32(False), False)
        buf.reuse()
        buf = None
        return msg
    except Exception:
        if buf:
            try:
                buf.reuse()
            except Exception:
                pass
        return None

def _format_date(ts):
    try:
        import datetime
        dt = datetime.datetime.fromtimestamp(int(ts))
        return dt.strftime("%d.%m.%y %H:%M")
    except Exception:
        try:
            return str(LocaleController.formatDateOnline(int(ts), None))
        except Exception:
            return ""

def _get_media_hash(msg):
    try:
        if not msg:
            return None
        media = getattr(msg, "media", None)
        if not media:
            return None
        if hasattr(media, "photo") and media.photo:
            return f"photo_{media.photo.id}"
        if hasattr(media, "document") and media.document:
            return f"doc_{media.document.id}"
        if hasattr(media, "webpage") and media.webpage:
            return f"web_{media.webpage.id}"
        return f"media_{type(media).__name__}"
    except Exception:
        return None

_CA_MSGS_FIELD = None
_CA_MSGS_FIELD_READY = False


def _get_message_from_chat(did, mid):
    global _CA_MSGS_FIELD, _CA_MSGS_FIELD_READY
    try:
        frag = LaunchActivity.getLastFragment()
        if not isinstance(frag, ChatActivity):
            return None
        if not _CA_MSGS_FIELD_READY:
            _CA_MSGS_FIELD = _ff(jclass("org.telegram.ui.ChatActivity").getClass(), "messages")
            _CA_MSGS_FIELD_READY = True
        if not _CA_MSGS_FIELD:
            return None
        msgs = _CA_MSGS_FIELD.get(frag)
        if not msgs:
            return None
        target = int(mid)
        for i in range(msgs.size()):
            mo = msgs.get(i)
            if mo and int(mo.getId()) == target:
                return mo
    except Exception:
        pass
    return None


def _describe_media(msg):
    try:
        if not msg:
            return ""
        media = getattr(msg, "media", None)
        if not media:
            return ""
        mn = str(media.getClass().getSimpleName())
        if "Photo" in mn:
            return L.g("has_photo")
        if "Document" in mn:
            doc = getattr(media, "document", None)
            if doc:
                try:
                    if MessageObject.isVideoDocument(doc):
                        return L.g("has_video")
                    if MessageObject.isStickerDocument(doc) or MessageObject.isAnimatedStickerDocument(doc):
                        return L.g("has_sticker")
                    if MessageObject.isVoiceDocument(doc):
                        return L.g("has_voice")
                    if MessageObject.isRoundVideoDocument(doc):
                        return L.g("has_round")
                except Exception:
                    pass
            return L.g("has_doc")
        if "Geo" in mn or "Venue" in mn:
            return "[geo]"
        if "Contact" in mn:
            return "[contact]"
        if "Poll" in mn:
            return "[poll]"
    except Exception:
        pass
    return ""

def _save_media_to_downloads(msg, context):
    try:
        if not msg:
            return False
        media = getattr(msg, "media", None)
        if not media:
            return False
        doc = getattr(media, "document", None)
        photo = getattr(media, "photo", None)
        acc = int(UserConfig.selectedAccount)
        fl = FileLoader.getInstance(acc)
        if doc:
            f = fl.getPathToAttach(doc, False)
            if not f or not f.exists():
                f = fl.getPathToAttach(doc, True)
            if f and f.exists():
                try:
                    MediaController = jclass("org.telegram.messenger.MediaController")
                    MediaController.saveFile(str(f.getAbsolutePath()), context, 2, None, None, None)
                    return True
                except Exception:
                    pass
        elif photo:
            try:
                sizes = photo.sizes
                ps = FileLoader.getClosestPhotoSizeWithSize(sizes, AndroidUtilities.getPhotoSize())
                if ps:
                    f = fl.getPathToAttach(ps, False)
                    if f and f.exists():
                        MediaController = jclass("org.telegram.messenger.MediaController")
                        MediaController.saveFile(str(f.getAbsolutePath()), context, 0, None, None, None)
                        return True
            except Exception:
                pass
    except Exception:
        pass
    return False


def _ensure_nomedia(dirpath):
    if not dirpath:
        return
    try:
        nm = os.path.join(dirpath, ".nomedia")
        if os.path.exists(nm):
            return
    except Exception:
        pass
    try:
        JFile = jclass("java.io.File")
        f = JFile(os.path.join(dirpath, ".nomedia"))
        f.createNewFile()
        return
    except Exception:
        pass
    try:
        nm = os.path.join(dirpath, ".nomedia")
        with open(nm, "wb") as f:
            pass
    except Exception:
        pass


def _get_custom_attach_dir():
    try:
        prefs = ApplicationLoader.applicationContext.getSharedPreferences(PREFS_NAME, 0)
        custom = str(prefs.getString("custom_attach_dir", "") or "")
        if custom and os.path.isdir(custom):
            _ensure_nomedia(custom)
            return custom
    except Exception:
        pass
    return None


def _get_attach_dir():
    custom = _get_custom_attach_dir()
    if custom:
        return custom
    try:
        from android.os import Environment
        dl = Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_DOWNLOADS)
        base = os.path.join(str(dl.getAbsolutePath()), "MAX re extera")
        os.makedirs(base, exist_ok=True)
        _ensure_nomedia(base)
        return base
    except Exception:
        pass
    try:
        base = os.path.join(ApplicationLoader.applicationContext.getFilesDir().getAbsolutePath(), "max_reextera", "attachments")
        os.makedirs(base, exist_ok=True)
        _ensure_nomedia(base)
        return base
    except Exception:
        return None


_FOLDER_SIZES = (300 * 1024 * 1024, 1024 * 1024 * 1024, 2 * 1024 * 1024 * 1024,
                  4 * 1024 * 1024 * 1024, 8 * 1024 * 1024 * 1024, 16 * 1024 * 1024 * 1024, 0)
_CLEANUP_SECONDS = (0, 3 * 86400, 7 * 86400, 14 * 86400, 30 * 86400, 90 * 86400)


def _enforce_folder_limit():
    try:
        attach_dir = _get_attach_dir()
        if not attach_dir or not os.path.exists(attach_dir):
            return
        try:
            prefs = ApplicationLoader.applicationContext.getSharedPreferences(PREFS_NAME, 0)
            idx = _int(prefs.getString("max_folder_size", "2"), 2)
        except Exception:
            idx = 2
        max_bytes = _FOLDER_SIZES[min(max(idx, 0), len(_FOLDER_SIZES) - 1)]
        if max_bytes <= 0:
            return
        files = []
        total_size = 0
        for fname in os.listdir(attach_dir):
            fpath = os.path.join(attach_dir, fname)
            if os.path.isfile(fpath):
                try:
                    st = os.stat(fpath)
                    files.append((st.st_mtime, st.st_size, fpath))
                    total_size += st.st_size
                except Exception:
                    pass
        if total_size <= max_bytes:
            return
        files.sort(key=lambda x: x[0])
        for mtime, fsize, fpath in files:
            if total_size <= max_bytes:
                break
            try:
                os.remove(fpath)
                total_size -= fsize
            except Exception:
                pass
    except Exception:
        pass


def _enforce_time_limit():
    try:
        attach_dir = _get_attach_dir()
        if not attach_dir or not os.path.exists(attach_dir):
            return
        try:
            prefs = ApplicationLoader.applicationContext.getSharedPreferences(PREFS_NAME, 0)
            idx = _int(prefs.getString("auto_cleanup_age", "0"), 0)
        except Exception:
            idx = 0
        max_age = _CLEANUP_SECONDS[min(max(idx, 0), len(_CLEANUP_SECONDS) - 1)]
        if max_age <= 0:
            return
        now = _time.time()
        for fname in os.listdir(attach_dir):
            fpath = os.path.join(attach_dir, fname)
            if os.path.isfile(fpath):
                try:
                    if now - os.path.getmtime(fpath) > max_age:
                        os.remove(fpath)
                except Exception:
                    pass
    except Exception:
        pass


def _get_file_from_msg(msg):
    if not msg:
        return None
    try:
        acc = int(UserConfig.selectedAccount)
        fl = FileLoader.getInstance(acc)
        media = getattr(msg, "media", None)
        if not media:
            return None
        doc = getattr(media, "document", None)
        photo = getattr(media, "photo", None)
        if doc:
            for b in [False, True]:
                f = fl.getPathToAttach(doc, b)
                if f and f.exists():
                    return f
        elif photo:
            try:
                if hasattr(photo, "sizes") and photo.sizes:
                    for ps in _jlist(photo.sizes):
                        for b in [False, True]:
                            f = fl.getPathToAttach(ps, b)
                            if f and f.exists():
                                return f
            except Exception:
                pass
    except Exception:
        pass
    return None


def _copy_path_to_vault(src_path, did, mid, media_hash=None):
    try:
        if not src_path or not os.path.exists(src_path):
            return None
        attach_dir = _get_attach_dir()
        if not attach_dir:
            return None
        ext = ""
        dot = src_path.rfind(".")
        if dot >= 0:
            ext = src_path[dot:]
        suffix = f"_{media_hash}" if media_hash else ""
        dst_path = os.path.join(attach_dir, f"{did}_{mid}{suffix}{ext}")
        if os.path.exists(dst_path):
            return dst_path
        import shutil
        shutil.copy2(src_path, dst_path)
        _enforce_folder_limit()
        return dst_path
    except Exception:
        return None


def _copy_attach_to_vault(msg, did, mid, media_hash=None):
    try:
        src = _get_file_from_msg(msg)
        if not src or not src.exists():
            return None
        return _copy_path_to_vault(str(src.getAbsolutePath()), did, mid, media_hash)
    except Exception:
        return None


AUTO_DOWNLOAD_MAX_SIZE = 10 * 1024 * 1024


def _ensure_attach_saved(msg, did, mid, media_hash=None, force_download=False):
    try:
        p = _copy_attach_to_vault(msg, did, mid, media_hash)
        if p:
            return p
        media = getattr(msg, "media", None) if msg else None
        if not media:
            return None
        doc = getattr(media, "document", None)
        photo = getattr(media, "photo", None)
        doc_size = int(getattr(doc, "size", 0)) if doc else 0
        if not force_download and doc and doc_size > AUTO_DOWNLOAD_MAX_SIZE:
            return None
        acc = int(UserConfig.selectedAccount)
        fl = FileLoader.getInstance(acc)
        mo_tmp = MessageObject(acc, msg, False, False)
        if doc:
            try:
                fl.loadFile(doc, mo_tmp, 1, 0)
            except Exception:
                pass
        elif photo:
            try:
                sizes = photo.sizes
                ps = FileLoader.getClosestPhotoSizeWithSize(sizes, AndroidUtilities.getPhotoSize())
                if ps:
                    fl.loadFile(ImageLocation.getForPhoto(ps, photo), mo_tmp, None, 1, 0)
            except Exception:
                pass
        else:
            return None
        def _retry():
            for delay in (2.0, 5.0, 10.0, 20.0):
                _time.sleep(delay)
                try:
                    saved = _copy_attach_to_vault(msg, did, mid, media_hash)
                    if saved:
                        return
                except Exception:
                    pass
        threading.Thread(target=_retry, daemon=True).start()
    except Exception:
        pass
    return None


def _get_vault_attach(did, mid):
    try:
        attach_dir = _get_attach_dir()
        if not attach_dir:
            return None
        prefix = f"{did}_{mid}"
        best = None
        best_time = 0
        for fname in os.listdir(attach_dir):
            if fname.startswith(prefix):
                full = os.path.join(attach_dir, fname)
                if os.path.isfile(full) and os.path.getsize(full) > 0:
                    t = os.path.getmtime(full)
                    if t > best_time:
                        best_time = t
                        best = full
        return best
    except Exception:
        pass
    return None


def _get_vault_attach_by_hash(did, mid, media_hash):
    if not media_hash:
        return _get_vault_attach(did, mid)
    try:
        attach_dir = _get_attach_dir()
        if not attach_dir:
            return None
        prefix = f"{did}_{mid}_{media_hash}"
        for fname in os.listdir(attach_dir):
            if fname.startswith(prefix):
                full = os.path.join(attach_dir, fname)
                if os.path.isfile(full) and os.path.getsize(full) > 0:
                    return full
    except Exception:
        pass
    return None


def _get_vault_attachs(did, mid):
    try:
        attach_dir = _get_attach_dir()
        if not attach_dir:
            return []
        prefix = f"{did}_{mid}"
        result = []
        for fname in sorted(os.listdir(attach_dir)):
            if fname.startswith(prefix):
                full = os.path.join(attach_dir, fname)
                if os.path.isfile(full) and os.path.getsize(full) > 0:
                    result.append(full)
        return result
    except Exception:
        return []


def _open_file(path):
    try:
        if not path:
            return
        JFile = jclass("java.io.File")
        f = JFile(path)
        if not f.exists():
            return
        ctx = ApplicationLoader.applicationContext
        uri = jclass("androidx.core.content.FileProvider").getUriForFile(ctx, ctx.getPackageName() + ".provider", f)
        mime = "*/*"
        name = str(f.getName()).lower()
        if name.endswith((".jpg", ".jpeg", ".png", ".webp")):
            mime = "image/*"
        elif name.endswith((".mp4", ".mkv", ".avi")):
            mime = "video/*"
        elif name.endswith((".mp3", ".ogg", ".wav", ".m4a")):
            mime = "audio/*"
        from android.content import Intent
        intent = Intent(Intent.ACTION_VIEW)
        intent.setDataAndType(uri, mime)
        intent.addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION)
        intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
        ctx.startActivity(intent)
    except Exception:
        try:
            from android.content import Intent
            ctx = ApplicationLoader.applicationContext
            intent = Intent(Intent.ACTION_VIEW)
            from android.net import Uri
            intent.setData(Uri.parse("file://" + path))
            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
            ctx.startActivity(intent)
        except Exception:
            pass


def _is_out_message(did, mid):
    try:
        frag = LaunchActivity.getLastFragment()
        if isinstance(frag, ChatActivity):
            msgs_field = _ff(CA_CLS, "messages")
            if msgs_field:
                msgs = msgs_field.get(frag)
                if msgs:
                    for i in range(msgs.size()):
                        mo = msgs.get(i)
                        if mo and int(mo.getId()) == int(mid) and int(mo.getDialogId()) == int(did):
                            return bool(mo.isOut())
    except Exception:
        pass
    try:
        mc = MessagesController.getInstance(UserConfig.selectedAccount)
        dm = getattr(mc, "dialogMessagesByIds", None)
        if dm:
            obj = dm.get(Integer(int(mid)))
            if obj and isinstance(obj, MessageObject):
                return bool(obj.isOut())
    except Exception:
        pass
    return False


def _save_attachments_for_mids(did, mids):
    try:
        frag = LaunchActivity.getLastFragment()
        if not isinstance(frag, ChatActivity):
            return
        msgs_field = _ff(CA_CLS, "messages")
        if not msgs_field:
            return
        msgs = msgs_field.get(frag)
        if not msgs:
            return
        mid_set = set(int(m) for m in mids)
        for i in range(msgs.size()):
            mo = msgs.get(i)
            if mo and int(mo.getId()) in mid_set:
                msg = mo.messageOwner
                if msg:
                    try:
                        _ensure_attach_saved(msg, did, int(mo.getId()), _get_media_hash(msg))
                    except Exception:
                        pass
    except Exception:
        pass


def _collect_deleted_text(did, mids, account=None):
    data_map = {}
    if account is None:
        account = UserConfig.selectedAccount
    try:
        frag = LaunchActivity.getLastFragment()
        if isinstance(frag, ChatActivity):
            msgs_field = _ff(CA_CLS, "messages")
            if msgs_field:
                msgs = msgs_field.get(frag)
                if msgs:
                    mid_set = set(int(m) for m in mids)
                    for i in range(msgs.size()):
                        mo = msgs.get(i)
                        if mo and int(mo.getId()) in mid_set:
                            owner = mo.messageOwner
                            text = str(owner.message) if owner and owner.message else ""
                            if text:
                                data_map[int(mo.getId())] = text
    except Exception:
        pass
    try:
        mc = MessagesController.getInstance(account)
        dm = getattr(mc, "dialogMessagesByIds", None)
        if dm:
            for mid in mids:
                if int(mid) in data_map:
                    continue
                try:
                    obj = dm.get(Integer(int(mid)))
                    if obj and obj.messageOwner and obj.messageOwner.message:
                        data_map[int(mid)] = str(obj.messageOwner.message)
                except Exception:
                    pass
    except Exception:
        pass
    return data_map


def _find_old_text(did, mid, account):
    try:
        frag = LaunchActivity.getLastFragment()
        if isinstance(frag, ChatActivity):
            msgs_field = _ff(CA_CLS, "messages")
            if msgs_field:
                msgs = msgs_field.get(frag)
                if msgs:
                    for i in range(msgs.size()):
                        mo = msgs.get(i)
                        if mo and int(mo.getId()) == int(mid):
                            owner = mo.messageOwner
                            if owner and owner.message:
                                return str(owner.message)
                            break
    except Exception:
        pass
    try:
        mc = MessagesController.getInstance(account)
        obj = mc.dialogMessagesByIds.get(Integer(int(mid)))
        if obj and obj.messageOwner and obj.messageOwner.message:
            return str(obj.messageOwner.message)
    except Exception:
        pass
    return None


def _fp_base_dir():
    try:
        d = os.path.join(ApplicationLoader.applicationContext.getFilesDir().getAbsolutePath(), "max_reextera")
        os.makedirs(d, exist_ok=True)
        return d
    except Exception:
        return "/tmp"

def _self_uid(acc=None):
    try:
        if acc is None:
            acc = UserConfig.selectedAccount
        uc = UserConfig.getInstance(acc)
        if uc and uc.isClientActivated():
            return int(uc.getClientUserId())
    except Exception:
        pass
    return 0

def _emoji_cache_path(acc=None, uid=None):
    try:
        d = _fp_base_dir()
        u = int(uid) if uid is not None else _self_uid(acc)
        if not u:
            return None
        return os.path.join(d, f"emoji_status_{u}.json")
    except Exception:
        return None

def _premium_cache_path(acc=None, uid=None):
    try:
        d = _fp_base_dir()
        u = int(uid) if uid is not None else _self_uid(acc)
        if not u:
            return None
        return os.path.join(d, f"premium_state_{u}.json")
    except Exception:
        return None

def _atomic_write_json(path, data):
    tmp = path + ".tmp"
    try:
        with open(tmp, "w", encoding="utf-8") as f:
            json.dump(data, f, ensure_ascii=False)
        os.replace(tmp, path)
    except Exception:
        try:
            os.remove(tmp)
        except Exception:
            pass

def _safe_read_json(path):
    try:
        if not path or not os.path.exists(path):
            return None
        with open(path, "r", encoding="utf-8") as f:
            d = json.load(f)
        return d if isinstance(d, dict) else None
    except Exception:
        return None

def _is_empty_es(es):
    if es is None:
        return True
    try:
        if isinstance(es, TLRPC.TL_emojiStatusEmpty):
            return True
    except Exception:
        pass
    return False

def _is_empty_color(c):
    if c is None:
        return True
    try:
        if int(getattr(c, "flags", 0)) == 0:
            return True
    except Exception:
        pass
    return False

def _emoji_to_dict(es):
    try:
        if es is None:
            return None
        if isinstance(es, TLRPC.TL_emojiStatusEmpty):
            return None
        try:
            if isinstance(es, TLRPC.TL_emojiStatusCollectible):
                return {
                    "type": "collectible",
                    "flags": int(es.flags),
                    "collectible_id": int(es.collectible_id),
                    "document_id": int(es.document_id),
                    "title": str(es.title or ""),
                    "slug": str(es.slug or ""),
                    "pattern_document_id": int(es.pattern_document_id),
                    "center_color": int(es.center_color),
                    "edge_color": int(es.edge_color),
                    "pattern_color": int(es.pattern_color),
                    "text_color": int(es.text_color),
                    "until": int(es.until)
                }
        except Exception:
            pass
        try:
            if isinstance(es, TLRPC.TL_emojiStatus):
                return {
                    "type": "status",
                    "flags": int(es.flags),
                    "document_id": int(es.document_id),
                    "until": int(es.until)
                }
        except Exception:
            pass
    except Exception:
        pass
    return None

def _dict_to_emoji(d):
    try:
        if not d:
            return None
        t = d.get("type")
        if t == "status":
            es = TLRPC.TL_emojiStatus()
            es.flags = int(d.get("flags", 0))
            es.document_id = int(d.get("document_id", 0))
            es.until = int(d.get("until", 0))
            return es
        if t == "collectible":
            es = TLRPC.TL_emojiStatusCollectible()
            es.flags = int(d.get("flags", 0))
            es.collectible_id = int(d.get("collectible_id", 0))
            es.document_id = int(d.get("document_id", 0))
            es.title = d.get("title", "") or ""
            es.slug = d.get("slug", "") or ""
            es.pattern_document_id = int(d.get("pattern_document_id", 0))
            es.center_color = int(d.get("center_color", 0))
            es.edge_color = int(d.get("edge_color", 0))
            es.pattern_color = int(d.get("pattern_color", 0))
            es.text_color = int(d.get("text_color", 0))
            es.until = int(d.get("until", 0))
            return es
    except Exception:
        pass
    return None

def _peer_color_to_dict(c):
    try:
        if c is None:
            return None
        try:
            if isinstance(c, TLRPC.TL_peerColorCollectible):
                d = {
                    "type": "collectible",
                    "flags": int(c.flags),
                    "collectible_id": int(c.collectible_id),
                    "gift_emoji_id": int(c.gift_emoji_id),
                    "background_emoji_id": int(c.background_emoji_id),
                    "accent_color": int(c.accent_color),
                    "dark_accent_color": int(c.dark_accent_color)
                }
                try:
                    d["colors"] = [int(x) for x in c.colors] if c.colors else []
                except Exception:
                    d["colors"] = []
                try:
                    d["dark_colors"] = [int(x) for x in c.dark_colors] if c.dark_colors else []
                except Exception:
                    d["dark_colors"] = []
                return d
        except Exception:
            pass
        try:
            if isinstance(c, TLRPC.TL_peerColor):
                return {
                    "type": "plain",
                    "flags": int(c.flags),
                    "color": int(c.color),
                    "background_emoji_id": int(c.background_emoji_id)
                }
        except Exception:
            pass
    except Exception:
        pass
    return None

def _dict_to_peer_color(d):
    try:
        if not d:
            return None
        t = d.get("type")
        if t == "plain":
            c = TLRPC.TL_peerColor()
            c.flags = int(d.get("flags", 0))
            c.color = int(d.get("color", 0))
            c.background_emoji_id = int(d.get("background_emoji_id", 0))
            return c
        if t == "collectible":
            c = TLRPC.TL_peerColorCollectible()
            c.flags = int(d.get("flags", 0))
            c.collectible_id = int(d.get("collectible_id", 0))
            c.gift_emoji_id = int(d.get("gift_emoji_id", 0))
            c.background_emoji_id = int(d.get("background_emoji_id", 0))
            c.accent_color = int(d.get("accent_color", 0))
            c.dark_accent_color = int(d.get("dark_accent_color", 0))
            try:
                cs = ArrayList()
                for x in (d.get("colors") or []):
                    cs.add(Integer(int(x)))
                c.colors = cs
                dcs = ArrayList()
                for x in (d.get("dark_colors") or []):
                    dcs.add(Integer(int(x)))
                c.dark_colors = dcs
            except Exception:
                pass
            return c
    except Exception:
        pass
    return None


class _DbWorker(threading.Thread):
    def __init__(self, db_path):
        super().__init__(daemon=True, name="MAX-DB")
        self._path = db_path
        self._queue = []
        self._lock = threading.Lock()
        self._event = threading.Event()
        self._conn = None
        self._stop = False

    def _get_conn(self):
        if self._conn is None:
            self._conn = sqlite3.connect(self._path, check_same_thread=False)
            self._conn.execute("PRAGMA journal_mode=WAL")
            self._conn.execute("PRAGMA synchronous=FULL")
        return self._conn

    def push(self, ops):
        with self._lock:
            self._queue.append(ops)
        self._event.set()

    def stop(self):
        self._stop = True
        self._event.set()
        try:
            self.join(timeout=2.0)
        except Exception:
            pass
        self._drain_final()

    def run(self):
        while not self._stop:
            self._event.wait()
            self._event.clear()
            self._drain()
        self._drain()

    def _drain_final(self):
        try:
            conn = sqlite3.connect(self._path, check_same_thread=False)
            conn.execute("PRAGMA journal_mode=WAL")
            conn.execute("PRAGMA synchronous=FULL")
            with self._lock:
                remaining = self._queue[:]
                self._queue.clear()
            for ops in remaining:
                for sql, params in ops:
                    try:
                        conn.execute(sql, params)
                    except Exception:
                        pass
            conn.commit()
            conn.close()
        except Exception:
            pass

    def _drain(self):
        while True:
            with self._lock:
                if not self._queue:
                    return
                batch = self._queue[:]
                self._queue.clear()
            try:
                conn = self._get_conn()
                for ops in batch:
                    for sql, params in ops:
                        try:
                            conn.execute(sql, params)
                        except Exception as e:
                            log(f"[MAX] db op: {e}")
                conn.commit()
            except Exception as e:
                log(f"[MAX] db drain: {e}")


class Vault:
    def __init__(self):
        base = os.path.join(ApplicationLoader.applicationContext.getFilesDir().getAbsolutePath(), "max_reextera")
        os.makedirs(base, exist_ok=True)
        self._db_path = os.path.join(base, "vault.db")
        self._removed = set()
        self._removed_ts = {}
        self._revisions = {}
        self._originals = {}
        self._rx = []
        self._rx_cache = []
        self._read_conn = None
        self._read_lock = threading.Lock()
        self._worker = _DbWorker(self._db_path)
        self._init_schema()
        self._load()
        self._worker.start()

    def _sync_conn(self):
        conn = sqlite3.connect(self._db_path, check_same_thread=False)
        conn.execute("PRAGMA journal_mode=WAL")
        conn.execute("PRAGMA synchronous=FULL")
        return conn

    def _get_read_conn(self):
        if self._read_conn is None:
            self._read_conn = sqlite3.connect(self._db_path, check_same_thread=False)
            self._read_conn.execute("PRAGMA journal_mode=WAL")
        return self._read_conn

    def _init_schema(self):
        conn = self._sync_conn()
        try:
            conn.execute("CREATE TABLE IF NOT EXISTS removed (dialog_id INTEGER NOT NULL, message_id INTEGER NOT NULL, timestamp INTEGER DEFAULT 0, data TEXT, PRIMARY KEY (dialog_id, message_id))")
            removed_cols = [str(r[1]) for r in conn.execute("PRAGMA table_info(removed)").fetchall()]
            if "timestamp" not in removed_cols:
                conn.execute("ALTER TABLE removed ADD COLUMN timestamp INTEGER DEFAULT 0")
            if "data" not in removed_cols:
                conn.execute("ALTER TABLE removed ADD COLUMN data TEXT")
            conn.execute("CREATE TABLE IF NOT EXISTS revisions (dialog_id INTEGER NOT NULL, message_id INTEGER NOT NULL, ver INTEGER NOT NULL, timestamp INTEGER DEFAULT 0, data TEXT, text TEXT DEFAULT '', media_hash TEXT DEFAULT '', PRIMARY KEY (dialog_id, message_id, ver))")
            revision_cols = [str(r[1]) for r in conn.execute("PRAGMA table_info(revisions)").fetchall()]
            required = {"dialog_id", "message_id", "ver", "timestamp", "data", "text", "media_hash"}
            if not required.issubset(set(revision_cols)):
                rows = conn.execute("SELECT * FROM revisions").fetchall()
                conn.execute("DROP TABLE IF EXISTS revisions_migrated")
                conn.execute("CREATE TABLE revisions_migrated (dialog_id INTEGER NOT NULL, message_id INTEGER NOT NULL, ver INTEGER NOT NULL, timestamp INTEGER DEFAULT 0, data TEXT, text TEXT DEFAULT '', media_hash TEXT DEFAULT '', PRIMARY KEY (dialog_id, message_id, ver))")
                for row in rows:
                    old = dict(zip(revision_cols, row))
                    did = _int(old.get("dialog_id", 0))
                    mid = _int(old.get("message_id", 0))
                    ver = _int(old.get("ver", old.get("idx", 0)))
                    ts = _int(old.get("timestamp", 0))
                    data = old.get("data")
                    text = old.get("text", "") or ""
                    media_hash = old.get("media_hash", "") or ""
                    if did and mid:
                        conn.execute("INSERT OR IGNORE INTO revisions_migrated(dialog_id,message_id,ver,timestamp,data,text,media_hash) VALUES(?,?,?,?,?,?,?)", (did, mid, ver, ts, data, text, media_hash))
                conn.execute("DROP TABLE revisions")
                conn.execute("ALTER TABLE revisions_migrated RENAME TO revisions")
            originals_cols = [str(r[1]) for r in conn.execute("PRAGMA table_info(originals)").fetchall()]
            if {"dialog_id", "message_id", "text"}.issubset(set(originals_cols)):
                conn.execute("INSERT OR IGNORE INTO revisions(dialog_id,message_id,ver,timestamp,data,text,media_hash) SELECT dialog_id,message_id,0,0,NULL,text,'' FROM originals")
            conn.execute("CREATE TABLE IF NOT EXISTS rx_patterns (id INTEGER PRIMARY KEY AUTOINCREMENT, pattern TEXT NOT NULL)")
            conn.execute("CREATE INDEX IF NOT EXISTS idx_rem_did ON removed(dialog_id)")
            conn.execute("CREATE INDEX IF NOT EXISTS idx_rev_did_mid ON revisions(dialog_id, message_id)")
            conn.commit()
        finally:
            conn.close()

    def _load(self):
        conn = self._sync_conn()
        try:
            cur = conn.cursor()
            cur.execute("SELECT dialog_id, message_id, timestamp FROM removed")
            for did, mid, ts in cur.fetchall():
                self._removed.add((int(did), int(mid)))
                self._removed_ts[(int(did), int(mid))] = int(ts or 0)
            cur.execute("SELECT dialog_id, message_id, ver, timestamp, text, media_hash FROM revisions ORDER BY dialog_id, message_id, ver LIMIT 20000")
            for row in cur.fetchall():
                did, mid, ver, ts, text, mh = row
                k = (int(did), int(mid))
                if k not in self._revisions:
                    self._revisions[k] = []
                self._revisions[k].append({
                    "ver": int(ver),
                    "timestamp": int(ts or 0),
                    "text": text or "",
                    "media_hash": mh or ""
                })
            cur.execute("SELECT pattern FROM rx_patterns ORDER BY id")
            self._rx = [r[0] for r in cur.fetchall()]
            self._rebuild_rx()
        except Exception as e:
            log(f"[MAX] vault load: {e}")
        finally:
            conn.close()

    def _db(self, ops):
        if ops:
            self._worker.push(ops)

    def _rebuild_rx(self):
        cache = []
        for p in self._rx:
            try:
                cache.append(re.compile(p, re.IGNORECASE | re.MULTILINE))
            except Exception:
                pass
        self._rx_cache = cache

    def mark_removed(self, did, mids, data_map=None):
        ts = int(_time.time())
        ops = []
        for mid in mids:
            k = (int(did), int(mid))
            if k not in self._removed:
                self._removed.add(k)
                self._removed_ts[k] = ts
                data = data_map.get(int(mid)) if data_map else None
                ops.append(("INSERT OR IGNORE INTO removed(dialog_id,message_id,timestamp,data) VALUES(?,?,?,?)", (int(did), int(mid), ts, data)))
        self._db(ops)

    def is_removed(self, did, mid):
        return (int(did), int(mid)) in self._removed

    def is_removed_obj(self, mo):
        try:
            return self.is_removed(mo.getDialogId(), mo.getId())
        except Exception:
            return False

    def get_removed_mids(self, did):
        return [mid for (d, mid) in self._removed if d == int(did)]

    def get_removed_list(self, did):
        result = []
        try:
            conn = self._sync_conn()
            try:
                cur = conn.cursor()
                cur.execute("SELECT mid, data, timestamp FROM removed WHERE dialog_id=? ORDER BY timestamp DESC LIMIT 200", (int(did),))
                for row in cur.fetchall():
                    mid = int(row[0])
                    text = str(row[1]) if row[1] else ""
                    ts = int(row[2] or 0)
                    result.append({"mid": mid, "text": text, "ts": ts})
            finally:
                conn.close()
        except Exception:
            pass
        return result

    def purge_removed(self, did, mids):
        ops = []
        for mid in mids:
            self._removed.discard((int(did), int(mid)))
            self._removed_ts.pop((int(did), int(mid)), None)
            ops.append(("DELETE FROM removed WHERE dialog_id=? AND message_id=?", (int(did), int(mid))))
        self._db(ops)

    def has_revisions(self, did, mid):
        return any(int(r.get("ver", 0)) > 0 for r in self._revisions.get((int(did), int(mid)), []))

    def save_message_version(self, did, mid, msg, is_original=False):
        k = (int(did), int(mid))
        ts = int(_time.time())
        text = ""
        data = None
        media_hash = ""
        try:
            if msg:
                text = str(msg.message) if msg.message else ""
                media_hash = _get_media_hash(msg) or ""
                try:
                    encoded = _encode_msg(msg)
                    data = str(encoded) if encoded else None
                except Exception as enc_e:
                    log(f"[MAX] encode_msg: {enc_e}")
                if is_original:
                    source_ts = _int(getattr(msg, "date", 0))
                else:
                    source_ts = _int(getattr(msg, "edit_date", 0))
                    if source_ts <= 0:
                        source_ts = _int(getattr(msg, "date", 0))
                if source_ts > 0:
                    ts = source_ts
        except Exception:
            pass
        if k not in self._revisions:
            self._revisions[k] = []
        lst = self._revisions[k]
        if lst:
            last = lst[-1]
            if last["text"] == text and last["media_hash"] == media_hash:
                return
        ver = 0 if is_original else (lst[-1]["ver"] + 1 if lst else 1)
        if is_original and lst:
            return
        mem_entry = {"ver": ver, "timestamp": ts, "text": text, "media_hash": media_hash}
        if is_original:
            lst.insert(0, mem_entry)
            self._db([("DELETE FROM revisions WHERE dialog_id=? AND message_id=?", (int(did), int(mid)))])
            ops = []
            for i, e in enumerate(lst):
                ops.append(("INSERT INTO revisions(dialog_id,message_id,ver,timestamp,data,text,media_hash) VALUES(?,?,?,?,?,?,?)",
                           (int(did), int(mid), i, e["timestamp"], data if i == 0 else None, e["text"], e["media_hash"])))
            self._db(ops)
        else:
            lst.append(mem_entry)
            self._db([("INSERT OR REPLACE INTO revisions(dialog_id,message_id,ver,timestamp,data,text,media_hash) VALUES(?,?,?,?,?,?,?)",
                      (int(did), int(mid), ver, ts, data, text, media_hash))])

    def get_revisions(self, did, mid):
        revs = list(self._revisions.get((int(did), int(mid)), []))
        if revs:
            try:
                conn = self._sync_conn()
                try:
                    cur = conn.cursor()
                    cur.execute("SELECT ver, data FROM revisions WHERE dialog_id=? AND message_id=? AND data IS NOT NULL", (int(did), int(mid)))
                    data_map = {int(r[0]): r[1] for r in cur.fetchall()}
                    for rev in revs:
                        v = int(rev.get("ver", 0))
                        if v in data_map:
                            rev["data"] = data_map[v]
                finally:
                    conn.close()
            except Exception:
                pass
        return revs

    def get_rx(self):
        return self._rx[:]

    def add_rx(self, pattern):
        self._rx.append(pattern)
        self._db([("INSERT INTO rx_patterns(pattern) VALUES(?)", (pattern,))])
        self._rebuild_rx()

    def remove_rx(self, index):
        if 0 <= index < len(self._rx):
            p = self._rx.pop(index)
            self._db([("DELETE FROM rx_patterns WHERE pattern=?", (p,))])
            self._rebuild_rx()

    def rx_match(self, mo):
        if not mo or not self._rx_cache:
            return False
        try:
            txt = mo.messageOwner.message
            return bool(txt) and any(r.search(txt) for r in self._rx_cache)
        except Exception:
            return False

    def wipe(self):
        self._removed.clear()
        self._removed_ts.clear()
        self._revisions.clear()
        self._db([("DELETE FROM removed", ()), ("DELETE FROM revisions", ())])

    def shutdown(self):
        try:
            self._worker.stop()
        except Exception:
            pass
        try:
            if self._read_conn:
                self._read_conn.close()
                self._read_conn = None
        except Exception:
            pass


class JavaRunnable(dynamic_proxy(Runnable)):
    def __init__(self, fn):
        super().__init__()
        self._fn = fn
    def run(self):
        try:
            self._fn()
        except Exception:
            pass


class Dex:
    def __init__(self, ec):
        self.ec = ec
        self.clazz = None
        self.loaded = False
        self._m = {}
    def load(self):
        if self.loaded:
            return
        if self.clazz is None:
            ctx = ApplicationLoader.applicationContext
            if not ctx:
                raise RuntimeError("no ctx")
            self.clazz = jclass("dalvik.system.InMemoryDexClassLoader")(jclass("java.nio.ByteBuffer").wrap(self._rp()), ctx.getClassLoader()).loadClass(self.ec)
            for m in self.clazz.getDeclaredMethods():
                m.setAccessible(True)
                self._m[str(m.getName())] = m
        self._m["load"].invoke(None)
        self.loaded = True
    def unload(self):
        try:
            if self.loaded and "unload" in self._m:
                self._m["unload"].invoke(None)
        except Exception:
            pass
        self.loaded = False
    def call(self, name, *args):
        try:
            if self.loaded and name in self._m:
                return self._m[name].invoke(None, *args)
        except Exception as e:
            log(f"[MAX] dex.{name}: {e}")
        return None
    def _rp(self):
        with open(__file__, "r", encoding="utf-8") as f:
            t = f.read()
        b = t.split(f"# {DEX_BEGIN}", 1)[1].split(f"# {DEX_END}", 1)[0]
        p = "".join(l.strip()[1:].strip() for l in b.splitlines() if l.strip().startswith("#"))
        if not p:
            raise RuntimeError("dex empty")
        return zlib.decompress(base64.b64decode(p))


CA_CLS = jclass("org.telegram.ui.ChatActivity").getClass()
IS_NEW = False
try:
    CA_CLS.getDeclaredField("actionsButtonsLayout")
    IS_NEW = True
except Exception:
    pass
HIDE_ACTION = _fm(CA_CLS, "hideActionMode", 0)
UPD_PINNED = _fm(CA_CLS, "updatePinnedMessageView", 1)


class NFLSelectorHook(MethodHook):
    def __init__(self, plugin):
        super().__init__()
        self.plugin = plugin
        self.msgs = {}
        self.grp = False
        self.grp_last = False
        self.grp_end = False
    def _idx(self, param):
        return int(param.args[0].getDialogId() != get_private_field(param.thisObject, "dialog_id"))
    def before_hooked_method(self, param):
        if not self.plugin._gs("no_forward_limit", False):
            return
        ca = param.thisObject
        smo = param.args[0]
        if self.grp:
            self.grp_last = bool(param.args[2])
            return
        if bool(param.args[1]) and bool(param.args[2]) and smo.getGroupId() != 0:
            self.grp = True
        idx = self._idx(param)
        jms = get_private_field(ca, "selectedMessagesIds")
        try:
            jms[idx] = ca.SparseArrayWithTouch()
        except Exception:
            jms[idx] = SparseArray()
        set_private_field(ca, "selectedMessagesIds", jms)
    def after_hooked_method(self, param):
        if not self.plugin._gs("no_forward_limit", False):
            return
        if self.grp_end:
            self.grp_end = False
            return
        if self.grp:
            if not self.grp_last:
                return
            self.grp = False
            self.grp_last = False
            self.grp_end = True
        ca = param.thisObject
        idx = self._idx(param)
        jms = get_private_field(ca, "selectedMessagesIds")
        sma = jms[idx]
        cm = get_private_field(ca, "chatMode")
        cc = get_private_field(ca, "currentChat")
        ism = cm == 1
        for ri in reversed(range(sma.size())):
            mid = sma.keyAt(ri)
            mo = sma.get(mid)
            if mid in self.msgs:
                try:
                    if not mo.canForwardMessage():
                        v = get_private_field(ca, "cantForwardMessagesCount")
                        set_private_field(ca, "cantForwardMessagesCount", Integer(max(0, int(str(v)) - 2)))
                except Exception:
                    pass
                try:
                    if not mo.canDeleteMessage(ism, cc):
                        v = get_private_field(ca, "cantDeleteMessagesCount")
                        set_private_field(ca, "cantDeleteMessagesCount", Integer(max(0, int(str(v)) - 2)))
                except Exception:
                    pass
                self.msgs.pop(mid)
            else:
                self.msgs[mid] = mo
        sma.clear()
        for k, v in self.msgs.items():
            sma.put(k, v)
        if not self.msgs:
            try:
                if HIDE_ACTION:
                    HIDE_ACTION.invoke(ca)
                if UPD_PINNED:
                    UPD_PINNED.invoke(ca, [True])
            except Exception:
                pass
            return
        try:
            if IS_NEW:
                abl = get_private_field(ca, "actionsButtonsLayout")
                if abl:
                    abl.showReplyButton(len(self.msgs) == 1, True)
            else:
                rb = get_private_field(ca, "replyButton")
                if rb:
                    rb.setVisibility(View.VISIBLE if len(self.msgs) == 1 else View.GONE)
        except Exception:
            pass
    def clear(self):
        self.msgs.clear()

class NFLClearHook(MethodHook):
    def __init__(self, sel):
        super().__init__()
        self.sel = sel
    def before_hooked_method(self, param):
        self.sel.clear()

class NFLMultiHook(MethodHook):
    def __init__(self, plugin):
        super().__init__()
        self.plugin = plugin
    def after_hooked_method(self, param):
        if not self.plugin._gs("no_forward_limit", False):
            return
        try:
            clv = get_private_field(param.thisObject, "chatListView")
            if clv:
                sl = get_private_field(clv, "multiSelectionListener")
                if sl:
                    self.plugin.hook_method(sl.getClass().getMethod("limitReached"), NFLBypassHook())
        except Exception:
            pass

class NFLBypassHook(MethodReplacement):
    def replace_hooked_method(self, param):
        return False

class NFLDeleteHook(XposedHook):
    def before_hooked_method(self, param):
        fa, sa = param.args[0], param.args[1]
        msgs, ai = (fa, 0) if fa.size() > 0 else (sa, 1)
        ml = _al_to_list(msgs)
        if len(ml) <= 100:
            return
        chunks = [ml[i:i+100] for i in range(0, len(ml), 100)]
        for i, chunk in enumerate(chunks):
            param.args[ai] = _to_al(chunk)
            if i < len(chunks) - 1:
                param.method.invoke(param.thisObject, param.args)


MATCH_PARENT = ViewGroup.LayoutParams.MATCH_PARENT
WRAP_CONTENT = ViewGroup.LayoutParams.WRAP_CONTENT


def _create_sticker_view(context, list_view, current_account, class_guid, resources_provider):
    layout = JFrameLayout(context)
    layout.setLayoutParams(ViewGroup.LayoutParams(MATCH_PARENT, WRAP_CONTENT))
    layout.setPadding(0, AndroidUtilities.dp(20), 0, AndroidUtilities.dp(20))
    iv = BackupImageView(context)
    iv.setRoundRadius(AndroidUtilities.dp(45))
    layout.addView(iv, LayoutHelper.createFrame(130, 130, Gravity.CENTER))
    return layout

def _bind_sticker_view(view, item, divider, adapter, list_view):
    MediaDataController.getInstance(UserConfig.selectedAccount).setPlaceholderImageByIndex(
        view.getChildAt(0), "MaxExteraPluginsStickers", 22, "130_130")

_StickerFactory = SimpleSettingFactory(_create_sticker_view, _bind_sticker_view, is_clickable=False, is_shadow=False)


def _find_field_in_hierarchy(obj, name):
    if obj is None:
        return None
    try:
        current = obj.getClass()
    except Exception:
        return None
    while current is not None:
        try:
            f = current.getDeclaredField(name)
            f.setAccessible(True)
            return f
        except Exception:
            pass
        try:
            current = current.getSuperclass()
        except Exception:
            current = None
    return None


def _set_field_value(obj, name, value):
    f = _find_field_in_hierarchy(obj, name)
    if f is None:
        return False
    try:
        type_name = str(f.getType().getName())
        if type_name == "int":
            f.setInt(obj, int(value))
        elif type_name == "long":
            f.setLong(obj, int(value))
        elif type_name == "boolean":
            f.setBoolean(obj, bool(value))
        else:
            f.set(obj, value)
        return True
    except Exception:
        return False


def _get_field_value(obj, name, default=None):
    f = _find_field_in_hierarchy(obj, name)
    if f is None:
        return default
    try:
        return f.get(obj)
    except Exception:
        return default


def _invoke_no_arg(obj, method_name):
    if obj is None:
        return None
    try:
        current = obj.getClass()
    except Exception:
        return None
    while current is not None:
        try:
            methods = current.getDeclaredMethods()
        except Exception:
            methods = []
        for method in methods:
            try:
                if str(method.getName()) != method_name:
                    continue
                try:
                    plen = len(method.getParameterTypes())
                except Exception:
                    try:
                        plen = int(method.getParameterTypes().length)
                    except Exception:
                        continue
                if plen != 0:
                    continue
                method.setAccessible(True)
                return method.invoke(obj)
            except Exception:
                continue
        try:
            current = current.getSuperclass()
        except Exception:
            current = None
    return None


class MaxReExteraPlugin(BasePlugin):
    def __init__(self):
        super().__init__()
        self.dex = Dex(ENTRY_CLASS)
        self.vault = None
        self._filters = []
        self._cpat = []
        self._unhooks = []
        self.nfl_sel = None
        self._presence_tracked = set()
        self._presence_lamp = {}
        self._presence_records = {}
        self._presence_last_seen = {}
        self._presence_open = {}
        self._presence_dirty = False
        self._presence_lock = threading.Lock()
        self._fp_cache = {}
        self._fp_dirty = False
        self._stop_event = None
        self._pass_read = False
        self._ctx_msg = None
        self._my_deleting = set()
        self._my_del_lock = threading.Lock()
        self._vault_purging = set()
        self._vault_purging_mids = set()
        self._vault_icon = None
        self._f_ts = None
        self._f_tw = None
        self._f_w = None
        self._cmc_fields_cached = False
        self._cc_cls = None
        self._ghost_for_stories = False
        self._ghost_was_on_before_stories = False
        self._story_viewer_open = False
        self._story_bypass = False
        self._last_offline_push = 0
        self._forward_copying = False
        self._gift_sticker_docs = []
        self._gift_list = []
        self._gift_pack = "DeletedGiftsStickers"
        self._gift_logged_missing = False

        self._gifts_url = "https://raw.githubusercontent.com/binbash-0/DeletedGifts-Plugin/refs/heads/main/gift_list.json"
        self._gifts_channel_username = "DeletedGiftsChannel"
        self._gifts_channel_id = "3530444329"
        self._ad_field_cache = {}
        self._pending_send_cancel = set()
        self._pending_send_lock = threading.Lock()
        self._decorated_cells = {}
        self._read_state_cache = {}
        self._sc = {}
        self._sc_ts = 0
        self._my_uids = {}
        self._my_uids_ts = 0
        self._keep_alive_active = False
        self._wakelock = None
        self._stamp_icon_extra = 0
        self._stamp_icon_extra_ready = False
        self._removed_dids = set()
        self._read_whitelist = set()

    _prefs_instance = None
    def _prefs(self):
        if MaxReExteraPlugin._prefs_instance is None:
            MaxReExteraPlugin._prefs_instance = ApplicationLoader.applicationContext.getSharedPreferences(PREFS_NAME, 0)
        return MaxReExteraPlugin._prefs_instance

    def _is_ghost(self):
        return self._gs("ghost_mode", False)

    def _chk(self, key):
        return self._gs("ghost_mode", False) and self._gs(key, False)

    def _gs(self, key, default=None):
        sc = self._sc
        v = sc.get(key)
        if v is not None:
            return v
        if len(sc) > 50:
            sc.clear()
        v = self.get_setting(key, default)
        sc[key] = v
        return v

    def _gs_flush(self):
        self._sc.clear()

    def _get_my_uid(self, acc=None):
        if acc is None:
            acc = int(UserConfig.selectedAccount)
        now = _time.monotonic() if hasattr(_time, 'monotonic') else _time.time()
        if now - self._my_uids_ts > 10.0:
            self._my_uids.clear()
            self._my_uids_ts = now
        uid = self._my_uids.get(acc)
        if uid is not None:
            return uid
        try:
            uid = int(UserConfig.getInstance(acc).getClientUserId())
        except Exception:
            uid = 0
        self._my_uids[acc] = uid
        return uid

    def _is_removed_fast(self, did, mid):
        if not self.vault:
            return False
        return (int(did), int(mid)) in self.vault._removed

    def _invalidate_removed_cache(self):
        self._decorated_cells.clear()

    def _save_read_state(self, did, mid, mo):
        try:
            owner = mo.messageOwner if hasattr(mo, "messageOwner") else mo
            if not owner:
                return
            unread = bool(getattr(owner, "unread", False))
            k = (int(did), int(mid))
            self._read_state_cache[k] = unread
        except Exception:
            pass

    def _save_read_states_bulk(self, did, mids):
        try:
            frag = LaunchActivity.getLastFragment()
            if not isinstance(frag, ChatActivity):
                return
            msgs_field = _ff(CA_CLS, "messages")
            if not msgs_field:
                return
            msgs = msgs_field.get(frag)
            if not msgs:
                return
            mid_set = set(int(m) for m in mids)
            for i in range(msgs.size()):
                mo = msgs.get(i)
                if mo and int(mo.getId()) in mid_set:
                    self._save_read_state(did, mo.getId(), mo)
        except Exception:
            pass

    def _restore_read_state(self, mo, did, mid):
        try:
            k = (int(did), int(mid))
            if k not in self._read_state_cache:
                return
            saved_unread = self._read_state_cache[k]
            owner = mo.messageOwner
            if owner and bool(getattr(owner, "out", False)):
                owner.unread = saved_unread
        except Exception:
            pass

    def _mark_my_deletion(self, mids):
        if not mids or not self._gs("track_own_deletions", False):
            return
        with self._my_del_lock:
            for m in mids:
                self._my_deleting.add(int(m))
        def cleanup():
            _time.sleep(3)
            with self._my_del_lock:
                for m in mids:
                    self._my_deleting.discard(int(m))
        threading.Thread(target=cleanup, daemon=True).start()

    def _is_my_deletion(self, mid):
        if not self._gs("track_own_deletions", False):
            return False
        with self._my_del_lock:
            return int(mid) in self._my_deleting

    def _resolve_did(self, mids, account=None):
        if account is None:
            account = UserConfig.selectedAccount
        try:
            frag = LaunchActivity.getLastFragment()
            if isinstance(frag, ChatActivity):
                f = _ff(CA_CLS, "dialog_id")
                if f:
                    v = int(f.get(frag))
                    if v:
                        return v
        except Exception:
            pass
        try:
            ctrl = MessagesController.getInstance(account)
            for mid in mids:
                obj = ctrl.dialogMessagesByIds.get(Integer(mid))
                if obj:
                    v = int(obj.getDialogId())
                    if v:
                        return v
        except Exception:
            pass
        return 0

    def _refresh_cells(self):
        if self.dex and self.dex.loaded:
            try:
                self.dex.call("refreshCells")
                return
            except Exception:
                pass

    def _stamp_cell(self, cell, mo):
        pass

    def _stamp_cell_legacy(self, cell, mo):
        if not self.vault or not self.vault._removed or not mo:
            return
        try:
            did = mo.getDialogId()
            mid = mo.getId()
            if (int(did), int(mid)) not in self.vault._removed:
                return
            if not self._ensure_cell_fields():
                return
            cur = self._f_ts.get(cell)
            if not cur:
                return
            show_opacity = self._gs("del_show_opacity", True)
            try:
                c0 = cur.charAt(0)
                if c0 == "\u2009" or c0 == "\u2022":
                    if show_opacity:
                        cell.setAlpha(jfloat(self._get_opacity_val()))
                    return
            except Exception:
                pass
            paint = Theme.chat_timePaint
            if not paint:
                return
            use_icon = self._gs("del_use_icon", True)
            if use_icon and self._vault_icon:
                sb = SpannableStringBuilder("\u2009")
                span = ColoredImageSpan(self._vault_icon)
                try:
                    span.setOverrideColor(cell.getThemedColor(Theme.key_color_red))
                except Exception:
                    span.setOverrideColor(-44462)
                span.setRelativeSize(paint.getFontMetricsInt())
                sb.setSpan(span, 0, 1, 0)
                sb.append(" ")
                if not self._stamp_icon_extra_ready:
                    fm = paint.getFontMetricsInt()
                    self._stamp_icon_extra = abs(int(fm.descent) - int(fm.ascent)) + int(Math.ceil(paint.measureText(" ")))
                    self._stamp_icon_extra_ready = True
                extra = self._stamp_icon_extra
            else:
                custom = self._gs("del_custom_label", "") or ""
                label = custom if custom else L.g("msg_deleted")
                try:
                    cs = str(cur)
                    if cs.startswith(label):
                        if show_opacity:
                            cell.setAlpha(jfloat(self._get_opacity_val()))
                        return
                except Exception:
                    pass
                sb = SpannableStringBuilder(label)
                try:
                    sb.setSpan(ForegroundColorSpan(cell.getThemedColor(Theme.key_color_red)), 0, sb.length(), 33)
                except Exception:
                    sb.setSpan(ForegroundColorSpan(-44462), 0, sb.length(), 33)
                sb.append(" ")
                extra = int(Math.ceil(paint.measureText(sb, 0, sb.length())))
            result = SpannableStringBuilder(sb)
            result.append(cur)
            self._f_ts.set(cell, result)
            self._f_tw.set(cell, Integer(int(str(self._f_tw.get(cell))) + extra))
            self._f_w.set(cell, Integer(int(str(self._f_w.get(cell))) + extra))
            if show_opacity:
                cell.setAlpha(jfloat(self._get_opacity_val()))
        except Exception:
            pass

    def _ensure_cell_fields(self):
        if self._cmc_fields_cached:
            return self._f_ts is not None
        self._cmc_fields_cached = True
        try:
            CLC = find_class("org.telegram.ui.Cells.ChatMessageCell")
            self._f_ts = _ff(CLC.getClass(), "currentTimeString")
            self._f_tw = _ff(CLC.getClass(), "timeTextWidth")
            self._f_w = _ff(CLC.getClass(), "timeWidth")
        except Exception:
            pass
        return self._f_ts is not None



    def _send_read_for_dialog(self, did, max_id=0):
        try:
            did = int(did)
            mid = int(max_id) if max_id else 0
            acc = int(UserConfig.selectedAccount)
            mc = MessagesController.getInstance(acc)
            if mid <= 0:
                try:
                    f = get_last_fragment()
                    if f and str(f.getClass().getSimpleName()) == "ChatActivity":
                        msgs = f.messages
                        if msgs and msgs.size() > 0:
                            mid = int(msgs.get(0).getId())
                except Exception:
                    pass
            if mid <= 0:
                mid = 999999999
            is_channel = False
            if did < 0:
                try:
                    chat = mc.getChat(jlong(-did))
                    if chat and ChatObject.isChannel(chat):
                        is_channel = True
                except Exception:
                    pass
            self._pass_read = True
            def _cb(resp, err):
                if err:
                    log(f"[MAX] sendRead err: {err}")
            if is_channel:
                rq = TLRPC.TL_channels_readHistory()
                rq.channel = MessagesController.getInputChannel(mc.getChat(jlong(-did)))
                rq.max_id = mid
                send_request(rq, RequestCallback(_cb))
            else:
                rq = TLRPC.TL_messages_readHistory()
                rq.peer = mc.getInputPeer(jlong(did))
                rq.max_id = mid
                send_request(rq, RequestCallback(_cb))
        except Exception as e:
            log(f"[MAX] sendRead: {e}")

    def _mark_local_read(self, dialog_id, max_id):
        try:
            acc = int(UserConfig.selectedAccount)
            mc = MessagesController.getInstance(acc)
            dialogs = mc.dialogs_dict
            if dialogs:
                try:
                    d = dialogs.get(jlong(int(dialog_id)))
                except Exception:
                    d = dialogs.get(int(dialog_id))
                if d:
                    d.unread_count = 0
            try:
                nc = NotificationCenter.getInstance(acc)
                nc.postNotificationName(NotificationCenter.updateInterfaces, Integer(256))
            except Exception:
                pass
        except Exception as e:
            log(f"[MAX] markLocalRead: {e}")

    def sendOffline(self):
        try:
            req = TL_account.updateStatus()
            req.offline = True
            send_request(req, lambda r, e: None)
            self._last_offline_push = _time.time()
        except Exception:
            pass

    def _ensure_offline(self):
        if not self._is_ghost() or not self.get_setting("ghost_no_online", True):
            return
        try:
            ConnectionsManager.getInstance(int(UserConfig.selectedAccount)).checkConnection()
        except Exception:
            pass
        self.sendOffline()

    def _offline_burst(self):
        plugin = self
        def worker():
            previous = 0.0
            for target in [0.0, 0.3, 1.0, 2.5, 5.0, 10.0]:
                delay = target - previous
                previous = target
                if delay > 0 and plugin._stop_event and plugin._stop_event.wait(delay):
                    return
                if not plugin._is_ghost():
                    return
                plugin.sendOffline()
        threading.Thread(target=worker, daemon=True).start()

    def _toggle_ghost(self, ctx=None):
        cur = self.get_setting("ghost_mode", False)
        nv = not cur
        self.set_setting("ghost_mode", nv)
        self._sc.clear()
        if nv:
            self.sendOffline()
        BulletinHelper.show_info(L.g("ghost_on") if nv else L.g("ghost_off"))

    def _enable_ghost_for_stories(self):
        self._ghost_was_on_before_stories = self._is_ghost()
        if not self._ghost_was_on_before_stories:
            self.set_setting("ghost_mode", True)
            self.sendOffline()
        self._ghost_for_stories = True

    def _disable_ghost_after_stories(self):
        if self._ghost_for_stories and not self._ghost_was_on_before_stories:
            self.set_setting("ghost_mode", False)
        self._ghost_for_stories = False
        self._ghost_was_on_before_stories = False

    def _do_open_story(self, method, obj, args, with_ghost):
        plugin = self
        def _run():
            plugin._story_viewer_open = True
            plugin._story_bypass = True
            try:
                ptypes = method.getParameterTypes()
                fixed = []
                for i, a in enumerate(args):
                    if i < len(ptypes):
                        pname = str(ptypes[i].getName())
                        if pname == "int":
                            fixed.append(jint(int(a)))
                        elif pname == "long":
                            fixed.append(jlong(int(a)))
                        elif pname == "boolean":
                            fixed.append(bool(a))
                        elif pname == "float":
                            fixed.append(jfloat(float(a)))
                        else:
                            fixed.append(a)
                    else:
                        fixed.append(a)
                method.invoke(obj, *fixed)
            except Exception as e:
                log(f"[MAX] openStory: {e}")
            plugin._story_bypass = False
        run_on_ui_thread(_run)

    def on_plugin_load(self):
        if self._unhooks:
            return
        self._stop_event = threading.Event()
        for h in TYPING_REQS | STORIES_REQS | ONLINE_REQS | DELETE_REQS | PRESENCE_UPD | BLOCKED_AD_REQS | {"updateStatus", "TL_account_updateStatus", "TL_messages_sendReaction"}:
            try:
                self.add_hook(h)
            except Exception:
                pass
        for u in ["TL_updateEditMessage", "TL_updateEditChannelMessage", "TL_updateDeleteMessages", "TL_updateDeleteChannelMessages", "TL_updateNewMessage", "TL_updateNewChannelMessage"]:
            try:
                self.add_hook(u)
            except Exception:
                pass
        self.add_on_send_message_hook()
        self.add_hook("TL_messages_forwardMessages")
        if self.get_setting("hidden_gifts", False):
            self.add_hook("getStarGifts")
            self.add_hook("TL_messages_getStickerSet")
        self.add_menu_item(MenuItemData(menu_type=MenuItemType.MESSAGE_CONTEXT_MENU, text=L.g("edit_history"), icon="msg_edit", on_click=self._on_eh_click))
        try:
            self.add_menu_item(MenuItemData(menu_type=MenuItemType.CHAT_ACTION_MENU, text=L.g("view_deleted"), icon="msg_delete", on_click=self._on_view_deleted_click, item_id="view_deleted"))
        except Exception:
            pass
        if self.get_setting("drawer_ghost_btn", True):
            self.add_menu_item(MenuItemData(menu_type=MenuItemType.DRAWER_MENU, text=L.g("ghost"), icon="ghost", item_id="ghost_drawer", on_click=self._toggle_ghost))
        threading.Thread(target=self._heavy_init, daemon=True, name="MAX-init").start()

    _JAVA_SYNCED_KEYS = [
        "track_own_deletions", "no_forward_limit", "save_deleted", "save_edits",
        "save_attachments", "hide_ads", "fake_premium", "keep_once",
        "allow_screenshots", "bypass_noforward", "bypass_story_save",
        "ghost_mode", "ghost_no_online", "ghost_no_typing", "ghost_no_read",
        "ghost_no_read_stories", "ghost_immediate_offline", "read_on_action",
        "story_ghost_prompt", "keep_app_alive", "del_show_mark", "del_use_icon",
        "del_show_opacity", "filters_enabled", "presence_track",
        "presence_only_hidden", "hidden_gifts", "fwd_mode",
        "silent_send", "del_opacity_level", "presence_decay",
    ]

    def _sync_settings_to_java(self):
        try:
            editor = self._prefs().edit()
            for key in self._JAVA_SYNCED_KEYS:
                v = self.get_setting(key, None)
                if v is None:
                    continue
                if isinstance(v, bool):
                    editor.putBoolean(key, v)
                elif isinstance(v, int):
                    editor.putInt(key, v)
            editor.apply()
            if self.dex and self.dex.loaded:
                self.dex.call("pyFlushConfig")
        except Exception:
            pass

    def _heavy_init(self):
        try:
            self.dex.load()
        except Exception as e:
            log(f"[MAX] dex: {e}")
        self._sync_settings_to_java()
        self.vault = Vault()
        self._load_filters()
        self._load_presence()
        self._load_read_whitelist()
        self._load_fp()
        try:
            from org.telegram.messenger import R as TGR
            self._vault_icon = ContextCompat.getDrawable(ApplicationLoader.applicationContext, TGR.drawable.msg_delete_filled)
        except Exception:
            pass
        try:
            _get_attach_dir()
        except Exception:
            pass
        if self.get_setting("hidden_gifts", False):
            try:
                self._refresh_gift_list()
            except Exception:
                pass
            run_on_ui_thread(self._load_gift_stickers, 2000)
        def _ui_hooks():
            self._setup_hooks()
            if self.get_setting("hide_ads", True):
                self._hook_ad()
            if self._is_ghost():
                self._offline_burst()
        threading.Thread(target=_ui_hooks, daemon=True, name="MAX-hooks").start()
        threading.Thread(target=self._loop, daemon=True).start()
        threading.Timer(1.5, self._restore_presence_statuses).start()
        self._check_for_updates()

    def on_app_event(self, event_type):
        name = str(event_type).upper()
        if name.endswith("START") or name.endswith("RESUME"):
            self._gs_flush()
            if self._is_ghost():
                self.sendOffline()
                self._offline_burst()
            if self._gs("presence_track", False) and self._presence_last_seen:
                run_on_ui_thread(self._restore_presence_statuses, 1500)

    def on_plugin_unload(self):
        if self._stop_event:
            self._stop_event.set()
        if self.get_setting("fake_premium", False):
            self._revert_fake_premium()
        self.dex.unload()
        for u in self._unhooks:
            try:
                self.unhook_method(u)
            except Exception:
                pass
        self._unhooks.clear()
        self._stop_keep_alive()
        if self.vault:
            self.vault.shutdown()
        self._save_presence()
        self._save_fp()

    def _revert_fake_premium(self):
        try:
            for acc in range(4):
                try:
                    uc = UserConfig.getInstance(acc)
                    if not (uc and uc.isClientActivated()):
                        continue
                    my = int(uc.getClientUserId())
                    cu = uc.getCurrentUser()
                    if cu and int(cu.id) == my and bool(getattr(cu, "premium", False)):
                        cu.premium = False
                    try:
                        mc = MessagesController.getInstance(acc)
                        mu = mc.getUser(Long(my))
                        if mu and bool(getattr(mu, "premium", False)):
                            mu.premium = False
                    except Exception:
                        pass
                except Exception:
                    pass
        except Exception:
            pass

    def _start_keep_alive(self):
        if self._keep_alive_active:
            return
        self._keep_alive_active = True
        try:
            ctx = ApplicationLoader.applicationContext
            pm = ctx.getSystemService("power")
            if pm:
                WakeLock = jclass("android.os.PowerManager$WakeLock")
                wl = pm.newWakeLock(1, "MAX:KeepAlive")
                wl.setReferenceCounted(False)
                wl.acquire()
                self._wakelock = wl
        except Exception:
            pass
        try:
            ctx = ApplicationLoader.applicationContext
            app_cls = ctx.getClass()
            try:
                m = app_cls.getMethod("startPushService")
                m.invoke(ctx)
            except Exception:
                pass
        except Exception:
            pass

    def _stop_keep_alive(self):
        if not self._keep_alive_active:
            return
        self._keep_alive_active = False
        try:
            if self._wakelock:
                self._wakelock.release()
                self._wakelock = None
        except Exception:
            pass

    def _gc_caches(self):
        try:
            if len(self._read_state_cache) > 200:
                self._read_state_cache.clear()
        except Exception:
            pass
        try:
            if len(self._decorated_cells) > 100:
                self._decorated_cells.clear()
        except Exception:
            pass
        try:
            if len(self._ad_field_cache) > 20:
                self._ad_field_cache.clear()
        except Exception:
            pass
        try:
            if len(self._sc) > 30:
                self._sc.clear()
        except Exception:
            pass
        try:
            v = self.vault
            if v and v._revisions:
                if len(v._revisions) > 5000:
                    keys = sorted(v._revisions.keys(), key=lambda k: max((r.get("timestamp", 0) for r in v._revisions[k]), default=0))
                    for k in keys[:len(keys) - 2000]:
                        del v._revisions[k]
                for k, lst in list(v._revisions.items()):
                    for rev in lst:
                        if rev.get("data"):
                            rev["data"] = None
        except Exception:
            pass
        try:
            v = self.vault
            if v and len(v._removed) > 50000:
                v._removed.clear()
                v._removed_ts.clear()
                v._load()
        except Exception:
            pass
        try:
            if len(self._presence_records) > 0:
                for k, rows in list(self._presence_records.items()):
                    if len(rows) > 500:
                        self._presence_records[k] = rows[-500:]
        except Exception:
            pass
        try:
            if len(self._my_uids) > 10:
                self._my_uids.clear()
        except Exception:
            pass

    def _loop(self):
        cnt = 0
        _last_cleanup = 0
        _last_gc = 0
        while self._stop_event and not self._stop_event.is_set():
            try:
                cnt += 1
                self._presence_tick()
                if cnt % 3 == 0:
                    self._save_presence()
                    self._save_fp()
                if cnt % 6 == 0:
                    self._sync_settings_to_java()
                now = _time.time()
                if now - _last_gc > 60:
                    _last_gc = now
                    self._gc_caches()
                if now - _last_cleanup > 1800:
                    _last_cleanup = now
                    if self._gs("save_attachments", False):
                        try:
                            _enforce_folder_limit()
                            _enforce_time_limit()
                        except Exception:
                            pass
                if self._gs("keep_app_alive", False):
                    if not self._keep_alive_active:
                        run_on_ui_thread(self._start_keep_alive)
                    if _time.time() - self._last_offline_push >= 20:
                        try:
                            for acc in range(4):
                                try:
                                    uc = UserConfig.getInstance(acc)
                                    if uc and uc.isClientActivated():
                                        ConnectionsManager.getInstance(acc).checkConnection()
                                except Exception:
                                    pass
                        except Exception:
                            pass
                        if self._is_ghost():
                            self.sendOffline()
                        self._last_offline_push = _time.time()
                elif self._keep_alive_active:
                    run_on_ui_thread(self._stop_keep_alive)
            except Exception:
                pass
            try:
                self._stop_event.wait(5.0)
            except Exception:
                break
        if self._keep_alive_active:
            try:
                self._stop_keep_alive()
            except Exception:
                pass

    def _on_eh_click(self, ctx):
        try:
            msg = ctx.get("message")
            if msg:
                did = int(msg.getDialogId())
                mid = int(msg.getId())
                self._show_revisions(did, mid)
        except Exception as e:
            log(f"[MAX] ehClick: {e}")

    def _setup_hooks(self):
        for fn in [self._hk_restore_loaded, self._hk_stories,
                   self._hk_story_save,
                   self._hk_media_seen,
                   self._hk_nfl,
                   self._hk_outgoing_params, self._hk_filter_incoming,
                    self._hk_story_viewer_prompt, self._hk_send_cancel,
                    self._hk_file_download, self._hk_open_document,
                    self._hk_forward_deleted, self._hk_fake_premium,
                    self._hk_feed_ads, self._hk_noforward]:
            try:
                fn()
            except Exception as e:
                log(f"[MAX] hook {getattr(fn, '__name__', '?')}: {e}")

    def _remove_messages_from_ui(self, mids):
        try:
            frag = LaunchActivity.getLastFragment()
            if not isinstance(frag, ChatActivity):
                return
            for mid in mids:
                try:
                    msgs_field = _ff(CA_CLS, "messages")
                    if not msgs_field:
                        break
                    msgs = msgs_field.get(frag)
                    if not msgs:
                        break
                    for i in range(msgs.size() - 1, -1, -1):
                        mo = msgs.get(i)
                        if mo and int(mo.getId()) == int(mid):
                            try:
                                frag.removeMessageWithThanos(mo)
                            except Exception:
                                msgs.remove(i)
                            break
                except Exception:
                    pass
        except Exception:
            pass

    def _hk_send_cancel(self):
        plugin = self
        class CancelHook(MethodHook):
            def before_hooked_method(self, param):
                try:
                    mo = param.args[0]
                    if mo is None:
                        return
                    mid = int(mo.getId())
                    did = int(mo.getDialogId())
                    if plugin.vault and plugin.vault.is_removed(did, mid):
                        plugin.vault.purge_removed(did, [mid])
                        plugin._invalidate_removed_cache()
                        run_on_ui_thread(lambda: plugin._reset_cell_appearance(did, mid))
                except Exception:
                    pass
        try:
            smh_cls = jclass("org.telegram.messenger.SendMessagesHelper").getClass()
            for mm in smh_cls.getDeclaredMethods():
                mn = str(mm.getName())
                if mn == "cancelSendingMessage" and len(mm.getParameterTypes()) == 1:
                    ptype = str(mm.getParameterTypes()[0].getName())
                    if "MessageObject" in ptype:
                        mm.setAccessible(True)
                        u = self.hook_method(mm, CancelHook())
                        if u:
                            self._unhooks.append(u)
        except Exception:
            pass

    def _hk_file_download(self):
        plugin = self
        try:
            FLC = find_class("org.telegram.messenger.FileLoader")
            class FileLoadHook(MethodHook):
                def after_hooked_method(self, param):
                    if not plugin._gs("save_attachments", False):
                        return
                    try:
                        f = param.args[0] if param.args else None
                        if f is None:
                            return
                        fpath = None
                        if hasattr(f, "getAbsolutePath"):
                            fpath = str(f.getAbsolutePath())
                        elif isinstance(f, str):
                            fpath = str(f)
                        if fpath:
                            plugin._on_file_downloaded_in_tg(fpath)
                    except Exception:
                        pass
            for mm in FLC.getClass().getDeclaredMethods():
                mn = str(mm.getName())
                if mn in ("onFinishLoadingFile", "fileDidLoaded") and len(mm.getParameterTypes()) >= 1:
                    mm.setAccessible(True)
                    u = self.hook_method(mm, FileLoadHook())
                    if u:
                        self._unhooks.append(u)
                    break
        except Exception:
            pass

    def _hk_open_document(self):
        plugin = self
        class OpenForViewHook(MethodHook):
            def before_hooked_method(self, param):
                try:
                    args = param.args
                    if args is None or len(args) == 0:
                        return
                    message = args[0]
                    if message is None:
                        return
                    if "MessageObject" not in str(message.getClass().getName()):
                        return
                    file_name = str(message.getFileName())
                    if not file_name.endswith(".mre_backup"):
                        return
                    param.setResult(True)
                    run_on_ui_thread(lambda: plugin._handle_backup_file(message))
                except Exception:
                    pass
        try:
            AU = jclass("org.telegram.messenger.AndroidUtilities").getClass()
            for mm in AU.getDeclaredMethods():
                if str(mm.getName()) == "openForView":
                    mm.setAccessible(True)
                    u = self.hook_method(mm, OpenForViewHook())
                    if u:
                        self._unhooks.append(u)
        except Exception:
            pass

    def _handle_backup_file(self, message):
        try:
            doc = message.getDocument()
            if doc is None:
                return
            acc = int(UserConfig.selectedAccount)
            fl = FileLoader.getInstance(acc)
            f = fl.getPathToAttach(doc)
            if f is None or not f.exists():
                try:
                    f = fl.getPathToMessage(message.messageOwner)
                except Exception:
                    f = None
            if f is None or not f.exists():
                fl.loadFile(doc, message, 1, 0)
                BulletinHelper.show_info("Downloading... try again")
                return
            content = read_file(str(f.getAbsolutePath()))
            if content is None:
                BulletinHelper.show_info(L.g("import_error"))
                return
            try:
                data = json.loads(content)
            except Exception:
                BulletinHelper.show_info(L.g("import_error"))
                return
            if not isinstance(data, dict):
                BulletinHelper.show_info(L.g("import_error"))
                return
            self._show_import_confirm_data(data)
        except Exception as e:
            log(f"[MAX] handle backup: {e}")
            BulletinHelper.show_info(L.g("import_error"))

    def _pick_attach_folder(self, _v=None):
        plugin = self
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                activity = frag.getParentActivity()
                from android.content import Intent
                intent = Intent(Intent.ACTION_OPEN_DOCUMENT_TREE)
                intent.addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION | Intent.FLAG_GRANT_WRITE_URI_PERMISSION | Intent.FLAG_GRANT_PERSISTABLE_URI_PERMISSION)
                REQUEST_CODE = 48271
                def _setup_result_hook():
                    try:
                        Activity = jclass("android.app.Activity")
                        class ResultHook(MethodHook):
                            def before_hooked_method(self_hook, param):
                                try:
                                    rc = int(param.args[0])
                                    result_code = int(param.args[1])
                                    data = param.args[2]
                                    if rc != REQUEST_CODE:
                                        return
                                    if result_code != -1 or data is None:
                                        return
                                    uri = data.getData()
                                    if uri is None:
                                        return
                                    try:
                                        ctx = ApplicationLoader.applicationContext
                                        ctx.getContentResolver().takePersistableUriPermission(uri, Intent.FLAG_GRANT_READ_URI_PERMISSION | Intent.FLAG_GRANT_WRITE_URI_PERMISSION)
                                    except Exception:
                                        pass
                                    real_path = plugin._resolve_uri_to_path(uri)
                                    if real_path and os.path.isdir(real_path):
                                        plugin._prefs().edit().putString("custom_attach_dir", real_path).apply()
                                        _ensure_nomedia(real_path)
                                        run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("folder_set")))
                                    else:
                                        uri_str = str(uri.toString())
                                        plugin._prefs().edit().putString("custom_attach_uri", uri_str).apply()
                                        run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("folder_set")))
                                    try:
                                        if hasattr(self_hook, '_unhook_ref') and self_hook._unhook_ref:
                                            plugin.unhook_method(self_hook._unhook_ref)
                                    except Exception:
                                        pass
                                except Exception:
                                    pass
                        hook_inst = ResultHook()
                        for mm in Activity.getClass().getDeclaredMethods():
                            if str(mm.getName()) == "onActivityResult" and len(mm.getParameterTypes()) == 3:
                                mm.setAccessible(True)
                                u = plugin.hook_method(mm, hook_inst)
                                if u:
                                    hook_inst._unhook_ref = u
                                    plugin._unhooks.append(u)
                                break
                    except Exception:
                        pass
                _setup_result_hook()
                activity.startActivityForResult(intent, REQUEST_CODE)
            except Exception as e:
                log(f"[MAX] pickFolder: {e}")
        run_on_ui_thread(_ui)

    def _resolve_uri_to_path(self, uri):
        try:
            uri_str = str(uri.toString())
            if "primary" in uri_str:
                from android.os import Environment
                root = str(Environment.getExternalStorageDirectory().getAbsolutePath())
                parts = uri_str.split("primary%3A")
                if len(parts) > 1:
                    rel = parts[1].replace("%2F", "/").replace("%20", " ")
                    path = os.path.join(root, rel)
                    if os.path.isdir(path):
                        return path
            try:
                from android.provider import DocumentsContract
                doc_id = str(DocumentsContract.getTreeDocumentId(uri))
                if ":" in doc_id:
                    split = doc_id.split(":")
                    storage_type = split[0]
                    rel_path = split[1] if len(split) > 1 else ""
                    if storage_type == "primary":
                        from android.os import Environment
                        root = str(Environment.getExternalStorageDirectory().getAbsolutePath())
                        path = os.path.join(root, rel_path)
                        if os.path.isdir(path):
                            return path
                    else:
                        path = os.path.join("/storage", storage_type, rel_path)
                        if os.path.isdir(path):
                            return path
            except Exception:
                pass
        except Exception:
            pass
        return None

    def _reset_attach_folder(self, _v=None):
        try:
            self._prefs().edit().remove("custom_attach_dir").remove("custom_attach_uri").apply()
            BulletinHelper.show_info(L.g("folder_reset"))
        except Exception:
            pass

    def _get_current_folder_display(self):
        try:
            custom = str(self._prefs().getString("custom_attach_dir", "") or "")
            if custom and os.path.isdir(custom):
                parts = custom.replace("\\", "/").split("/")
                return parts[-1] if parts else custom
        except Exception:
            pass
        return L.g("attach_folder_default")

    def _show_browse_attachments(self, _v=None):
        plugin = self
        def _bg():
            try:
                attach_dir = _get_attach_dir()
                if not attach_dir or not os.path.isdir(attach_dir):
                    run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("no_attachments")))
                    return
                files = []
                for fname in os.listdir(attach_dir):
                    fpath = os.path.join(attach_dir, fname)
                    if not os.path.isfile(fpath) or os.path.getsize(fpath) == 0:
                        continue
                    parts = fname.split("_", 2)
                    if len(parts) < 2:
                        continue
                    try:
                        did = int(parts[0])
                        mid = int(parts[1].split("_")[0].split(".")[0])
                    except Exception:
                        continue
                    mtime = os.path.getmtime(fpath)
                    size = os.path.getsize(fpath)
                    files.append({"did": did, "mid": mid, "path": fpath, "name": fname, "mtime": mtime, "size": size})
                if not files:
                    run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("no_attachments")))
                    return
                chats = {}
                for f in files:
                    did = f["did"]
                    if did not in chats:
                        chats[did] = []
                    chats[did].append(f)
                for did in chats:
                    chats[did].sort(key=lambda x: -x["mtime"])
                chat_list = sorted(chats.items(), key=lambda x: -x[1][0]["mtime"])
                run_on_ui_thread(lambda: plugin._show_chat_list(chat_list))
            except Exception as e:
                log(f"[MAX] browse attach: {e}")
                run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("error")))
        threading.Thread(target=_bg, daemon=True).start()

    def _chat_display_name(self, did):
        try:
            acc = int(UserConfig.selectedAccount)
            mc = MessagesController.getInstance(acc)
            did = int(did)
            if did > 0:
                user = mc.getUser(Long(did))
                if user:
                    nm = (str(getattr(user, "first_name", "") or "") + " " + str(getattr(user, "last_name", "") or "")).strip()
                    if nm:
                        return nm
                    un = str(getattr(user, "username", "") or "")
                    if un:
                        return "@" + un
            else:
                chat = mc.getChat(Long(-did))
                if chat:
                    t = str(getattr(chat, "title", "") or "")
                    if t:
                        return t
        except Exception:
            pass
        return f"id{did}"

    def _fmt_size(self, size):
        if size < 1024:
            return f"{size} B"
        if size < 1024 * 1024:
            return f"{size // 1024} KB"
        return f"{size / (1024 * 1024):.1f} MB"

    def _show_chat_list(self, chat_list):
        try:
            frag = get_last_fragment()
            if not frag or not frag.getParentActivity():
                return
            plugin = self
            items = []
            chat_data = []
            for did, files in chat_list:
                name = plugin._chat_display_name(did)
                total_size = sum(f["size"] for f in files)
                last_date = _format_date(int(files[0]["mtime"]))
                items.append(f"{name}\n{len(files)} {L.g('files_count')} \u00B7 {plugin._fmt_size(total_size)} \u00B7 {last_date}")
                chat_data.append((did, files))
            def on_pick(dlg, which):
                dlg.dismiss()
                did, files = chat_data[which]
                plugin._show_file_list(did, files)
            d = AlertDialogBuilder(frag.getParentActivity())
            d.set_title(f"{L.g('browse_attachments')} ({len(chat_list)})")
            d.set_items(items, on_pick)
            d.set_negative_button(L.g("close"), lambda dd, w: dd.dismiss())
            d.show()
        except Exception:
            pass

    def _show_file_list(self, did, files):
        plugin = self
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                name = plugin._chat_display_name(did)
                items = []
                for f in files:
                    dt = _format_date(int(f["mtime"]))
                    sz = plugin._fmt_size(f["size"])
                    ext = os.path.splitext(f["name"])[1].lower()
                    if ext in (".jpg", ".jpeg", ".png", ".webp"):
                        icon = L.g("has_photo")
                    elif ext in (".mp4", ".mkv", ".avi"):
                        icon = L.g("has_video")
                    elif ext in (".mp3", ".ogg", ".wav", ".m4a", ".oga"):
                        icon = L.g("has_voice")
                    else:
                        icon = L.g("has_doc")
                    items.append(f"{icon} #{f['mid']}  {sz}  {dt}")
                def on_pick(dlg, which):
                    dlg.dismiss()
                    f = files[which]
                    plugin._show_file_actions(did, f)
                d = AlertDialogBuilder(frag.getParentActivity())
                d.set_title(f"{name} ({len(files)})")
                d.set_items(items, on_pick)
                d.set_negative_button(L.g("back"), lambda dd, w: (dd.dismiss(), plugin._show_browse_attachments()))
                d.show()
            except Exception:
                pass
        run_on_ui_thread(_ui)

    def _show_file_actions(self, did, f):
        plugin = self
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                dt = _format_date(int(f["mtime"]))
                sz = plugin._fmt_size(f["size"])
                title = f"#{f['mid']} \u00B7 {sz} \u00B7 {dt}"
                btn_items = [L.g("view"), L.g("go_to_msg"), L.g("delete")]
                def on_action(dlg, w):
                    dlg.dismiss()
                    if w == 0:
                        _open_file(f["path"])
                    elif w == 1:
                        plugin._scroll_to_message(did, f["mid"])
                    elif w == 2:
                        try:
                            os.remove(f["path"])
                            BulletinHelper.show_info(L.g("cleared"))
                        except Exception:
                            pass
                d = AlertDialogBuilder(frag.getParentActivity())
                d.set_title(title)
                d.set_items(btn_items, on_action)
                d.set_negative_button(L.g("back"), lambda dd, w: (dd.dismiss(), plugin._show_file_list(did, [ff for ff in plugin._get_chat_files(did)])))
                d.show()
            except Exception:
                pass
        run_on_ui_thread(_ui)

    def _get_chat_files(self, did):
        try:
            attach_dir = _get_attach_dir()
            if not attach_dir:
                return []
            prefix = f"{did}_"
            result = []
            for fname in os.listdir(attach_dir):
                if not fname.startswith(prefix):
                    continue
                fpath = os.path.join(attach_dir, fname)
                if not os.path.isfile(fpath) or os.path.getsize(fpath) == 0:
                    continue
                parts = fname.split("_", 2)
                try:
                    mid = int(parts[1].split("_")[0].split(".")[0])
                except Exception:
                    continue
                result.append({"did": did, "mid": mid, "path": fpath, "name": fname, "mtime": os.path.getmtime(fpath), "size": os.path.getsize(fpath)})
            result.sort(key=lambda x: -x["mtime"])
            return result
        except Exception:
            return []

    def _show_import_confirm_data(self, data):
        plugin = self
        ts = data.get("_ts", 0)
        ver = data.get("_version", "?")
        import datetime
        date_str = datetime.datetime.fromtimestamp(int(ts)).strftime("%d.%m.%Y %H:%M") if ts else "?"
        msg = f"{L.g('import_confirm')}\n\nv{ver} — {date_str}"
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                d = AlertDialogBuilder(frag.getParentActivity())
                d.set_title(L.g("import_title"))
                d.set_message(msg)
                d.set_positive_button(L.g("yes"), lambda dlg, w: (dlg.dismiss(), plugin._do_import(data)))
                d.set_negative_button(L.g("no"), lambda dlg, w: dlg.dismiss())
                d.show()
            except Exception:
                pass
        run_on_ui_thread(_ui)

    def _on_file_downloaded_in_tg(self, fpath):
        if not fpath:
            return
        def _work():
            try:
                if not os.path.exists(fpath):
                    return
                fname = os.path.basename(fpath)
                nums = re.findall(r'\d{6,}', fname)
                if not nums:
                    return
                v = self.vault
                if not v:
                    return
                for (did, mid), rev_list in list(v._revisions.items()):
                    for rev in rev_list:
                        mh = rev.get("media_hash", "")
                        if mh and any(n in mh for n in nums):
                            _copy_path_to_vault(fpath, did, mid, mh)
                            return
            except Exception:
                pass
        threading.Thread(target=_work, daemon=True).start()

    def _reset_cell_appearance(self, did, mid):
        try:
            frag = LaunchActivity.getLastFragment()
            if not isinstance(frag, ChatActivity):
                return
            lv = frag.getChatListView()
            if not lv:
                return
            for i in range(lv.getChildCount()):
                ch = lv.getChildAt(i)
                if ch is None:
                    continue
                cn = ch.getClass().getSimpleName()
                if cn is None or str(cn) != "ChatMessageCell":
                    continue
                mo = ch.getMessageObject()
                if mo and int(mo.getId()) == int(mid) and int(mo.getDialogId()) == int(did):
                    cell_id = id(ch)
                    self._decorated_cells.pop(cell_id, None)
                    ch.setAlpha(jfloat(1.0))
                    try:
                        ch.forceResetMessageObject()
                    except Exception:
                        pass
                    adapter = lv.getAdapter()
                    if adapter:
                        pos = lv.getChildAdapterPosition(ch)
                        if pos >= 0:
                            adapter.notifyItemChanged(pos)
                    break
        except Exception:
            pass

    def _hk_track_user_deletions(self):
        plugin = self
        class H(MethodHook):
            def before_hooked_method(self, param):
                try:
                    ml = param.args[0]
                    if not ml or ml.isEmpty():
                        return
                    mids = [int(x) for x in _jlist(ml)]
                    did = plugin._resolve_did(mids)
                    if not did:
                        try:
                            did = int(param.args[3])
                        except Exception:
                            did = 0
                    already_deleted = []
                    if did and plugin.vault:
                        for mid in mids:
                            if plugin.vault.is_removed(did, mid):
                                already_deleted.append(mid)
                    if already_deleted and did:
                        plugin.vault.purge_removed(did, already_deleted)
                        plugin._invalidate_removed_cache()
                        for m in already_deleted:
                            plugin._vault_purging.add((int(did), int(m)))
                            plugin._vault_purging_mids.add(int(m))
                        def _cleanup_purging(keys=[(int(did), int(m)) for m in already_deleted]):
                            _time.sleep(10)
                            for k in keys:
                                plugin._vault_purging.discard(k)
                                plugin._vault_purging_mids.discard(k[1])
                        threading.Thread(target=_cleanup_purging, daemon=True).start()
                    own_mids = []
                    try:
                        for mid in mids:
                            mo = _get_message_from_chat(did, mid)
                            if mo and mo.messageOwner and bool(getattr(mo.messageOwner, "out", False)):
                                own_mids.append(mid)
                    except Exception:
                        own_mids = mids
                    plugin._mark_my_deletion(own_mids if own_mids else [])
                except Exception:
                    pass
        mc = get_messages_controller()
        if not mc:
            return
        for mm in mc.getClass().getDeclaredMethods():
            if str(mm.getName()) == "deleteMessages" and len(mm.getParameterTypes()) >= 7:
                mm.setAccessible(True)
                u = self.hook_method(mm, H())
                if u:
                    self._unhooks.append(u)
                break

    def _hk_intercept_delete(self):
        plugin = self
        class H(MethodHook):
            def before_hooked_method(self, param):
                if not plugin._gs("save_deleted", True):
                    return
                try:
                    did = int(param.args[0])
                    mids = [int(x) for x in _jlist(param.args[1])]
                    if not mids:
                        return
                    purged = []
                    other = []
                    for m in mids:
                        if (did, m) in plugin._vault_purging:
                            purged.append(m)
                        elif not plugin._is_my_deletion(m):
                            skip_own = plugin._gs("track_own_deletions", False)
                            if skip_own and _is_out_message(did, m):
                                pass
                            else:
                                other.append(m)
                    my = [m for m in mids if plugin._is_my_deletion(m)]
                    pass_through = purged + my
                    if not other and not pass_through:
                        return
                    if other:
                        run_on_ui_thread(lambda: plugin._save_read_states_bulk(did, other))
                        data_map = _collect_deleted_text(did, other)
                        plugin.vault.mark_removed(did, other, data_map)
                        if plugin._gs("save_attachments", False):
                            threading.Thread(target=lambda: _save_attachments_for_mids(did, other), daemon=True).start()
                        plugin._invalidate_removed_cache()
                        run_on_ui_thread(plugin._refresh_cells)
                    if pass_through:
                        fl = ArrayList()
                        for m in pass_through:
                            fl.add(Integer(m))
                        param.args[1] = fl
                    else:
                        param.setResult(None)
                except Exception as e:
                    log(f"[MAX] intercept: {e}")
        MSC = find_class("org.telegram.messenger.MessagesStorage")
        for mm in MSC.getClass().getDeclaredMethods():
            if str(mm.getName()) == "markMessagesAsDeletedInternal" and len(mm.getParameterTypes()) == 5:
                mm.setAccessible(True)
                u = self.hook_method(mm, H())
                if u:
                    self._unhooks.append(u)
                break

    def _hk_suppress_delete_ui(self):
        plugin = self
        class H(MethodHook):
            def before_hooked_method(self, param):
                if not plugin._gs("save_deleted", True):
                    return
                try:
                    ml = param.args[0]
                    if not ml or ml.isEmpty():
                        return
                    mids = [int(x) for x in _jlist(ml)]
                    try:
                        f = _ff(CA_CLS, "dialog_id")
                        did = int(f.get(param.thisObject)) if f else 0
                    except Exception:
                        did = 0
                    purged = []
                    other = []
                    for m in mids:
                        if did and (did, m) in plugin._vault_purging:
                            purged.append(m)
                        elif not plugin._is_my_deletion(m):
                            skip_own = plugin._gs("track_own_deletions", False)
                            if skip_own and _is_out_message(did, m):
                                pass
                            else:
                                other.append(m)
                    my = [m for m in mids if plugin._is_my_deletion(m)]
                    pass_through = purged + my
                    if not other and not pass_through:
                        return
                    if not other and pass_through:
                        return
                    if pass_through:
                        fl = ArrayList()
                        for m in pass_through:
                            fl.add(Integer(m))
                        param.args[0] = fl
                    else:
                        param.args[0] = ArrayList()
                    try:
                        f = _ff(CA_CLS, "dialog_id")
                        did = int(f.get(param.thisObject)) if f else 0
                        if did and other:
                            plugin._save_read_states_bulk(did, other)
                            data_map = _collect_deleted_text(did, other)
                            plugin.vault.mark_removed(did, other, data_map)
                            plugin._invalidate_removed_cache()
                            if plugin._gs("save_attachments", False):
                                att_data = []
                                ca = param.thisObject
                                msgs_field = _ff(CA_CLS, "messages")
                                chat_msgs = msgs_field.get(ca) if msgs_field else None
                                if chat_msgs:
                                    other_set = set(other)
                                    for i in range(chat_msgs.size()):
                                        mo = chat_msgs.get(i)
                                        if mo and int(mo.getId()) in other_set:
                                            att_data.append((mo.messageOwner, did, int(mo.getId())))
                                if att_data:
                                    def _bg_copy(data=att_data):
                                        for msg, d, m in data:
                                            try:
                                                _ensure_attach_saved(msg, d, m, _get_media_hash(msg))
                                            except Exception:
                                                pass
                                    threading.Thread(target=_bg_copy, daemon=True).start()
                            plugin._refresh_cells()
                    except Exception:
                        pass
                except Exception:
                    pass
        m = _fm(CA_CLS, "processDeletedMessages", 4)
        if m:
            u = self.hook_method(m, H())
            if u:
                self._unhooks.append(u)

    def _hk_allow_delete(self):
        plugin = self
        class H(MethodHook):
            def before_hooked_method(self, param):
                v = plugin.vault
                if not v or not v._removed:
                    return
                try:
                    mo = param.thisObject
                    did = int(mo.getDialogId())
                    if did not in plugin._removed_dids:
                        return
                    if (did, int(mo.getId())) in v._removed:
                        param.setResult(Boolean(True))
                except Exception:
                    pass
        MOC = find_class("org.telegram.messenger.MessageObject")
        CC = find_class("org.telegram.tgnet.TLRPC$Chat")
        m = MOC.getClass().getDeclaredMethod("canDeleteMessage", Boolean.TYPE, CC)
        m.setAccessible(True)
        u = self.hook_method(m, H())
        if u:
            self._unhooks.append(u)

    def _get_opacity_val(self):
        idx = _int(self._gs("del_opacity_level", 2), 2)
        return (1.0, 0.8, 0.6, 0.4, 0.2)[max(0, min(idx, 4))]

    def _hk_measure_time(self):
        plugin = self
        _opacity_cells = set()
        _jhash = jclass("java.lang.System").identityHashCode
        class H(MethodHook):
            def after_hooked_method(self, param):
                v = plugin.vault
                cell = param.thisObject
                mo = param.args[0]
                cell_id = int(_jhash(cell))
                if not v or not v._removed or not mo:
                    _opacity_cells.discard(cell_id)
                    return
                try:
                    if (int(mo.getDialogId()), int(mo.getId())) in v._removed:
                        _opacity_cells.add(cell_id)
                        plugin._stamp_cell(cell, mo)
                    else:
                        _opacity_cells.discard(cell_id)
                except Exception:
                    _opacity_cells.discard(cell_id)
                if len(_opacity_cells) > 500:
                    _opacity_cells.clear()
        class SetAlphaGuard(MethodHook):
            _cached_target = [0.6]
            _n = [0]
            def before_hooked_method(self, param):
                a = float(param.args[0])
                if a < 0.95:
                    return
                cell_id = int(_jhash(param.thisObject))
                if cell_id not in _opacity_cells:
                    return
                self._n[0] += 1
                if self._n[0] & 63 == 0:
                    if not plugin._gs("del_show_opacity", True):
                        self._cached_target[0] = 1.0
                    else:
                        self._cached_target[0] = plugin._get_opacity_val()
                target = self._cached_target[0]
                if target < 1.0:
                    param.args[0] = jfloat(target)
        CLC = find_class("org.telegram.ui.Cells.ChatMessageCell")
        MOC = find_class("org.telegram.messenger.MessageObject")
        m = CLC.getClass().getDeclaredMethod("measureTime", MOC)
        m.setAccessible(True)
        u = self.hook_method(m, H())
        if u:
            self._unhooks.append(u)
        cmc = CLC.getClass()
        try:
            sa_method = cmc.getMethod("setAlpha", Float.TYPE)
            sa_method.setAccessible(True)
            u = self.hook_method(sa_method, SetAlphaGuard())
            if u:
                self._unhooks.append(u)
        except Exception:
            pass

    def _hk_restore_loaded(self):
        pass

    def _hk_edit_capture(self):
        plugin = self
        _EDIT_NAMES = frozenset(["TL_updateEditMessage", "TL_updateEditChannelMessage"])
        _NEW_NAMES = frozenset(["TL_updateNewMessage", "TL_updateNewChannelMessage"])
        _INTERESTING = _EDIT_NAMES | _NEW_NAMES
        class H(MethodHook):
            def after_hooked_method(self, param):
                v = plugin.vault
                if not v:
                    return
                try:
                    upds_obj = param.args[0]
                    if not upds_obj:
                        return
                    upd_list = getattr(upds_obj, "updates", None)
                    if not upd_list:
                        return
                    save_edits = plugin._gs("save_edits", True)
                    save_deleted = plugin._gs("save_deleted", True)
                    if not save_edits and not save_deleted:
                        return
                    it = upd_list.iterator()
                    while it.hasNext():
                        upd = it.next()
                        cn = str(upd.getClass().getSimpleName())
                        if cn not in _INTERESTING:
                            continue
                        if save_edits and cn in _NEW_NAMES:
                            try:
                                msg = upd.message
                                if msg and not bool(getattr(msg, "out", False)):
                                    did = _peer_did(msg)
                                    mid = int(msg.id)
                                    if did and mid:
                                        v.save_message_version(did, mid, msg, is_original=True)
                                        if plugin._gs("save_attachments", False):
                                            _ensure_attach_saved(msg, did, mid, _get_media_hash(msg))
                            except Exception:
                                pass
                        elif save_edits and cn in _EDIT_NAMES:
                            try:
                                msg = upd.message
                                if not msg or bool(getattr(msg, "out", False)):
                                    continue
                                did = _peer_did(msg)
                                mid = int(msg.id)
                                if not did or not mid:
                                    continue
                                save_att = plugin._gs("save_attachments", False)
                                if not v.has_revisions(did, mid):
                                    mo = _get_message_from_chat(did, mid)
                                    if mo and mo.messageOwner:
                                        old_msg = mo.messageOwner
                                        v.save_message_version(did, mid, old_msg, is_original=True)
                                        if save_att:
                                            _ensure_attach_saved(old_msg, did, mid, _get_media_hash(old_msg))
                                v.save_message_version(did, mid, msg, is_original=False)
                                if save_att:
                                    _ensure_attach_saved(msg, did, mid, _get_media_hash(msg))
                            except Exception:
                                pass
                except Exception:
                    pass
        MCC = find_class("org.telegram.messenger.MessagesController")
        for mm in MCC.getClass().getDeclaredMethods():
            if str(mm.getName()) == "processUpdates" and len(mm.getParameterTypes()) >= 2:
                mm.setAccessible(True)
                u = self.hook_method(mm, H())
                if u:
                    self._unhooks.append(u)
                break

    def _hk_story_viewer_prompt(self):
        plugin = self
        try:
            sv_cls = jclass("org.telegram.ui.Stories.StoryViewer").getClass()
            class OpenHook(MethodHook):
                def before_hooked_method(self, param):
                    if plugin._story_bypass:
                        return
                    if not plugin._gs("story_ghost_prompt", False):
                        return
                    if plugin._is_ghost():
                        plugin._story_viewer_open = True
                        return
                    the_method = param.method
                    the_obj = param.thisObject
                    the_args = list(param.args)
                    param.setResult(None)
                    def show_dialog():
                        try:
                            frag = get_last_fragment()
                            if not frag or not frag.getParentActivity():
                                plugin._do_open_story(the_method, the_obj, the_args, False)
                                return
                            ctx = frag.getParentActivity()
                            def on_yes(dlg, w):
                                dlg.dismiss()
                                plugin._enable_ghost_for_stories()
                                plugin._do_open_story(the_method, the_obj, the_args, True)
                            def on_no(dlg, w):
                                dlg.dismiss()
                                plugin._do_open_story(the_method, the_obj, the_args, False)
                            d = AlertDialogBuilder(ctx)
                            d.set_title(L.g("story_dlg_title"))
                            d.set_message(L.g("story_dlg_msg"))
                            d.set_positive_button(L.g("yes"), on_yes)
                            d.set_negative_button(L.g("no"), on_no)
                            d.show()
                        except Exception:
                            plugin._do_open_story(the_method, the_obj, the_args, False)
                    run_on_ui_thread(show_dialog)
            class CloseHook(MethodHook):
                def after_hooked_method(self, param):
                    if plugin._story_viewer_open:
                        plugin._story_viewer_open = False
                        if plugin._ghost_for_stories:
                            def delayed_disable():
                                _time.sleep(1.0)
                                if not plugin._story_viewer_open:
                                    run_on_ui_thread(plugin._disable_ghost_after_stories)
                            threading.Thread(target=delayed_disable, daemon=True).start()
            for mm in sv_cls.getDeclaredMethods():
                nm = str(mm.getName())
                if nm == "open" or nm == "openFor":
                    mm.setAccessible(True)
                    u = self.hook_method(mm, OpenHook())
                    if u:
                        self._unhooks.append(u)
            for close_name in ["close", "dismiss"]:
                for pc in [1, 0]:
                    m = _fm(sv_cls, close_name, pc)
                    if m:
                        u = self.hook_method(m, CloseHook())
                        if u:
                            self._unhooks.append(u)
        except Exception as e:
            log(f"[MAX] storyPrompt: {e}")

    def _hk_ctx_menu(self):
        plugin = self
        class H(MethodHook):
            def after_hooked_method(self, param):
                try:
                    mo = param.args[0]
                    plugin._ctx_msg = mo
                    did, mid = mo.getDialogId(), mo.getId()
                    icons, items, options = param.args[1], param.args[2], param.args[3]
                    if plugin._gs("save_edits", True) and plugin.vault.has_revisions(did, mid):
                        items.add(0, L.g("edit_history"))
                        options.add(0, Integer(_OPT_REVISIONS))
                        try:
                            from org.telegram.messenger import R as TGR
                            icons.add(0, Integer(TGR.drawable.menu_premium_clock))
                        except Exception:
                            icons.add(0, Integer(0))
                    is_once = False
                    try:
                        is_once = mo.isSecret() or mo.isSecretMedia() or mo.isVoiceOnce() or mo.isRoundOnce()
                    except Exception:
                        pass
                    if (plugin._chk("ghost_no_read") or is_once) and not mo.isOut():
                        items.add(0, L.g("mark_read"))
                        options.add(0, Integer(_OPT_MARK_READ))
                        try:
                            from org.telegram.messenger import R as TGR
                            icons.add(0, Integer(TGR.drawable.msg_markread))
                        except Exception:
                            icons.add(0, Integer(0))
                    if plugin._gs("keep_once", False) and is_once:
                        try:
                            from org.telegram.messenger import R as TGR
                            items.add(L.g("forward"))
                            options.add(Integer(2))
                            icons.add(Integer(TGR.drawable.msg_forward))
                            items.add(L.g("save_media"))
                            options.add(Integer(4))
                            icons.add(Integer(TGR.drawable.msg_gallery))
                        except Exception:
                            pass
                except Exception:
                    pass
        m = _fm(CA_CLS, "fillMessageMenu", 4)
        if m:
            u = self.hook_method(m, H())
            if u:
                self._unhooks.append(u)

    def _hk_ctx_action(self):
        plugin = self
        class H(MethodHook):
            def before_hooked_method(self, param):
                try:
                    opt = int(param.args[0])
                    mo = plugin._ctx_msg
                    if not mo:
                        try:
                            fd = _ff(CA_CLS, "selectedObject")
                            if fd:
                                mo = fd.get(param.thisObject)
                        except Exception:
                            pass
                    if not mo:
                        return
                    if opt == _OPT_REVISIONS:
                        plugin._show_revisions(mo.getDialogId(), mo.getId())
                        param.setResult(None)
                    elif opt == _OPT_MARK_READ:
                        plugin._send_read_for_dialog(mo.getDialogId(), mo.getId())
                        param.setResult(None)
                except Exception:
                    pass
        m = _fm(CA_CLS, "processSelectedOption", 1)
        if m:
            u = self.hook_method(m, H())
            if u:
                self._unhooks.append(u)

    def _hk_outgoing_params(self):
        plugin = self
        class H(MethodHook):
            def before_hooked_method(self, param):
                try:
                    p = param.args[0]
                    if p is None:
                        return
                    reply = getattr(p, "replyToMsg", None)
                    quote = getattr(p, "replyQuote", None)
                    top = getattr(p, "replyToTopMsg", None)
                    if reply or quote:
                        try:
                            did = reply.getDialogId() if reply else quote.message.getDialogId()
                            mid = reply.getId() if reply else quote.message.getId()
                        except Exception:
                            did = mid = 0
                        if did and mid and plugin.vault and plugin.vault.is_removed(did, mid):
                            try:
                                acc = UserConfig.selectedAccount
                                uid = quote.peerId if quote else reply.getSenderId()
                                sender = MessagesController.getInstance(acc).getUser(Long.valueOf(uid))
                                if sender:
                                    body = str(quote.getText()) if quote else str(reply.messageText or "")
                                    hdr = str(sender.first_name or "")
                                    header = f"{hdr}\n{body}\n"
                                    is_media = bool(getattr(p, "photo", None) or getattr(p, "document", None) or getattr(p, "path", None))
                                    existing = (p.caption if is_media else p.message) or ""
                                    combined = header + existing
                                    shift = len(header)
                                    new_ents = ArrayList()
                                    bq = TLRPC.TL_messageEntityBlockquote()
                                    bq.offset = 0
                                    bq.length = len(header) - 1
                                    try:
                                        bq.collapsed = True
                                    except Exception:
                                        pass
                                    new_ents.add(bq)
                                    mn = TLRPC.TL_inputMessageEntityMentionName()
                                    iu = TLRPC.TL_inputUser()
                                    iu.user_id = sender.id
                                    iu.access_hash = sender.access_hash
                                    mn.user_id = iu
                                    mn.offset = 0
                                    mn.length = len(hdr)
                                    new_ents.add(mn)
                                    if p.entities:
                                        for i in range(p.entities.size()):
                                            ent = p.entities.get(i)
                                            try:
                                                ent.offset = int(ent.offset) + shift
                                            except Exception:
                                                continue
                                            new_ents.add(ent)
                                    p.entities = new_ents
                                    if is_media:
                                        p.caption = combined
                                    else:
                                        p.message = combined
                                    p.replyToMsg = top if top else None
                                    p.replyToTopMsg = top if top else None
                                    p.replyQuote = None
                            except Exception:
                                pass
                    if plugin._is_ghost():
                        mode = _int(plugin._gs("silent_send", 0), 0)
                        if mode == 1 or (mode == 2 and plugin._is_ghost()):
                            p.notify = False
                    if plugin._gs("fake_premium", False) and p.entities:
                        try:
                            acc = UserConfig.selectedAccount
                            gids = set()
                            peer_val = getattr(p, "peer", 0)
                            if peer_val and int(peer_val) < 0:
                                try:
                                    cf = MessagesController.getInstance(acc).getChatFull(jlong(-int(peer_val)))
                                    if cf and cf.emojiset:
                                        ss = MediaDataController.getInstance(acc).getGroupStickerSetById(cf.emojiset)
                                        if ss and ss.documents:
                                            for doc in _jlist(ss.documents):
                                                gids.add(int(doc.id))
                                except Exception:
                                    pass
                            ents = p.entities
                            for i in range(ents.size()):
                                ent = ents.get(i)
                                if isinstance(ent, TLRPC.TL_messageEntityCustomEmoji):
                                    if int(ent.document_id) not in gids:
                                        ue = TLRPC.TL_messageEntityTextUrl()
                                        ue.offset = ent.offset
                                        ue.length = ent.length
                                        ue.url = f"tg://emoji?id={ent.document_id}"
                                        ents.set(i, ue)
                        except Exception:
                            pass
                except Exception:
                    pass
        SMH = find_class("org.telegram.messenger.SendMessagesHelper")
        SMP = find_class("org.telegram.messenger.SendMessagesHelper$SendMessageParams")
        m = SMH.getClass().getDeclaredMethod("sendMessage", SMP)
        m.setAccessible(True)
        u = self.hook_method(m, H())
        if u:
            self._unhooks.append(u)

    def _hk_filter_incoming(self):
        plugin = self
        class H(MethodHook):
            def before_hooked_method(self, param):
                if not plugin._gs("filters_enabled", True) or not plugin.vault or not plugin.vault._rx_cache:
                    return
                try:
                    msgs = param.args[0]
                    if not msgs or msgs.isEmpty():
                        return
                    to_rm = []
                    for i in range(msgs.size()):
                        m = msgs.get(i)
                        if m and not m.isOut() and plugin.vault.rx_match(m):
                            to_rm.append(i)
                    for idx in reversed(to_rm):
                        msgs.remove(idx)
                except Exception:
                    pass
        m = _fm(CA_CLS, "processNewMessages", 2)
        if m:
            u = self.hook_method(m, H())
            if u:
                self._unhooks.append(u)

    def _hk_notif_guard(self):
        plugin = self
        class H(MethodHook):
            def before_hooked_method(self, param):
                if plugin._gs("save_deleted", True):
                    try:
                        if not bool(param.args[1]):
                            param.setResult(None)
                    except Exception:
                        pass
        try:
            NCC = find_class("org.telegram.messenger.NotificationsController")
            m = _fm(NCC.getClass(), "removeDeletedMessagesFromNotifications", 2)
            if m:
                u = self.hook_method(m, H())
                if u:
                    self._unhooks.append(u)
        except Exception:
            pass

    def _hk_stories(self):
        plugin = self
        try:
            stories_cls = None
            for name in ["org.telegram.messenger.StoriesController", "org.telegram.ui.Stories.StoriesController"]:
                try:
                    stories_cls = jclass(name).getClass()
                    break
                except Exception:
                    pass
            if stories_cls:
                class BH(MethodHook):
                    def before_hooked_method(self, param):
                        if plugin._is_ghost() and plugin._gs("ghost_no_read_stories", True):
                            param.setResult(Boolean(False))
                class BV(MethodHook):
                    def before_hooked_method(self, param):
                        if plugin._is_ghost() and plugin._gs("ghost_no_read_stories", True):
                            param.setResult(None)
                for mname in ["markStoryAsRead", "markStoriesAsRead"]:
                    m = _fm(stories_cls, mname)
                    if m:
                        u = self.hook_method(m, BH())
                        if u:
                            self._unhooks.append(u)
                m = _fm(stories_cls, "incrementStoryViews")
                if m:
                    u = self.hook_method(m, BV())
                    if u:
                        self._unhooks.append(u)
        except Exception as e:
            log(f"[MAX] hookStories: {e}")

    def _hk_keep_once(self):
        plugin = self
        class MDH(MethodHook):
            def before_hooked_method(self, param):
                if plugin._gs("keep_once", False):
                    try:
                        mo = param.args[0]
                        if mo:
                            mo.forceExpired = False
                    except Exception:
                        pass
                    param.setResult(None)
        class MRH(MethodHook):
            def before_hooked_method(self, param):
                if plugin._gs("keep_once", False):
                    param.setResult(None)
        m = _fm(CA_CLS, "sendSecretMediaDelete", 1)
        if m:
            u = self.hook_method(m, MDH())
            if u:
                self._unhooks.append(u)
        m = _fm(CA_CLS, "sendSecretMessageRead", 2)
        if m:
            u = self.hook_method(m, MRH())
            if u:
                self._unhooks.append(u)
        try:
            svp = jclass("org.telegram.ui.SecretVoicePlayer").getClass()
            class DH(MethodHook):
                def before_hooked_method(self, param):
                    if plugin._gs("keep_once", False):
                        try:
                            f = _ff(svp, "closeAction")
                            if f:
                                f.set(param.thisObject, None)
                        except Exception:
                            pass
            m = _fm(svp, "dismiss", 0)
            if m:
                u = self.hook_method(m, DH())
                if u:
                    self._unhooks.append(u)
        except Exception:
            pass
        mc = get_messages_controller()
        if mc:
            class CT(MethodReplacement):
                def replace_hooked_method(self, param):
                    if plugin._gs("keep_once", False):
                        return Boolean(False)
                    try:
                        return param.invokeOriginalMethod()
                    except Exception:
                        return Boolean(False)
            m = _fm(mc.getClass(), "checkDeletingTask", 1)
            if m:
                u = self.hook_method(m, CT())
                if u:
                    self._unhooks.append(u)

    def _hk_screenshots(self):
        plugin = self
        try:
            fsr = jclass("org.telegram.messenger.FlagSecureReason").getClass()
            class AH(MethodHook):
                def before_hooked_method(self, param):
                    if plugin._gs("allow_screenshots", False):
                        param.setResult(None)
            m = _fm(fsr, "attach")
            if m:
                u = self.hook_method(m, AH())
                if u:
                    self._unhooks.append(u)
        except Exception:
            pass

    def _hk_noforward(self):
        plugin = self
        class RF(MethodHook):
            def before_hooked_method(self, param):
                if plugin._gs("bypass_noforward", False):
                    param.setResult(Boolean(False))
        class RT(MethodHook):
            def before_hooked_method(self, param):
                if plugin._gs("bypass_noforward", False):
                    param.setResult(Boolean(True))
        MCC = find_class("org.telegram.messenger.MessagesController")
        CC = find_class("org.telegram.tgnet.TLRPC$Chat")
        for name in ["isChatNoForwards", "isUserNoForwards", "isPeerNoForwards"]:
            for sig in [[CC], [Long.TYPE], []]:
                try:
                    m = MCC.getClass().getDeclaredMethod(name, *sig)
                    m.setAccessible(True)
                    u = self.hook_method(m, RF())
                    if u:
                        self._unhooks.append(u)
                except Exception:
                    pass
        try:
            MOC = jclass("org.telegram.messenger.MessageObject").getClass()
            m = _fm(MOC, "canForwardMessage")
            if m:
                u = self.hook_method(m, RT())
                if u:
                    self._unhooks.append(u)
        except Exception:
            pass
        m = _fm(CA_CLS, "hasSelectedNoforwardsMessage")
        if m:
            u = self.hook_method(m, RF())
            if u:
                self._unhooks.append(u)
        class DropNoFwd(MethodHook):
            def before_hooked_method(self, param):
                if not plugin._gs("bypass_noforward", False):
                    return
                try:
                    rq = param.args[0]
                    if rq and hasattr(rq, "drop_author"):
                        rq.drop_author = True
                    if rq and hasattr(rq, "noforwards"):
                        rq.noforwards = False
                except Exception:
                    pass
        try:
            SMH = find_class("org.telegram.messenger.SendMessagesHelper")
            for mm in SMH.getClass().getDeclaredMethods():
                if str(mm.getName()) == "sendMessage" and len(mm.getParameterTypes()) == 1:
                    ptype = str(mm.getParameterTypes()[0].getName())
                    if "SendMessageParams" in ptype:
                        mm.setAccessible(True)
                        class DropAuthor(MethodHook):
                            def before_hooked_method(self, param):
                                if not plugin._gs("bypass_noforward", False):
                                    return
                                try:
                                    p = param.args[0]
                                    if p and hasattr(p, "noforwards"):
                                        p.noforwards = False
                                except Exception:
                                    pass
                        u = self.hook_method(mm, DropAuthor())
                        if u:
                            self._unhooks.append(u)
                        break
        except Exception:
            pass
        try:
            class FwdHook(MethodHook):
                def before_hooked_method(self, param):
                    if not plugin._gs("bypass_noforward", False):
                        return
                    try:
                        rq = param.args[0]
                        if hasattr(rq, "drop_author"):
                            rq.drop_author = True
                    except Exception:
                        pass
            mc = get_messages_controller()
            if mc:
                for mm in mc.getClass().getDeclaredMethods():
                    if str(mm.getName()) == "sendMessagesForward":
                        mm.setAccessible(True)
                        u = self.hook_method(mm, FwdHook())
                        if u:
                            self._unhooks.append(u)
        except Exception:
            pass
        self._hook_reforward()

    def _is_noforward_msg(self, mo):
        try:
            owner = mo.messageOwner
            if not owner:
                return False
            if bool(getattr(owner, "noforwards", False)):
                return True
            try:
                if bool(mo.hasRevealedExtendedMedia()):
                    return True
            except Exception:
                pass
            if _int(getattr(owner, "ttl", 0)) != 0:
                return True
            acc = int(UserConfig.selectedAccount)
            mc = MessagesController.getInstance(acc)
            did = int(mo.getDialogId())
            if did == 489000:
                return True
            chat = mc.getChat(Long(did))
            if chat is None:
                chat = mc.getChat(Long(-did))
            if chat is not None:
                if bool(getattr(chat, "noforwards", False)):
                    return True
                migrated = getattr(chat, "migrated_to", None)
                if migrated:
                    cid = _int(getattr(migrated, "channel_id", 0))
                    if cid:
                        chat2 = mc.getChat(Long(cid))
                        if chat2 and bool(getattr(chat2, "noforwards", False)):
                            return True
            elif did > 0:
                try:
                    uf = mc.getUserFull(jlong(did))
                    if uf and (bool(getattr(uf, "noforwards_my_enabled", False)) or bool(getattr(uf, "noforwards_peer_enabled", False))):
                        return True
                except Exception:
                    pass
            try:
                if mo.isSecret() or mo.isSecretMedia() or mo.isVoiceOnce() or mo.isRoundOnce():
                    return True
            except Exception:
                pass
            if self.vault and self.vault.is_removed_obj(mo):
                return True
            return False
        except Exception:
            return False

    def _get_file_path(self, mo):
        try:
            acc = int(UserConfig.selectedAccount)
            fl = FileLoader.getInstance(acc)
            owner = mo.messageOwner
            attach_path = getattr(owner, "attachPath", None)
            if attach_path:
                ap = str(attach_path)
                if ap:
                    JFile = jclass("java.io.File")
                    af = JFile(ap)
                    if af.exists() and not af.isDirectory():
                        return ap
            f = fl.getPathToMessage(owner)
            if f and f.exists() and not f.isDirectory():
                return str(f.getAbsolutePath())
            doc = mo.getDocument()
            if doc:
                for b in [False, True]:
                    f = fl.getPathToAttach(doc, b)
                    if f and f.exists() and not f.isDirectory():
                        return str(f.getAbsolutePath())
            try:
                photo = MessageObject.getPhoto(owner)
                if photo:
                    ps = FileLoader.getClosestPhotoSizeWithSize(photo.sizes, AndroidUtilities.getPhotoSize())
                    if ps:
                        for b in [False, True]:
                            f = fl.getPathToAttach(ps, b)
                            if f and f.exists() and not f.isDirectory():
                                return str(f.getAbsolutePath())
            except Exception:
                pass
        except Exception:
            pass
        return ""

    def _map_document(self, acc, mo, doc, file_path):
        try:
            new_doc = TLRPC.TL_document()
            new_doc.flags = doc.flags
            new_doc.file_reference = jarray(jbyte)(0)
            new_doc.dc_id = 0
            new_doc.user_id = doc.user_id
            new_doc.version = doc.version
            new_doc.mime_type = str(doc.mime_type or "")
            new_doc.file_name = str(getattr(doc, "file_name", "") or "")
            new_doc.file_name_fixed = str(getattr(doc, "file_name_fixed", "") or "")
            new_doc.date = int(ConnectionsManager.getInstance(acc).getCurrentTime())
            JFile = jclass("java.io.File")
            jf = JFile(file_path)
            new_doc.size = jf.length() if jf.exists() else doc.size
            new_doc.thumbs = doc.thumbs
            new_doc.video_thumbs = doc.video_thumbs
            new_doc.localThumbPath = getattr(doc, "localThumbPath", None)
            new_doc.attributes = doc.attributes
            new_doc.localPath = file_path
            try:
                if mo.isGif() or mo.isNewGif():
                    new_doc.mime_type = "video/mp4"
            except Exception:
                pass
            return new_doc
        except Exception as e:
            log(f"[MAX] mapDoc: {e}")
            return doc

    def _map_photo(self, acc, photo, file_path):
        try:
            smh = SendMessagesHelper.getInstance(acc)
            new_photo = smh.generatePhotoSizes(file_path, None)
            if new_photo:
                new_photo.flags = photo.flags
                new_photo.has_stickers = photo.has_stickers
                new_photo.date = int(ConnectionsManager.getInstance(acc).getCurrentTime())
                new_photo.geo = getattr(photo, "geo", None)
                new_photo.caption = getattr(photo, "caption", None)
                return new_photo
        except Exception as e:
            log(f"[MAX] mapPhoto: {e}")
        return photo

    def _send_non_media(self, ai, mo, peer, notify, schedule_date, reply_top):
        try:
            acc = int(UserConfig.selectedAccount)
            owner = mo.messageOwner
            media = getattr(owner, "media", None)
            mn = str(media.getClass().getSimpleName()) if media else ""
            smh = ai.getSendMessagesHelper()
            SMP = jclass("org.telegram.messenger.SendMessagesHelper$SendMessageParams")
            if any(x in mn for x in ["Geo", "Venue"]):
                smh.sendMessage(SMP.of(media, jlong(peer), reply_top, reply_top, None, None, bool(notify), int(schedule_date), 0))
            elif "Contact" in mn:
                u = TLRPC.TL_user()
                u.id = int(getattr(media, "user_id", 0))
                u.first_name = str(getattr(media, "first_name", "") or "")
                u.last_name = str(getattr(media, "last_name", "") or "")
                u.phone = str(getattr(media, "phone_number", "") or "")
                smh.sendMessage(SMP.of(u, jlong(peer), reply_top, reply_top, None, None, bool(notify), int(schedule_date), 0))
            elif "Poll" in mn:
                smh.sendMessage(SMP.of(media, jlong(peer), reply_top, reply_top, None, None, bool(notify), int(schedule_date), 0))
            elif "Document" in mn and getattr(media, "document", None):
                doc = media.document
                is_stk = bool(MessageObject.isStickerMessage(owner)) or bool(MessageObject.isAnimatedStickerMessage(owner))
                try:
                    is_stk = is_stk or bool(MessageObject.isVideoSticker(doc))
                except Exception:
                    pass
                if is_stk:
                    try:
                        smh.sendSticker(doc, None, jlong(peer), reply_top, reply_top, None, None, None, bool(notify), int(schedule_date), 0, False, mo, None, 0, jlong(0), jlong(0), None)
                    except Exception:
                        try:
                            smh.sendSticker(doc, None, jlong(peer), reply_top, reply_top, None, None, None, bool(notify), int(schedule_date), 0, False, mo, None, 0, jlong(0), jlong(0))
                        except Exception:
                            pass
                else:
                    path = self._get_file_path(mo)
                    if path:
                        mapped = self._map_document(acc, mo, doc, path)
                        caption = str(owner.message or "")
                        ents = getattr(owner, "entities", None)
                        if ents is not None and not isinstance(ents, ArrayList):
                            ents = _to_al(_jlist(ents))
                        has_spoiler = False
                        try:
                            has_spoiler = bool(mo.hasMediaSpoilers()) and not bool(mo.isHiddenSensitive())
                        except Exception:
                            pass
                        invert = bool(getattr(owner, "invert_media", False))
                        smh.sendMessage(SMP.of(mapped, None, path, jlong(peer), reply_top, reply_top, caption, ents, None, None, bool(notify), int(schedule_date), 0, 0, mo, None, False, has_spoiler))
                    else:
                        text = str(owner.message or "")
                        if text:
                            ents = getattr(owner, "entities", None)
                            if ents is not None and not isinstance(ents, ArrayList):
                                ents = _to_al(_jlist(ents))
                            smh.sendMessage(SMP.of(text, jlong(peer), reply_top, reply_top, None, True, ents, None, None, bool(notify), int(schedule_date), 0, None, False))
            else:
                text = str(owner.message or "")
                if text:
                    ents = getattr(owner, "entities", None)
                    if ents is not None and not isinstance(ents, ArrayList):
                        ents = _to_al(_jlist(ents))
                    smh.sendMessage(SMP.of(text, jlong(peer), reply_top, reply_top, None, True, ents, None, None, bool(notify), int(schedule_date), 0, None, False))
        except Exception as e:
            log(f"[MAX] non-media fwd: {e}")

    def _send_message_copies(self, ai, messages, peer, notify, schedule_date, reply_top):
        grouped = {}
        singles = []
        for mo in messages:
            gid = int(mo.getGroupId())
            if gid != 0:
                grouped.setdefault(gid, []).append(mo)
            else:
                singles.append(mo)
        for group in grouped.values():
            try:
                self._send_group(ai, group, peer, notify, schedule_date, reply_top)
            except Exception as e:
                log(f"[MAX] group send: {e}")
        if singles:
            try:
                self._send_group(ai, singles, peer, notify, schedule_date, reply_top)
            except Exception as e:
                log(f"[MAX] singles send: {e}")

    def _send_group(self, ai, messages, peer, notify, schedule_date, reply_top):
        if not messages:
            return
        acc = int(UserConfig.selectedAccount)
        is_grouped = len(messages) > 1
        media_infos = []
        try:
            SMI = jclass("org.telegram.messenger.SendMessagesHelper$SendingMediaInfo")
        except Exception:
            for mo in messages:
                self._send_non_media(ai, mo, peer, notify, schedule_date, reply_top)
            return
        for i, mo in enumerate(messages):
            owner = mo.messageOwner
            media = getattr(owner, "media", None)
            mn = str(media.getClass().getSimpleName()) if media else ""
            is_photo = "Photo" in mn
            doc = getattr(media, "document", None) if media else None
            is_video = False
            is_doc_file = False
            if "Document" in mn and doc:
                is_stk = False
                try:
                    is_stk = bool(MessageObject.isStickerMessage(owner)) or bool(MessageObject.isAnimatedStickerMessage(owner))
                    try:
                        is_stk = is_stk or bool(MessageObject.isVideoSticker(doc))
                    except Exception:
                        pass
                except Exception:
                    pass
                is_voice = bool(MessageObject.isVoiceMessage(owner))
                is_round = bool(MessageObject.isRoundVideoMessage(owner))
                is_vid = bool(MessageObject.isVideoDocument(doc))
                try:
                    is_vid = is_vid and not bool(MessageObject.isVideoSticker(doc))
                except Exception:
                    pass
                if is_vid:
                    is_video = True
                elif not is_stk and not is_voice and not is_round:
                    is_doc_file = True
            if is_photo or is_video or is_doc_file:
                path = self._get_file_path(mo)
                if not path:
                    self._download_then_send(ai, mo, peer, notify, schedule_date, reply_top)
                    continue
                if is_doc_file and not is_grouped:
                    caption = str(owner.message or "")
                    try:
                        SendMessagesHelper.prepareSendingDocument(ai, path, path, None, caption, None, jlong(peer), reply_top, reply_top, None, None, None, bool(notify), int(schedule_date), None, None, 0, False)
                    except Exception as e:
                        log(f"[MAX] doc copy: {e}")
                    continue
                if is_doc_file:
                    info = SMI()
                    info.path = path
                    if i == 0 and owner.message:
                        info.caption = str(owner.message)
                        ents = getattr(owner, "entities", None)
                        info.entities = _to_al(_jlist(ents)) if ents else ArrayList()
                    info.isVideo = False
                    try:
                        info.forceDocument = True
                    except Exception:
                        pass
                    media_infos.append(info)
                else:
                    info = SMI()
                    info.path = path
                    if i == 0 and owner.message:
                        info.caption = str(owner.message)
                        ents = getattr(owner, "entities", None)
                        info.entities = _to_al(_jlist(ents)) if ents else ArrayList()
                    info.isVideo = bool(is_video)
                    media_infos.append(info)
            else:
                self._send_non_media(ai, mo, peer, notify, schedule_date, reply_top)
        if media_infos:
            al = _to_al(media_infos)
            try:
                SendMessagesHelper.prepareSendingMedia(ai, al, jlong(peer), reply_top, reply_top, None, None, False, bool(is_grouped), None, bool(notify), int(schedule_date), 0, 0, False, None, None, 0, jlong(0), False, jlong(0), jlong(0), None)
            except Exception:
                try:
                    SendMessagesHelper.prepareSendingMedia(ai, al, jlong(peer), reply_top, reply_top, None, None, False, bool(is_grouped), None, bool(notify), int(schedule_date), 0, 0, False, None, None, 0, jlong(0), False, jlong(0), jlong(0))
                except Exception:
                    for info in media_infos:
                        try:
                            p = info.path
                            SendMessagesHelper.prepareSendingDocument(ai, p, p, None, str(getattr(info, "caption", "") or ""), None, jlong(peer), reply_top, reply_top, None, None, None, bool(notify), int(schedule_date), None, None, 0, False)
                        except Exception as e:
                            log(f"[MAX] fallback doc: {e}")

    def _download_then_send(self, ai, mo, peer, notify, schedule_date, reply_top):
        plugin = self
        acc = int(UserConfig.selectedAccount)
        fl = FileLoader.getInstance(acc)
        owner = mo.messageOwner
        media = getattr(owner, "media", None)
        if not media:
            self._send_non_media(ai, mo, peer, notify, schedule_date, reply_top)
            return
        doc = getattr(media, "document", None)
        photo = getattr(media, "photo", None)
        if doc:
            fl.loadFile(doc, mo, 3, 0)
        elif photo:
            try:
                ps = FileLoader.getClosestPhotoSizeWithSize(photo.sizes, AndroidUtilities.getPhotoSize())
                if ps:
                    fl.loadFile(ImageLocation.getForPhoto(ps, photo), mo, None, 3, 0)
            except Exception:
                pass
        def _retry():
            _time.sleep(3)
            path = plugin._get_file_path(mo)
            if path:
                run_on_ui_thread(lambda: plugin._send_group(ai, [mo], peer, notify, schedule_date, reply_top))
            else:
                _time.sleep(5)
                path2 = plugin._get_file_path(mo)
                if path2:
                    run_on_ui_thread(lambda: plugin._send_group(ai, [mo], peer, notify, schedule_date, reply_top))
                else:
                    run_on_ui_thread(lambda: plugin._send_non_media(ai, mo, peer, notify, schedule_date, reply_top))
        threading.Thread(target=_retry, daemon=True).start()

    def _refresh_gift_list(self):
        try:
            import requests
            resp = requests.get(self._gifts_url, timeout=5).json()
            self._apply_gift_list(resp)
        except Exception:
            self._fallback_gift_list()

    def _fallback_gift_list(self):
        try:
            resolve_req = TLRPC.TL_contacts_resolveUsername()
            resolve_req.username = self._gifts_channel_username
            def handle_resolve(response, error):
                if error or not response:
                    self._apply_gift_list({})
                    return
                try:
                    if str(int(response.peer.channel_id)) != self._gifts_channel_id:
                        self._apply_gift_list({})
                        return
                    input_peer = get_messages_controller().getInputPeer(response.chats.get(0))
                    history_req = TLRPC.TL_messages_getHistory()
                    history_req.peer = input_peer
                    history_req.offset_id = 0
                    history_req.offset_date = 0
                    history_req.add_offset = 0
                    history_req.limit = 10
                    history_req.max_id = 0
                    history_req.min_id = 0
                    history_req.hash = 0
                    def handle_history(resp, err):
                        if err or not resp:
                            self._apply_gift_list({})
                            return
                        try:
                            for i in range(resp.messages.size()):
                                msg = resp.messages.get(i)
                                text = str(getattr(msg, "message", "") or "").strip()
                                if not text:
                                    continue
                                try:
                                    data = json.loads(text)
                                except Exception:
                                    continue
                                if isinstance(data, dict):
                                    self._apply_gift_list(data)
                                    return
                        except Exception:
                            pass
                        self._apply_gift_list({})
                    send_request(history_req, RequestCallback(handle_history))
                except Exception:
                    self._apply_gift_list({})
            send_request(resolve_req, RequestCallback(handle_resolve))
        except Exception:
            self._apply_gift_list({})

    def _apply_gift_list(self, resp):
        if not isinstance(resp, dict):
            resp = {}
        gifts_data = resp.get("gifts")
        if not gifts_data:
            if self._gift_list:
                return
            gifts_data = []
        new_list = gifts_data
        new_pack = resp.get("stickerpack", "DeletedGiftsStickers")
        if new_list != self._gift_list or new_pack != self._gift_pack:
            self._gift_list = new_list
            if new_pack:
                self._gift_pack = new_pack

    def _load_gift_stickers(self):
        try:
            acc = int(UserConfig.selectedAccount)
            mdc = MediaDataController.getInstance(acc)
            cached = mdc.getStickerSetByName(self._gift_pack)
            if cached:
                docs = self._extract_sticker_docs(cached)
                if docs:
                    self._gift_sticker_docs = docs
                    self._gift_logged_missing = False
                    return
        except Exception:
            pass
        self._fetch_gift_sticker_pack()

    def _fetch_gift_sticker_pack(self):
        try:
            req = TLRPC.TL_messages_getStickerSet()
            inp = TLRPC.TL_inputStickerSetShortName()
            inp.short_name = self._gift_pack
            req.stickerset = inp
            req.hash = 0
            def cb(resp, err):
                if err or not resp:
                    return
                self._ingest_sticker_set(resp)
            send_request(req, RequestCallback(cb))
        except Exception:
            pass

    def _ingest_sticker_set(self, response):
        if response is None:
            return
        if isinstance(response, TLRPC.TL_messages_stickerSetNotModified):
            return
        if not isinstance(response, TLRPC.TL_messages_stickerSet):
            return
        st = getattr(response, "set", None)
        sn = str(getattr(st, "short_name", "")) if st else ""
        if sn.lower() != self._gift_pack.lower():
            return
        docs = self._extract_sticker_docs(response)
        if docs:
            self._gift_sticker_docs = docs
            self._gift_logged_missing = False

    def _extract_sticker_docs(self, sticker_set_response):
        if sticker_set_response is None:
            return []
        docs = getattr(sticker_set_response, "documents", None)
        if docs is None:
            return []
        return _jlist(docs)

    def _get_preview_sticker(self, index, fallback=None):
        if not self._gift_sticker_docs:
            return fallback
        count = len(self._gift_sticker_docs)
        if index >= 1:
            idx = min(index, count - 1)
        else:
            idx = min(max(0, index), count - 1)
        return self._gift_sticker_docs[idx]

    def _clone_gift_donor(self, donor):
        from org.telegram.tgnet.tl import TL_stars
        clone = TL_stars.TL_starGift()
        for field in [
            "flags", "limited", "sold_out", "birthday", "require_premium",
            "resale_ton_only", "limited_per_user", "peer_color_available",
            "can_upgrade", "auction", "theme_available", "burned", "crafted",
            "id", "gift_id", "sticker", "stars",
            "availability_remains", "availability_total", "availability_resale",
            "convert_stars", "upgrade_stars", "resell_min_stars",
            "first_sale_date", "last_sale_date",
            "per_user_total", "per_user_remains", "locked_until_date",
            "title", "slug", "num",
            "owner_id", "owner_name", "owner_address",
            "availability_issued", "gift_address",
            "theme_peer", "peer_color", "host_id", "released_by",
            "background", "attributes",
            "value_amount", "value_currency", "value_usd_amount",
            "resell_amount", "auction_slug",
            "gifts_per_round", "offer_min_stars",
            "auction_start_date", "upgrade_variants", "craft_chance_permille",
        ]:
            try:
                setattr(clone, field, getattr(donor, field))
            except Exception:
                pass
        return clone

    def _patch_star_gifts(self, response):
        try:
            from org.telegram.tgnet.tl import TL_stars
            if isinstance(response, TL_stars.TL_starGiftsNotModified):
                return None
            if not isinstance(response, TL_stars.TL_starGifts):
                return None
            if not self._gift_list:
                self._refresh_gift_list()
            if not self._gift_list:
                return None
            if not self._gift_sticker_docs:
                self._load_gift_stickers()
                if not self._gift_sticker_docs and not self._gift_logged_missing:
                    self._gift_logged_missing = True
            gifts_list = response.gifts
            if not gifts_list:
                return None
            donor = None
            for i in range(gifts_list.size()):
                c = gifts_list.get(i)
                if isinstance(c, TL_stars.TL_starGift):
                    donor = c
                    break
            if not donor:
                return None
            existing = set()
            for i in range(gifts_list.size()):
                try:
                    existing.add(int(gifts_list.get(i).id))
                except Exception:
                    pass
            to_inject = [g for g in self._gift_list if int(g.get("id", 0)) not in existing]
            if not to_inject:
                return None
            donor_sticker = donor.sticker
            insert_pos = min(11, gifts_list.size())
            for idx, gift in enumerate(to_inject):
                clone = self._clone_gift_donor(donor)
                clone.id = Long.valueOf(str(int(gift["id"])))
                clone.stars = Long.valueOf(str(int(gift.get("price", 50))))
                clone.sticker = self._get_preview_sticker(int(gift.get("sticker_number", 0)), donor_sticker)
                clone.attributes = ArrayList()
                pos = min(insert_pos + idx, gifts_list.size())
                gifts_list.add(pos, clone)
            return response
        except Exception as e:
            log(f"[MAX] patch gifts: {e}")
            return None

    def _hook_reforward(self):
        plugin = self
        class ForwardHook(MethodHook):
            def before_hooked_method(self, param):
                if plugin._forward_copying:
                    return
                if not plugin._gs("bypass_noforward", False):
                    return
                if _int(plugin._gs("fwd_mode", 0)) == 1:
                    return
                try:
                    objects = _jlist(param.args[0])
                    if not objects:
                        return
                    no_fwd = []
                    regular = []
                    for mo in objects:
                        if plugin._is_noforward_msg(mo):
                            no_fwd.append(mo)
                        else:
                            regular.append(mo)
                    if not no_fwd:
                        return
                    peer = int(param.args[1])
                    notify = bool(param.args[4])
                    schedule_date = int(param.args[5])
                    reply_top = None
                    ptypes = param.method.getParameterTypes()
                    for i in range(6, len(param.args)):
                        try:
                            if "MessageObject" in str(ptypes[i].getName()) and param.args[i] is not None:
                                reply_top = param.args[i]
                                break
                        except Exception:
                            pass
                    if regular:
                        param.args[0] = _to_al(regular)
                    else:
                        param.setResult(Integer(0))
                    ai = AccountInstance.getInstance(int(UserConfig.selectedAccount))
                    plugin._forward_copying = True
                    try:
                        plugin._send_message_copies(ai, no_fwd, peer, notify, schedule_date, reply_top)
                    except Exception as e2:
                        log(f"[MAX] copy send: {e2}")
                    finally:
                        plugin._forward_copying = False
                except Exception as e:
                    log(f"[MAX] reforward hook: {e}")
        try:
            SMH_CLS = jclass("org.telegram.messenger.SendMessagesHelper").getClass()
            for method in SMH_CLS.getDeclaredMethods():
                try:
                    types = method.getParameterTypes()
                    if str(method.getName()) == "sendMessage" and len(types) >= 7 and str(types[0].getName()) == "java.util.ArrayList" and str(types[1].getName()) == "long":
                        method.setAccessible(True)
                        u = self.hook_method(method, ForwardHook())
                        if u:
                            self._unhooks.append(u)
                except Exception:
                    pass
        except Exception as e:
            log(f"[MAX] reforward setup: {e}")

    def _is_removed_any(self, mo):
        try:
            did = int(mo.getDialogId())
            mid = int(mo.getId())
            if self.vault and (did, mid) in self.vault._removed:
                return True
            if self.dex and self.dex.loaded:
                try:
                    r = self.dex.call("pyIsRemoved", jlong(did), jint(mid))
                    if r is not None and bool(r):
                        return True
                except Exception:
                    pass
        except Exception:
            pass
        return False

    def _hk_forward_deleted(self):
        plugin = self
        class ForwardDeletedHook(MethodHook):
            def before_hooked_method(self, param):
                if plugin._forward_copying:
                    return
                try:
                    objects = _jlist(param.args[0])
                    if not objects:
                        return
                    deleted = []
                    regular = []
                    for mo in objects:
                        try:
                            if plugin._is_removed_any(mo):
                                deleted.append(mo)
                            else:
                                regular.append(mo)
                        except Exception:
                            regular.append(mo)
                    if not deleted:
                        return
                    peer = int(param.args[1])
                    notify = bool(param.args[4])
                    schedule_date = int(param.args[5])
                    reply_top = None
                    ptypes = param.method.getParameterTypes()
                    for i in range(6, len(param.args)):
                        try:
                            if "MessageObject" in str(ptypes[i].getName()) and param.args[i] is not None:
                                reply_top = param.args[i]
                                break
                        except Exception:
                            pass
                    if regular:
                        param.args[0] = _to_al(regular)
                    else:
                        param.setResult(Integer(0))
                    ai = AccountInstance.getInstance(int(UserConfig.selectedAccount))
                    plugin._forward_copying = True
                    try:
                        plugin._send_message_copies(ai, deleted, peer, notify, schedule_date, reply_top)
                    except Exception as e2:
                        log(f"[MAX] fwd deleted: {e2}")
                    finally:
                        plugin._forward_copying = False
                except Exception as e:
                    log(f"[MAX] fwd deleted hook: {e}")
        try:
            SMH_CLS = jclass("org.telegram.messenger.SendMessagesHelper").getClass()
            for method in SMH_CLS.getDeclaredMethods():
                try:
                    types = method.getParameterTypes()
                    if str(method.getName()) == "sendMessage" and len(types) >= 7 and str(types[0].getName()) == "java.util.ArrayList" and str(types[1].getName()) == "long":
                        method.setAccessible(True)
                        u = self.hook_method(method, ForwardDeletedHook())
                        if u:
                            self._unhooks.append(u)
                except Exception:
                    pass
        except Exception as e:
            log(f"[MAX] fwd deleted setup: {e}")

    def _hk_story_save(self):
        plugin = self
        try:
            hc = jclass("org.telegram.ui.Stories.PeerStoriesView$StoryItemHolder").getClass()
            class AS(MethodHook):
                def before_hooked_method(self, param):
                    if plugin._gs("bypass_story_save", False):
                        param.setResult(Boolean(True))
            m = _fm(hc, "allowScreenshots", 0)
            if m:
                u = self.hook_method(m, AS())
                if u:
                    self._unhooks.append(u)
        except Exception:
            pass
        try:
            sv = jclass("org.telegram.ui.Stories.StoryViewer").getClass()
            class VS(MethodHook):
                def before_hooked_method(self, param):
                    if plugin._gs("bypass_story_save", False):
                        try:
                            if len(param.args) >= 1:
                                param.args[0] = Boolean(True)
                        except Exception:
                            pass
            m = _fm(sv, "allowScreenshots", 1)
            if m:
                u = self.hook_method(m, VS())
                if u:
                    self._unhooks.append(u)
        except Exception:
            pass

    def _hk_media_seen(self):
        plugin = self
        mc = get_messages_controller()
        if not mc:
            return
        class BS(MethodHook):
            def before_hooked_method(self, param):
                if plugin._is_ghost() and plugin._gs("ghost_no_read", True):
                    param.setResult(None)
        for mm in mc.getClass().getDeclaredMethods():
            if str(mm.getName()) == "markMessageContentAsRead":
                mm.setAccessible(True)
                u = self.hook_method(mm, BS())
                if u:
                    self._unhooks.append(u)

    def _mc_acc(self, mc):
        try:
            return int(mc.currentAccount)
        except Exception:
            return UserConfig.selectedAccount

    def _hk_fake_premium(self):
        plugin = self

        def _load_cached_es(acc=None, uid=None):
            try:
                return _dict_to_emoji(_safe_read_json(_emoji_cache_path(acc=acc, uid=uid)))
            except Exception:
                return None

        def _load_cached_premium(acc=None, uid=None):
            try:
                d = _safe_read_json(_premium_cache_path(acc=acc, uid=uid))
                if not isinstance(d, dict):
                    return (None, None)
                return (_dict_to_peer_color(d.get("color")), _dict_to_peer_color(d.get("profile_color")))
            except Exception:
                return (None, None)

        def _resolve_uc_acc(uc, acc):
            if acc is not None:
                return acc
            try:
                return int(uc.currentAccount)
            except Exception:
                return None

        def _resolve_uc_uid(uc):
            try:
                if uc.isClientActivated():
                    return int(uc.getClientUserId())
            except Exception:
                pass
            try:
                cu = uc.getCurrentUser()
                if cu is not None:
                    return int(cu.id)
            except Exception:
                pass
            return 0

        def _save_emoji_state_direct(acc, uid, es):
            try:
                p = _emoji_cache_path(acc=acc, uid=uid)
                if p is None:
                    return
                d = _emoji_to_dict(es)
                if d is not None:
                    _atomic_write_json(p, d)
            except Exception:
                pass

        def _save_premium_state_direct(acc, uid, color, profile_color):
            try:
                p = _premium_cache_path(acc=acc, uid=uid)
                if p is None:
                    return
                d = {}
                cd = _peer_color_to_dict(color)
                if cd is not None:
                    d["color"] = cd
                pd = _peer_color_to_dict(profile_color)
                if pd is not None:
                    d["profile_color"] = pd
                if d:
                    _atomic_write_json(p, d)
            except Exception:
                pass

        def _save_emoji_state(uc, acc=None):
            try:
                if uc is None:
                    return
                cu = uc.getCurrentUser()
                if cu is None:
                    return
                acc = _resolve_uc_acc(uc, acc)
                uid = _resolve_uc_uid(uc)
                if not uid:
                    return
                p = _emoji_cache_path(acc=acc, uid=uid)
                if p is None:
                    return
                d = _emoji_to_dict(getattr(cu, "emoji_status", None))
                if d is not None:
                    _atomic_write_json(p, d)
            except Exception:
                pass

        def _save_premium_state(uc, acc=None):
            try:
                if uc is None:
                    return
                cu = uc.getCurrentUser()
                if cu is None:
                    return
                acc = _resolve_uc_acc(uc, acc)
                uid = _resolve_uc_uid(uc)
                if not uid:
                    return
                p = _premium_cache_path(acc=acc, uid=uid)
                if p is None:
                    return
                d = {}
                try:
                    cd = _peer_color_to_dict(getattr(cu, "color", None))
                    if cd is not None:
                        d["color"] = cd
                except Exception:
                    pass
                try:
                    pd = _peer_color_to_dict(getattr(cu, "profile_color", None))
                    if pd is not None:
                        d["profile_color"] = pd
                except Exception:
                    pass
                if d:
                    _atomic_write_json(p, d)
            except Exception:
                pass

        def _pick_kept_es(acc, mc, uid):
            try:
                uc = UserConfig.getInstance(acc)
                cu = uc.getCurrentUser() if uc else None
                es = getattr(cu, "emoji_status", None) if cu is not None else None
                if not _is_empty_es(es):
                    return es
            except Exception:
                pass
            try:
                old = mc.getUser(Long(uid)) if mc is not None else None
                es = getattr(old, "emoji_status", None) if old is not None else None
                if not _is_empty_es(es):
                    return es
            except Exception:
                pass
            try:
                es = _load_cached_es(acc=acc, uid=uid)
                if not _is_empty_es(es):
                    return es
            except Exception:
                pass
            return None

        def _pick_kept_color(acc, mc, uid, field):
            try:
                uc = UserConfig.getInstance(acc)
                cu = uc.getCurrentUser() if uc else None
                c = getattr(cu, field, None) if cu is not None else None
                if not _is_empty_color(c):
                    return c
            except Exception:
                pass
            try:
                old = mc.getUser(Long(uid)) if mc is not None else None
                c = getattr(old, field, None) if old is not None else None
                if not _is_empty_color(c):
                    return c
            except Exception:
                pass
            try:
                ca, pa = _load_cached_premium(acc=acc, uid=uid)
                c = ca if field == "color" else pa
                if not _is_empty_color(c):
                    return c
            except Exception:
                pass
            return None

        def _patch_user(user, mc=None, acc=None):
            try:
                if user is None:
                    return
                try:
                    uid = int(user.id)
                except Exception:
                    return
                if acc is None:
                    try:
                        acc = plugin._mc_acc(mc) if mc is not None else UserConfig.selectedAccount
                    except Exception:
                        acc = UserConfig.selectedAccount
                try:
                    my = int(UserConfig.getInstance(acc).getClientUserId())
                except Exception:
                    my = 0
                if uid != my:
                    return
                user.premium = True
                try:
                    if _is_empty_es(getattr(user, "emoji_status", None)):
                        kept = _pick_kept_es(acc, mc, uid)
                        if kept is not None:
                            user.emoji_status = kept
                except Exception:
                    pass
                for field in ("color", "profile_color"):
                    try:
                        if _is_empty_color(getattr(user, field, None)):
                            kept = _pick_kept_color(acc, mc, uid, field)
                            if kept is not None:
                                setattr(user, field, kept)
                    except Exception:
                        pass
            except Exception:
                pass

        def _apply_to_user(u, es, color_c, profile_c):
            if u is None:
                return
            try:
                u.premium = True
            except Exception:
                pass
            try:
                if es is not None and _is_empty_es(getattr(u, "emoji_status", None)):
                    u.emoji_status = es
            except Exception:
                pass
            try:
                if color_c is not None and _is_empty_color(getattr(u, "color", None)):
                    u.color = color_c
            except Exception:
                pass
            try:
                if profile_c is not None and _is_empty_color(getattr(u, "profile_color", None)):
                    u.profile_color = profile_c
            except Exception:
                pass

        def apply_all():
            if not plugin._gs("fake_premium", False):
                return
            try:
                for acc in range(4):
                    try:
                        uc = UserConfig.getInstance(acc)
                        if not (uc and uc.isClientActivated()):
                            continue
                        try:
                            my = int(uc.getClientUserId())
                        except Exception:
                            continue
                        cached_es = _load_cached_es(acc=acc, uid=my)
                        cached_color, cached_profile = _load_cached_premium(acc=acc, uid=my)
                        user = uc.getCurrentUser()
                        _apply_to_user(user, cached_es, cached_color, cached_profile)
                        mu = None
                        try:
                            mc = MessagesController.getInstance(acc)
                            mu = mc.getUser(Long(my))
                            _apply_to_user(mu, cached_es, cached_color, cached_profile)
                        except Exception:
                            pass
                        try:
                            nc = NotificationCenter.getInstance(acc)
                            target = mu if mu is not None else user
                            def _fire(t=target, n=nc):
                                try:
                                    if t is not None:
                                        n.postNotificationName(NotificationCenter.userEmojiStatusUpdated, t)
                                except Exception:
                                    pass
                                try:
                                    n.postNotificationName(NotificationCenter.updateInterfaces, Integer(524288 | 1 | 2))
                                except Exception:
                                    pass
                            run_on_ui_thread(_fire)
                        except Exception:
                            pass
                    except Exception:
                        pass
            except Exception:
                pass

        run_on_ui_thread(apply_all)

        try:
            UCC = find_class("org.telegram.messenger.UserConfig")
            class SaveHook(MethodHook):
                def after_hooked_method(self, param):
                    if not plugin._gs("fake_premium", False):
                        return
                    try:
                        uc = param.thisObject
                        _save_premium_state(uc)
                        _save_emoji_state(uc)
                    except Exception:
                        pass
            for sig in [[Boolean.TYPE], []]:
                try:
                    m = UCC.getClass().getDeclaredMethod("saveConfig", *sig)
                    m.setAccessible(True)
                    u = self.hook_method(m, SaveHook())
                    if u:
                        self._unhooks.append(u)
                except Exception:
                    pass

            UC_cls0 = find_class("org.telegram.tgnet.TLRPC$User")
            class SetCurrentUserHook(MethodHook):
                def before_hooked_method(self, param):
                    if not plugin._gs("fake_premium", False):
                        return
                    try:
                        user = param.args[0]
                        if user is None:
                            return
                        try:
                            acc = param.thisObject.getCurrentAccount() if hasattr(param.thisObject, "getCurrentAccount") else UserConfig.selectedAccount
                        except Exception:
                            acc = UserConfig.selectedAccount
                        try:
                            uid = int(user.id)
                            my = int(UserConfig.getInstance(acc).getClientUserId())
                            if uid == my:
                                es = getattr(user, "emoji_status", None)
                                if not _is_empty_es(es):
                                    _save_emoji_state_direct(acc, uid, es)
                                color = getattr(user, "color", None)
                                profile_color = getattr(user, "profile_color", None)
                                if not _is_empty_color(color) or not _is_empty_color(profile_color):
                                    _save_premium_state_direct(acc, uid, color, profile_color)
                        except Exception:
                            pass
                        _patch_user(user, None, acc)
                    except Exception:
                        pass
            try:
                m = UCC.getClass().getDeclaredMethod("setCurrentUser", UC_cls0)
                m.setAccessible(True)
                u = self.hook_method(m, SetCurrentUserHook())
                if u:
                    self._unhooks.append(u)
            except Exception:
                pass

            class IPH(MethodHook):
                def before_hooked_method(self, param):
                    if not plugin._gs("fake_premium", False):
                        return
                    try:
                        obj = param.thisObject
                        if obj is None:
                            return
                        try:
                            acc = int(obj.currentAccount) if hasattr(obj, "currentAccount") else int(UserConfig.selectedAccount)
                        except Exception:
                            acc = int(UserConfig.selectedAccount)
                        my = plugin._get_my_uid(acc)
                        if my <= 0:
                            return
                        cu = obj.getCurrentUser()
                        if cu and int(cu.id) == my:
                            param.setResult(Boolean(True))
                    except Exception:
                        pass
            for mname in ["isPremium", "hasPremium"]:
                m = _fm(UCC.getClass(), mname)
                if m:
                    u = self.hook_method(m, IPH())
                    if u:
                        self._unhooks.append(u)
        except Exception as e:
            log(f"[MAX] fpHook uc: {e}")

        try:
            MC = find_class("org.telegram.messenger.MessagesController")
            class PutUserHook(MethodHook):
                def before_hooked_method(self, param):
                    if not plugin._gs("fake_premium", False):
                        return
                    try:
                        user = param.args[0]
                        if not user:
                            return
                        uid = int(user.id)
                        acc = int(param.thisObject.currentAccount) if hasattr(param.thisObject, "currentAccount") else int(UserConfig.selectedAccount)
                        if uid != plugin._get_my_uid(acc):
                            return
                        try:
                            es = getattr(user, "emoji_status", None)
                            if not _is_empty_es(es):
                                _save_emoji_state_direct(acc, uid, es)
                            color = getattr(user, "color", None)
                            profile_color = getattr(user, "profile_color", None)
                            if not _is_empty_color(color) or not _is_empty_color(profile_color):
                                _save_premium_state_direct(acc, uid, color, profile_color)
                        except Exception:
                            pass
                        _patch_user(user, param.thisObject, acc)
                    except Exception:
                        pass
            for mm in MC.getClass().getDeclaredMethods():
                if str(mm.getName()) == "putUser" and len(mm.getParameterTypes()) == 2:
                    mm.setAccessible(True)
                    u = self.hook_method(mm, PutUserHook())
                    if u:
                        self._unhooks.append(u)
                    break

            EMS_cls = None
            try:
                EMS_cls = find_class("org.telegram.tgnet.TLRPC$EmojiStatus")
            except Exception:
                pass
            SG_cls = None
            try:
                SG_cls = find_class("org.telegram.tgnet.tl.TL_stars$StarGift")
            except Exception:
                pass

            class UpdateEmojiHook(MethodHook):
                def after_hooked_method(self, param):
                    try:
                        mc = param.thisObject
                        try:
                            acc = plugin._mc_acc(mc)
                        except Exception:
                            acc = UserConfig.selectedAccount
                        uc = UserConfig.getInstance(acc)
                        if uc is None:
                            return
                        try:
                            if len(param.args) >= 3:
                                did = int(param.args[0])
                                if did != 0:
                                    try:
                                        my = int(uc.getClientUserId())
                                    except Exception:
                                        my = -1
                                    if did != my:
                                        return
                        except Exception:
                            pass
                        try:
                            _save_emoji_state(uc, acc=acc)
                        except Exception:
                            pass
                        try:
                            uc.saveConfig(True)
                        except Exception:
                            pass
                    except Exception:
                        pass

            sigs_em = []
            if EMS_cls and SG_cls:
                sigs_em.append([Long.TYPE, EMS_cls, SG_cls])
                sigs_em.append([EMS_cls, SG_cls])
            if EMS_cls:
                sigs_em.append([EMS_cls])
            for sig in sigs_em:
                try:
                    m = MC.getClass().getDeclaredMethod("updateEmojiStatus", *sig)
                    m.setAccessible(True)
                    u = self.hook_method(m, UpdateEmojiHook())
                    if u:
                        self._unhooks.append(u)
                    break
                except Exception:
                    pass
        except Exception:
            pass

        try:
            LA = find_class("org.telegram.ui.LaunchActivity")
            if LA is not None:
                class AccountSwitchHook(MethodHook):
                    def after_hooked_method(self, param):
                        try:
                            run_on_ui_thread(apply_all)
                        except Exception:
                            pass
                try:
                    m = LA.getClass().getDeclaredMethod("switchToAccount", Integer.TYPE, Boolean.TYPE)
                    m.setAccessible(True)
                    u = self.hook_method(m, AccountSwitchHook())
                    if u:
                        self._unhooks.append(u)
                except Exception:
                    pass
        except Exception:
            pass

    def _hk_nfl(self):
        self.nfl_sel = NFLSelectorHook(self)
        try:
            sel_m = _fm(CA_CLS, "addToSelectedMessages", 3)
            clr_m = _fm(CA_CLS, "hideActionMode", 0)
            mul_m = _fm(CA_CLS, "startMultiselect", 1)
            if sel_m:
                u = self.hook_method(sel_m, self.nfl_sel)
                if u:
                    self._unhooks.append(u)
            if clr_m:
                u = self.hook_method(clr_m, NFLClearHook(self.nfl_sel))
                if u:
                    self._unhooks.append(u)
            if mul_m:
                u = self.hook_method(mul_m, NFLMultiHook(self))
                if u:
                    self._unhooks.append(u)
            mc = get_messages_controller()
            if mc:
                for m in mc.getClass().getDeclaredMethods():
                    if str(m.getName()) == "deleteMessages" and len(m.getParameterTypes()) >= 7:
                        m.setAccessible(True)
                        u = self.hook_method(m, NFLDeleteHook())
                        if u:
                            self._unhooks.append(u)
                        break
        except Exception as e:
            log(f"[MAX] NFL: {e}")

    def _hk_feed_ads(self):
        plugin = self
        class RetFalse(MethodHook):
            def before_hooked_method(self, param):
                if not plugin._gs("hide_feed_ads", True):
                    return
                try:
                    param.setResult(Boolean(False))
                except Exception:
                    pass
        class RetNull(MethodHook):
            def before_hooked_method(self, param):
                if not plugin._gs("hide_feed_ads", True):
                    return
                try:
                    param.setResult(None)
                except Exception:
                    pass
        class RetEmptyList(MethodHook):
            def before_hooked_method(self, param):
                if not plugin._gs("hide_feed_ads", True):
                    return
                try:
                    param.setResult(ArrayList())
                except Exception:
                    pass
        class ClearInjectorHook(MethodHook):
            def after_hooked_method(self, param):
                if not plugin._gs("hide_feed_ads", True):
                    return
                try:
                    if param.thisObject is not None and hasattr(param.thisObject, "clear"):
                        param.thisObject.clear()
                except Exception:
                    pass
        try:
            controller_cls = find_class("com.exteragram.messenger.feed.ads.FeedAdController")
            injector_cls = find_class("com.exteragram.messenger.feed.ads.FeedAdInjector")
            parser_cls = find_class("com.exteragram.messenger.feed.ads.FeedAdParser")
        except Exception:
            return
        if controller_cls is None and injector_cls is None and parser_cls is None:
            return
        if controller_cls is not None:
            try:
                max_accounts = int(UserConfig.MAX_ACCOUNT_COUNT)
            except Exception:
                max_accounts = 4
            for acc in range(max_accounts):
                try:
                    instance = controller_cls.getInstance(acc)
                    if instance is not None:
                        _set_field_value(instance, "eligibleAds", ArrayList())
                except Exception:
                    pass
            for mn in ("isEnabled", "nextAd"):
                for mm in controller_cls.getClass().getDeclaredMethods():
                    if str(mm.getName()) != mn:
                        continue
                    mm.setAccessible(True)
                    u = self.hook_method(mm, RetFalse() if mn == "isEnabled" else RetNull())
                    if u:
                        self._unhooks.append(u)
                    break
        if injector_cls is not None:
            for mm in injector_cls.getClass().getDeclaredMethods():
                if str(mm.getName()) != "refresh":
                    continue
                mm.setAccessible(True)
                u = self.hook_method(mm, ClearInjectorHook())
                if u:
                    self._unhooks.append(u)
                break
        if parser_cls is not None:
            for mm in parser_cls.getClass().getDeclaredMethods():
                if str(mm.getName()) != "parse":
                    continue
                mm.setAccessible(True)
                u = self.hook_method(mm, RetEmptyList())
                if u:
                    self._unhooks.append(u)
                break

    def _hook_ad(self):
        plugin = self
        class PromoNoOpHook(MethodHook):
            def before_hooked_method(self, param):
                try:
                    param.setResult(None)
                except Exception:
                    return
                try:
                    controller = param.thisObject
                    plugin._run_on_stage_queue(
                        lambda: plugin._cleanup_messages_controller(controller)
                    )
                except Exception:
                    pass
        class RetF(MethodHook):
            def before_hooked_method(self, param):
                param.setResult(Boolean(False))
        class VideoAdsLoadHook(MethodHook):
            def before_hooked_method(self, param):
                video_ads = param.thisObject
                try:
                    _set_field_value(video_ads, "loading", False)
                    _set_field_value(video_ads, "loaded", True)
                    ads = _get_field_value(video_ads, "ads")
                    if ads is not None:
                        try:
                            ads.clear()
                        except Exception:
                            pass
                except Exception:
                    pass
                try:
                    param.setResult(None)
                except Exception:
                    pass
        class VideoNoOp(MethodHook):
            def before_hooked_method(self, param):
                param.setResult(None)
        mc = get_messages_controller()
        if mc:
            c = mc.getClass()
            for n, pc, h in [("checkPromoInfo", 1, PromoNoOpHook()), ("checkPromoInfoInternal", 1, PromoNoOpHook()), ("isPromoDialog", 2, RetF())]:
                m = _fm(c, n, pc)
                if m:
                    u = self.hook_method(m, h)
                    if u:
                        self._unhooks.append(u)
        video_noop = VideoNoOp()
        for vmethod, vpc, vhook in [("load", 0, VideoAdsLoadHook()), ("schedule", 0, video_noop), ("show", 0, video_noop), ("isPopupShown", 0, RetF())]:
            try:
                va_cls = jclass("org.telegram.messenger.video.VideoAds").getClass()
                m = _fm(va_cls, vmethod, vpc)
                if m:
                    u = self.hook_method(m, vhook)
                    if u:
                        self._unhooks.append(u)
            except Exception:
                pass
        self._cleanup_all_accounts_soon()

    def _run_on_stage_queue(self, fn):
        try:
            Utilities.stageQueue.postRunnable(lambda: fn())
        except Exception:
            try:
                fn()
            except Exception:
                pass

    def _cleanup_all_accounts_soon(self):
        for delay in (0, 500, 2000):
            try:
                run_on_ui_thread(
                    lambda: self._run_on_stage_queue(self._cleanup_all_accounts),
                    delay,
                )
            except Exception:
                pass

    def _cleanup_all_accounts(self):
        try:
            max_accounts = int(UserConfig.MAX_ACCOUNT_COUNT)
        except Exception:
            max_accounts = 4
        for account in range(max_accounts):
            try:
                controller = MessagesController.getInstance(account)
                if controller is not None:
                    self._cleanup_messages_controller(controller)
            except Exception:
                continue

    def _cleanup_messages_controller(self, controller):
        if controller is None:
            return
        should_remove_dialog = False
        try:
            promo_dialog = _get_field_value(controller, "promoDialog")
            promo_dialog_id = _int(_get_field_value(controller, "promoDialogId", 0), 0)
            should_remove_dialog = promo_dialog is not None or bool(promo_dialog_id)
        except Exception:
            pass
        try:
            request_id = _int(_get_field_value(controller, "checkingPromoInfoRequestId", 0), 0)
            if request_id:
                try:
                    controller.getConnectionsManager().cancelRequest(request_id, True)
                except Exception:
                    pass
            _set_field_value(controller, "checkingPromoInfoRequestId", 0)
        except Exception:
            pass
        try:
            current_time = int(controller.getConnectionsManager().getCurrentTime())
            next_check_time = current_time + ONE_YEAR_SECONDS
            _set_field_value(controller, "checkingPromoInfo", False)
            _set_field_value(controller, "nextPromoInfoCheckTime", next_check_time)
            _set_field_value(controller, "promoDialogId", 0)
            _set_field_value(controller, "promoDialogType", 0)
            _set_field_value(controller, "proxyDialogAddress", None)
            self._clear_promo_preferences(controller, next_check_time)
        except Exception:
            pass
        if should_remove_dialog:
            try:
                run_on_ui_thread(lambda: self._cleanup_promo_dialog_on_ui(controller))
            except Exception:
                pass

    def _cleanup_promo_dialog_on_ui(self, controller):
        if controller is None:
            return
        try:
            _invoke_no_arg(controller, "removePromoDialog")
        except Exception:
            pass
        try:
            _set_field_value(controller, "promoDialog", None)
            _set_field_value(controller, "promoDialogId", 0)
            _set_field_value(controller, "promoDialogType", 0)
            _set_field_value(controller, "checkingPromoInfo", False)
            _set_field_value(controller, "checkingPromoInfoRequestId", 0)
            _set_field_value(controller, "proxyDialogAddress", None)
        except Exception:
            pass

    def _clear_promo_preferences(self, controller, next_check_time):
        seen = set()
        try:
            self._clear_promo_preferences_in(controller.getMainSettings(), next_check_time, seen)
        except Exception:
            pass
        try:
            self._clear_promo_preferences_in(
                MessagesController.getGlobalMainSettings(),
                next_check_time,
                seen,
            )
        except Exception:
            pass

    def _clear_promo_preferences_in(self, preferences, next_check_time, seen):
        if preferences is None:
            return
        try:
            key = f"{preferences.getClass().getName()}:{preferences.hashCode()}"
        except Exception:
            key = str(id(preferences))
        if key in seen:
            return
        seen.add(key)
        editor = preferences.edit()
        editor.putLong("proxy_dialog", 0)
        editor.putInt("promo_dialog_type", 0)
        editor.putInt("nextPromoInfoCheckTime", int(next_check_time))
        editor.remove("proxyDialogAddress")
        try:
            editor.commit()
        except Exception:
            editor.apply()

    def on_send_message_hook(self, account, params):
        try:
            if self._is_ghost() and self.get_setting("read_on_action", True) and self.get_setting("ghost_no_read", True):
                try:
                    did = 0
                    if hasattr(params, "peer") and params.peer:
                        did = int(params.peer)
                    if not did:
                        f = get_last_fragment()
                        if f and str(f.getClass().getSimpleName()) == "ChatActivity":
                            did = int(get_private_field(f, "dialog_id") or 0)
                    if did:
                        self._send_read_for_dialog(did)
                except Exception:
                    pass
        except Exception:
            pass
        return HookResult(strategy=HookStrategy.MODIFY, params=params)

    def on_update_hook(self, update_name, account, update):
        if update_name in PRESENCE_UPD and (self._gs("presence_track", False) or self._presence_tracked):
            try:
                self._presence_on_update(update_name, account, update)
            except Exception:
                pass
        if self._gs("save_edits", True) or self._gs("save_attachments", False):
            if update_name in ("TL_updateNewMessage", "TL_updateNewChannelMessage"):
                try:
                    msg = update.message
                    if not msg.out:
                        did = _peer_did(msg)
                        mid = int(msg.id)
                        if did and mid and self.vault:
                            if self._gs("save_edits", True):
                                self.vault.save_message_version(did, mid, msg, is_original=True)
                            if self._gs("save_attachments", False):
                                _ensure_attach_saved(msg, did, mid, _get_media_hash(msg), force_download=False)
                except Exception:
                    pass
            elif update_name in ("TL_updateEditMessage", "TL_updateEditChannelMessage"):
                try:
                    msg = update.message
                    if msg.out:
                        return HookResult()
                    did, mid = _peer_did(msg), int(msg.id)
                    if did and mid and self.vault:
                        save_att = self._gs("save_attachments", False)
                        if not self.vault.has_revisions(did, mid):
                            mo = _get_message_from_chat(did, mid)
                            if mo and mo.messageOwner:
                                old_msg = mo.messageOwner
                                if self._gs("save_edits", True):
                                    self.vault.save_message_version(did, mid, old_msg, is_original=True)
                                if save_att:
                                    _ensure_attach_saved(old_msg, did, mid, _get_media_hash(old_msg), force_download=False)
                        if self._gs("save_edits", True):
                            self.vault.save_message_version(did, mid, msg, is_original=False)
                        if save_att:
                            _ensure_attach_saved(msg, did, mid, _get_media_hash(msg), force_download=False)
                except Exception:
                    pass
        return HookResult()

    def pre_request_hook(self, request_name, account, request):
        if request_name == "getStarGifts" and self._gs("hidden_gifts", False):
            try:
                if _int(getattr(request, "hash", 0)) != 0:
                    request.hash = 0
                    return HookResult(strategy=HookStrategy.MODIFY, request=request)
            except Exception:
                pass
        if request_name in READ_REQS and self._pass_read:
            self._pass_read = False
            return HookResult()
        if request_name in BLOCKED_AD_REQS and self._gs("hide_ads", True):
            return HookResult(strategy=HookStrategy.CANCEL)
        if self._is_ghost():
            if request_name == "TL_messages_sendReaction" and self._gs("read_on_action", True) and self._gs("ghost_no_read", True):
                try:
                    peer = getattr(request, "peer", None)
                    did = 0
                    if peer:
                        if hasattr(peer, "user_id") and peer.user_id:
                            did = _int(peer.user_id)
                        elif hasattr(peer, "chat_id") and peer.chat_id:
                            did = -_int(peer.chat_id)
                        elif hasattr(peer, "channel_id") and peer.channel_id:
                            did = -_int(peer.channel_id)
                    mid = _int(getattr(request, "msg_id", 0))
                    if did:
                        self._send_read_for_dialog(did, mid)
                except Exception:
                    pass
            if request_name in READ_REQS and self._gs("ghost_no_read", True):
                if self._is_whitelisted_peer(request):
                    return HookResult()
                try:
                    did = 0
                    if hasattr(request, "peer"):
                        peer = request.peer
                        if hasattr(peer, "user_id") and peer.user_id:
                            did = _int(peer.user_id)
                        elif hasattr(peer, "chat_id") and peer.chat_id:
                            did = -_int(peer.chat_id)
                        elif hasattr(peer, "channel_id") and peer.channel_id:
                            did = -_int(peer.channel_id)
                    if did:
                        self._mark_local_read(did, 0)
                except Exception:
                    pass
                return HookResult(strategy=HookStrategy.CANCEL)
            if request_name in TYPING_REQS and self._gs("ghost_no_typing", True):
                return HookResult(strategy=HookStrategy.CANCEL)
            if request_name in STORIES_REQS and self._gs("ghost_no_read_stories", True):
                return HookResult(strategy=HookStrategy.CANCEL)
            if request_name in ("updateStatus", "TL_account_updateStatus") and self._gs("ghost_no_online", True):
                request.offline = True
                return HookResult(strategy=HookStrategy.MODIFY, request=request)
        silent = _int(self._gs("silent_send", 0), 0)
        if request_name in SEND_REQS and (silent == 1 or (silent == 2 and self._is_ghost())):
            try:
                request.silent = True
            except Exception:
                pass
            try:
                request.noforwards = getattr(request, "noforwards", False)
            except Exception:
                pass
            return HookResult(strategy=HookStrategy.MODIFY, request=request)
        return HookResult()

    def post_request_hook(self, request_name, account, response, error):
        if request_name in ONLINE_REQS and self._is_ghost() and self.get_setting("ghost_immediate_offline", True):
            run_on_queue(lambda: self.sendOffline(), "externalNetworkQueue", 1000)
        if request_name == "getStarGifts" and self.get_setting("hidden_gifts", False) and not error and response:
            patched = self._patch_star_gifts(response)
            if patched:
                return HookResult(strategy=HookStrategy.MODIFY, response=patched)
        if request_name == "TL_messages_getStickerSet" and self.get_setting("hidden_gifts", False) and not error and response:
            try:
                self._refresh_gift_list()
                self._ingest_sticker_set(response)
            except Exception:
                pass

    def _on_ghost_change(self, v):
        self.set_setting("ghost_mode", v)
        self._sc.clear()
        if v:
            self.sendOffline()

    def _on_view_deleted_click(self, ctx):
        try:
            if self.dex and self.dex.loaded:
                self.dex.call("pyShowDeletedList")
            else:
                BulletinHelper.show_info(L.g("no_deleted"))
        except Exception as e:
            log(f"[MAX] viewDel: {e}")

    def _navigate_to_message(self, did, mid):
        did = int(did)
        mid = int(mid)
        plugin = self
        def _do():
            try:
                frag = get_last_fragment()
                if not frag:
                    return
                frag_did = 0
                if isinstance(frag, ChatActivity):
                    f = _ff(CA_CLS, "dialog_id")
                    if f:
                        frag_did = int(f.get(frag))
                if frag_did == did:
                    try:
                        frag.scrollToMessageId(mid, 0, True, 0, True, 0)
                    except Exception:
                        pass
                    return
                try:
                    acc = int(UserConfig.selectedAccount)
                    args = jclass("android.os.Bundle")()
                    if did > 0:
                        args.putLong("user_id", jlong(did))
                    elif did < 0:
                        chat_id = -did
                        try:
                            mc = MessagesController.getInstance(acc)
                            chat = mc.getChat(Long(chat_id))
                            if chat and ChatObject.isChannel(chat):
                                args.putLong("chat_id", jlong(chat_id))
                            else:
                                args.putLong("chat_id", jlong(chat_id))
                        except Exception:
                            args.putLong("chat_id", jlong(chat_id))
                    args.putInt("message_id", jint(mid))
                    ca = ChatActivity(args)
                    frag.presentFragment(ca)
                except Exception as e:
                    log(f"[MAX] navigate: {e}")
            except Exception:
                pass
        run_on_ui_thread(_do)

    def _scroll_to_message(self, did, mid):
        self._navigate_to_message(did, mid)

    def _try_save_media(self, mo):
        try:
            frag = get_last_fragment()
            ctx = frag.getParentActivity() if frag else None
            if not ctx:
                return
            msg = mo.messageOwner if mo else None
            if msg and _save_media_to_downloads(msg, ctx):
                BulletinHelper.show_info(L.g("saved_to"))
            else:
                BulletinHelper.show_info(L.g("no_text"))
        except Exception:
            pass

    def _show_deleted_list(self, did):
        if self.dex and self.dex.loaded:
            try:
                self.dex.call("pyShowDeletedList")
                return
            except Exception:
                pass
        BulletinHelper.show_info(L.g("no_deleted"))

    def _download_revision_media(self, dmsg):
        try:
            if not dmsg:
                BulletinHelper.show_info(L.g("error"))
                return
            did = _peer_did(dmsg)
            mid = int(getattr(dmsg, "id", 0))
            mh = _get_media_hash(dmsg)
            p = _ensure_attach_saved(dmsg, did, mid, mh, force_download=True)
            if p:
                BulletinHelper.show_info(L.g("saved_to"))
            else:
                _save_media_to_downloads(dmsg, ApplicationLoader.applicationContext)
                BulletinHelper.show_info(L.g("saved_to"))
        except Exception:
            BulletinHelper.show_info(L.g("error"))

    def _show_revisions(self, did, mid):
        if not self.vault:
            return
        revs = self.vault.get_revisions(did, mid)
        if not revs:
            BulletinHelper.show_info(L.g("no_revisions"))
            return
        if self.dex and self.dex.loaded:
            try:
                items = []
                for rev in revs:
                    mh = rev.get("media_hash", "")
                    file_path = ""
                    if mh:
                        fp = _get_vault_attach_by_hash(did, mid, mh)
                        if fp:
                            file_path = fp
                        else:
                            fp = _get_vault_attach(did, mid)
                            if fp:
                                file_path = fp
                    items.append({
                        "ver": rev.get("ver", 0),
                        "timestamp": rev.get("timestamp", 0),
                        "text": rev.get("text", ""),
                        "media_hash": mh,
                        "data": rev.get("data") or "",
                        "file": file_path
                    })
                j = json.dumps(items, ensure_ascii=False)
                from java.lang import String as JString
                self.dex.call("pyShowRevisions", JString(j))
                return
            except Exception as e:
                log(f"[MAX] showRevJava: {e}")
        BulletinHelper.show_info(L.g("no_revisions"))

    def _export_settings(self, _v=None):
        plugin = self
        def _bg():
            try:
                sdk_settings = {}
                try:
                    sdk_settings = plugin.export_settings()
                    if not isinstance(sdk_settings, dict):
                        sdk_settings = {}
                except Exception:
                    pass
                data = {"_version": __version__, "_ts": int(_time.time()), "_sdk": sdk_settings}
                prefs = plugin._prefs()
                try:
                    raw = prefs.getString("filters_list", "[]")
                    data["_filters"] = json.loads(str(raw))
                except Exception:
                    pass
                try:
                    raw = prefs.getString("read_whitelist", "[]")
                    data["_read_whitelist"] = json.loads(str(raw))
                except Exception:
                    pass
                try:
                    raw = prefs.getString(PRESENCE_KEY, "{}")
                    pd = json.loads(str(raw))
                    if pd.get("tracked"):
                        data["_presence_tracked"] = pd["tracked"]
                except Exception:
                    pass
                try:
                    lang = prefs.getString(L._prefs_key, "auto")
                    data["_lang"] = str(lang)
                except Exception:
                    pass
                payload = json.dumps(data, ensure_ascii=False, indent=2)
                dir_path = os.path.join(
                    str(ApplicationLoader.applicationContext.getCacheDir().getAbsolutePath()),
                    "mre_backup"
                )
                ensure_dir_exists(dir_path)
                fpath = os.path.join(dir_path, "max_reextera.mre_backup")
                write_file(fpath, payload)
                my_id = int(UserConfig.getInstance(int(UserConfig.selectedAccount)).getClientUserId())
                send_document(my_id, fpath)
                run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("export_done")))
            except Exception as e:
                log(f"[MAX] export: {e}")
                run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("export_error")))
        threading.Thread(target=_bg, daemon=True).start()

    def _do_import(self, data):
        plugin = self
        def _bg():
            try:
                sdk_settings = data.get("_sdk")
                if isinstance(sdk_settings, dict) and sdk_settings:
                    try:
                        plugin.import_settings(sdk_settings, reload_settings=False)
                    except Exception:
                        pass
                prefs = plugin._prefs()
                editor = prefs.edit()
                if "_filters" in data:
                    try:
                        editor.putString("filters_list", json.dumps(data["_filters"]))
                        plugin._filters = data["_filters"]
                    except Exception:
                        pass
                if "_read_whitelist" in data:
                    try:
                        wl = data["_read_whitelist"]
                        editor.putString("read_whitelist", json.dumps(wl))
                        plugin._read_whitelist = set(wl)
                    except Exception:
                        pass
                if "_presence_tracked" in data:
                    try:
                        tracked = data["_presence_tracked"]
                        pd_raw = prefs.getString(PRESENCE_KEY, "{}")
                        pd = json.loads(str(pd_raw))
                        pd["tracked"] = tracked
                        editor.putString(PRESENCE_KEY, json.dumps(pd))
                        plugin._presence_tracked = set(tracked)
                    except Exception:
                        pass
                if "_lang" in data:
                    try:
                        editor.putString(L._prefs_key, str(data["_lang"]))
                        L._lang_cache = [None, 0]
                    except Exception:
                        pass
                editor.apply()
                plugin._sc.clear()
                plugin._load_filters()
                run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("import_done")))
            except Exception as e:
                log(f"[MAX] import: {e}")
                run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("import_error")))
        threading.Thread(target=_bg, daemon=True).start()

    def _load_filters(self):
        try:
            self._filters = json.loads(str(self._prefs().getString("filters_list", "[]")))
        except Exception:
            self._filters = []
        self._cpat = []
        for f in self._filters:
            if f.get("enabled", True) and f.get("expression", ""):
                try:
                    flags = re.IGNORECASE if f.get("ignore_case", True) else 0
                    self._cpat.append((re.compile(f["expression"], flags), f.get("inverted", False)))
                except Exception:
                    pass

    def _save_filters(self):
        self._prefs().edit().putString("filters_list", json.dumps(self._filters)).apply()
        self._load_filters()

    def _should_filter(self, text):
        if not text or not self._cpat:
            return False
        if not self.get_setting("filters_enabled", True):
            return False
        ts = str(text)
        for pat, inv in self._cpat:
            try:
                found = pat.search(ts) is not None
                if (inv and not found) or (not inv and found):
                    return True
            except Exception:
                pass
        return False

    def _compare_versions(self, v1, v2):
        try:
            def parse(v):
                return [int(x) for x in str(v).strip().split(".")]
            p1, p2 = parse(v1), parse(v2)
            while len(p1) < len(p2):
                p1.append(0)
            while len(p2) < len(p1):
                p2.append(0)
            for a, b in zip(p1, p2):
                if a < b:
                    return -1
                if a > b:
                    return 1
            return 0
        except Exception:
            return 0

    def _fetch_json_url(self, url):
        try:
            import requests
            return requests.get(url, timeout=7).json()
        except Exception:
            pass
        try:
            from java.net import URL as JURL
            from java.io import BufferedReader, InputStreamReader
            conn = JURL(url).openConnection()
            conn.setConnectTimeout(7000)
            conn.setReadTimeout(7000)
            conn.setRequestProperty("User-Agent", "MAX-reextera")
            reader = BufferedReader(InputStreamReader(conn.getInputStream(), "UTF-8"))
            sb = []
            line = reader.readLine()
            while line is not None:
                sb.append(str(line))
                line = reader.readLine()
            reader.close()
            conn.disconnect()
            return json.loads("\n".join(sb))
        except Exception:
            pass
        return None

    def _check_for_updates(self):
        if not self.get_setting("check_updates_on_start", True):
            return
        plugin = self
        def _bg():
            try:
                _time.sleep(3.0)
                resp = plugin._fetch_json_url(UPDATE_JSON_URL)
                if not isinstance(resp, dict):
                    return
                if resp.get("id") != __id__:
                    return
                remote_ver = str(resp.get("versionName", ""))
                if not remote_ver:
                    return
                if plugin._compare_versions(__version__, remote_ver) >= 0:
                    return
                changelog = str(resp.get("changelog", ""))
                download_url = str(resp.get("downloadUrl", ""))
                author_url = str(resp.get("author", ""))
                _time.sleep(2.0)
                run_on_ui_thread(lambda: plugin._show_update_dialog(remote_ver, changelog, download_url, author_url))
            except Exception:
                pass
        threading.Thread(target=_bg, daemon=True).start()

    def _show_update_dialog(self, version, changelog, download_url, author_url):
        try:
            frag = get_last_fragment()
            if not frag or not frag.getParentActivity():
                return
            plugin = self
            ctx = frag.getParentActivity()
            msg = changelog if changelog else ""
            d = AlertDialogBuilder(ctx)
            d.set_title(f"{L.g('update_available')} MAX re:extera: v{version}")
            d.set_message(msg)
            if download_url and author_url:
                d.set_negative_button(L.g("download_verified"), lambda dd, w: (dd.dismiss(), plugin._open_tg_link(download_url)))
                d.set_neutral_button(L.g("view_author_channel"), lambda dd, w: (dd.dismiss(), plugin._open_author_with_warning(author_url)))
                d.set_positive_button(L.g("remind_later"), lambda dd, w: dd.dismiss())
            elif download_url:
                d.set_negative_button(L.g("download_verified"), lambda dd, w: (dd.dismiss(), plugin._open_tg_link(download_url)))
                d.set_positive_button(L.g("remind_later"), lambda dd, w: dd.dismiss())
            elif author_url:
                d.set_negative_button(L.g("view_author_channel"), lambda dd, w: (dd.dismiss(), plugin._open_author_with_warning(author_url)))
                d.set_positive_button(L.g("remind_later"), lambda dd, w: dd.dismiss())
            else:
                d.set_positive_button(L.g("remind_later"), lambda dd, w: dd.dismiss())
            dlg_obj = d.show()
            try:
                dlg_obj.setCancelable(False)
                dlg_obj.setCanceledOnTouchOutside(False)
            except Exception:
                pass
        except Exception as e:
            log(f"[MAX] updateDlg: {e}")

    def _open_tg_link(self, url):
        try:
            from android.content import Intent
            from android.net import Uri
            ctx = ApplicationLoader.applicationContext
            intent = Intent(Intent.ACTION_VIEW, Uri.parse(url))
            intent.setPackage(ctx.getPackageName())
            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
            ctx.startActivity(intent)
        except Exception:
            pass

    def _open_author_with_warning(self, url):
        plugin = self
        if not self.get_setting("author_channel_warning", True):
            self._open_tg_link(url)
            return
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    plugin._open_tg_link(url)
                    return
                ctx = frag.getParentActivity()
                d = AlertDialogBuilder(ctx)
                d.set_title(L.g("view_author_channel"))
                d.set_message(L.g("author_warning"))
                d.set_positive_button("OK", lambda dlg, w: (dlg.dismiss(), plugin._open_tg_link(url)))
                d.set_negative_button(L.g("dont_show_again"), lambda dlg, w: (dlg.dismiss(), plugin._disable_author_warning(), plugin._open_tg_link(url)))
                d.show()
            except Exception:
                plugin._open_tg_link(url)
        run_on_ui_thread(_ui)

    def _disable_author_warning(self):
        self.set_setting("author_channel_warning", False)
        self._sc.clear()

    def _load_read_whitelist(self):
        try:
            raw = self._prefs().getString("read_whitelist", "[]")
            self._read_whitelist = set(json.loads(str(raw)))
        except Exception:
            self._read_whitelist = set()

    def _save_read_whitelist(self):
        try:
            self._prefs().edit().putString("read_whitelist", json.dumps(list(self._read_whitelist))).apply()
        except Exception:
            pass

    def _is_whitelisted_peer(self, request):
        if not self._read_whitelist:
            return False
        try:
            peer = getattr(request, "peer", None)
            if not peer:
                return False
            uid = 0
            if hasattr(peer, "user_id") and peer.user_id:
                uid = _int(peer.user_id)
            if uid and uid in self._read_whitelist:
                return True
        except Exception:
            pass
        return False

    def _show_read_whitelist(self, _v=None):
        plugin = self
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                ctx = frag.getParentActivity()
                uids = sorted(plugin._read_whitelist)
                items = [f"{plugin._presence_user_name(u)} (id{u})" for u in uids]
                def on_pick(dlg, which):
                    dlg.dismiss()
                    uid = uids[which]
                    def confirm():
                        f2 = get_last_fragment()
                        if not f2 or not f2.getParentActivity():
                            return
                        d = AlertDialogBuilder(f2.getParentActivity())
                        d.set_title(L.g("remove"))
                        d.set_message(plugin._presence_user_name(uid))
                        d.set_positive_button(L.g("yes"), lambda dd, w: (dd.dismiss(), do_rm(uid)))
                        d.set_negative_button(L.g("no"), lambda dd, w: (dd.dismiss(), run_on_ui_thread(_ui)))
                        d.show()
                    def do_rm(u):
                        plugin._read_whitelist.discard(int(u))
                        plugin._save_read_whitelist()
                        BulletinHelper.show_info(L.g("removed"))
                        run_on_ui_thread(_ui)
                    run_on_ui_thread(confirm)
                def on_add(dlg, w):
                    dlg.dismiss()
                    plugin._show_read_wl_add()
                d = AlertDialogBuilder(ctx)
                if items:
                    d.set_title(f"{L.g('read_wl_users')} ({len(items)})")
                    d.set_items(items, on_pick)
                else:
                    d.set_title(L.g("read_wl_users"))
                    d.set_message(L.g("read_wl_empty"))
                d.set_positive_button(L.g("add"), on_add)
                d.set_negative_button(L.g("close"), lambda dd, w: dd.dismiss())
                d.show()
            except Exception:
                pass
        run_on_ui_thread(_ui)

    def _show_read_wl_add(self):
        plugin = self
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                ctx = frag.getParentActivity()
                d = AlertDialogBuilder(ctx)
                d.set_title(L.g("add"))
                inp = EditText(ctx)
                inp.setHint("@username / id")
                inp.setInputType(InputType.TYPE_CLASS_TEXT)
                inp.setSingleLine(True)
                d.set_view(inp)
                def on_add(dlg, w):
                    raw = str(inp.getText()).strip()
                    dlg.dismiss()
                    if raw:
                        plugin._resolve_and_add_wl(raw)
                d.set_positive_button(L.g("add"), on_add)
                d.set_negative_button(L.g("cancel"), lambda dd, w: dd.dismiss())
                d.show()
            except Exception:
                pass
        run_on_ui_thread(_ui)

    def _resolve_and_add_wl(self, raw):
        plugin = self
        try:
            v = raw.lstrip("@").strip()
            if v.lstrip("-").isdigit():
                uid = int(v)
                if uid <= 0:
                    BulletinHelper.show_error(L.g("invalid_id"))
                    return
                plugin._read_whitelist.add(uid)
                plugin._save_read_whitelist()
                BulletinHelper.show_info(L.g("added"))
                return
            req = TLRPC.TL_contacts_resolveUsername()
            req.username = v
            def done(resp, err):
                try:
                    if err or not resp:
                        run_on_ui_thread(lambda: BulletinHelper.show_error(L.g("not_found")))
                        return
                    users = _jlist(getattr(resp, "users", None))
                    if users:
                        uid = int(str(users[0].id))
                        plugin._read_whitelist.add(uid)
                        plugin._save_read_whitelist()
                        run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("added")))
                    else:
                        run_on_ui_thread(lambda: BulletinHelper.show_error(L.g("not_found")))
                except Exception:
                    run_on_ui_thread(lambda: BulletinHelper.show_error(L.g("error")))
            send_request(req, RequestCallback(done))
        except Exception:
            pass

    def _load_presence(self):
        try:
            data = json.loads(str(self._prefs().getString(PRESENCE_KEY, "{}")))
            self._presence_tracked = set(data.get("tracked", []))
            self._presence_records = data.get("records", {})
            self._presence_last_seen = {str(k): _int(v) for k, v in data.get("last_seen", {}).items() if _int(v) > 0}
        except Exception:
            self._presence_tracked = set()
            self._presence_records = {}
            self._presence_last_seen = {}

    def _save_presence(self):
        if not self._presence_dirty:
            return
        with self._presence_lock:
            try:
                for k, rows in list(self._presence_records.items()):
                    if len(rows) > 2000:
                        self._presence_records[k] = rows[-2000:]
                data = {"tracked": list(self._presence_tracked), "records": self._presence_records, "last_seen": self._presence_last_seen}
                self._prefs().edit().putString(PRESENCE_KEY, json.dumps(data)).apply()
                self._presence_dirty = False
            except Exception:
                pass

    def _presence_now(self):
        try:
            return int(ConnectionsManager.getInstance(int(UserConfig.selectedAccount)).getCurrentTime())
        except Exception:
            return int(_time.time())

    def _presence_decay(self):
        i = _int(self.get_setting("presence_decay", 1), 1)
        return [30, 45, 60, 120][i] if 0 <= i < 4 else 45

    def _presence_is_hidden(self, uid, account=None):
        try:
            acc = int(UserConfig.selectedAccount if account is None else account)
            u = MessagesController.getInstance(acc).getUser(Long(int(uid)))
            if not u:
                return False
            st = getattr(u, "status", None)
            if not st:
                return True
            name = str(st.getClass().getSimpleName())
            return name in ("TL_userStatusEmpty", "TL_userStatusRecently", "TL_userStatusLastWeek", "TL_userStatusLastMonth") or _int(getattr(st, "expires", 0)) <= 0
        except Exception:
            return False

    def _presence_apply_last_seen(self, uid, ts, account=None):
        try:
            acc = int(UserConfig.selectedAccount if account is None else account)
            user = MessagesController.getInstance(acc).getUser(Long(int(uid)))
            if not user or not self._presence_is_hidden(uid, acc):
                return
            current = _int(getattr(getattr(user, "status", None), "was_online", 0))
            if current >= int(ts):
                return
            status = TLRPC.TL_userStatusOffline()
            status.was_online = int(ts)
            user.status = status
            NotificationCenter.getInstance(acc).postNotificationName(NotificationCenter.updateInterfaces, Integer(4))
        except Exception:
            pass

    def _restore_presence_statuses(self):
        if not self.get_setting("presence_track", False):
            return
        for uid, ts in list(self._presence_last_seen.items()):
            self._presence_apply_last_seen(_int(uid), _int(ts))

    def _presence_remember(self, uid, ts, via, account):
        uid = int(uid)
        ts = int(ts)
        if uid <= 0 or ts <= 0:
            return
        key = str(uid)
        if ts > _int(self._presence_last_seen.get(key, 0)):
            self._presence_last_seen[key] = ts
            self._presence_dirty = True
        hidden = self._presence_is_hidden(uid, account)
        if hidden:
            run_on_ui_thread(lambda: self._presence_apply_last_seen(uid, ts, account), 100)
        if uid in self._presence_tracked and (not self.get_setting("presence_only_hidden", True) or hidden):
            self._presence_lamp[uid] = {"last_active": ts, "via": str(via)}

    def _presence_lamp_on(self, uid):
        e = self._presence_lamp.get(int(uid))
        if not e:
            return False
        return (self._presence_now() - int(e["last_active"])) < self._presence_decay()

    def _presence_tick(self):
        try:
            decay = self._presence_decay()
            now = self._presence_now()
            for uid, e in list(self._presence_lamp.items()):
                la = int(e["last_active"])
                via = e.get("via", "?")
                on = (now - la) < decay
                cur = self._presence_open.get(uid)
                if on:
                    if cur is None:
                        self._presence_open[uid] = {"start": la, "end": la, "via": via}
                    else:
                        cur["end"] = la
                        cur["via"] = via
                else:
                    if cur is not None:
                        k = str(int(uid))
                        rows = self._presence_records.get(k)
                        if rows is None:
                            rows = []
                            self._presence_records[k] = rows
                        row = [int(cur["start"]), int(cur["end"]), str(cur.get("via", "?"))]
                        if not rows or rows[-1] != row:
                            rows.append(row)
                            self._presence_dirty = True
                        self._presence_open.pop(uid, None)
        except Exception:
            pass

    def _presence_on_update(self, update_name, account, update):
        try:
            uid = 0
            via = "?"
            ev_date = 0
            if update_name == "TL_updateUserStatus":
                uid = _int(getattr(update, "user_id", 0))
                status = getattr(update, "status", None)
                expires = _int(getattr(status, "expires", 0))
                was_online = _int(getattr(status, "was_online", 0))
                now = self._presence_now()
                if expires > now:
                    ev_date = now
                    via = "online"
                elif was_online > 0:
                    ev_date = was_online
                    via = "offline"
                else:
                    cached = _int(self._presence_last_seen.get(str(uid), 0))
                    if cached > 0 and status is not None:
                        exact = TLRPC.TL_userStatusOffline()
                        exact.was_online = cached
                        update.status = exact
                    return
            elif update_name == "TL_updateUserTyping":
                uid = _int(getattr(update, "user_id", 0))
                via = "typing"
            elif update_name in ("TL_updateChatUserTyping", "TL_updateChannelUserTyping"):
                fid = getattr(update, "from_id", None)
                if fid and hasattr(fid, "user_id"):
                    uid = _int(fid.user_id)
                via = "typing"
            elif update_name == "TL_updateReadHistoryOutbox":
                peer = getattr(update, "peer", None)
                if peer and hasattr(peer, "user_id"):
                    uid = _int(peer.user_id)
                via = "read"
            elif update_name in ("TL_updateNewMessage", "TL_updateNewChannelMessage"):
                msg = getattr(update, "message", None)
                if msg and not bool(getattr(msg, "out", False)):
                    ev_date = _int(getattr(msg, "date", 0))
                    peer = getattr(msg, "peer_id", None)
                    if peer and hasattr(peer, "user_id"):
                        uid = _int(peer.user_id)
                    if not uid:
                        fid = getattr(msg, "from_id", None)
                        if fid and hasattr(fid, "user_id"):
                            uid = _int(fid.user_id)
                    via = "message"
            if uid <= 0:
                return
            try:
                my = int(UserConfig.getInstance(account).getClientUserId())
                if uid == my:
                    return
            except Exception:
                pass
            now = self._presence_now()
            if ev_date > 0 and via not in ("offline", "online") and abs(now - ev_date) > 30:
                return
            ts = ev_date if ev_date > 0 else now
            self._presence_remember(uid, ts, via, account)
        except Exception:
            pass

    def _presence_user_name(self, uid):
        try:
            u = MessagesController.getInstance(int(UserConfig.selectedAccount)).getUser(Long(int(uid)))
            if u:
                nm = (str(getattr(u, "first_name", "") or "") + " " + str(getattr(u, "last_name", "") or "")).strip()
                if nm:
                    return nm
                un = str(getattr(u, "username", "") or "")
                if un:
                    return "@" + un
        except Exception:
            pass
        return f"id{uid}"

    def _presence_fmt_dur(self, sec):
        sec = int(sec)
        if sec < 60:
            return f"{sec}s"
        if sec < 3600:
            return f"{sec // 60}m"
        return f"{sec // 3600}h{(sec % 3600) // 60}m"

    def _load_fp(self):
        try:
            self._fp_cache = json.loads(str(self._prefs().getString(PREMIUM_CACHE_KEY, "{}")))
        except Exception:
            self._fp_cache = {}

    def _save_fp(self):
        if not self._fp_dirty:
            return
        try:
            self._prefs().edit().putString(PREMIUM_CACHE_KEY, json.dumps(self._fp_cache)).apply()
            self._fp_dirty = False
        except Exception:
            pass

    def _show_presence_tracked(self):
        plugin = self
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                ctx = frag.getParentActivity()
                uids = sorted(plugin._presence_tracked)
                items = [f"{plugin._presence_user_name(u)} (id{u})" for u in uids]
                def on_pick(dlg, which):
                    dlg.dismiss()
                    uid = uids[which]
                    def confirm():
                        f2 = get_last_fragment()
                        if not f2 or not f2.getParentActivity():
                            return
                        d = AlertDialogBuilder(f2.getParentActivity())
                        d.set_title(L.g("remove"))
                        d.set_message(plugin._presence_user_name(uid))
                        d.set_positive_button(L.g("yes"), lambda dd, w: (dd.dismiss(), do_rm(uid)))
                        d.set_negative_button(L.g("no"), lambda dd, w: (dd.dismiss(), run_on_ui_thread(_ui)))
                        d.show()
                    def do_rm(u):
                        plugin._presence_tracked.discard(int(u))
                        plugin._presence_lamp.pop(int(u), None)
                        plugin._presence_open.pop(int(u), None)
                        plugin._presence_dirty = True
                        BulletinHelper.show_info(L.g("removed"))
                        run_on_ui_thread(_ui)
                    run_on_ui_thread(confirm)
                def on_add(dlg, w):
                    dlg.dismiss()
                    plugin._show_presence_add()
                d = AlertDialogBuilder(ctx)
                if items:
                    d.set_title(f"{L.g('tracked_users')} ({len(items)})")
                    d.set_items(items, on_pick)
                else:
                    d.set_title(L.g("tracked_users"))
                    d.set_message(L.g("list_empty"))
                d.set_positive_button(L.g("add"), on_add)
                d.set_negative_button(L.g("close"), lambda dd, w: dd.dismiss())
                d.show()
            except Exception as e:
                log(f"[MAX] presTracked: {e}")
        run_on_ui_thread(_ui)

    def _show_presence_add(self):
        plugin = self
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                ctx = frag.getParentActivity()
                d = AlertDialogBuilder(ctx)
                d.set_title(L.g("add"))
                inp = EditText(ctx)
                inp.setHint(L.g("username_or_id"))
                inp.setInputType(InputType.TYPE_CLASS_TEXT)
                inp.setSingleLine(True)
                d.set_view(inp)
                def on_add(dlg, w):
                    raw = str(inp.getText()).strip()
                    dlg.dismiss()
                    if raw:
                        plugin._resolve_and_add(raw)
                d.set_positive_button(L.g("add"), on_add)
                d.set_negative_button(L.g("cancel"), lambda dd, w: dd.dismiss())
                d.show()
            except Exception as e:
                log(f"[MAX] presAdd: {e}")
        run_on_ui_thread(_ui)

    def _resolve_and_add(self, raw):
        plugin = self
        try:
            v = raw.lstrip("@").strip()
            if v.lstrip("-").isdigit():
                uid = int(v)
                if uid <= 0:
                    BulletinHelper.show_error(L.g("invalid_id"))
                    return
                plugin._presence_tracked.add(uid)
                plugin._presence_dirty = True
                BulletinHelper.show_info(L.g("added"))
                return
            req = TLRPC.TL_contacts_resolveUsername()
            req.username = v
            def done(resp, err):
                try:
                    if err or not resp:
                        run_on_ui_thread(lambda: BulletinHelper.show_error(L.g("not_found")))
                        return
                    users = _jlist(getattr(resp, "users", None))
                    if users:
                        uid = int(str(users[0].id))
                        plugin._presence_tracked.add(uid)
                        plugin._presence_dirty = True
                        run_on_ui_thread(lambda: BulletinHelper.show_info(L.g("added")))
                    else:
                        run_on_ui_thread(lambda: BulletinHelper.show_error(L.g("not_found")))
                except Exception:
                    run_on_ui_thread(lambda: BulletinHelper.show_error(L.g("error")))
            send_request(req, RequestCallback(done))
        except Exception as e:
            log(f"[MAX] resolve: {e}")

    def _show_presence_log(self):
        plugin = self
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                uids = sorted(plugin._presence_tracked)
                if not uids:
                    BulletinHelper.show_info(L.g("list_empty"))
                    return
                items = []
                for uid in uids:
                    cnt = len(plugin._presence_records.get(str(uid), []))
                    lamp = "[ON] " if plugin._presence_lamp_on(uid) else ""
                    items.append(f"{lamp}{plugin._presence_user_name(uid)} — {cnt}")
                def on_pick(dlg, which):
                    dlg.dismiss()
                    uid = uids[which]
                    def _detail():
                        f2 = get_last_fragment()
                        if not f2 or not f2.getParentActivity():
                            return
                        rows = list(plugin._presence_records.get(str(int(uid)), []))
                        lines = []
                        for r in reversed(rows[-200:]):
                            try:
                                start, end, via = int(r[0]), int(r[1]), str(r[2])
                                a = _format_date(start)
                                dur = max(0, end - start)
                                lines.append(f"- {a} ({plugin._presence_fmt_dur(dur)}, {via})")
                            except Exception:
                                pass
                        body = "\n".join(lines) if lines else L.g("no_records")
                        d = AlertDialogBuilder(f2.getParentActivity())
                        d.set_title(f"{plugin._presence_user_name(uid)} ({len(rows)})")
                        d.set_message(body)
                        d.set_negative_button(L.g("close"), lambda dd, w: dd.dismiss())
                        d.show()
                    run_on_ui_thread(_detail)
                d = AlertDialogBuilder(frag.getParentActivity())
                d.set_title(L.g("online_journal"))
                d.set_items(items, on_pick)
                d.set_negative_button(L.g("close"), lambda dd, w: dd.dismiss())
                d.show()
            except Exception as e:
                log(f"[MAX] presLog: {e}")
        run_on_ui_thread(_ui)

    def _wipe_vault(self, _v=None):
        plugin = self
        def _ui():
            frag = get_last_fragment()
            if not frag or not frag.getParentActivity():
                return
            d = AlertDialogBuilder(frag.getParentActivity())
            d.set_title(L.g("wipe_storage"))
            d.set_message(L.g("wipe_confirm"))
            def do_wipe(dlg, w):
                dlg.dismiss()
                if plugin.dex and plugin.dex.loaded:
                    try:
                        plugin.dex.call("pyWipeAll")
                    except Exception:
                        pass
                if plugin.vault:
                    plugin.vault.wipe()
                plugin._invalidate_removed_cache()
                try:
                    attach_dir = _get_attach_dir()
                    if attach_dir and os.path.isdir(attach_dir):
                        import shutil
                        shutil.rmtree(attach_dir, ignore_errors=True)
                        os.makedirs(attach_dir, exist_ok=True)
                except Exception:
                    pass
                BulletinHelper.show_info(L.g("cleared"))
            d.set_positive_button(L.g("delete"), do_wipe)
            d.set_negative_button(L.g("cancel"), lambda dd, w: dd.dismiss())
            d.show()
        run_on_ui_thread(_ui)

    def _show_rx_manager(self, _v=None):
        plugin = self
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                act = frag.getParentActivity()
                patterns = plugin.vault.get_rx() if plugin.vault else []
                if not patterns:
                    d = AlertDialogBuilder(act)
                    d.set_title(L.g("patterns"))
                    d.set_message(L.g("list_empty"))
                    d.set_positive_button(L.g("add"), lambda dlg, w: (dlg.dismiss(), run_on_ui_thread(lambda: plugin._show_rx_add())))
                    d.set_negative_button(L.g("close"), lambda dlg, w: dlg.dismiss())
                    d.show()
                    return
                items = [f"#{i+1}: {p}" for i, p in enumerate(patterns)]
                def on_pick(dlg, which):
                    dlg.dismiss()
                    plugin.vault.remove_rx(which)
                    BulletinHelper.show_info(L.g("removed"))
                    run_on_ui_thread(_ui)
                dlg = AlertDialogBuilder(act)
                dlg.set_title(f"{L.g('patterns')} ({len(patterns)})")
                dlg.set_items(items, on_pick)
                dlg.set_positive_button(L.g("add"), lambda d, w: (d.dismiss(), run_on_ui_thread(lambda: plugin._show_rx_add())))
                dlg.set_negative_button(L.g("close"), lambda d, w: d.dismiss())
                dlg.show()
            except Exception as e:
                log(f"[MAX] rxMgr: {e}")
        run_on_ui_thread(_ui)

    def _show_rx_add(self):
        plugin = self
        def _ui():
            try:
                frag = get_last_fragment()
                if not frag or not frag.getParentActivity():
                    return
                act = frag.getParentActivity()
                d = AlertDialogBuilder(act)
                d.set_title(L.g("add_pattern"))
                inp = EditText(act)
                inp.setHint("regex")
                inp.setInputType(InputType.TYPE_CLASS_TEXT)
                inp.setSingleLine(True)
                d.set_view(inp)
                def _on_add(dlg, w):
                    p = str(inp.getText()).strip()
                    dlg.dismiss()
                    if not p:
                        return
                    try:
                        re.compile(p)
                    except Exception:
                        BulletinHelper.show_error(L.g("invalid_regex"))
                        return
                    plugin.vault.add_rx(p)
                    BulletinHelper.show_info(L.g("added"))
                d.set_positive_button(L.g("add"), _on_add)
                d.set_negative_button(L.g("close"), lambda dlg, w: dlg.dismiss())
                d.show()
            except Exception as e:
                log(f"[MAX] rxAdd: {e}")
        run_on_ui_thread(_ui)

    def create_settings(self):
        return [
            Custom(factory=_StickerFactory.instance.java),
            Text(text=L.g("ghost"), icon="ghost", create_sub_fragment=self._pg),
            Text(text=L.g("spy"), icon="msg_secret", create_sub_fragment=self._ps),
            Text(text=L.g("filters"), icon="msg_search", create_sub_fragment=self._pf),
            Text(text=L.g("hacker"), icon="msg_fave", create_sub_fragment=self._ph),
            Divider(),
            Text(text=L.g("settings_misc"), icon="msg_settings", create_sub_fragment=self._pmisc),
        ]

    def _pg(self):
        return [
            Header(text=L.g("ghost_header")),
            Switch(key="ghost_mode", text=L.g("ghost_toggle"), default=False, subtext=L.g("ghost_toggle_sub"), on_change=self._on_ghost_change, icon="ghost"),
            Text(text=L.g("ghost_settings"), icon="msg_settings", create_sub_fragment=lambda: [
                Switch(key="ghost_no_online", text=L.g("no_online"), default=True),
                Switch(key="ghost_immediate_offline", text=L.g("instant_offline"), default=True),
                Switch(key="ghost_no_typing", text=L.g("no_typing"), default=True),
                Switch(key="ghost_no_read", text=L.g("no_read"), default=True, subtext=L.g("no_read_sub")),
                Switch(key="ghost_no_read_stories", text=L.g("no_read_stories"), default=True),
            ]),
            Switch(key="read_on_action", text=L.g("read_on_action"), default=True, subtext=L.g("read_on_action_sub")),
            Switch(key="use_scheduled", text=L.g("use_scheduled"), default=False),
            Selector(key="silent_send", text=L.g("silent_send"), default=0, items=[L.g("silent_never"), L.g("silent_always"), L.g("silent_ghost")]),
            Divider(),
            Header(text=L.g("read_whitelist")),
            Text(text=L.g("read_wl_users"), icon="msg_contacts", subtext=L.g("read_whitelist_sub"), on_click=lambda v: self._show_read_whitelist()),
            Divider(),
            Header(text=L.g("outside_ghost")),
            Switch(key="story_ghost_prompt", text=L.g("story_prompt"), default=False, subtext=L.g("story_prompt_sub")),
            Switch(key="drawer_ghost_btn", text=L.g("drawer_btn"), default=True),
        ]

    def _ps(self):
        plugin = self
        items = [
            Header(text=L.g("max_vigilance")),
            Switch(key="keep_app_alive", text=L.g("keep_alive"), default=False, subtext=L.g("keep_alive_sub")),
            Divider(),
            Header(text=L.g("spy_mode")),
            Switch(key="save_deleted", text=L.g("save_deleted"), default=True, subtext=L.g("save_deleted_sub")),
            Switch(key="save_edits", text=L.g("save_edits"), default=True, subtext=L.g("save_edits_sub")),
            Switch(key="save_bot_chats", text=L.g("save_bots"), default=False),
            Switch(key="presence_track", text=L.g("save_online"), default=False, subtext=L.g("save_online_sub")),
            Switch(key="track_own_deletions", text=L.g("save_own_del"), default=False),
            Divider(),
            Header(text=L.g("appearance")),
            Switch(key="del_show_mark", text=L.g("show_mark"), default=True),
            Switch(key="del_use_icon", text=L.g("use_trash_icon"), default=True, subtext=L.g("use_trash_icon_sub")),
        ]
        if not self.get_setting("del_use_icon", True):
            items.append(Input(key="del_custom_label", text=L.g("del_text"), default="del"))
        items.append(Input(key="del_color", text=L.g("del_color"), default="FF5252"))
        items += [
            Switch(key="del_show_opacity", text=L.g("transparent_del"), default=True, subtext=L.g("transparent_del_sub")),
            Selector(key="del_opacity_level", text=L.g("opacity_level"), default=2, items=["100%", "80%", "60%", "40%", "20%"]),
            Divider(),
            Header(text=L.g("storage")),
            Switch(key="save_attachments", text=L.g("save_attachments"), default=False),
            Text(text=L.g("attach_folder"), icon="msg_folder", subtext=plugin._get_current_folder_display(), on_click=plugin._pick_attach_folder),
            Text(text=L.g("attach_folder_reset"), icon="msg_reset", on_click=plugin._reset_attach_folder),
            Selector(key="max_folder_size", text=L.g("max_folder"), default=2, items=["300 MB", "1 GB", "2 GB", "4 GB", "8 GB", "16 GB", "No limit"]),
            Selector(key="auto_cleanup_age", text=L.g("auto_cleanup"), default=0, items=["Off", "3 days", "7 days", "14 days", "30 days", "90 days"]),
            Text(text=L.g("browse_attachments"), icon="msg_gallery", subtext=L.g("browse_attachments_sub"), on_click=plugin._show_browse_attachments),
            Text(text=L.g("wipe_storage"), icon="msg_clear", on_click=plugin._wipe_vault),
            Divider(),
            Header(text=L.g("tracker")),
            Switch(key="presence_only_hidden", text=L.g("only_hidden"), default=True),
            Selector(key="presence_decay", text=L.g("decay"), default=1, items=["30s", "45s", "60s", "120s"]),
            Text(text=L.g("tracked_users"), icon="msg_contacts", on_click=lambda v: plugin._show_presence_tracked()),
            Text(text=L.g("journal"), icon="msg_log", on_click=lambda v: plugin._show_presence_log()),
            Text(text=L.g("wipe_tracker"), icon="msg_clear", on_click=lambda v: self._wipe_presence()),
        ]
        return items

    def _wipe_presence(self):
        self._presence_records = {}
        self._presence_lamp = {}
        self._presence_open = {}
        self._presence_dirty = True
        self._save_presence()
        BulletinHelper.show_info(L.g("cleared"))

    def _pf(self):
        self._load_filters()
        items = [
            Header(text=L.g("filters")),
            Switch(key="filters_enabled", text=L.g("enable_filters"), default=True, subtext=L.g("enable_filters_sub")),
            Text(text=L.g("regex_patterns"), icon="msg_search", on_click=self._show_rx_manager),
            Divider(),
            Text(text=L.g("add_filter"), icon="msg_add", create_sub_fragment=lambda: self._fe(-1)),
        ]
        for i, f in enumerate(self._filters):
            idx = i
            items.append(Text(text=f"{'[+]' if f.get('enabled',True) else '[-]'} {f.get('expression','')}", subtext=L.g("inverted") if f.get("inverted") else None, create_sub_fragment=lambda idx=idx: self._fe(idx)))
        return items

    def _fe(self, index=-1):
        is_new = index == -1
        f = {"expression": "", "enabled": True, "ignore_case": True, "inverted": False}
        if not is_new and 0 <= index < len(self._filters):
            f = self._filters[index]
        pl = self
        def save(v):
            try:
                e = pl.get_setting("fe_expr", "")
                if e:
                    entry = {"expression": e, "enabled": pl.get_setting("fe_on", True), "ignore_case": pl.get_setting("fe_ic", True), "inverted": pl.get_setting("fe_inv", False)}
                    if is_new:
                        pl._filters.append(entry)
                    else:
                        pl._filters[index] = entry
                    pl._save_filters()
                    BulletinHelper.show_info(L.g("saved"))
                else:
                    BulletinHelper.show_info(L.g("enter_expr"))
            except Exception as ex:
                log(f"[MAX] fSave: {ex}")
        def delete(v):
            try:
                if not is_new and 0 <= index < len(pl._filters):
                    del pl._filters[index]
                    pl._save_filters()
                    BulletinHelper.show_info(L.g("removed"))
            except Exception:
                pass
        items = [
            Header(text=L.g("new_filter") if is_new else L.g("editor")),
            Input(key="fe_expr", text=L.g("regex_expr"), default=f["expression"]),
            Switch(key="fe_on", text=L.g("enabled"), default=f["enabled"]),
            Switch(key="fe_ic", text=L.g("case_insensitive"), default=f["ignore_case"]),
            Switch(key="fe_inv", text=L.g("inverted"), default=f["inverted"], subtext=L.g("inverted_sub")),
            Divider(),
            Text(text=L.g("save"), icon="msg_check", on_click=save),
        ]
        if not is_new:
            items.append(Text(text=L.g("delete"), icon="msg_delete", on_click=delete))
        return items

    def _ph(self):
        return [
            Header(text=L.g("hacker")),
            Switch(key="hide_ads", text=L.g("hide_ads"), default=True),
            Switch(key="hide_feed_ads", text=L.g("hide_feed_ads"), default=True, subtext=L.g("hide_feed_ads_sub")),
            Switch(key="no_forward_limit", text=L.g("no_fwd_limit"), default=False, subtext=L.g("no_fwd_limit_sub")),
            Switch(key="fake_premium", text=L.g("local_premium"), default=False, subtext=L.g("local_premium_sub")),
            Switch(key="keep_once", text=L.g("once_media"), default=False, subtext=L.g("once_media_sub")),
            Switch(key="hidden_gifts", text=L.g("hidden_gifts"), default=False, subtext=L.g("hidden_gifts_sub")),
            Divider(),
            Header(text=L.g("bypass")),
            Switch(key="allow_screenshots", text=L.g("screenshots"), default=False, subtext=L.g("screenshots_sub")),
            Switch(key="bypass_noforward", text=L.g("noforwards"), default=False, subtext=L.g("noforwards_sub")),
            Selector(key="fwd_mode", text=L.g("fwd_mode"), default=0, items=[L.g("fwd_copy"), L.g("fwd_try")]),
            Switch(key="bypass_story_save", text=L.g("stories_bypass"), default=False, subtext=L.g("stories_bypass_sub")),
        ]

    def _pmisc(self):
        plugin = self
        def _open_channel(v):
            try:
                from android.content import Intent
                from android.net import Uri
                ctx = ApplicationLoader.applicationContext
                intent = Intent(Intent.ACTION_VIEW, Uri.parse("https://t.me/MaxExteraPlugins"))
                intent.setPackage(ctx.getPackageName())
                intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
                ctx.startActivity(intent)
            except Exception:
                pass
        def _on_lang_change(new_index):
            try:
                new_lang = ("auto", "ru", "en")[new_index]
                ApplicationLoader.applicationContext.getSharedPreferences(PREFS_NAME, 0).edit().putString(L._prefs_key, new_lang).apply()
                L._lang_cache = [None, 0]
                plugin.set_setting("plugin_lang", int(new_index), reload_settings=True)
            except Exception:
                pass
        def _cur_lang_idx():
            try:
                raw = ApplicationLoader.applicationContext.getSharedPreferences(PREFS_NAME, 0).getString(L._prefs_key, "auto")
            except Exception:
                raw = "auto"
            return 0 if raw == "auto" else (1 if raw == "ru" else 2)
        return [
            Header(text=L.g("info")),
            Text(text=L.g("author_channel"), icon="msg_channel", on_click=_open_channel),
            Text(text=L.g("credits"), icon="msg_fave", create_sub_fragment=lambda: [
                Header(text=L.g("credits")),
                Text(text="@exteraOwner", subtext=L.g("cr_extera")),
                Text(text="@bleizix", subtext=L.g("cr_bleizix")),
                Text(text="@shiawasez", subtext=L.g("cr_shiawasez")),
                Text(text="@ThisisGame_tig", subtext=L.g("cr_thisisgame")),
                Text(text="@summering", subtext=L.g("cr_summering")),
                Text(text="@homewatcha", subtext=L.g("cr_testing")),
                Text(text="@Jebebr", subtext=L.g("cr_testing")),
                Text(text="@AdzusaNakano", subtext=L.g("cr_testing")),
                Text(text="@Farewalls", subtext=L.g("cr_testing")),
                Text(text="@dyr0v_ukraine", subtext=L.g("cr_testing")),
                Text(text="@ablipo", subtext=L.g("cr_testing")),
                Text(text="@kvuco", subtext=L.g("cr_kvuco")),
                Text(text="@binbash_ch", subtext=L.g("cr_binbash")),
                Text(text=L.g("cr_ai_title"), subtext=L.g("cr_ai")),
            ]),
            Divider(),
            Header(text=L.g("params")),
            Selector(key="plugin_lang", text=L.g("language"), default=_cur_lang_idx(), items=[L.g("lang_auto"), "Русский", "English"], icon="msg_language", on_change=_on_lang_change),
            Divider(),
            Header(text=L.g("updates")),
            Switch(key="check_updates_on_start", text=L.g("check_updates"), default=True, subtext=L.g("check_updates_sub")),
            Switch(key="author_channel_warning", text=L.g("author_warn_toggle"), default=True, subtext=L.g("author_warn_toggle_sub")),
            Divider(),
            Header(text=L.g("backup")),
            Text(text=L.g("export_to_saved"), icon="msg_saved", subtext=L.g("export_to_saved_sub"), on_click=plugin._export_settings),
        ]


__plugin__ = MaxReExteraPlugin


# __DEX_BEGIN__
# eNrMvXd8VMX6+D+nbDbZtM2GUAKaJYAGRU2oQUFCkSKIlAWkKIhgQYgEQRBBQQRRUbH3dkVsqFxE
# RURFxauIKCqIKHYUvRev2Lv83s+cZzcLRD+f7/eP3+sLr3eemXnmzMyZM2f6nB03fkakvE07c9fc
# jT/884amZ5x43JMTi29+sCK/5obKzTd8/Vurtx0z2RgzY2jbmNF/o7Y45qGQse5NHGOaNzCmG/La
# xjgglx1izLkesrUxI3Ca2p/rZzlm5XBjIr0dkwP5EIP6cCA0g8OgEo6GPjAUzoe5MB8uhSfhaXgB
# XoHPJIw+jukJt8Dz8CuceKxjVsAG+AQO7OuYEfAIHNTPMVVQDTfCemh4nGP6w2AYDmPgVJgIU2AG
# nA8XwRVwLdwM98CDsAJWw3OwAd6Ct+Fd+BA+g13QuD9pgUNhHNwFLY/HL8wb4Jgt8D58AbvhN/gT
# YgO5T2gBR0IP6Aez4Uq4A5bBi/AW7IRvoNEgxzSBptACDoH20BGqoDcMgRNgFIyB8XAGTIIpMB1m
# w3OQMdgxCbgUvoBd8DV8Bz/D7+Ak8AdZkAsFUAQl0BwOgSOgDVTC0dAdekE/GAzDYTSMgwlQAzPg
# fFgIl8NVcD+8CjvBG+KYg6EbTIMbYQV8AN5Q8h2GwhxYBlvhW2gyjDTACJgHt8ETsBl+hbITHDMA
# ToHZcA3cB6/CTsgbzr3AiTAVFsISWA/fQniEYw6H3jARboDH4D34CZqNJM0wGmbCXfAy/BdKRzlm
# GFwFr4J/omPawTi4CO6Bf8FOyDyJsgTHwpmwCJbBq7ALckY7phX0g4mwCJbBG2DG8EzgJLgc1sB/
# oORk7h1mw2PwFbQYy73CdbABfoIWpzhmEFwAy2AbZIwjrTAWroQ18CXUH8/9wllwK6yHH6HZqcQF
# s+EB2Ar+aY4phxPhUngCdkDh6YQBNXA7bICfocUZpAMugAfhHQhNcEwFnAiXwGPwMeSeSbmDSbAY
# noVvID6R+gTGwUJ4CDbCbqg/iXuCgXAJrIKPIFztmAbQGvrDKTAbFsP9sBbegl8g7yzuCbpBAs6B
# S+F2WAkvw6fwE2RPpgzAMXAGXAaPwpvwHeTVkB4YCJNgISyFp2Ez7AJ/imMOgHbQC0ZBDVwMN8GD
# 8Cy8DTvhZ8g62zHFcDycC0vhOdgOn8FvEJvqmM5wBlwBj8IHkDGN8g7DYS48DO9A5BzKGAyCC2AJ
# bISfoHQ6aYNT4WJYBlvgNyiZwbsDZ8Bl8E/4DDLOdUwHmA63w8vwLRw8k3cAzoZrYC38F+LncT8w
# E+6HD8GhXToI2kEPGAwj4VSogQWwCG6C2+BeeBAeh63wAeyAr+BbaDqb6+F6WAV55/OcYTZshLfg
# XfgKvoXsC3gn4Vg4FRbCXfA8bINvIW8OdRD0htFwAdwAT8Am+Ax+hay5tKtQDM1hEVwHN8Jd8Ag8
# CqtgDTwH/4JX4U3YBh/D57ALvoGf4A/wL6R+gVwohIZwADSDg6EVtIaO0AV6Ql8YCCfAiTAOzoQa
# mA7nwwK4HK6H2+AfsBSWw5PwDDwHL8J6eA02wzvwPnwKX8BXsBu+h5/hdzDzKCOQByXQBnrDiTAT
# 5sNiuBZuhFvgDlgCy2ENrIe34VPYDX9A5CLedWgOFdAZjoVhMA5q4Hy4DG6Ee2AFPAuvwjb4HL4D
# M588hWI4GNpAF+gLw+AUOAtmwSVwNdwGj8IaeA7Ww1vwGXwNGQsoB1AKbaEXnAJnwgWwAK6Ge+BB
# eBSegnWwBd6BXfAnZF7Mew5NoAzaQw8YCmfAVFgAN8GtcD+sglfgffgcdsE38CtkLHRMPTgADoOj
# YSCMg0lwLsyBK+F2uB+Ww9PwMmyH38C/hHIITeBwqIRjYBCMhPEwCy6CG2AZPAkvw3b4An6AzEvp
# k0EZHAVHQ18YCMNgDEyAapgLl8B1cDPcCffDcngetsAO+BH8y2hvoQm0giOhOxwLo2ECnAVz4GK4
# Ee6Ch2EVrIHn4G3YBh/CLtgNv0F4EWUQmkI5dIE+MBhGwhiogZlwCdwOS+ERWAsb4U3YCtvhY/gc
# /gO74Sf4HZzLaT+hEBpBc2gHPaEfjIIz4Xy4Ae6FR2E1PA/r4U34D3wH3hWkG5rCEdARukE/GAhj
# oRpmwXy4Cu6Bh+FZ2ADvwg74HkJXUs6hFRwFPWEojIGz4Qr4B6yA52E9vAf/BXcxdQMcCIdCJRwD
# x8MUWADXwFJ4HF6ALfAZ/AL+VdTfcAAcDp2gDwyE0TABzoNL4Wq4G5bBU/ASbIMdsBt+gcyrKS9Q
# BpXQF06Cs2A2XAa3wQpYA5vgffgP/AjeNTwjKIP20BUGwFiYADNhHlwNt8A98Aisg5fhY/gK/oTc
# a3nW0A46Qjc4Bo6HE2ACnAPXws3wD1gOa+EV2AwfwL/hB/gDwtdRL0E9KIam0AoqoTv0hSFwGkyG
# c2EuLIQr4S64Fx6B1fASbIS3YBvsgN3wK2RdT18NDoCDoTW0h6PhWBgICTgBJsDZcDFcB7fDElgG
# T8IL8DpshX9D6AbKDcShHVTBIBgP58ICuBJuhDvgYXgcnoWXYTN8Bl/B9+DeSHgQh5bQFnpBfxgF
# p8I0mAPXwG3wCKyC9fAmfAb/gR9gD2TdxDsLzeBwaAe94VQ4D66BZbAW3oNvIPdm/ENrGAIjYQZc
# BytgI3wBv0PBLfTvoBoehxdhG3wP+beSt3AWrIQ34Qsou438hwRcCw/BL1DvdtoCOBpOgClwGSyF
# tfARmDtIHzSEZnAEdIBjYAAMhTEwGWbCQlgMd8EyeASegJdhJ3wL/p2ECfWhMTSDttAfxkI1nAcL
# 4Uq4BR6BVfAveAs+gV3wK5i7eIchF2JQDAdDG+gJ/WEUjIWZMB+uhJvhTrgXVsKzsBE+hK/hd3D/
# Qb5CUzgM2kN3GA5nwRQ4D+bBdXA73Adr4DV4D76Ab+EP8O4mnVACZVAOR8NxMBROhkXwEvwXmi2h
# TMBUeAQ+gAb3UOdBV+gHQ2E4jINqOBfmwSVwJdwE98AyWA6PwwuwDT6CnbAb9kDuUtIGh8HRMABO
# gqkwH66FJbASXoT34RvIvJcyAgdDJXSBnjAcToIJMAVmwwK4FZbDGtgI22EnfAPh+6g/oAKOgYEw
# Cs6Ac+FquB9WwQuwET6Cb8DcT9sAjaEFHArt4RgYAqfARJgJV8L1cAc8DE/AOtgM2+Aj+AK+A+8B
# 3hfoBAPhJDgTpsM8uBqWwKOwFv4FG+Bd+BQ+h58g40HHROFAOBg6Qi8YDOPgXLgEroe74SFYDS/B
# dvgOIsso59AADoEj4GgYCafBTFgMS2EdvAafwe+Q/RDPF9pAFxgGp8J0uARugXvgMfgXvA2fwQ/g
# P0z9AyVwKHSC3jAIxsK5MBeuhiXwGLwIr8F7sBN+gexHeJ+gLfSGUTAVLoIb4QF4HNbDFvgMfoXs
# 5dQZcBC0hW7QD0bCOKiBc+FCWAz3wApYA2/Dp/At/AaRf1L3Qz1oAe2gGwyGcTANLoSb4C54GJ6G
# F+AVeAO2wgfwJeyGPZC/gmcLh0MVDIbRcBrMgIvhFlgCj8Bj8DS8CG/ANvgCvoM/IfQozxpKoCW0
# h64wFEbDeDgDJsE0mA1XwPWwCj6B3eCupF2Cg6AtdIYhcDJMgClwLlwIl8FiuAluhfvgYXgMVsFT
# 8BJsgE2wBd6FGY8RP8yHS+FKuBbuhn/CK/AmbIOP4Uv4Bn4B53HaTiiARtAUqpjD7ifz3jBe5rPh
# GrgWroPr4Qa4EW6Cm+EWuBVug9vhDrgT7oJ/wN2wBO6BpXAv3Af3wwPwICyDh+BheASWwz9hBTwK
# K+ExeByegFXwJKyGp2ANPA3PwLOwFp6D5+EFWAcvwr/gJXgZ1sMrsAFehY3wGrwOm+ANeBPegs2w
# Bd6GrfAObIN34T3YDu/DB/AhfAQfwyfwKeyAz+Bz2AlfwJfwb/gP7IKv4L/wNeyGb+Bb+A6+hx/g
# R/gJfoZf4Ff4DX6HP+BP2GOCtQkHXPDAhxBkQBgyIQsikA05kAt5kA9RKIAYFEI9KIL60AAaQiMo
# hsZOsDZyABwIJRCHplAKzWTdBFrAQXAwlEFLOAQOhVZwGBwOR0A5VEBraANtoR20hw5QCR3hSDgK
# OkFnOBq6QBV0dYK1mu7QA46BntALWEoxLJ0YlksMyySGZRHDkoVhicKwPGFYhjAsKRiWAQzT/IYp
# d8PUumEK3TBNbpgKN0x3G6atDdPRhilno1PLhmljwzSxYVrYMNVrmNo1TOMapmcNU66GqVXDNKph
# etQw7WmYtjRMQxqmGQ1TiobpQ8M0oWE60DD1Z5jmM0zpGabwDNNuhqk2w3SaYbrMMBVmmLIyOgVk
# dJrFMJ1hmIIwTB8Yhv6GYbphKG0Y5hqGpobhpGFYaBjaGYZjhiGVuUbWtYAhhaHrb+iuG7rXhu6y
# oRts6O4auraGbqzRbqrRrqChm2XoOhm6Sma7lCeXcgINoREUwwFwIJRAHJpC/6Commt5WFLnuLru
# dqKab8X9JDUvwSx1kqd+TlXzMtxPU/NKzKereQ3mM9S8DvNENb+aZt6MeYKat6e570hzl3jPVPMu
# zJPU/H2aWQra2WrO6VZ7bT3MUof6muZpapZ0nqPmJviZrubmmM9Vs6Qt6b49zSzpmaHmVmnXtsU8
# ExnSuBZABnTCPWnugfkyNfdT/2H1L+6ZkMB9NjJX03mZmkep/zz1f7Gad6kfMY/Dz1Vqnqj+89P8
# 56f5j0r7on5iaWmup/d4IbJIn91CNc/S9It5oV7bSK8V8wFpcYl5MX6uRMalzdJ8EPMSvbY07dpm
# ap4rz0HKFX7EfJDkg157kOah5M/hmj8L1LwG90VqXof5cjVvwnw+srU8R8xz1Lwbs6Szq9Tfmp6h
# aflwnZaHBdrmSp7M1PY2+bykTfW7B+73pV17n/pPmnO6B89FzPW6B3ko5iaYr9Y2OEfT8FBaOA+l
# xfuwus/S9lneu9naRq/U57VSn9dMbaPl32w1i58r1Ny8e/BcxFzePUjnKi3nF6u5k6Zzlb6PSXOP
# tGv7pfk5IS2c5L2LeYzmz+q09KzW9Fyg5jWa/tWa/nlqbqL5v1rTdomaT9d4V2uZT5qndq/1PyvN
# nJNmvgjzedpvWdQ9yMOn0vLwKU3DAjVvTzMn70v6M9dqmC+q+2Xar7lV73dD2nPcoGVppvZp5N8F
# 2rdZo/f7lt7vRWpeouG/pWmYqf2g5Pvytprnq3m5xrs1rXxu1Wd3pZpX4Wexmtdp+LY/lWZOPq93
# 0uJ6N+1e3tN38GLtd+3Scvh+Wh6+n5aH76fl4fuaV7O1r5Z8fz9Iy0Mxf6TP5RP1c7H24b7QdIp5
# t5YBMf+cZpbOxkzt7yXTvzOt7O3UMOepuYnWJ1+o+2ztBybNX6t5pvYFk/kgfcLMHk7KHNV4f0jz
# I/3Fhj2CPPlJ8+RS7T/G9dqftZ26WPuRrTScP9PC+TMtfOlPJu8rV83btb8o/z5Us9QPH6lZ2vGP
# 1Sz38oma2xLmp2qWcrhDzZ1w/0zNUk9+rubeuO9UcwLzF2oeh/lLNU/E/G81z0gzS33+HzXPwX2X
# mhdh/krNN6b5X4L5v2qWfPtazfK8difD7FFrXoX5m6R/zN+q+dU0s7wj36l5c49a80dp5l1p/n/G
# /H0yb4+pNUtb84eaM6Vj6wbm6DG17k0wu+reHLOj5nLMvpo7Yc5Qc++0axOYw+o+CvMPyXxOS8PE
# NPNUzD+qeVaa+8I08+K0cG5Mc78zLa770tyXp5lXpcfbtdY8K80s/Zyf1bw27V5eTTNvTgtne5p5
# R5p5V5r/79Pcf08z+z1rzTk9a++rXpp7E8y/qLk55t/VXN6z9ll0SvMv73Iy3t5p7gPSzFKH/6bm
# E3rW+h+XZp6Y5n9qmnlJWpmZleb/ojQ/Uq+m8qdr7X1JW/xT8n3B/6/J55h2L3emhSl1ftJ9Wc/a
# 57sqLa5xac9uSVpcUp8nw1mbFv7LaeFvTgtzYtp9be9ZW+Z3pMW1O+0ZSf2WLCc/p92L36v2Pcrp
# VXttwzTzRWlpi6e5l2H2ku9Xr9r3qwpzSM390vyPSjOPS4+LtP2ZfI69asvMDMx7kmlIT0/3tPcr
# zf3GXrXpvLNX2vuFOTN5j+RblpqXp10rbUHyXV6X5r4Js4zrYlrP56pZ6vM8NUs9ma9mqSejan6H
# awvULO9p0twjzbwjzY+UvaRZyklMzbvS/EiZKVSzlI16ap6YFmbDNP/SB0u596g1705z/z7Nv+Rh
# kZp/7xW0fYVObTte6NT2DQq13C5Qs9878N8gra1s6NS2p8Wa/ovVvEvb98bqpz6xLvVEeuZeldP8
# QJ7jy5jHM+PcQD6DbIy8KhTIq5FN6WkOIJyD+S9T9G35L+OM9ugnu4Gc5AWywA/kuyqnhGRMcrjZ
# ir4HPcdP8H8M4YnsaSIqV5tPVe5A9lJ/x9KbCKRv9ccS3g4rd5rPkH1NO6vvywhO9AM0vAEa3kC1
# D1T7IA13EOGIfTD58omVq9UehC/yM5Wfq9xpZTvzBTKh4SToqX5qpW+vG6LuQ3E/x8p8M518GGZG
# W/dhGu8w/O2w8kkbzwlI0Q9Xf8M1PcM1P0ao+wh1H6Huo9R9lObPKE3/KA33JO7vASt986CVq80y
# K28yb3sibzDtSd9ornvHCeQ2KyPmRlekb563crxxvEDfwPrfYN5DjsFd4j8ZeSrPeazaT0nJiE3X
# OLUTijPAytFmkJUR5zQrc505VuY5N1j5vfmHlX+ae6wM0jHedDSbrDzcuJ7ImFOjsrFv/TnvIE9n
# ZlnSc4bKCSrPVDlR5SRTbU5DVqv9LJWTVdaonKLybJVTVU5TeY7K6SpnqDxX5UyV56mcpXK2yvNV
# XqByjsq5Ki9UOY8RmeTjPGbAJV8vUvtFap+v9vlqX6DXLcD94pDMYwT2hSovUblI5RWMzuT6K5jJ
# luuvVPfFKq9SebXKa1Req/I6yn2VLzKwX88IbKUTyGdVrlW51UrXTHdF+maGG7ivsTLmfGnli+bf
# Kv+jcpeVG8xdXuDvQSuvM/19kW+bC/xA/4GV95j/+jLfQb3nBvIAtU+10jcfWrnajA/JGoRnKn2R
# SbtvzgnJukRgvzll98xlIZk7udFM80Ryf77IDeZFP7Bv8GUdwzPH2/WMUt4RkavNo04gn1f5vhP4
# +8DKoWa2K/IW85CVB5hHrbze3O6JbG62WfmkeddK3+TZeGJOsZVbTTeVPax815yn+oVWvm0uUXmp
# ystULvKDcK/Q6xf7ss7SyDnFFfmS6WDtq8zpIZnfecg+v+UqV6hcqXIt/0U+z3+WhljDaGxeckV6
# ZkxI1jMiZrQr49Vl5jwrfzYVfmBv7csahmeOUNlZZRdf1jVob5xAspzIuNYzX/gyXl1tJoVkrLra
# fO2K9M1Aa99gzgjJmNQzUg/+zAqH74n8xRzmi1xtyq2MOQNUSrn4hStfdkX+Zub4si7imwvcQM5V
# uSAkayUbzKCQjHM9U+0F8vKQrJ38aM53RXrmIF/Gu6vNz24g831ZU4k5Q31ZQ+luLnRF8vy9QH6o
# 8iOVH6v8ROWnKneo/Ezl5yp3qvxC5ZeezM+uo+YUeaCJh0SWmOYqW4UC96OtPMB0szJujgnJ/G1T
# 04d0ZrHqdTj2CC30oZ7Ik0wfK1uaYVaOMqOsfN2cELLzu05HO89baoa4IpuZ/JDM4y41Y1yRB5s7
# fJEtzJ1WHmTu8mU+t7nZ6IqscLI9kXmm0srxZoQnc7xLzSJX5JFOxNozrD7KLG8XK0ervxbO81Y2
# d47yA9nJl3nhpWaC9MFMK3OtlVGz2g3c37TSOO9ZeYhZo/67kO5C6s+Odi75CXOmK3K5mWjl/WaS
# lY+aaiuXmktULrKy0Gy30jfvuzLPfLi500rftPYCeYrtjy214TcwR5jlss7BqmUb4i025aaVJ9Iz
# h5KeA9XfgcQbCYls5mRbea/JCQXu7ZAl6q+E/PhI1kjMYabcC9xf9kUaJxqSueu2xvOkf9fLtPRk
# /nqrva6U1bMLfZGHO/Os3GM2+TKn3d5UeYHs7su89lJzvCuyufO0F9hftu4lzhGE38J0MP18mecO
# 0nMQ70UgO5qTXJGV5gFP5C+mA/7LSL/oW7IK9wj6Q3jyryIP1etFLrT2DPO+J3KFud0XGbzvh9JD
# uyQk8+RBeT9c8/lwenwneoH9FCuD+zuc+NxQIEut/Ml0VHt3ZLk50uR7In0TRVZoOlpTAwbyISvb
# aHxtmO0b7opcal73RX5omhBOO9W3I18yrf1N08DKj0xLKz82bUPSXw7CbU89KLKDXteBWUoJt4OG
# 30HT1UHT1YHVZCckMrgfkUVWBvF30PvrwCrzQVa+aw5W9zIrHzaHIis1vkqNp5L01sO9o7p31Hg6
# ajwdmVWNqb3IyhKnoZVBvB1ZFThA9SVWBvF31PjEf3srfzRHqr+j1L2TlQ+YzlZuNlWql+dyFDl/
# MPfdiRweg+ys6Ttan09X8ktkN+qpO7xASvsl8hZfxiGZtn7qgf/tvoxHgvrqGLX3ZDVf7D0l330Z
# jwT+e6m+t8bTW+191N6HnvnDnoxPGlr/x6q+LzO/Yu+r9n7qv5/aj1P7cWrvr/b+aj9e7ccz49zb
# k/HNS9Y+QPUD1T5Q7YNYjRf7IE3/YFbQxT5Y9QlW6MWeUPsQvb8hah+q+TmUevh0O45Zan6zMs/8
# wxPZ3FltZZlZYP2Pt/2oofTvf7QyZOUwZqElnGEa7gnUI2I/Qe3D2bEg9uGazhFqH8Fq/knYR+p9
# jzQ55io7rvnS2kfp9Sfqcz5J/Y1mR0YgnzNn6/ilkRfIjlbWM0damWGOsjLL7A7GNc4vVpY7v1n5
# mPnTyj1mj5WrjWPHPUvNYCs/NydYWWVGWvm0WW/HQ/+x8Y9h54fcz8lqP5lye6Iv46PAPlbTfwo9
# GbGfovc/TvXjVD8ee7bdOxPc33h6nsfa8c9R5i43cP9Jx0fFdhz0mvnWyhfN916g92w4zZ1RfmB/
# xcpVpiAk8n5zfkjWvi+w/eLTNJ7TNP7T6WGJ/XR2jpzhyngqsJ+h9glqn6D2M9V+ptonqn2i2iex
# K0Xsk9Rerfpq7CNcGX8F9rNUP1ntk1Vfo/Ya1U9R+xS1n632s9X/2eo+Vd2nqn2a2qep/Ry1n6P2
# 6WqfruHMUPsM1Z+r9nPVPlPtM9V+ntrPU/sstc9S+2y1z1b7+Wo/X+M7X90vUPcL1D5H7XPUPlft
# c9V+odovVPs8duiIfZ4+z4vUfpGWz/lqn6/2BewS6mjnlILrF7C75z9eIHdZmeF85ck4MojnYvW3
# UO0L1X6J2i9R+6XcqZSvy7gzkYtUv0j1l3MH4n4Fu4XE/QpN75Xq70r1t1jti9V+ldqv0ny7Wu1X
# q/0atV+j/q9V+7Vqv07t17FDKpDBddel9EF/43pkpQlkRytnmcOtOz02N7CXq6xQ2drKiGljZZFp
# Z+VM097K001HKw9wOlnZ3OlsZQfnaCvZ22llfVNlZTfTzcqzTXcrvzY91N7TyibOySovtfI8c6XK
# xRreSk3HK3o/r2s6Pray0DSx4+hsM9jKhibhBf4mqjxX5UyV56mcpXK2yvNVzlM5X+UClRerXKjy
# EpWXqrxM5SKVV6hcrPIqlVervF7lDSpvVHmTyptV3qryNpX3e0F+P2plmfOclQc766zMd7Lt/MGB
# TokfPKdD/OC60Sqfs/JJc2wo0A8NyTxCUD5uYCTb0e5rfEnloWaAF8hxVm4yI3yRb5gxdr5hpfV3
# M7sUO9o9GetUHuQMdUUe4QyzMtPcbOWd5lkrl5rNVp5jyuw8xBRzvMqBVk43g6w82DnLC8Jbr/ZX
# rGyjcqlpaOct1ppmVhaZuVY+Y261cpW5Td1f9YNw3vSDdL1l5VPmE5U7rOzkfG/l82ZuKAj/ypDM
# KwTv0a2aT7fSU2tp5zfamUOsXG8OtbLCtLJynjnMDfy3tXKN6WDltabSylPNkVY+a46y8jrT1co/
# zTFWnmF6WdnD9Layu+lj5VjT18pOpp+GP1btNVZ2NlPU/WIrvzc3WZkwt2q8b1j5ndlipets03C/
# s/JY872m5wdN749W9jRZXnDfDbwgXSVecL/NrextDrFyuBkfzPfYftatjGefVbnWyk/Mf62MO197
# QTp/sDLT+VHlTyp/tzLH8e28TkMnZOUIk2HlSyZsZcTJVHuWH4QXsfIVk2OlcXLVvUhlIyuHmQP9
# IJ6mVmY7pVbeZPqqv4SGe4of5P8MKxub8/3geV6u/q60cpW528qDnCVWHuHco/EvVX/3Wlnh3Gdl
# nrnfyinmAZUPqlym8iErD3Ye9oP8fETtyzW8f2q8KzTeRzXelVaGnMesdJzH1f8Tmp5Van9S5WqV
# T6l8QfNlndq3WfmM+cgPyutH+jw+Vv2nKnfpPNxXVhY53/lBeT7Mvj9/mB5W/mZ6hoL3pZe+V31C
# QbqOs7KRM0LdR6o8UeXJKmus3GOmq/1clXNCwXszPyTzfYPMEE/kT6YiJPvDlpoZIdl3Hby/9zJi
# utOTvV8/WPt9Oo66j3y+ww3kB1aeZMJeII/xAvdVVhaamC+ys1PPD9yP9GUv93c2vPt1HuV+ZmKe
# 9mV/d+D+gLo/wI7gxXY+srlzliuyr7k0JPu/fRvuMuKbHJJ9Z8eZ312R/U3ME3mWaWjlZBO3ssY0
# tbKrU6r27mrvYeVJ5gQr+5mRVjZ3/mnloc4KKweZXr7sNT/JdPVkXvR4m45/an6sID3LPJG55lxf
# 5ACrXyntri+ykZlp5UDr/hjjkzI32KM+y8qwaeGLdE1HP7CLfELDfULDWaXPZRXpLPBEBvf/pPan
# VrND+jpXJPW2la2cd6zMMPd5Ips7T1oZ5N9qdj8PCYmsUnmEc5KVo804K8eoPMmcpXKa6i+18mSV
# p1j5lKbvKUYMMv/zFCOFQE5UOUlltcogHU/pfazhPu92RWabJZ7s0Q/Gk09zp4NDIpeaYSHZz3ai
# fR4vMhLN8ERm23nCF+nXdLGynxlt5e/mR+uvxPnJyj+NHxL5s2lk5a/mwJDsgwvK7cvsCD/WypF2
# nP+yXreekfOxvuz/9+3zfoUR1J+u7JML7ncD61uBXGEGW/dWTiBbOwn1t1Ddr7NysE33Bn0eGxi5
# vOMF7hv9wF8gg3mvDaybJaycZk4JyfmD4LpX9bqNjFwGhETOMTNDciZhlU3XJk3fJnbod7RnFAL7
# GzoP94am+w1N7xuavjc0/Dc0HW9in+yJHGSO82WPX5D+t9TfW5qOtzS9mxk5NfPkDMQg01nlWE/2
# /QX58DbhXm/lLLPVyuA5vq3hvc0Mgsz7iftGK4Nwt2o6t2o4WzW9WzX+rZrereT7a1YG123T+97G
# yKm3L/sDS+x9v8vJgKvdwC7l5D3i/9aVfYLzzTVWXmSa+7IPMLj+fX3f32fE08kTGZTf9xk5nWpl
# PXO6lRfb9L/PiEnkB3r9B3r9B6T3357IPDPe6i+x13/ACOo0KxvZcD5gZjiQ480ZVi7S8IL35UN2
# b6+36y4nmz1W+uY4T2RzZ5OVe8z7vpwtaeus9GR/4l2mnpWXmw5W9jH9rCxw6vsiG1i5gxHZYZ7I
# xSqvModbeYU5wpO9iiXOH34gm4VELjXDQ3JeZZzdyP65ztPv5AROR7tvsdC0sHZaYCt908YL3Ida
# SbttZTtnudqfUvsatT+j8gWVL6p8SeUGla+q3KjyNQ3ndbW/qfa31L7Zk72Ut5hcK28z7ay83bS3
# 8nqbT19wKulkT9ajCk2hlf8wB1p5g9X/l3Ae92TvJeMiV6RvDrD2G63+W33fvuWUSyAZL7kiC8wz
# vsiOTrVdx7rDvC37jLi+p13Hquf84Ms6U1B+9qikt2Sl42y387gOk1eNPZHNnV5WNnZ+9kWWOL+q
# /N3KLXbDv9hbWPmjXRdyneB98DRczwnmkzynvXOtXcfaaG6z61gVJseuO2WYIiubO0+o3CLrUFz/
# hyvyGtPN2jPMSer+kJX3mj99OVN0j8nzRDYxfT05X1Tfxp/pFKvMNPU9OXMUjJOynKtNW2uPOfVD
# IgtV3m9ay7qVs8H6ixBOhrWXOIUqW1j5o6lUebSVxU7XkJxl2mKvyyacQ6z9LVMekvNNR5vbXZFd
# rMx1XrX+cp1PzS++yBInHApklpU7TLGVn5nGVgbz33ma/jwnmN/Pcz5Q+bbxrD5IX76zxBxt57F5
# Ju2Dfeij2ONznPQPNnJ+e02w0acHFEQD/TjVL0Jfo/pj6tDfin6B6num6Seqfhn6W1Tfq47r16B/
# XPW969C/in6D6vuk6aeqfjv691V/bB3X70L/ter71qH/HX3u04G+Xx36nNc4H6r643QPclJ/v+wr
# Q99B9f3/Qt9H9cervkKl6FuhH6n6AXXE3wn9JNUPrEPfD/181Q+qI/9Hob9N9YPruH4i+sdUn6hD
# Pwv9dtUPqat8oP9J9UP1/NSqtPu7FX3OM4F+WB3Xr0R/jOpPqEO/Dv0pqh9ex/1tRj9D9SPS9LNU
# vwP9Naofmaa/SPXfo39Y9aPqiN9/nfPcqj9R76tr2v3VQ/+B6k9Ke/5JfXP0/1b9aHU/Ok3fFr1s
# bpm0nvcwLf5FGn8C/WnPBtefnPwQpFN7/Tj0s1Q/tq7nh/4q1Z9SR/4tQr9U9ePqer/RP6P68Rq1
# lxb/MvRvqf7UtOvT35eWawP9aXW9/1zfVvWnq/+lafnzKvqeqj+jDv0O9CNVP6EO/e/oJ6v+zDr0
# 9Tbx/qh+Yh36VuhvVv2kOvQ90D+i+uo69KPQr1P9WXXop6LfpvrJdegXof+v6mvq0C9B7z0X6KfU
# oV+DvqHqz65Dvxn9YaqfWod+F/peqp9Wh95/g7OSqj+nDn0T9DWqn16Hvi36BaqfUYe+H/pbVH9u
# Hfpx6JerfmYd+lnoX1T9eXXor0X/rupn1aFfhv5r1c+uQ78Ovf98oD+/Dv129I1Uf0Ed+u/Rt1H9
# nDr0OW/yjQTVz61D3xz9iaq/sA59J/RTVD+vjvcvgf5i1V9UR/0zDv0dqp9fh34q+idUv6CO+C9C
# /7rqL65Dfyv6X1S/sA79SvT5LwT6S+qqH9C3UP2lf1H/HKX6y/5C31P1i+qqXwh/oOov/4vrT1P9
# FXX1LyT/VX9lXc/3Lc5qqX5xXc8X/V2qv6qu54v+CdVfXYc+gX6j6q+pQz8R/aeqv7au54f+F9Vf
# V9fzQ5+/LtBfX0f7vwZ9pepvqKt93Mw3IV4M9Deqe2F6/YB+t+pvqiP8HujD/wr0N9ehT6AvVv0t
# dejHoT9M9bfWoV+H/kX0k2mfXf6LvpyPAsiZ5clR2W050ilgnC0a0bVTXSJaYGqicnoi7y+vi9V5
# XYzrMu0uN5//ouuILpJ2XaEZ6dZjJrEIPyFC8DVs8ZMoLzSJinom0brI1FSF3Wi3v46/fp3x1yf+
# PTbdTprOT12Xp6617jX21FaQXk/TK+e+a8pdt6mX48h5jb8KK4P/4l6Fe05a2hqwktqQVeVGzEAX
# W39BmO3UX2JAAzN4YEMzaHAjk0gUy3eK3WjrPOL3bHi98ZeZFl5j8qyJybNaV8Ox58vjjU2iaRNT
# E//Vif4f3fPve+VR/338y5MTBuHew/p/S9521q+6pMzFzntezK0uf4mTKRFGcp4pK5fSH2W1N4+n
# 0IZDPDmmOlpBppa5ueYd13XKvqkNqzoqXwyJEGYIt3Z2h8NmjSU7Za7vxPtXV/Uy5U3zSFcje8ae
# NDuPOE1N7XMZ+xfpPw33Ipt+2eEvqag7joWFyTiCf57G4afFUZ2K4wx7Kj8LX1Lmzsa9vuijJ5O2
# iFcdHS3SLwrHu1IWXczhC6fFnJgbnjM1o7299q+e8wE85wPrfM4H8JwP5DmH3OjfXF/C9fE6ry/h
# +jjXe397fVNG4KU2/1z7LpSnrm9qBjUt5Z3w3aiXh1umvX42+ljq+lFOM+JvboZ7LSj7BzHrdbAZ
# nlFmRoZbmjx7RVjTJNcMmdzMJGqam0FTWphBZx9kBk092AyaVmYS57Q008O/2fIZ4n9ddcghxHMo
# s2itrJ/gubRL1iFVh5hEV3ZDdmtFHWLse/VXdchhddYhh1F6/9C6T9t2dJ7VHcGslEwTedZ+Oe5N
# bf4cbqbE33HLKU1x50FmTrPYZ/ogZWIU8US8fC8RP8JMKa/PjuuIL75roie6PnMoU8rfdcsd2Rct
# T0TCvFbzvNic5sUceb9cJ8Je0GScN9UZ5wqNcwVxTt4rzgZpcU7ZJ05fw7zziODZyP1NKW/IulCO
# K2e3kuXgXvT1NN9cdsRuwyRnbUYSarFp6Us6sxlZ5Xgjmx2RlqePpPKUtEa3uMEzDeJcia6B3sfe
# uWHTHZX3NofUxEh/jjeFFsmz38JIXv/UXtdP+cvrC/e7PlRnnV1O2a+gXWrNrrY2+5QrW2dXlZtB
# XSv4pkdrk+jexlRXfW7+rmy1rbNstSWtO83ftY/tKNvtSUeHOtvHdrSP7WkfOxD/DiPtY7qfbOun
# Ej8d8XMkfj7DT8QPnnXd8VUSX0fiO1LrBVfrhSzZQ+2cxDdmTqLe+JQyF3Hy/y/awqNoCztRH3Sm
# LTy6zrbwKNrCTrSFnWkLjzbViS9S+SppeT4ZT/x2k97Gvbxf/J4NdyPuMpcQq9fmmAQ7f8mJ8p5c
# vRBtkRnQtbr8BuYCC/0CUx1fhKnID+ppn1wpcu6sEjM55Kqbr3aneuDVJtSt1D+QGDsSc5EZY6+T
# NuxCwuqE2xjW1Gqi/e1pgOr4kbhUxy8hjjH+H9ZdwquOX4dLqX8iaXqPWasCX8yloTaazomkM6lJ
# 9kk+Tr6fXk/q4C1OoRsryLdHbIO8+Hfq/T2W9/8pm6Z8U6v/+i/0nubx98myI/r447QhOa489yGl
# XewZ1LB+TehX/B1sn1l35uvupOX8wutMbZUYgn3InWbu4LKMxJBuZvDge80zhYnxgWvnED7UPHcI
# PsbjYwg+iiqY6c23ZzOCdIT5WFUj+64F6RjAzv6SEOkY0IUWO0ve9/bj1w0eVGXWxIOYBg25x8wd
# ELVhBO91HmEU2HJxG/ZS1gUnR2U//UjubWSspxlZ2NuMqNfL1gW5tjahnHONfP+lMnsJNcfrbraJ
# ZFWyGaSlnF7KCkLIQrMJTU5kSvRVejyR7MrsxrhtxJyTXZJ9E3Gx8hSV72iMzO5mEk+xzyD6scMV
# 2SOzu3M/Kwij7JPSyEh8bbUlZI3L3vTIK6Y4+z63xp7CyDGTyx9yV5vSbFzNNSF1dcRVvi5WHV1F
# KDlZMSOpyKJcVjp9bCpwdeR9TcTI6VKejcezIW1J+9wWt4VCXlkoESP3W9xrSt3Xiet5d1BGdfxh
# WtMctzo+j9o9Eu4YbpJ2L7EGo8JdTGk48D3M+p5pcsIjw13NM+1GhKrsneWYcFxkE1O2Ms/mbbLs
# 9izXui9VdqXO8u3z7o+uocz9RvuwZ1Dq50RMSuhK0mFN8Ufd1ti/5ElWsjg9JTqHd7K2DhipYUsf
# UnTG9jOCGv7k8qCs1tgvZ+Rw6qieeycf86oe9KxTVZTD7onguTI6GvgBO+tL/WFm8rDXTdv6NVGP
# N7UsksubeQLv4hvMLQVuyf5hE+0fHmC/gST1Tpjccex+kUxcMzmNIul3vRJbjlsn36dmlGMvh3c7
# SltNzVTaFf21Rvps4hb2Sjxalmg1YSaa89yq/mFi9duwTS7WoMyNhQc37GXqR2QOPjNylOxTjPYm
# Zb+YoEebt5e9JurbekjqqliUq/GRYzqGI2Zw77ttCJWiaYgmM7irysyBNoTq6O+2Z14TlfNOOWZw
# P/zn5UQ65zWy5pr4NHe7ycmpjrfy1iIrc8Tvh04OfstCufTuykpiJpZb1lB8z3611n+Z+s8yibXb
# TXhjWUauCa8tC4c34j9HbCLDuTFSTZ5mElK22yQTk0uel4WcObHMWE4st5Ic7piTYTqyMSuWV+Y6
# eUVmTpdkbVyUz4GJAyVNe9x8E4kUZc+xtTpvdXZRdG1VUYGpmu7soSccyS+Kmq5qziqKxa05ZiIZ
# RfXmWHM9zhq5RWEzNrzJDJqeYS6cXpmXYerVb9OmTZcyv179ja/263JfOFz/4ulu/TxTmtfBZDaQ
# sis1+XTO0sTkrUIW0k+rjp+PqXrAReaZ5aWc4oh96zaMmNi/J5dPMPFIdfkkvm9YWX8A716WkyiU
# EEZ65awl3mfbGU/GFvHRuFSyLS38QpkbfqsyJ9OU5mY6k+PDvY2mLFyaI+YTPMnbWE5NPNd7gZJX
# nENvYfCJXnx7KT2LIq+sm4aXkwx3cvk087wn91uAbXgzTm4HeYI5z1SXn8tXCWT0xGkotwNlJLg7
# 18QK5e5ak+KgVaS2OLEw6ptCNuAU8rG6wuIwa8KZprKopblwSGFj8m9Nszb1nEIGAH1DXqyQA/7I
# wr45XiMowByFXGgMxdXxozh/NMb7xbaeQQ4ciUvZwUFYFw5JhZajoeUQWojQQn8d2vf7hFYdn8W9
# VI++yIw9o5TzR5IbkyP1WIScQ8p52iEx8bRDHnJ4KMfmyOE8q6OD/kU02b+Qd3m+7RnEGko/Y76J
# ZVWX32iuyxzDLjHtGWTa98nkHCi1lpjL/MFNetn3ZARPLpIZ5LT0E+abZHrujLiZzChkZtu4Bxmp
# CTJDDWiJaQNpu2rKP3FuNDmZlZkNbXpqyj915oSlVsin7m5vYr9IKDdEhl8QhOCGg1gSVUsMD8nW
# EuHJZZ61Z5XatmCxbQUZ17vhcLG5LSRuYTti5+RhZrJWqY7OF9esjlkVhHIEKSZHGgU5stjmyGRT
# lDWna6yJ5MjarEoOS43J+pk9u2OyfrJ5QiuWJXe7mNyql3PzTTd1qS6/0izOKc0K7v3+iE1x1qAq
# +hLEL3KuXz/nC+8238+hRSMPBvu0aJzlCmKeLmHlSNlcawqzBtg3bzHPsoLWnNQ1CVK33hTn3B2S
# FK43RTlVXWMHSAq35YzJ+dWmK4c3RNK13oZ1ObI0J0jRskhRbpWtU3JNJHf4xuAdkRSnh16aQxue
# O9ZoG54nbfj2vNLcoCU9LiQtKfWiLynoQz+0PJkC3+aR/5vkkf/jXj3HPrbX2pG/M7ijUi9Iz92R
# 4Uflm+Htc4Nnaz+jWMqK4+Sqt0z04ur4XCOjthHYt1j7hdjzdE2v2ATfUPN5qjIHno39Gtt3zLbf
# He1Lu1Rgz50W2f3FhVyRQdt3uzSAc+TPDfLnK5c///D5s8XnSt+2y35FMEYbNLmPeYdGefi0PqTj
# adreUSY8xZ8XXnpOxjbimxy/hdqmOnqbG3zv8K/GVH3tuCkY//VOjan6ctd/MGatHYtFKwLdSKcf
# 18rugTzb1xVd/YpgbBPb3YZym4j1I97fND/+Kt7j6hzLHceVe/52Pqc/Y6vj65xP6c98zPGk+08T
# 9f56DDmgzngHUCJ+svGKVubDDuCe5NuisYJRz42kzamJ9/MOo40dSDnp7sWtqcaWi4j3BHV2T1oR
# cSv1XNqL3l4z/B2jbtXxHnpFqXcyT+Zrzj9Xx7upVk5aVcd7YSul7xnzwm1LfM9JrGM0kxjjbWo2
# cu1g/A1OxuY/Ua86fopXZd2q42M15MS6waYoEu8Wy459VpQx59nCcKEpyhzTLZY1PTyBejcnVF3V
# xasaNDnuOa2IKY9Unu1tYq10CNcP2Sv06TZ0cZPdXTl+jT1tEAlVl5/jVYWklitlz4lNYflEb5M/
# cm0C/wlSU21TI6azMNXY0730l9eRB9Gu9o2TPucY/w1qwJi/vCqxLiEnPW1rHCKG5aEivoSZ7HVU
# D5jiNW8h4ZUy1p0cv5WdftXxGps6iWOyzbPRZvA6+pRD3jeb+o1cO8jqEusGoZ+k9yGxxH6oKT/T
# G+LSJy+f5lW5UqdKnSI5MBxfwzlJ/i6lICZzrG7Qgwxc99jagl68K7VF4FrkzrGj3qgrtkI3GPkG
# ukI/E9tNJhn3cBP7XOI+1c3JsHFn1MZd7C+ztWUVddXkIERfWrdyR+IaoKGWOkFfQL74U2XzSlJz
# YamMkJuSy3JGI+JWurSrbps2r+6hXXXbdfxsj7TFTe14OUoYp/C0vjPP5VbHr8Ke2DjEFGVUdS0K
# U0t/U+SWdyv0BpAKbCEpMcwkOJJanvPGIVqKp3q59rqRG0cgR5D347iPH2gRKIcm17oV5Abrprna
# n5dv+4ZNsFYykneqvZ3X/Z33Lq03fPIJdv9VBR8YEHNnerzSw20qaaBHHMXPyDHDTFk98XFUqMBe
# Mbug1k+Z+hlqgjb4rOixXoaOyKUkBfOjyTHGEToHbfcYVOg8JLtVCtLmpo1+t3Zy1NlrXr+6IjlH
# 0sdLn3s7G/fc2rGRNzh+AnXRy47nSAiujt8Cv7MqgnFUKWmcHJfz78WutEI+JvqWnrzX/PoH5lJv
# DD6+si0EJZpD964nPnpzP9UcERM/1dEMkww/mKO+jPBb2e/WjiofxcwVV0Y/NDLjIm+/rAcNZ27z
# ACv3LfVNrWuitVzzkZFy1pGKocwrNEGpdtLm8W4gnqNtj2eQXa3Io/8yqmqUtUuMkvMSo48LMcoI
# DjnG2WZjtCWMGKPWNdE1iFFS2ZErYg5xOkGcURvi4K4nkGNjCfEbzn1UV+0yyfUd+XePPscRzgk6
# J+la3YO4T7NzeIM1jYnyoay0JEhPsXVNtB7KqHG7sfcZSlRgK9hOvOMpU1aPS3X8ZGxl8UT5MK6j
# P4iU8ZmU3kTrwbh57C6Ua2J6zTCuOVWuyRB7ENoQnoYdU7YdzLxWkAt23SY+TkbHTYJUdvJzqCGD
# p+Lbvsw46rGy3GLnQVY8mpU2cmrKC7yubkoTzvWLzXmhmvgup0rvPZinyrZ78By7z6q+ln3Zk5Jl
# 38hgrUHG2zHWDRqx1/E4LorFqPPdWCn7rt1Ya05/uLFux1HAYoOOY8oqNva4TMM8XsJ+RVv6MP/7
# 9bF2bvra5wup/oTMihyra0zBnNX6iiBdyXkGyfGkOVGxnjnUneTZC6zPSc+Z8V5UZruDnOztenv1
# O15PxTM6FU9I+w5bKoJ1ptr1t6lu7frb/iH7ael/LxXumP3CFV3R34SbZWfn3bRQa9P7cSrck/cJ
# 17G6A/4m3AK+OvqS6en7brEZ4GsclJaOru9QL/p5+q3sZN3cOC3enal4x6biTdfZuXJ0NdEZjn1z
# o6c5Tpqfr1LXn7Jffny1X34c7/9v86M23HGpcDM0P0R3wl+GG7ztTXgvi/kaGH0mV9qIWEF1fAmm
# xJD1xHaHnQ/tGBKf1dHz6YMQitfVF9/lNj3it05/rEIX+s2aNqSVPNUNOTmhRNU47uNcm5JSZxL1
# VIHTlXasr1skvS9m8gZI6xQK5vJ66TMYmnav36XudXzqXjPtvbpWN+l/uNfE5Jdkz61bwgxtouYl
# 5hJ6Oo5Xwqyc5LHOUvudfamr0lwYAXqcY6p0OlsXedaF/jjTns5bWZukSzu33M78qt1/J+SOq4nP
# dBMyiip/2m3q57iT48uZ505MeYmdLq6XupL56MRkzrZwviUxZb1JTH2JtusBN+5LW+0MKns9z+SG
# 8/S77H3JiZF2zETpIX/OhNPS8uiXVB6dmsqjbLteEzJ70Mmek8Sd3LtDTrB6u9Mk/mFzwmXm0CVf
# lmBrwTsSKvGxPYCtXc9IJFQS2ZDM21Bxxk5fcna8FwlVhtom655QdVROM2RnVEfr21mH6qq1zviL
# xU8p+7Glvf+nWy9UGPKayOqZ7XPjUhwaE3rbpLvEQmVZuSFxcVIuNfYLphJW+7R0nOFpOrxK7xDy
# /gCdf2xO2/WAY+Ojb3C6jc/WUhKfGRN5x8bHLHrEhh4py86NaHxJFzc3kvxafm9ytrdtFzLsnuDD
# TPAdeGYR7B4paS08nsM1UmAHBZ261LiwdfJ5nJZ6HmH7PDxTH53sUU2MqX0eqdVUes2nN4kVxvw2
# 8SbBW+o+Z9/SuL53jI7Gyjt6Fy7Zbk15e7e11GK2juhJLIWO16RNtI2+4f/2k2+4Xnty8H5H7Vru
# Tvo7ISOloSwUriqjHxuu0ndm4GlGn6urz5U17plOUy+IhxgzJJcK7Pea+G0l+12lkN3bHZUdC2TH
# QdAHRqeV0+apfDk9lS+Nbb7km0PQjZffnXAWhIoZBxV7b7vF/AhKcWirX5yxxS8Ov+0XZw5jHEEP
# hbOusayeEZNVGTnWTbaCMrfUkjP8NfEfnZkyM42cwKqIyBHMB8dzueNcvWNX+r7FueV+pfQag3st
# 0HvNdp4JZzsfhfIKTH1usaxZZXYTG9p7hCYu8WcJKb+eCS+XsPTq/L2v9v8sC4vfcLaEEzMV0byC
# yui31DG7ncJoqh3Pqq563vkyP6Kp/8SpzA4TYgMv22TnlXlyLenM6u8WF8p8PGFw5RhOSSXHCZn1
# gnHBnyYnV8YFm2zf4GTu7WebE9hzQ0V5pjgv3xf7R8wQxT9cTfrJidzV9GIzcwtN/Gt1ybMueYWm
# NP8pntFVzGxXx693PySUYpboxPyHjel69yuR0RspfSIvt1JiWCylK+8JU5knOwWe4G7kfCV300Tu
# pqxRrinLgUzwc236JX2SCqvPSrqITVxl5i/X9mr4Akvue+QQbfryzdhzrUzIfDbvV9JM+nn/S7OD
# 9GfbNMuTE9lXZbZ9J653q+wOIfkOZw79xTk2hyK2LBVRzl52a+252Gel9NK3LaNfSzmKyvwqfU1X
# 4qXm4Yd31Fd2/ClyNTsrZS/JtrmbzbihQfY+KZSUVTbIMpkNG6D1TabfkPcyMBUmffiSVun/Sv8l
# wrpRsLZEvphJ9G3GBPVdVL6blpM93Zli33Txexm/U7PAbUAu8o3+PZmFrIDwrGbYfE3Wk+LvUEK6
# wp3unEgdytPEJTs3qF9WuCuNrJzYZ8b1FaaIstgD/2fuGzPrjBOtm6xJipvI6e40afNtmI1zK3Ip
# yblZqZIrrrfYv9FseTs07bafNseRtBcyBtLa2rrGHUmTb8e2em+O5PORUvpo0yrdw02JbZtXSZpc
# Wcd0bSkqteuY9KyyxP8l4j+bfS3ZsfR9LWZ5ND1fLjHJXLjWlL0nLkfaZzAzR/6OyInZNE1wynzp
# yeRquy11TFNqviPtuZngdztk/8LNtp7Mtr+h1QbTh9Z/kf19p6vxJ7/bI+VPvuuYTapka3sW7dsG
# +W0o7LLLrIraQn6jR8bv8r2T+qwU9JdvQ9vR4iPOJzJH+pM0Tj/In+/kzzfy5w2pnV+XP6/In5vE
# 30Y/VUdXp+roM/bqs8p4ZSq6Aba/Zdtl8rJvWt/rVC8W22ufXifpecek721KfE6V+8meoh3BRU8P
# SrL9VSV20MQ/5G2UK8lxMyHdjfmx+slddJijKXOpkfY1ZH/l6gtP9tb5TYP8D/qH8yS9sr91rbS1
# WnrcoM39TvuHkh6Zm5D0eMga+1XNHD9WVF2+0004Erv40/v0ZT9hMGf3Ysot2ScZF4r49VnlqPQP
# Td6DL/fQ1vpnFmHlGZT4MxnJZ2dV+FnhzrzZ2g6gz02Zq8ufc66NBDGXhaTNbZWWguKsM21s14Yi
# mhsT94kpw+x/zYR9rpn0v7jm9LRrmG3xLyQtc93u1n829qF2PBWSN4nSX5VVe+Vav/bKitq80zyq
# 9A/m6o2OrPRI7B19eebV+6SoaK/8CsxnOZL3FRlZ4UoOGUr/536/9ikcT79xvRnHM9PQMiS09qyz
# VmY0Ma0T+SZ2UJtxDep4ltTufv+Q3k2GvZsMGR3Whj3Dq0299e3V5kVI80595xT751jf/ZwIq9AF
# rP8Mt75fMBG7Kl2cM9WTONbKamdOnv1eeZb0N+U332Qkxyt5q/x5zU29l8tS7+WE/cbAK1rvO5Yc
# tdcYOFP6OHYUKfdU+3tQBTreiqX10dak4jlzr76rxCO6jnvF85W733hrDHMRnMatjspp5Rz2jTAO
# cO/2pK5tau3FzFUEvQJmYxgzy+nmbK+Cr1/Lu7/37EJGqq8plGl6O6Sl9/lUeiful97n/x9M78up
# 9E7aL72i6/A/pLc62otYcijRMt4J0plheyRN0ntbzGFKH/GIYA6TNLPO5vL9X7cjPWjSHP/A2Tut
# B2la26Wl9bVUWqv3S+tr/4u0Jk5l7J2R4f7/kdbNqbSetV9aN/8/ltZ3U2mdvN+8kOhie6V1u5f+
# Lmek3uXaeaaPUuHV7BfeR/8X4X2eCm/KfnWN6Br+TXjMRvZgxdtc7sb2mmXLtb9VF+RF/bS82JWK
# 6+z90r5rv3pt77j+bo7s21S4U1PhRnVO5Gd0r+4V7jX7zxvtoTzk5zNzMC8kZSHf5OTHd29QbUl+
# 0LMUV+lZ5tve0BLbs8y3PWHbq3CkVyE91mJ3s23t2PPDSsWCkHzhodIdR00z2oYuOyFFMr+WUZKx
# kuvP5i3Pzqh06WFnfmh1F7LvIn4JPdWsiCnJDHqqmcy4zF1YFvJ7M37P9HsXR66w4f2D8OKH4DeC
# XzfwK+sWcw/B77H4df1jg5LcRktya7uaN7dVhZuZ05m89F8pC1cwxugsKwD4rnAzMGebwC3MfMA2
# 55zVPFu32N2SvDdqy7jR++CtCe5D3hq+2MdVMVNnnKUSVrS19LBbmhF78uzZ4ShPcbAJvj99hpUR
# ezawFPcH7boVc+OyF0AK1GkwG152dN4rbHL4EVLZu1r7jD/3a/unjN+cvecNfgvmvQzzXib5zGWu
# KH5n6pkzG5Y+8i521trnYudXKAuR2j2b5MOa5J5NR8qCY+/3dCmrdotrsfeWzbNmrJQX+219+ZJC
# LFzpNaVEzPa17XHjzX5lHx+jaFd2isRcqWt4foPs8xskvrra3ZgbTLhBWatcp+yQYu/NZLisfUpL
# doFbGxpjck/KQ1CbSWizS/8yPJ/wcK1w3FClw2pOWNarn5A9OKxF9bV3HJa98NyxY3tBXe0dc6d+
# iR+UON/OrO7ek5orZK6jRXeZz6NM7aiJZ8v4OxQfymxKBmu25ZOd1n52Ro39fVVGBowNz6HMT4m/
# 7V5ke4n5Zh9dRklop5Hd+ONCZWuY+9mjep7DdnkOkUKzKdwhkm1q3bfgnpzny9PfpexkgjlzKWuT
# 7BgtYs/hyh7yC6x7nj3Xl8G8wlJ7Bpy95RS3y5KTsdOSE4G3Ocn6p3ObZP0zbb96TXQHmr8um6yX
# VSVnrVOnOyiFddXV3VPxnLPPWoBr+rQJfps0McSWbPaXlYQozaG08Qxxf+Omxz2WuM+htJ2nq0wj
# 9rJLCQjS0t/OerM/wfyTFfVedv9pyJYemVc8ys7LOnallLMEsc3BSuIRiQLmaAo+NbKaKb+ik+gW
# hB3UB6vs+6EjdWZ3GyZH6uxXldXH/zALICO1L+1uwkpvF6v5TxIr5dcr+0xmNsqeyg3V9m9PIYdO
# 0f3X8qjOgZq0NmJYKu+m7/eMRLf3Oti0//U62ImpcGfsF67oGvxNuNl2vao27OP4Zao8u4e+7r0/
# 57L3Z2adZ7HOZe/PTGrjPs7fncU6j+tn6Zq9n6ZPxM/j+lnU872caPe/vn42159f5/Wzuf58ru+9
# 1/WntAn0I50LuHLOfvkzAX3ZXvkzNrRv27z/2T67zq7PIVE+h7Wmeezs6MJHeWqi3d1gfd63KZi8
# d/5T+3L+KVpd/hrr58F6bKIZ6zGlwXpMhYzQnOQaYfJMYFFq33kQnl0HjHFVga7i2Dn5oBQmr8lM
# W3+XPOhjv0XxJnHOsOOOeIz1J9pzvmpDizFMdgLYuZBE1XpbIiRcT1aH2MdV6gR7JJgdp0WSPRKO
# zamRdo9E1Jqz3VJqzcnlXalTZY9E1BXXh51Sdz6u3a2rK66e7JWQNl5CDv5FdW9ImBSzAM9vBDj2
# /ekFxWnvz7mpcj53nz435/jbBO133XWc7oxgReIl07SgXbTKmliZM/EC5u7oLSftdqWOE3SlbjDX
# 6dqWx6lteagHipMtD/VE0PJI/VAT/Znf5pCWadcemfuR2R2ZwWttyj6V0I/jCuoMp+yZ9H7jotQ9
# Xbhf31d0bf5yrVn7jbraLCtplZzikdTKPJek1rdltZ5NrW97SNSV5vs9CYfZbGf3ngR7uUa5O0zZ
# v4N16Tzt70j5qUhL49WpNM7bZ03UsboL/4c01upCnsyYiCZW0Ca673ppJV9z0RqfHg1lZEjtaYZS
# LzB5yRlWe1LE2Pwus3dobE+AO/Sb8OuU3GGooZVDhuygDZKRbFPZ/cYKMjtaoqXWXBnKsLOsXU3Z
# NpHdTdkWSYGTesvF5ukeB3mqyd5/mBWFxHSeqhdmJj5YPW1i37Eg/+R3133NP6mj7PxsfN89HrW7
# OEJaX8k5NlmDiZXXvR8kfd9H8vncmHo+F+019pA9Qrej6+j8/XuR2F27NpjqQ3viWvtkOAsZPBny
# TPqvEfs1roqwF+oYljBKQ8HzCZnSjMCUkYwzXJx5dkhivNjhvNneTy9cGT4x+fTCJeHgbZKzxZ3C
# QX7T5wvLk+FryDu1X5cp/brKqRF2hVNGtst7xxXYiinVUTkpovb9QvjXPvZnaqLv4lf2lwercpkm
# O7MikunJOGYfv/el26eZsjslP6Q/EuP7ZVN5bzZx1y9wH6XhwBS2JTUkPUruNdPuOh9s7zWTGcyS
# rOBeZZe49ES7Z2VHtNeYI73N1dJ7jL/nLkOWRPj2dIT8JLWch6L6r4w0tumNmOzsitxsryPrVqTP
# rvrUxN/jrG5O9txbkydUpMf6EqvN4caV2Zl2fYszI5FKPgdcnH2bXQOXs1fia3F22QOpXB7wolOZ
# qKna7nSfxgwEdyJ3nGF7Y2E7Xj0uI98t8+VtKOFt4B3ILzZzPdkDtduk6hMneB9WuEH/1tX9wO1t
# XfOGaTpwcvxfrL00HRhz28Va8g28kCsjGN7z+MusIQfzncEp+9LQP7kmWMWSldsNrqxTBevN1wV7
# RYK1NVqrs0jvwdzbc2IPFYT+qj8xn17Bgjr7M/PpTyygP7PU9meS79qW1Lt2cepdy9V37T10L/4P
# bVCwNrDPu2YSL/CuuZ6bPnYOdmMarzJ0venkyjt3dWpno7wBXlNpi2z9yhsQ9GAD27vSv8WWbcuI
# J7OH3H+l7YdLPar1GH2VtbatkD0j3dnfG5Rbn3c5MIXsGCnqpsZImclxkJTM0+3cASUzIyMZZ4Yd
# VWYEZTMjeJcouUHZzLDXv8dafE7m3JYS0v1a6q5j7js8ozIzKJuZdv6cspkZlE3bR8dX80y9e7c6
# 8aLjVkjJjA6WkunZGnlWUAZzis2FtgyuTZXBh7VOfsaprTP5PKY+x4Wp59jAPkfGM+juDb5RWPss
# XZ5loTzLuJd6llGepazvuGE/aH8v8+Qd8XX1uCVfzK+rXiwNB6ZwbehaQ85xyTBTP8sEOyqyg90Y
# y2t3VDQOdmMs572XHRT+Ldrep7WGg9Jaw9pxcSc/kjxP7ksNxq6KndQcWZW59ZKlKldK1dP9IrmV
# uXv77WXK3pAyRjjoqGUzg1o2sO8X7lP72B9Ntx9jyh6sYH2lkl/R0vG9Pc2TrE3tSl44sx5j3wL5
# 1aiNblq5zAxMnA7LegPTi25Wcl4gef/UavWS90+9Gdy/jMxTe3GoVfiFpC+1bv4/vs6evOV9Wbxv
# P4QQ+tQdAu/Qhkh2rr5D+VLyZS+DvEOyz6HE7hM5JLlbKE/elVzr7z3nQ5nvo+zvyE9qq+OPuZ+Z
# SF5lXsTMfVjXs/PEzxe5tWndIGldudfsUSSYPbpT92uURoL7uHnfuSSe6PHJuSTSFtyHnAeS+ZNN
# spvja9mNUmnTK+nMjDJXxel/eV6so8v8lBv+tMTbaeQOWuvb+72r/l25gxZ2vijHzH1T7sCeI80L
# ZlbK8mr9bXBb2pOXZfkSdsyNFZY9LKaysP3LuphI2cv+irSjlJ1RuouprZ/cxUT9y5sTt+Ml2YsU
# t/1g6dEcs+8TJEVHJZ8gs1fUbNSdZd+WuLLnpr7MHXq1/fr1fF1Aatz4fjp5ftRZEbn7QyRfuau2
# EfXlJms0vbsXcvdK/1pNf7u/TX8lJ+N0xpd3aIJtLSrdW/gN5xK3kbNfCfXk/oI+7AJ7f57Ng9Te
# g0jTw2J57T44g96em8ddmUx+TDmTj97KWxirXzZY48oX92I3w0vpCpu1b+iM9V6lF5xRIHswj8jI
# cbUUerH6zc5q5Bzn5RfIWNLNl30SlBxZtwx8uHIueKdtGyTfZHQieebZ71tQ3yf9GckrzpiXUBJy
# c+UEc7ArZK9UvpOX52Qy45spu+Qz6pmyG/fWp+tamhl7xDWoZ4JdQRmyU4a/yTqpI19mre3TaS2T
# Nld0UNpcUfoY0JaBDPmCyTjbPrIjJ+PzPdKuZdgz6B+lxob2qx4ZZVunRLfZ0teRe0y1cFWvOyty
# auvEY/dJS2ivtLT8X6clGXdlRpapTZPsLSrOuC2kKdqcSgU9wBWjpJ3dMD/oAbJDSGvmqc4Tcoo4
# 6AvSDh5XzxSU5Yqpdsx2HEMZOzbSdjhqf+8vOK9QQOs7yH5fl29sOvJ7dRF71qslbcFmJzhH8a4j
# 3yetZ38nROzyuyBVfEnhIms/0Fxqx0DN7O+I5TGLI7/DeDA7J3+1v08Q7EtZEubPANgku09ekD8r
# ZB7/e5lQfU3+vCp/XpE/L4dq+wd3pPoHl+y3N/6etsm5uuSMfnCmQVZ2M5xEG9vD8zwnqAPPZnWi
# tv/4SCrcS1PhJr9X8Ri6wv3DLSVcN4OR0yD77SkJce9zfc+mwrxsr7SK7kV0B9kwpzq1c1fJtNZE
# pznG1I7Tz7G2ImdOl8PpMh3FVx6Cu6mJVuvsoOfU3ssbqXgXpeKN2PUQx2xFJ3PXiZvsXHAWqwp8
# caIy6xb7hgVfeJAe0jXJPpBT28Nq6smcv03n3EWU2emOrIQnTydVhmak+sG2hp5cO7cW9KLYJbHP
# fIEd3wRflqCH1D/5ZYm9ekjpqwZF3WSce2Bq17H03bQXE5Zes4wDpY49hzMwLzlFY9N7OMwhNKlk
# n2lN+fv8gorsWA/6x+gJM9/OVIfsODrL7HPdIzrbwKljdOW5bqGrO7yykrPMd2pf9u60vuxvbYNz
# TyOdK/Q5lDpBbcE+budyk5zPlTLmtkt+d+YUO67J0jnCrHYy9/X3e9tpg6NNmC8s9oJ+aYU9PbSE
# WBLzgn5phcwbzLUzaa49IcI4gGcXqmDmR553rKAsJKeKcpnZkGuK/S/t6lN3nl68bSMn2LNV4jVy
# enJqq8Q51ATlgDUJeZ+9f3pHemXENIL9gHz9adrlyRzMkFUbfns8lDgn5ebL7I70suiD0ic6UU6n
# h5IrOD1D73jeHmZ9osMJOXH25ZSJrUG9HP1IpW01fZlP0laT8rIF81sSOiEFZUee5ZToZhujzD9V
# l480A/2yjxM1EuY2+5WdSqeRCXJuutesuZxXGMcqvuTeg5J7C9ebZvFGjrSTc2VmNFNKPrnjDbR9
# tgq7L6St0ZW9zGKvbyjWvNkxtL0ZrzAKD7kqjbTBlaGczOB+ttt2oA0zXfSc3Jgnc/xBD8XO9Yea
# 9ZD22efMtm/nexv6ZSWSlmJns30qDWUFeC5fBHDqsQJ8uAnuaLu9ozaOPZHmyumwU6l7g1QHdXxj
# cvQhu1c7xO92yDekI3aPdht43i5buVruqJvaBd/k/PtyF+wvf27/EyRa7sql3M1Jzlel9kiHKgzl
# jlTEYjJDnpgj9xaUOLsnnXJW6ck3S+g1eYfyxpxAfDLXJrkguwka2tjz084tVqR9f6hdu+TZ5ctT
# 3wFLnls8sl1wFu4UvnCZn3amsUs7/Q5j9G4n/XtG8m4egy7fziFeh/5mm8/JPaiysyyq36oK61ev
# +uK/wu77TJwsM7qHWjmo4CN+q4un00xrL6e6ar1Tkcj26jtSg3r8DqtHPInx+B3yEd8ukt2MfEXH
# aeCM+TdfICuUt7UzIcSi+fjL9fL1G5wHpp3dTH4HZ0Q73Z8htZ0n5TScXIsjrVLveWZf1xgkz6Z1
# aZc8+3mrnv2szT+bT2ZOKp/Cth7jm9fo+lvdTXb25GR2yfIbXm59f1O4IsP1O2eUJ8sTux9WubF6
# Up5a+9LHCsu54S6nudeYssaxQc1K6zkHuQfstT7zuBtrIbPU3UMRL9ZDztfQnng9OLsiV53OF1nH
# 0GrZU62eb9PIV4K7yBdTbr6pEfLVozEfnfzO19l6H5Pj8+1ZudrvUzlmZrvg+3PF5mhbJmVcEI8/
# Yeeag1Xz82zfPp9dDzE3302ulcgzKKizTM2z71CBnkOVmn4OuidtHORxhZSShlbKqmglO/GSZukN
# Buc+MwO36KfyTjviHszHi98tXJNHWT/JfkusM2//lOhoKZ91+H3rf+lXzpw2k++WELd8paehNdv5
# /6rq6MFqkz3vS0JlR1VH42kuD4TKWldHW6S53BMqa1kdLU5zGc2Z0epoSZrLg6GyBtXRJmku94bK
# 8qqjRV5QVv8/3t4EPsrq3B8/7zJbJstksgADhMkkwKAMCQmBYCaQEILCBMEQVAZ7NYEBBkMSkoCD
# ba+KS7SbVWuk2LpH0arXJa5trRVqtS5taW9re2srtnWtim3tXvP/Ps857yxJANPP//MjzLuc9+zn
# Oec851khMSkO2ILQLrvTllnXXNWH0KevRh9Vvw4JB5KGv9um4hjy/eu2fJ7X5PebrHVJrk6tsrB5
# OvslyGF5kqm0pihe19e1lE50al7sT+pEZ4bfNGa+GLx2fJ3Ds3h+6uKGGmkDLHV+yOQArhPTdNqX
# 5ImZTuJk/eYOsk+F06aX45F0+1LmjI3iheHUWW3xwvjEXGv8eUSelwtJUo3ojo7FtndGxp6fH1Hn
# 51+NjD0/X6pZNL9KPfhCDqb9eQ5JyXAwN0FSMgbwXOuotfC7NCr7vPGp7JLOrRGm4LEg7A2iatzA
# raSz6W9GLGo4YS9UE03ZX/yyFvwR1SRf6faehZ4/l/1RS17/p1g2SK7NtK/9D/qdfGukqFfn2WQb
# pN6ibIONccIF3AaSsqQ22BlLP4nbYM88qQuqrS1Z2+KkLoXDS71u9RzXHSdpr6o7ZOJ+SnIK+aas
# +xK1hiwfF36uPgZcXXuM8GuOEf5lka/OKJnr4BUa6SJ4FE5qpH3bWXkJUZvVt8y1rcdzmaardA6l
# b/69GmlrEfpE8M13yXmT9RH8oy9P+58xPabUxYfsXY2ls8uQD100QD5wGsbIlMYbyd5KWkspYxQa
# l5XD5zyPattk1W9TFG+d8v5pjeSt+4SUoyIs0N/wD5YWnGFKOSqT8/KJK2wqBrCQS3j8C/ncI2Wm
# yiklVv1aPUOPCSn9HrUnpOSCYfce4Q0q3JThVE7wzzlmep2Xqzo3q/2Tfq/WSL0Kn+2Q5rN/Dzz2
# 54BdfR81eRYrwUHNZz6vXeAweS+JAx584of4/jL0XdYy3hQHLPjEjxH2o7SwaxD2A4S9iHxegCzm
# GZquUThsFsELxCB6//csoxjXrsf5RNpa1URc+zq+3oh9kHbPrdhZi9nrxFcUT4ZG+oMajfcvy05r
# 0vahf3/G2014ux6n1Z7KzZpf2+j/msjNgD/Wgfcf4HOPyX9C/NU6DzV8Q5O2DCTMjNRIm40W/pE6
# E3uAfW7XbiSOqH89Vi56BpVSp/ts3eI5SPgtTObnWijbQHif1AsqQr/s9N+FN5kflUA5erk/OEch
# c8xL5lfE/UH5FSC/OsYvBy15FM929hZsR//fkYTpuHaD6MBPntPk1+221Nd9+Lov4+v5aV+/iq9f
# VV+lDbw4LMbnsmyt1FEl2FqYhotMQ71W8FiRVlGP51a+fomvX+Xr1Xz9Ol+/xtfP8/VKvl7F1+v4
# +mW+DvL1Br5+ga/7+Ho9Xz/H16/w9Rq+3szXm/h6iyZlRDTuv5MWKjuf5+1HW98ELXOP7i0rq8Y5
# yvyB8NpWm4Ztmvln4S2iJzqjEUYbAKerx+8gvXvokUtbJw9hB5FWTiwZljLVF+W8NsjyqhfKdaet
# wSrvQpzCqLxU3tB68ru0ZcfNe4rK22fpAeMXXqjsJXNb3kLel2HnzmxLqpTPopSqcVtglWHh9jOE
# ZStGE40oo4bPPm9hfYnxTlOqEZ/2ZPJVBn3Zb4L+fIB2bbai1san5SmIezeflkuXe3ctaMvnOgY0
# WEzzt8OiAtWhIFUHG/N2f57e3nmqLlVpfdmW0Ze/RXtjY/oSktd+/wT60qXa+Snk3cTtfC+jnV6j
# pmAxpGuE4bO9a1JLidJALY2xHAnwh5nUVqIGlJ7pvWDB7r+PrMBpfBr4e9W7/zTiLYYMpL3D/pKo
# jv1hpO1i6gVwG/0tkF+iOham9wLohcHnU+NRr+rZqGyXES4XWyjlC33Cb3pLvV5vtddsG0J/2N8W
# 3rPKLsDYO18Sq50Oc5rzBbrrHc4f0B3hL9LdoLt3JZ5c1Gt2R7Y94ADtzt8KL21Uo93cvofQPlg0
# ge77pcxJhTUWVQrDlwNlOAyzw0SepqF36ChDN3gc7AbZXbgaOZ6l3co5Su78Q0Q7dkCPSbuMc5Ty
# MHR2niukzkxA4VFEIzgnDc53L5T0SwnntD7NNSWcTwOtZTScX0PWJcfCOZd8cVrJY+GdyrpooTxP
# y7IINziqe5syy5oGPq9cH/hpZvKpJrMm0LTwTzrBmmGtn/PS6nBlxhpFdRg05RrFpXAdeIaPWqMg
# 2ehfPIE1ysnlmeIalHcT0xC8+fIkcLbuXVC2HOWBUkaUoQ7bD+iu0d07C092KrXKJB7WN3BCaOvb
# D3sSX+OSlzFn6yHIg5OkvswPFC9TztLMNAETGlT+POhLHCvlObayQsCb7UUhKVRjc4CekL/gGDnI
# NYvw4dKymsp5mMfgXtnkipU+j2tt00RyFq/37l4QKxRUBvx19NCMhX6PvwhyvGNmLMny/TSHcQyp
# d3MS075sjPfPwOpCvrMq1TlvDn4rlOrA15T+HWG/t6H/b2H4h56d361dzKs51ZxOHpjkhtesKV8I
# aWoNWoGPGFR7G0shXYrZmmVfbPcJn/3H44RDB9/+7JjwWuhUt12xn1tjUzZR2GKX7RUT54QX2i7Z
# L0+EZp9RViPHbZmNtEUiSvbtIcIFAFejV8sabz2tlfp4a2Wt7eT0Pi6ujpUl37FGsjYsn4vs39VI
# c1Gmwc7BayY0A/05462ZtHN8T0L4QrWeL+fzgI390jUo2xAN+J2tztfWPHsiY115B/vJj5Pz7JTj
# zLOLUJdFx9lJx84xQzyFsv6LqeegeveiPIP690OzzIbSIEEDyi33c5Mt2wiYF6KEuQqmm1IypIDp
# VNo/mt6ZZetTqVP0X84F3I9PI5d50HGiXCrVuOlcT4LqaboH8+tZvSxfzUyN2vYZpKmEDtZ4bdMV
# DdTPOuY23i8nq/6dgd+yUev2iwsl3SO1jt1gWvjJi2PWbeiRgf59vNUrfb229u3/RRmr/rN9e1ba
# vj2pevcM0lbQjw+F0E3zTx0LhSbv3M/JPeaLGXuMdc5uSvaLTbyzUNJjiGZL4/A+qPzPZayQAQMa
# HH6yFUglValVrZSpZqk0vaPSQBvDX36CNCWj0kALwT/rBGlybN5C2g18tm62sUD9k74aBwzoHPiD
# 4+aSuZLV+AlrxCnIvM08AdY4szqJNe5B7ieNhzXS6vtzOes0dTafyncb46wEm2sYVnPYB18I1Aji
# 8frwW4Df6aSbofhoFsx+rPDdFMxeb1r7f2rvfUmtCbw6zEw+jdr/v4x6bzgGJnJ1BpRk4gEWbGct
# +n+Hk4LD7T99gjipU9XTi3pGlPX4D8bU9CRV06Pj7AhT0neEMxfE8sU025+sMDvpxnb5z1dxDas2
# P/eakkcYgOfEHv90aLRSrYmmsrHnZtW/16f1r8Y0uhS9y67GOrBIrv/TwAX3aR9DTrMsQLIsGGdz
# taGbxEkjHYa2M9BDoPv3+M8FpZrK8qd2QS7tuuPildRHcxeRJUlZVuaOGVDnqFLb2BlhzYBrUfan
# xs4Ak3e/38g6fCWjDpYO8Mlp63Ed6lDPZ4e+NPsp6XWp4Lqoepg+W7tJdWlmqIL1Bdtm651HIFgA
# rgqwpCbRthm1tF0BLY9mLWZQLdepXWuBlNv9SToOukjV7ZRk3XRxKurWyHXbCC1en97LNSTMrW3T
# fkE4IM0rnVYbE5of/jrYoRuL8fn0vox0ct2ysFeVA/oTehb+etWf1unESO7ghrIdPYNxOHkmKcFv
# adp4tqq+nCbeHtWHC0b14WdsqT6Enqfts9Y7xndq+orXvKCtQPUk5Ir9q2B9LrMnSV9GyP58Ser6
# KwuPSt/fqtumRXJPHO/UPltB275x1t/iUetvbvLdW0SW2qgfq9sMC+v6pZdlL3IVjP436rxgLIwy
# bN6YhE2N/fSm9kSLNta3SEJul+ceUMN6PAO4yrQXZcC1XaV1cOt01gFLIO2DeiZtV8oMzBBSs4lo
# jjIE3hQakt4UdJG04ltkNFi+CMwi28WWJWS7suoLLgOs+rrmlZSMgIv+d37Lqj/5ZLLxm/X2R2+N
# cIh7bVWIQtz//uNvZUjOa6+9RiE5paWlMiT3niuvpJDcXbt2yZC8V7/5TQrJezpWhBCXKPTUzpw5
# UphN16Cj0PPRRx/hja4B+E4iG/AF8G5BNuAvBeR+FvRSss5OloynAHakbDw4Ws4NbMPpcoxxQvsx
# tMphKc9/Hb/7nI7kN5KXl6eArwBjprZLC/9X2bw2soG8wUk2kP8uCpzSuvJV8GN0rYhv6fJ/jizB
# O2GB/w/eEbLV/rdsstF+nTMAOYEe/39h/+X2OdZXV1P7HNQXoCvCnyzb7Z8iU/3VRakudQTAxwGH
# FBLY1LYBeCUNuGRrsUugrSj3zMvEymXePKs1NVd7NCc0Pnr9L+t/E9lepw32lGz7Res598CHBiQe
# pmGssgNOmctkoRdAU8NHeR2A7Ku05QwoKPxyo17kFsonhnvvN8meM2T2sjHz3UWQcNDdBezz6En2
# p3AA7b9MbNscyFoo9EKU8mfvu9SOVe4Nt8HClilHopDT+9w32YLFKatbvuxbbdTvL0A3g2pJ4/EC
# W6zaxxartLlOiDita71bFENhcP4UfVL9lAL2Z6Ntck5yiHVn3s0W9jscTpKwh5X9WVjpgk75lcJ8
# LB14ufJ2A3hwyLLleD9GvDO3mXym+2yWIN1Ha6abYGCYrVt7JxEM9LOt7d8Jr5sgYdjhdXStvVb0
# X8Dj90fZ7tluGr/THAFdthxYbdsByMHn8p3qIu3dYN0wKQRyhmYZ2zhexta2ZUm97APgMHbeQlt1
# 9XXsJ2EZPNegnI9kOW1cDnRNcL0q65U8yA8s1J7RoVmgm4U8Uk6jWDgdgB+b9w9zxPsfU19Sv7BH
# kncJ2raLo24n7HZ1VV4B/gV5oCgyLl7qdfRA3/joIkpPucMOtNiZ3CG9k8p6CfvLYpnH1aYOuZMO
# 9iOFL/2QntFMhNzEO5HPzLWV7aIzGfyjOHAacxAO0WDDCjq57HKStKGQe/Rym8+8UaeY6rugnKQ1
# zmzRARnr1oBD7A0Umlu2zFp6ALvWQKDIfK4p6bkLEgnL2BL6xaIsKQfsM5ttXoeVK53YrVx1MziV
# rMVLHU7woWCdmXQ45d5nE5XMj3pAo3GG3qab8AKizMkV+GU9E1edCbxnpk5+v+3iy7qkp9IeeQ8h
# 11fbkvyWt7Auz2F+x35eb2th6bZDuz7Jsez1v6prLOnS6/mttEWp/VlbBH92MuzXCAtmZaY4olt0
# Qylj+JdFluxQurxkrW1ZMoQsFK+1tAoWElfaJtwssW0jWeyGOzRPK/FiZqZSyDgayQ+qeDbLbihJ
# /tGdJP/WseQfW5GwUSs/q5FG9k7/vbBqnZt8F3wGtmSHlib5OMDvaxWdz3+ztCaIe5dnJekD+/eP
# lkzXpCQFS6bjtD3EMYF/fgibCR/I+MPKx8TUND+fMxT9nPbYvFqJg6b4ZZZ0UK02K/nc5XlKWRYe
# zecvTPL5Z0A3rcAgXvQ7yfOeXwTfzdGDb29suAm1mJmmhxziOkhe2xTUITRmvJI93/Atzd+UjbKm
# s7cyS6MpWBQw4HHEvD/DlxjpK0HasXK9Vm6QzrWp7CtkM15gF3NR1hLk63PD0qqH+GxZaZKmj7GE
# WSnLnJB05mfwlWqyF9+JWybxBck3u1m/NYlBdHku4j7wGV/FWn4V2p8FPAgnCOMhPfX+Mc7NN7Lt
# siolETJH/Fl0GH8SEmdKWvmyPcH1IAys0NbaOjLig9NeaFCZKxHnZ8pOG1rqnoX27wfcnYGzVsB0
# a+qrs8e/1bhMnOf8GPVcZ3yOd/ltCAGvjp/gocGzSEhML05vZ7WJiEOFiVZo1rViZB7EerebtZa6
# /LXgMGQZV1Xv9F+APnYbn6t+oDJ4VlI6z3wENX5RNLizmfOspF7kCq873AGcPQKm3HXkTjPM0q2D
# JIGetCZJ7WhAvCBWJPg1QC5XkEaircAJ3vvZbaLhrDF5sBXN89N2S6rpw9ixr3o4WdM6h0t7wHGj
# v2AW5FByWSZiFtZ06Fy6O9wYycrb9W9owfwA4L0H3kbgM83fKh7ka6lIxehwWzzRWzDyWVgZuyrv
# 5HCLG5oZjhXALSEoaLcgSdr7IJsbC1HOOcr2e6+y7/tZ9msN/rJGdIosUa2l7Pt+kxbTz0iFSVPx
# VM+tlXLhxP+GVJ7YUHkbfuSnEPJ382/H863MW95YeUeGXHi81rIHf59m+UkgWYruWuk30SvIun/P
# GUHIxuWm+U3cXSvPPz5QU7v8MxHmw35tPa1IPjUln5qTT6eqJ8tXHuW3V9UDKwXz3i1fpZ+rTffZ
# uQHr2kb9Tvi/vSstzrWj4twDv5t3w5fpAebrGtxH+xGnkvsYeBNWaNKW9NryYA38Xrb4GWOpn1dx
# IvF62hrJwsmDmrSS1Is6BQTTZ+BfV4t5ZxZjftPzGZArqyAY8RA3klb5PLZRMDWNVmPp6Q/VSpub
# 8EVnkjzSF9lrqyVRx3JGc1Wo2Tb/PkHST43qC+QwK+/jM2gD1W5+eu12pmpXCT6ZOapGJklnlrJN
# mfRzoJQ/eqRW2iAjSTzK8wGV5+OQ2LgaZz7S/vwSyRfZvLPKtvi01bDI6C1cbbMDx5olSu3ZkGmm
# 8y9xDPKw95bw82LojfT6p/Jzvd1gDzK9nil4zzOpPpLeUp1mg8xUcuffqrV8ccoxYb0wyLT6Wb5R
# KHsZsK9XK2Vg2zz3Qx7oWbLQo48X31Q2Qp+vlTKUtYbX0nblfZv9oEGSX58vV2ApAZ0HDW8JMz+s
# lfwaghmvPh/aqmGkydPb1sr67WaYOSJh5oy0UTF+CVzrkE69SNzSMgf6zu6Axk6ZmN9PltgknFhj
# EmRamZzLv6yVOBH8ADoM1nn8otQol77GLHhoJ3h4EqPvzW/rkCXrvOd83/SZz5plRSjRAVo/Sqxq
# Q4mOPE3aFbLsK85Onq3hN6BWyucupjkBqwEB8UfkeApJ3METyQKDLOvXGWTlotaQ+mQLcc/X7cqC
# ynvJOmfC0WNJOPIWla0n+CHuAyAH+K4QBDvSvoKD4YWe0f82ghVY37alYGXmOHX+Z63UwSZcpa30
# XtZmg7eu5HcyaJD6fl/yu5utrWGNw/e2DFynbWd63Z/iupNfOZoDWeR39KSy230aKH9OA9j6apfh
# 5LYYpDXiF1JbZTZbEOr1Q9qTrKd5nuRRqSVfpGWQpQUEtZWyzCwsoeznVq8EDPbCsvpVkETvOeMR
# rSqmcKWfUT+4WGaeRs2m+uEkltvC+qXsnNNWcAZ+dcoWP62IUxZLemta29rT2/ZNbtsA66lw27iN
# 7Fv1lLJ70EZjtQNyzZNWO7Mc3uBqV5ZsaVaypdBjnYQT8le5BTgjYxZMh1Unmn3cErNn/bDmW0Yt
# uQXyk8mWpMlGWvTW09Jk9eYuPr6snkutW9WLJZ5K87KtVbZMQv+3NWvOUYsg3Yn6l13Jo2Z3mt7i
# 1TgXU1uqnKSHThBoZ9oNQeA07Fpu+AY0HVRfu/A4cuwehiWL9l4pUr7Kly2Wslq1kP8v9fPZoYDm
# TY+/Bvqe5PtyEe5ZZG/Q8OipOUPzy67s0axdLG230L5lWrJvYmPD/WnP96ZLrzXch7d9oKXRudkl
# bNzm2WZPZQs4Cl2etYyP91SSMtvGhm8g1TdxwujxHFLnDIuufe5iJcOwVq4cUqfiIY10KmaR/R2C
# YYy0xOpxGtTvhtwQjfF8xtQu5bNlrf4B0boM6i3IZ8AmnA+nF9rZjyfD4GSJc/jIQB26qQ496XX4
# Nii0B5EnrVueKp/2pMY1MuiUIevjNWv1KfBPcT+PWCNgzVtMUNhOtDjsBW12N1tia2O7va+ipsHf
# yhr6tCfG5Eata+u918rLpjABG6VsFcH3rJTfslJyv8jTFuj+O+8T1CsNKY7Wh5RCjGr/+ar9ncru
# F7V/L9p/Kftd3os8nyLcFjPpMwo/cfFaPAc2Q9ouTvUPrNMQxgVpA9rz2Ra51G6Anud3GOOls5ib
# ce3T+CzmHqPhDzh9HzZCiStTxDjM3d6Zl6zgp93eGonV1MIWvtKPspFm6DmsyfQk7eQ27wq2hwNc
# o91Gdj1ORs9LnKPtcrkb30328uTuyCMAbf5HJD4CSX2m4JZz3+S45NNsVy7LXEGCm3HZz6KlUnfl
# dvTRBu6js1kj0K10E4Nrx/YKnUbTegDjU2X1AE7DsgcI0/LpgDHje7w3360wHC2J4YAmY9odCscB
# DPo3te1VrdLcBrWqSe35sLP/wvHa47V4YbA45VL4xKOLpdxj23mjYV7iCgOkEQ2OBOphg86VjWfB
# svRVHFAPzbPMU/ektFO3tBZCUvUFNr84tE7K1r87ovaUN9q20rx5nSwT8WosIXRZBg9awuezqOsC
# pj2k17XHgjzSrgKseJq8ZZe00VNlm4JFQ0LRWtCiXiZ7HvJ8bpIcXoHmXd5VOQfeBgliBGOpIs3K
# 1/w0mb5sVY8fox79o9YJOeI0Ex63dmld7WQG6byh/+yrdZudbCLZ5Jpmox6bIaRtLi/jqu18Ds8W
# avUwHigj7wN0Gg/SaqdmfldyrlGrSvddcoDujfu8tZcspqdL7lbtdnpX0fut+6jdbrYXRNpvZdvQ
# 6g3U6muc6bj5KNhxKthx0jmmneGmG2ulnAfvoQ9ITqjWsZW1BR3WPDi3rSF9dH6pWaNDeoqENRK0
# yPuLCntcytDi4FlBeoo/Zcip1Y+OWM9Eu1EUYoSjlNdpvRAi/Tvp4M4VlbA3RW32x0jfURs10y7J
# 91YzZJR2eWjnDD6VHG+btHRJvkijasw3K30k+jlP0Zg+1FY5du37NlkOo3kR8GnVpnySfdk2/36m
# CWqpXtVUr7IsFeEVGkunWHoIBadYegiPZegnTDpGeCr+Axxu6SZMO8XSTfi9RvzgfC4LtGX8yvAt
# rpOdGA+8ZsDrSlatuUlXVG9lAWYjMO4DwBxW43zX5Vmj7EysRqx1vLpLK1o6rGhR7EaEn6HsOnxB
# l3gPfA759zLsYoWeutiYDurSnGTYYgOeG4yqtHdQbY2a5DvPUMPLcO+zV3B4B5/kSiAfNjftvRjv
# wbR3kh8LWe8SWkytCjkZuBYUZZ/XCB+nS73vwXVK42LAjaRzPZr0hZniVxHXZR37Lc8lDpvjaebC
# kY0uFeZS746usy4TTfsCpuTPmFYax94LiC/TL9i/pmND80LmKRGnjXgz/cybaUrjv/gcB5in1S9Q
# Gvv0HHZYeXWtv1a07SJPw5RC1rGQ6zgMjonMAVwCOQ5OymVAUA6PZXPpzl/d899UuvPw7ikjxLeg
# r/vEXaBIXQeY6nDCsoL/8xSKEvozcuQRx+lSGJL2/hWsyEWigev3mGybuOerzCEUxCGk3B1WPuQx
# G5yKtTuIS9Ek8mFZqBi9tADQec5Gwal3+mHHUAQXF+hHRYGjAZZAZpCfYKa8LAMmU0BWzvxL4Vcq
# 21ngKuBneM9x1YIjuNOzk607Q6fX2aBtOBv2olwy1KVCj4oNZ9M5kTj15JllA1irG1oJh26Fa9oC
# fS58l9n5WXMgvmOu1rpWFxyGWFqQ8pir6dhd9/r39j7v3+nfo5+HeQIOhxgI7L38AOzFDqwtsj23
# zOK/1nTkC+v54Vl7OywPtXsxowR2VK21yDbYZMVYdJ4nFdu+97y9AZmzChMQHp4ZcNVgNHqAkXQw
# tbMfa0fKy3tRnp95v3lJz65+8nUL/ckAZIl6PI9zT/bxeiOkx2fwAXY7nwbOT76e/crXcwCYf4+/
# kzy+Yg4S56kVJ5Md4JfUGqeq+ZHimPm0rzGseqEB8hrDwkNaoYbD0Ys0/t40z7Nd/nrQK4sMsYWg
# fj57lfVym7NZU5jh21AtMJPwfta1wtsUwM5bYOQDhs4XP66n2vi1gCb9bJ8BqZxu8oct8mFbJT8r
# P0vqCTXgTNmiEY5vF2SP9211Jn2DBFphZGMaW77OETdhHdzG9rImY12cInR9KnzZtZmvsw+WfH5q
# Ww4vcAJ38Qb4zH5eO3yOWXqRc7CpyPXcMq/P64anC62t5V1wT118J7yQvJUvztb4fSd8IAi2RGQz
# yK5d2+p3RUFuVNA7WVRanA2ver+qzX5HtD4Nf625BJE2fibfqYDHH/5B1OeZovXlP4C/tlc8rxMM
# 0uom4WnvGwcgdTTAcOYi+DtPwp9LQZT1bEGWxiFkr2xRh4Q9p4JUC0JVmA73ZHog1wWKy0ajxIJS
# jN+5Bts2wJXWgzzjlrS3XOMBtCzbaIHlndoct6h6ycXvq8kLF9k/Mpz5Rpo9POIn+5WfqR6+X4f5
# lfI3lR5TeqLy2WazBb21qVjeggI6xxVg5ZjO5wRgO4VOjyn6Hf+FegWnK9s7CCGOqBMwMwM84vFS
# FQBNVqnIl5HVLg/Mfgrpvzo5SzQ5S0p5lryWhMvSJFzW4sy00zOLvWPv9JTTHVCBtdQzhZ9rs3mX
# y8YuBz98UxlKinLPa/R+UOTB7gRNwNocXSua4m9ss/0GHMJ/WfbG8hfn/0UU+bCKSNhyEixdzlqZ
# gKKz3xP1Thvf5brYeg4gCLx9uiMk+yjB0ZXP703CkdOpD1yx9zm+71Xw4qy5IglHzoe37b3CemY4
# sgOOriD5A5JqWLTXI6znh4N79+7dwhC5RYXpAaeEockcawp2ToKhfaQXSnql3kxoOG8UNPhT41xQ
# UEgjVihHr5iei9NHb1IBCIrOfCePIPyiQQqW+MF8J899l6eNnkOO3ladRm8tuFfL03ditboVJle3
# O9JWt8JRq9ut5ImcV7czeHUrtDAAjdoLrV2bardDvTu7zr5WFO7i1c0mV7cPz6V6NCShqF1PQlF2
# K9Zd2YNYbc3iFE/dk0XUT2dm/zWM6r80720ub5bjFuo7b7bj6bR+y/G6HbOpzxzSchn11eyxkA7v
# pqhjT7KO5O1U1nGxeUgsznkGMixPizb9VezrNr5DckJ/X19n/trCr9DvEneZxLjLY8LqkSKXxK8A
# YdkqLEe9u7o2XCYGfhVwyj53Wmlce79Ku90+9s9R6FL4lcvCr/YxfgWZHKcc1cnwECjxq30CpfGo
# HnBZeXVtvFY8fmPABemNbMJY/v5DwlgGXClZHCWFk5RbopzmMI51dy7XQFc4lm7hWHMYx1qcRTjW
# 3ZCRkDjWHMaN9qXtpYRbtSmf1rSfStzqNo1zHYNb6Zw+mMStHujmmop8B1F48g2H8tNLPPz7iGeA
# +3Tk9D6fD9yQi6KzADhazL8rEpNw/w7ivcLcNp94R/lP+wvuFyP8Asb/ytl+lYHafwH3V0g3FNvp
# 7+1klIpOS3+kyxdpnx2ky5+JK3iULp8jEYsrbfLMQr+qOo31fDvEI+wJhe4pG+Wg93vI72Re0if5
# ojpJg+0A5nLJfOW53SCuCuysgjdixatPxntq3HiGonE2Jct/VJX/KMbpMZyTpHUnWPvRydaHZd9k
# ZZ2U0c6I739c84j0+LU4YXeIx1kqgu4kFSjYt4ZV7po6yUuS7XbKdkuLMYo3JG3Dnzle/eAv1lOV
# WT/rDPipOsvmyF+TNkesvDrGzeuZMXlZdYyP2zc/OGbfdI/bNy984r4Ze459JON8u6tudLjkLV9o
# tdmTr1u2Q1yWPhy+fY0x3RtIq9p/P2pT64Q+uEbz7ypQx6Wf+DnicnCSCZ+5jMPqYNdgo/MO5PWK
# rmEufk4P4iS7Ub8DpwSkrPy8HtRgxUgrx8o8oJOVodJLpLz4uZAXd0JeHBo4s7oqr9Tb7JBHba6K
# YXdf7bOtN1fYTd2SIPcW10IuGbKpdjql0Cq+wbwNpx+f7S59hUPG28U8qNGxwLeY5bP9xhw/Vj/p
# 0ZtR83ZQ/X226aNiZY3K61bBcum3S07OYifZrRWq9bNZk13SQK4dd4wezRijW+sywy1YunNceH8+
# Ce+W36n7EG+L5EPbNkNy/XWWYwn+Fz215b8BK0jkYbpcWO9dlQ6j2JAWT4g2oSlJjnwtI4Z+7Bhv
# sq38tvV4Xg/8fn0SR9cIjypiPGo653JmZjlMl8jX+Iuezf1KX2zK9m++je1V4M+pbG1QH3Yo+Wyi
# zHwPbe1J9sk8tfa9ICS17E6dZCF7KyezH4JaUWJJqgBeJzOPhSgyRE1qZVvvtBd2edYru+907n0X
# q0K+lsMlOPG+XEif4gfAMRnSiCuj6aotSG1Tqa38RudBNKAO7TBzdiSfLsicHY+uaEKol8xDcDpq
# O2nb5yf1oecrefR1TE+XelcVij+4I03O/9U6SVMFjxn8+XzYViSrI3by76C1bfmpCm3reF2Q3+da
# /SR+bgu8AUyjPPlM40LexGt1OWK60sLI1zNiGMeKQXZhZEyyUwII2YLnLYCQLUkI0QlCZrFnOQkh
# 7ZnlSTjT+YtBsSSE2NV6mG/PsVMv0dq8XcHHzqTdG4f4CP1wK8uwpElz5pd5IZ8J2TWl46FkOcsK
# IJ1pN0yS5IScgd2nQ5KzgDRg50F3wGBdEL+h4jno/R69w5A6B5DkbJIyl4ghypZJmcvzdNJ02Qxc
# gmjxta56ltO0s/Uam61sCvQSBPQShIvzHnARt/ZTxJNxlk1BGU4Xl7HVFfTnOFIp/2keK6U2NiXz
# AFJpR8wJlCq5uehhrz3fLtcmn5A80QLgPxVK96Ce52IW61xORUg7373sw2gqzhOE0tzEzEFSGsLv
# POU360ZN2U+Bf/Kw1OuXNt9prkIOF/YnGjLssdxMtgaNfIOovnEey+AfSB/CizBZP8Jd7Iq3UKje
# CX2aoj+qFeBmKD50ICz3W5pbWcr24UkIa+A22g1Y83fyyoT6OEnOl7g2bGnvRdKwNKjndFu2ZTXf
# XnYqwY7DoJ673JGNI1OOjeJPsz+eHl9YdoUIQla7dI5/sc7xnUoOQLOremcz/9smyvku53o9fmVK
# FsCSsa0Lq73b/w7bYBl/X1+R3NctmnYyXeV7SZp2tuqLRnz7qbIXVut+WbQ9+FMh9/jneJRmGMSB
# kzsJYyXLyP6W28JSSoOnkM2u5Pt8ss/lT703Bk8ia19uXvHK2dqX25KRmkZ2u5Ix24NFZKMr+b4z
# mEMWutwssWpjyTVwniB95z0DvYRloO1RWOTacgT2dv1i3dZ3xHyXw6x1YcXc/I4IFq/b+rYKsSPk
# bfHZ5cHsdVvfUmGQrt/8FoXptKpQTq1bXkPtSqh8d62bJZ30NFl+I4tPhnUsiXgd/Fhnu6EtyPL9
# J6uw8znshlRY5Vf0iI3CBjPiTaV4msb50fNi+GVwoF2OW60QJfMIulwOvmJ1w5N1p/VvlZC81FLM
# gP9Wvp8J9l+gufcZOnNIWZ5NGNszeWxnkbdE8SFbyKvVI7xv6cw9QEubAoZsJ2ynos6XuogKPkiz
# gXVIDoi0d9RM3tvYw/ViF82d0v1eUXMLdNt4NT+DLRMTvngp7/v5TAknPqblQaTeQdYB11I8zAfW
# 7DyCfZPnBMFrRK3v66x5jXsibMm3UZ3zsTPLOoNXXXUUe46D71RP+EGu+hAhJB92g7JF5wAmSrTd
# a+lr9Yccbz6f7TX2+PVFaaUMOEPSFp3uqMxXtiCpTpej/P0ZcjgBDVLKelJK2SAp5VKjQz/EHDvp
# aey1UR7HPp1muVlaFpVWRiWvl/BQ4syxdWzIo/PovE5+0FAru3fSg7tPsf99hN6JclXgek7U7Ptw
# hDyjbUJPznAWawGX7BXIb2TdblAryecCjepKW7bLl+UUVlgg64fQ9Gh2O93FObAG6XbnkK7Jk9mk
# Z3Ir25SX8XxZtow0SOEYE5vh46p0+ACP+mLxit0+ErwrX9qhxe/L4ZQcV4f2bFKOyzrfXh+WvnDk
# mcglz0SebzPPMGhP9byFQ9Nv/7hpnjpmmkx8/DscbldfbkZeZUoWiTRrPdDOwboCOxLkk2Td2nc4
# 5G0VgnVl7dtsl2td61tpYW9xmGfctfmoduy1+YPk2uzik4Am7g5Lm3LeDBt+5Oku/xNa8ZtzDCt+
# 00dZ8QMlN83qffWl7nSLfvbFjOcFj0gMTPZjfZqOraXX9+2wPNMey4slWbPzs83LN5nyRGXmA6sS
# LKkox6ZY5TtJyZZRD34/LGky8KcE3V0veKRdwOc03HtMyHcYmlkDLUTomuuE+cmZ3Klm0Z9A18kx
# WIfxfS+fMAhnlGVMU3Obiv5xWOqWkDWub6XZLvt22vNTmPO/BNS+wXbMvsPy2NuwDqViPJth6exQ
# xtszGW/fzXg7iFKfRhtXI2SYqAnQPwvAZmSP51F++57SbZbwRPBrJmWkczN412zzrLIOJw1LYikV
# fycobSLNFhrBHoc3FOhkC80KT+bTUMXh6fiHtKlGsla5PD6ERfxO4VMkX+4RNZ6F7MFY6pqAo+yp
# VJ4apa56iCxE6+QPm2DSW0i7O+XYaMGMZsn/dvnnGqXKmqyMQVYG6CekZ7Xndwo6DxPVNfixrM9U
# ZbOCpCwX8/hKfIpsAFQRXqXsEBHM/jMsZQ9hC0dDD4ia0rlcB5oXXNtSn3G5rqwnm2QfuMqEJohJ
# 2Icll2aF7jeV3pIKCTJcv6Jp0IeWPjMF8LuUnrZNyQi76+WaEwCds8d/JihuAeMesiUA6aQCU/jL
# lheAonI28OMs4IQOkkZs2Kg3eYN/ga4r6/9FpTazkZJRm6HK8SdlHA0xpV7un9SuXOvMlTzJ6nxi
# 9UEqkk6pfi0luSBjn648FmDf1WXtdC6Z9XbBqWg1SWvMr97OUG+WfrU1Jk6UdpKlo0QymGQnR0jZ
# bop3cr3UkUr3+VSc5vNpkFcPH/TR6Fn69NzBp4jU+z4pRSHuhLTmWbD4nJuGP1fVW3oJvexzNYul
# 53WxuF7a9vRCJhqeAA3JRTeYi94kiulcVdkg+pyk0dKIcKk9XU1SzsYvHTZ9YR+dVavUu11f1Afe
# fMN8QdQscNBg0SfoCxiSe0/tvA7j+Esd6UpL8ezEM9KUYhSnbKjUmKu/QJBUda1ZBC2iBwThUHQP
# 2CIYnwfE9Vj1V4AyELT9Eo0L/sIae8p7Nvp2HvtUlLrsJys7LXKHg334enlukthgyo6NpUNmJnXI
# sI7Xy3Eb62NwUpqPwZS10g6slpdY0vtKfy/4Rm4yvy31UrZ8bH7+T5wf0ahViJLXD76aq2ThDbSs
# E2V4NYI1z5hdM1d5vzyTVx6lNc6SwLXK859gnBj+hgz4G/J/n2cWWWSCdrSHKHtZogY6113+reBX
# QffcbtcW2+msdJ9wwN+G9NlA528p3XOb0lIoNhzKm6DUa/wNe2eRfqG/rDCXqxQF+CDk8qhHLiFM
# GnnBd4nIUedlB3uOmm+UrZQUiItdoECcJikQPc5sXge9YppTWp0gPKz0Km9Wzf4zxQq3M8uXLa1O
# ENeK6Ju/Yp4qWrjMO40sO7zAFgDoa+mvvRXeZQUe9GS4et40tuQtpXc+xbkqKoKrLED0Ed1cLXTP
# aqeeh3NuNtXlfD04rfRw9TPFVkpd+VHXy56jFE6kcHpW6848ju2EjYGsLC14r2xnm0E+Wkij0i5e
# gZXyOaJpRFnCB77tNMajv0iqX1HSL7oPnDMrHtlnseJp5G81KdFLMPkpjGo7w7+d5fLgZZz1tTbg
# +68Une1NltcvYHsyV9Ok2oLfp/H7pWTdOFgPQRf31Uu7FgTfRoYfnkXj+eHB/PaISzTFb9FIOrdQ
# eTgmnntwildqKwIzjDG82i0MkvkC5PWYzz247zyjWm/FtA5+P30fIJ2CSVx3uRYswS+QlMc0xaF6
# 6bNDnQyVfmYaBRFz733oQZCV9FmQZanlfcNgyl8V+JHg6SVPyHTG+Lw6OZ426uTI7zgXGnQWwfu+
# tJMC7TcOJ+VLELbYSbjAW7BjBa3JhiX6Y6UF7n2nFGiPuwuMhhnllUXafHe226t5q2vmrxLLDbd2
# Sc1O/9d0sp4ha/r/upa8wz9B3kBTMrj5bLkV/C9lM5pgi2jZdWrfg0XYh7VbpTEia296s97CrTyM
# WzmUB4/31Z7Y5TlP2Jl6Otmi8mF3fBZUirIyCeck0V5WquiRGiDILqkbdD5s41PmE4DCi3UzCR8W
# Dc2r4GOqsnVs2av/d71sh098HjtutpBSoWQtPQbpeofwM47mFNL6UwruLL0kr5CymsRRcyyRepGZ
# NplPxxr/qORaQHmE8Glp70zaaG5iGWo61zSxjWbybEi+uM0C8mqw2DYVFoQQ2z7TgCZ8QvZCzAk/
# F7tlL9jsJOsuz9Y2SgtpVZmSLQphVvV4/kaSCJW6Nt8kb1qwaumPQFqPPQuif0+zpWvD07nBo3R2
# aEwmoXXNijZKOOZ6/EqV3rflL6FkSSZOPod8uyPdbITfbwjWMUr2iZ7cp1K2THQZEjBexZ5GGrTv
# sY+bD9QewjPW/D/gBqBGiCQ1wk7UCJtl0UTy+R11oVLS8vgrvzkTrWzRxPnrkfdHpBwA2y8xilxC
# vmcp/nlWzwM9MsStrJe42XoJhWQvDE6lkOy//fmoDBGlxbnMdX/vrd+MFOVI2YQcls+RFliAd+Wm
# Sd7lFXkaGpUsgb3Ic3Gjnu+w5BQcugd7sZIjCMIDmI00HSBNcDbbQsE3ISUB/Gm2UPRCWN7JuoVp
# bkNs84T7imQJMt51NNQ7pXo/LHe4yBuYzyXtZlw/yk7KWsiowHLFFJIs+LLNm0WSCbe4SJ7gAVHg
# knZS4HvtTNht2k12UnCOI1mId73/IkmP59mShdMVcEmLJ3NEIFu2Z66yT3KvICnYP7yPnNWo1mSd
# B/h/X7+dLcd/HpZZ/qrbsrLcPtFpcxY7JUba8E/dNuCeDO008Ez+pdugw2LzOSAJG7PR+y6NuADz
# THq+XFsMOyDeyRh7p/Sfx7yxac7p0C4uWzytDqvOmWwXomAaTgKG1HzCKlDiFF4naT9dSrTzGVnk
# I8UwnKAOQQMKeDBoQ+tv+T3hSEJzsr+2Avw8UmaMUlOaoDP1TPl7s7zOoCsjxOnNCgjZK36SR0ev
# 0DlISq1A1hRrTUEp/JAFQAcugyQaBML0mU5RO7NM7N2mz3KKva/osyGPBo+iZVcWagXY9CKznJMK
# CjS6F+A3Db/yyGxnGX4BPJfi59dnY9z9SEs5bKO3Ajty2GLlYFc52AsiTvs0/MrxK8MvgF8pfn7H
# NmhTQvendibgDhpmsB9jz2f7MZtYhqZSdH3qMnH04w4DVj6q7WJvdaG98P5Tlx4wDPtANfn1xT5+
# J/hndpxwG97QbluQjVXba/c539flyMGflD2bR8zGO4+dJdNbmUNmjWmtE/ZQpoFGAukiGBUReqmT
# LNW8xZJGzg3T3WzpBHuCg2JZMSh2bSnZtEEev7Ni187EHIZlyYsxhoXgHV4sNhRnC8qjyO5fxvGd
# QUftzI9HaP6CO+fcMDlHqHCkxkr+r9QXYNX+i7BeKo1XSJnjfGWXc3YWywHtFkVOzC4fza47nTpa
# QjPrCrINk/XSi19g2zArsmimDAJX6fJ/GbI7AQPz63dU3ya2EHOeEcyilOTFue321yXd1NTh4TM4
# I2DI0mDnRVmiaYIk2HmqxNs5FpV4u2D7M29QrpvtlGulmUpbW4JVwrzZFixMWSrw2W+zJS0dYG0Z
# TrN4MN8+2VcPafuAXca288qygmxA2A1hpQrYfyiKfW8Z2v/phbni6zmTfURV/VmuM8fL9n52sP28
# TFsKQbuMLePI1lQ2eoupNZtM2ZJCk8aP1qUm2aq/eN/nduUW2UWDGh37hhV5QraT+WTGCshE3SnK
# zoIdC4JbfMnWXsH5CCK7WHkxh3MLhdMJqzugmzmhlaCbfsD8bF4dac3Vvu19V8uaI276N61RbIeH
# YXC7aHCSDZ7npQ0eWL33Okn6qmEy+dulcq4BdoT5AZxnryg0qqurl0Ka1hiA5PM/mpSdLoOs4FSS
# 923fzoa3Ne8toCZWngJKBUmmw9MVnss0acGmJblnE14VxlMT+xe0i5UanW3d4gx+94iz+F4En2mE
# 5/vEZbiPIP6lBsl9zRC3G/JcTC4F76LLHwlL+gNd3qHLWywcTZdbHZacwlNLND4/bAIVMWWn/1uY
# bQHs8+Q5Ilvv8TcDZ4prD7CMQAD2eOETm2zt64/g7Ua8ParR26PiPP3nWJv3C2md/zHGswLaIGAq
# rhHd8HHkQFJBt+GtQ5PSYJrycrNT1OiCQ316wlD2TwR5x9op3mSso1mX8hQ+7T095YNLxvgdx8hi
# uzENupJ+11pQ8kMogaTfPUr6XfZ6kZnClAqU36IyRfMhjaPTFO67G7+5Sb4KeI/or8NKrkNiM5mS
# HdeYPv0rNkuyI6D9GmV/pBNvuh+z7298KuzQn2VtIewkei2kJaBzip2UZIdrmryiw3wWc+FD8hmW
# oRPaJiV53vUJD+bN32VOxjPs2Qt7G/CNLs9f0v2pQkKhJM0z3JOwlP2MUPJDlpYk5us/QKtzO+h9
# gGgc8McSM4O/8YlnUqVoh7gUDaWst0qxqPM4wUQs6jzo109yCcB8TWot9mgj5Ve6Fj7MYKVLYg3O
# mgHwV/1H9T7WV5vKOCL5x/GetODuQu6BILxIA4/B0z7hzik7UKy1wEJWS47TRfyaK8QrDvAhf9Fh
# HEpSN6n2TfbgwznKoxHsLyjc/oea5JvQ4vGPJVL3WeLVozwtAxYnpdkySumUdeig7hSk9DBfJT3M
# N0gfq5ahAPQubT1G+xBoLcX6lQVdDacKokYLpYOJubxU+gUjGZdcy6dOWrlVJy5XaQfTGXtAI1xb
# nrGlHl0+0y90zgtyeJqkPDWxbvyrpCH8QocDtcXZj+4B53LU9jZQEYqFrO2AJ5d5+3Semoy6fp3P
# UpeBi+3VC3DKqKm6Hpzl7abFtfCZiwxYVziTbO5D+sBh5xNNkz3bVF6E7WVnsfQBdCLpfLPZgf3E
# cRH77+hjC2936GexvZqtgrT9y65CTlnIKculTctCyiyXjXI0XNnsHYm0Cb2nWF6kyzZSDLdW9jmi
# 0WRpq91ZNiplZZbPcdkxyiA6P/GBictOFH0Xn1DLxHFKNqySyzZQW1xa2RVUnh1SEnYur4nOrUbK
# /uUccf6ISPogaQXMRZliZ+f15HoCxus0ZZsF8xb9/C0lgZWhhWfZkU1ZGvJkkVUkjXbYQtqZtWph
# PVu77c7KG7GH0q6bgzEgnVqKN9vSFhOktSe9xUutPT9TyTvwPiftPYr3qrT3VrzXJN9Z0+40n16R
# FmMJYsxNe1+I92Da+zy8hzJzKPchNBUDVuKJw518h94RrIqm3nFGgkSCemfbTj5tix60+7Qv2HAW
# 0oaMAJ2A9U9ht8olvahKD2TsC8lJkAeaK+D3dPlz2Hd7DuC2g1f/Beqc7FIyLVOFnem9n1dr/yPK
# QIpDnY03YKxKeO1/xppZmlqh2RuYBz35XeuLob7wmlQJ2+LbjadFrpLZxX6LvF4Q1n523Rifkpbv
# wGLjLeOpygIjDO5KAjoEJcD5Q6K8oUiLNWUbHSZS2641CZ96HvT7LNNbQ5hHG9vhS5CtBHO5Qb4w
# vstcfknFfi2dmg2q016Lmg3qg+Tyk0X+GQ5JiXBgdyAuv01kUCdcX3ML1yX1823unDrY2/YWkYZ+
# h/ugUDtJNtEHyX7kDHex5p1ba5skFruxu9mSMRxlz2FFd2FFdznt1Eu7nMFTWlxue3BRrXshYh6y
# Yroor+u5fEgZ2KCnYJN7ik10uJKx2JP53bZml1tvznHby16g3N3YL9zsM+SRjLLJ7+ousd3+NEb8
# MyObcAqWeMQCZafOpuhcn1kqbVtJ2wiGqNdprX82Ocq0slYKa3QtX5mwsYB0xK8lu4LK0pLUmy6w
# ZrqHpVrkyu9QUpUWXUrqJ1v2cyenyQ9cvVTam5JypQ6FfTwnLN+e9PtKMs5oSeyUL+evLpU+ea22
# lKbsCcHb09MiN02+9+al0hZ1RpmVLwNafeL/DKLNe5KYGuFoPvGerkJ5J2LdT8bQQCeH5rZVD5Yd
# Hq89lS+JdH7wfUstfnBY+TCVfOWHrHBPvZ6i5gnxeDL+Mj29X76dLOtRVRbJXvxIS4/zzFLpO7tD
# HFI+U2lOW08H1RPhyE8nocVK+/2lUjaCdB4rVI1MhXO8tFTaLAtAQqLHfwAnFskpm8T3Yuz9tAMb
# pfJtrpb+5jfkW65aOwi+fon8yhUv2LusptTHPOwUHlBpSAkeN+9z0hKQ5Fvrkgb8xvi8aIK30jS9
# gjdTbQJ3vZT5k5J/AL9nSyUeRXRryxc8w/hJcjRnqdXt96NWt+AUJZcEeZq1oCXg9GX7EWFO5NcH
# q0jA3iZ62p4R9hqynGjn+ZCr+ADMC2/QxMlskwDSwT2vyzzLZZn+Y5VZIE8Eq4Sj17xUG3I8utt+
# /6iTgVWOW+H6xSjnF3w2otXhZwrnJy7Yj1B/YPaeP7Nc1at4+gPx+hivHy1DlZ0mQ4XV0/8n7HPB
# D306zgOV/9ZJy1XmROmfOY4s1uRxZbF2er6sVnMph0V5NhrB3/t0nBLG5H9sWS/IWhtNGSUEbLJd
# NqHyBsfwhdGeIGG/aJrlCRI7BiS6nDKVUzRDcrGr8o/6vizvFDy75DO1/3IRPKLqb1D9rXrPtwWf
# 8OnPqHrL8+ELQkoCFSm59GuA63ws+ZKVhXTOROkkI7PdfDrJJ8tl2rcQP1Dw/L+alJmhVeOUBskj
# CIgR1LOeZHTzz9H+iNmzSE957N0Gjn6mHZ10HoFDySsub5A8gnT5oTBoUYX2kef313VA4oYkhN/C
# 2sqejQ1TqzPKxWcbrfW2yx8kTXv2vr3TYxpS1gk2lc1/o25hg+wLzKE5bdvY/L7YsOIDriXbcRW5
# uuSrTQLO81n02EkGWfIlWY5SWGGbxRg4Wc+jkEYbpWN/ALDp+R7jPs2qZW2qXeuTfrzQpgYphynb
# VW1IzFFyKrs8WzM8/zQpzz8fmGM9/GDWVRCPFqH20q3eYPVAUKxw2I1prj+J6hsC6PkVOvF+2yWv
# FXb04VUBOLaJM+NK9MC9gqiH9+rL7Ph6GWHlprFaNyWvBBYh2SPQd6QtS/q3Jk0O067w6x605Qw6
# czE27RMDJvF2yUcp9YxJ0jlYzal1JsPX35OeCaVXQTo5NDJdgWwPQbfd/2hV0FZoQEP0N49UkYXN
# Rl3u2qk8/nGcPGzi1zbIdhmv2vSPOTV58DVT8hCEi+YpXYr5bJtXygPlKH2K0xX80fo8gLadJ5Il
# Y42Ksmz62jTZdH/HMPZf4jTfxvJzHQY8a1RB67MjzWIPZHTu0hurglX+jodGxX4xGVueSMkyi2r1
# VD+s/2XG/kEyNllPOUfFHqLYJlkvbbRLyyhKRv6A1fIUb28t2n12mnz8pyAffxZuFn/x5gYpLyJl
# WmS7fRpsS4/LV2yAXWIqI2BK69JtbFF6M9u2fYBPpQZLvZBFaVNYIyHl7yUvukDxGouUHFQh2yiF
# TX/Ug2a1UyMdIK9w0rw1JFYKrXdTPpmKy8/8C/hYyAO2HmFrmuuwvq6FdMyT9B3vbehJv2X7BruM
# L3ki9FsnQuSF9OJBTXqECACueyq/hR2O3k3egQ21A+cmtXC9f2Qul435Xrr3I36zn7qgnHhe9n/8
# /W+KC/beO28xFyw/Px+n1yKH5H2RzZMip/Q+4GTdXel9gOzwBdySAwKNimnEAXmBeVwbycJ5jpIB
# zq15j2zrBHIlXbqEebs/gb2AGXn3MjX4Y9aqfZm5U7miIM/DVGCERq8V998TyJMclTzmUHSLIs95
# jZkawTezFvbHkoP1AdGM5+Rt2AfbH/tyoaUMGLo6qGsXbbiZbFlyjfIe/GH1v2W4N2/DzcCO/F+C
# f7lAXiv68imxBpIm13Ct9oh3Xa/k5GjU4uBXi8SCJsszA9FzH1xL+FQRz71PCzfL2cCGgP9CtJ+k
# u2LAQ0gWBBawmZd6iu515zKSmKPWKLJNbuGNHwCWXLwnnYb23sMYbZPCHy39y78m4zSjL+8gaaFk
# nGymFiPfRvj+oDX9YmnHAvqYgBdIibnAZ7gSYVdCL+oMl/F5V3BpwPUmLOsWGHdPHvWlSqYmideQ
# sJ7bRKZelJ9Xm+mwwQJO+VeCjlRuwb/L/N5kWxAXE0e82Ovw2hwP9dudhp6xq56u+mKNwmtpzk9C
# G5Yx/v0EZgFssoPa6tMuN4mf72F5lJl4v4vWt8o14MqTB98nODb5uz0dcei5y9PCOMBilrCm90jS
# Mp6k2Hn53qGR34TTOJV8K9Z/5EhR7kguLSUDaarfzEbrjPBAkgZMqb+Xkdf3MqiAuhrLUKM8B1Ct
# akV+Rm1rk7VdrWor6+oaVdfxcBJT7R8Lk3V7RJCvmq1MSfdD+/U3iNxhyJNbmbZYWBT33KTd0iWN
# 0harLLUI31cxdCk+gFaNVZIkMjCKdopDUn1+Ti/LXt4ozySZ5867Nas2tH4xdq6/p1u1kqfGSsa9
# GvlKZ6rt2rfS8l2brFc6zd2n/Z+RzgWgM+ZSXZ4xG+XMkOT9XJlLruwkuvPXBH4XQje+0BP9ypKS
# w0f1xHTxJTIMjamZGz0qnrbh+RkbxdWK8qTFGZn+UkIx/5XDuVxCzx/L54tz+fZPYAO5pyTEXvr0
# b/lpRN7ucFGVbnTh8qGLQsKJaFSblBcdGgwPbhfXuLmiUXEfxfgdXd7kFC9xZK0gT8vPw8KEoAFx
# l4sj6zshqHFBKBQSh2TAUHQkGhoIl3x+eLgE0bXCvMF4NDwcDs8Il+ibpourZSHiMRk9Go2Go+Ip
# LuZhGXQwGr56asmVYi+hLLlHIpHojGg4NKDNyDtcUlIybyg+9Bn903X1w9sX18f1XQ/rOauuTAzE
# B+LxuFbgGY5Ew4fmJQajR4aOxuORikQkelibnncnfjeEw9Ej9aFEOB7ZjpqFwtHIkoPz4qF4qKVi
# aOjwAD4cSVw1ghShRDQ+GIkO1CfQOfEjSBcK1ZeUDHxOPMA9EdfzfhwWdzmp0vc4uTtupVs4Eh0a
# it6TODwU1Yrz6hNDHw6if9EJ3ryh6GDiUGJ4OJFIHKI2I8+hkfDVWpEnERkS+5zII37yYUQMiSs5
# 3ysRJH7hpOIGw+J/+SEaH+5M6Ik6cQXGJjwYGdY8HvExIkb5JR/srOs48Zt03RgfGYjEBxIlGyMH
# /XNCdx5KiIFCzpqvX+Trl/h6HV9v5OvtfL2Dr0N8/R5fn6NrArCoLcnTFqOeP+Xgn9F1WPPnRbT5
# HvFMESegq1aad3CgBOOvleRpk/MGS7SL8gYIhqbkadPgqAZPZXlR5KStyItEtMo89IneuUG/cLo+
# dXpIXJPPlaEr8hga0IJ5U8R19Kpn1+n9qxIDB4e0RXmaj/9Pz9PqPENaeR4GTJuaJ64rptSDdOU+
# vYnfb+brLXy9la+38fVOvt7F1wN8/QZfH+I6/C9djxwNHRk4qHXA+1IJbAIOc72KPCOhUCR+ZKCk
# 5DA6BpUszrut9CDqeziCJk31zAjhOaS3Txf3cob/I7Ol6xD6R4sgq29x2Lf5eqiY5+kh9aIWjJf4
# 9WW6HjkS10LZS8UrHPQrvr7FtXyHrq8Px4cSQ0PUYcPxkcSMCBqfKKFJXpgXORIvGRq4YRjv4foj
# +jnT9fLpcFfupcRf9XLioaHhRGT4cDRKcYcpboIGZft0vRhRX+KoP6Qr4G5g6OhQCdpLZeFGpSVK
# aC1BYdNohDHOkSOho6jBnhnopBC9RwAISPA5fc/c+fqeulOaI9sPDoSORENHAA6PzJi3/WD8YBgR
# 40Naa7bm8eqfvmLxcr1vlZ43Nx7V5uX9BNf5eYmw/sRFq+BXeZV+10VaUa7+SwGAGS6g6j1K10Qk
# ERYH+f178j0SFj/l95+p75H4DeINDnmbQ4aOxGnWUmMK8wZQ34ELS+J6zyqfXoS8DxCQR3Hj4XmY
# 3o6UaLPyBqKhSFgL5YW1k/KwPmnRPHGv+lgGYPfmiXsKkeDveXzh+nno+lePXOkGMA3cpeLtIsoX
# lUqI27C/RoaQElA9D2m4wH/wrPotJQ0RtBfz/zNpXRkQ/+bEGyORg5FISDslfx7WwUUA+Se9HM5d
# X0hdXwhwFPs9nOOHRRhEX574C89UiDaM5Ml5NqTnPxOOJ0JRrnR4TwJ3uWC/T8VH4uFHxP/RUzwS
# PiS+jtoOaFV54ose3qboP2bfABqtlQO2v0gPPjx8o5h2lAI0GYXoU1/DTqDPqAthnlKEohAAGg/L
# 8gDMmOAhrSp3jz5DC+QmMKz4UFKcwCqEh3mTQ5hElPukkLiNQk7iyY+5VINZjwAs9+V5Dy7B3DyI
# ATriawhh7vEcR2Q/rTnU7p9S0hnI4yl0xOHDNE90/2s0KWbXJbBa4OtUa21BCrTwJS8q9rrQQqjT
# r1Rlv4jKYuaX5bSE9ckPJ8RPUNcR3f1aQnyXmnrwLX33dD0HGUK2OzeOjH6zhOY7FU1VFj/A40Ga
# Ex/pF8zV3XXifsrwoOanDOcjm8sI23qToS5CoIY9Lh5JDNK/o9FIOIoGYRywmwxFDw5FsfWFE0PY
# u/ASDg+Goqr20/PC8XAkAvD20lNiSN9Uh9715o78JE6AVJR3ZBhbT/7UEm2KJyHuZRCJhAYxy28Q
# I/yWOPw6sglhE9cvmC5+Qx06gLpF0f0hcYQrmBgYHNb/V1yh333RdPEkp4qPXDkYQSVC4TuPJAaP
# JMIAwjzeEorzjmAdHRnBR337Kn3SqpD4rQPwFg5F6InA/Q8S9t+naRPlf+IjBYr/zpX3t9X7TRRn
# YBAbjfgGPR6JDEbRq/qnp4tvylxu5Mn3gMSQHszlHU4lmpwnHqbHSCRB81+8xlFfoTioZsnBcEmJ
# +BpN4Lvp8hTntxGdgWnlzRspofrK6my8UpucfTrgBkH/I0t6hG8YbZnN65R1HMtrmKr2JSpiUTx+
# IYoJh8Ug8LU4ltCivPBASNxKbwNxNCkcR63C8RHk+rQsaCReH6eeDAPfiBwZCh8uwSo9Td+xQVzL
# tRuM12un5FHQhRvEh1RKHEhIFBAXD4kXZJ9p1XniZfkYr6d1G5WIUElYiJFSXMI53RC3goYiR8No
# q947XfwK/RAPI/40IMKYWNRdHWogbHw/SpA2gOmH/3DQ9mMAJpoVHTgYxigmAK+4HAzTfTh6MDyg
# X3/RdGBYqwcB38PD2BKAjkaoSpNpNxkKf76kRJ9yRzg6EKL0wCaRXUHenQP6/1w0Xf9QTNe/eNFc
# cYspd0usggCqoYFwaDhOAFdELYsC7Ypgi/ZogfypwHg9g9GBAX8oMhIZjAwMhQYHVcxCxOQHP2ob
# yiisNBSNW7kNDMyID4YyWzVIMVWrEtQqf0jcRV3zLXbL9TRf/8Ab9gd8/b3c638rb+/QlHpdPr/H
# 34/y9Q7uXU5LsyJ+MB69Mn44EZHDgv/6hZjITSF953QxxKifzOP6bLpdOTAwOHBD/EgI7dwX0rum
# wxArIP3IACpYOi+eGIyHZ8w7PBguEW9m0xTA7AsfpCRDKom41y67leEAC0V03sgTAN4ZIwcrRw6G
# ANNv5sjV4vD2kPiaLsHgx1z49gh2clpSQiD9U/BzDroNAQsZGDg8MPBA6Yx4aHDozlBkQGvMK9Hj
# q8RX3bySDBKuALifkrOoPCQuJmQ2Eh2kjVn8Bf0UHdau0vLEH6nL3qXL+3T5kM490cGhIfEUFx+N
# DgAJCx2JhD7Evvdrah+Qo5D4Bc5XmA7fXQIye25DPJ5oiQM6EiF9ULuATqnYSgcjke2RgXhI75+r
# 59Zhbu5zUHPCg8NDwFyWl4hLcmQJLcNhTMtDbqSqHwSIzYsumVpyTkI8xC0Vr/LxTfzAljy81Uex
# AGNytLRESw6HWkpC+oUfNIXEII3cfQwij/OVdGaxUmDJD5UMbB+M0PqNZIewm6BSM2iVOAgcUwtg
# kdMm5S6b16JvrdOnryLEARhnSLwtj1nY9N/QL5xbgn1jY0lJSPzLLYcT+WJDwPQcnBEeiURorg4C
# 8w4vCInX7FzpvSyjh4HFrAkNil9wKOaKeAJ9ERmkGmARxEkmjpPrzLi4Ws0/8TWDq459JhEaxmY1
# QLsVbVwDiXiYkDhrx8IyCDRsKD4QjZaU3BBJHEZWe7TJuVdHIxu1CsKnwtjGhkviGxMDLdHQ4dCp
# O4aHQ0cQDYVinoaHDtIEvYGWeixn6Nh4AgfPkgH9wYvq9B8LbWruwZ9gLcCQtCTEFQQd4UdCCdoh
# hw+Hcao7nBBfgELfxiPIBfGou7WAh27YZQFwwOCzlw4CeL5BoxFGhUbgTJXPzYQqRWhVLRkRf8LH
# K+OUARaRmR4aqjgGJqT3zNW9q0rEIHdd9EJtKnbYFhxkSxlfKcs5pSJB27H4EUPT4cPAWVGdgwlC
# U4rp2HSQWrYwL5QQf+LF4GcEaDdEh0K0fVxB5/sPCfL/zB/fkBP/LXm7jU9PLGH4NuNedPknXV4D
# /EcEKVLn6vM2RBKJPeJODGk0MhBZvwyw/muegxHxuoSgwZGS6KCeewUWj2nibwp8EiP6pNfC+lYE
# 3Ycq3xml8y4us/IkPQFowXAJPYVD+o7p4hvc92FMvu9x92HpjMcHB6LxUNhPC31C3MDwfvRwhM4S
# BInLQ+Jhjbv6EcyxkqHIvCHcEphtP8+RgDlvgLYI/h8uweLznJ3DaVeeMY8mgfiJDBmODkS/iJUy
# emUiAmCkuQDQG0CzeLEGkMVDocTB8NV6DFNow9C8xO4l88LiMXSKnnOF2E/wrO/VrhD3ZqvWPxJN
# lGBRHdI8+SHxMq1OQ4PbjxCaLa4i6k0C80VjgsmwGELAYCIqHmSkBqZ9cgcJDQujwtQrdEZQdNjv
# qPsT6j5X3Z9V95C6v6zu7jS5DD3Nr9mTaWFams6+5BpaPHwh/gt/dB8EnYzCH1fhj+NP53yknvyT
# +HtK5UPPPmWrzavuBYq/aar8B1U+TzJFFfKOqh7z1P1e9f1etEDe7+W7Hyl0VYa8ezV597GebRHX
# xwor5jhZqo5ZCDmg8j+QbO+T6n4vh2epNmRxy2Q/ZIm/cQ6OpLyJntQxM9LsTtB7pfLV9X1I1lN+
# b6rwP6k6/Q32m2We87ielO5+MVl7NtmvhrrL8h+HzaApmvVtCttZnKJZ/Wwkvz2JkujbD1X7Pk6z
# zUH3ycl4kzVDjYNVZxN8EUON6eNCUhEfz/iTefw8OWaPqzbJsfxIhf8LT/L+Tw6PqXTD6v5n1V9H
# ueUyzQuqvZasjaHezWS/Sn971rOeHDdTxTcZlmXY4yrsySSt2UAJTyTz+xtJomgybkGyH55QcU2O
# YfWJqe5PqG8vQLrCivdi2vNPWEZOxv0Zt9Op3uUY0z2WnE9Zqm50z0+DyyeTsEbP89Szgdqamkxn
# 3WX9s1Xf56i+9ar6eFWempD+zDTlO0ZnPq1MM129T0de9F6m4s1WeVCtK1SceeT3me8VHKdCxZ2v
# 7gtUvBr21gb6vApfqu4N6nujKnu1aue5amTksxwFg5+fTI5njLUDwb9S9RpUYy7Xo9Xq/hzHuUOF
# H8CfvMt+fjwNbh5X6R/nkRHq+cm0Nc1aN/4nuY7NS8L8T/n+HEoy1d0agxdUXgTLVvwXVFkENabq
# ixfVt5+w1IdgGCLY/p2a7dZ68zu8TUeYXT1b75TmQ5XXn1Tcj1QLTV5bqMw31fObeH5drgFqDbar
# OWeHNrS8e9kPvFd7Mjm/alRbaHZYcFSopebHmyrsPtWWJ9T6bai8aX5pXD7iYfigz+L3C5vf/8xj
# uBv+kF84/btjvX3x7i7hsp768AURhKtsdePZq5vbGsuEO/nYXS9mlffWlne27+jY3F5+Ru+pq7Z2
# 9e1u69qya/m2bSsvPK09ui2aaN6xArrBQdhumCOM4JwzRSi4smV7++72il398c6Kxt7e9j0t8b7+
# OhnY2d61taJ1V1dXe0dnrA7R9eAqYQZXrZwjbLjiNgW3Y8StCLa0d23u7Y5vrtjU3dUf6+qvaKJ7
# ArlbH3bHYxdUnIkLJZh3nASpItb198a7tlL8dROJP07IuJWYOTbT5fH2zu6tK/HSu6V9U6xuJaIt
# TEW7IL55a6y/YuWO9q0xziX5ZWtve8+2+Ka+imXx/h3tPZR9e7BlU/eOih3tiZ7OXVvjXX0Vfe07
# ejpjFavb413ly2Odsf7Y5sbN7T0orLy1G5lNLPqcldQtqbau6dge25TRH72xLZ0Iqlgd69/Wvblu
# 49jIc8YGiUnBY4xyUXDcsTkl+J8PwX+eNprZ+FFNbdkcq+jt7thdYaVM9HT3xTZXnN10roxxWnf3
# +eWpx7Xtve07qEK7/n/P9BPB2OTguFMTX+aO/2Xl+P0ZDB5ninPgagDnKjllU4FpkWhOVayLE+Qt
# b++Preju3dHOFVkdbOnu3YrvnTEA+46K/q1dmAptLa1rm9Dmvj5MiTosEBlxdiA81rU11luhYlhg
# d6ZYEIyOHeiV+HfsyYZUxkkVJwlzXvvuuLDN294T24qX7T103bGgna7n76ZrN197qvm6QDjndXXv
# iG2OU4zurRS7p2urcM3r6e3eHd8c60XABe27keMFsY4eYZ8/r3pezXxhq6k8t6ZSOMObOuNd8f4l
# wh6Wd9h30pfUCWNJ3RxhLqFq2ZZwn4YaW9qaW/1tjctamv2b5Yw99/zYnj5/4/Ll/qY1LetXn+7f
# 3N7f7m9rhktq+E9zNzWfjiTnNrWuWSsWNbU2N7Y1+1eevrz5bP/KFf7T17T5m89eua5tnT++OXEu
# cvSvOT0j4+Dm+OY5ou4ECWObKd0OOQR4i/dzwrk7KPHlmkotq52ZenRZKKKt+VS0keKcvr6lhfIY
# G9jfNzYs2e65a1tXrm5s3eCPNG9IVmOOuOd49YglNsV6+rExnrurDzvk+FVJReqNtW8GPI2J4l/e
# vKJxfUubvzItcv+enhPEHVXhE1V2TEd/wl7D3j9ut8WO0ZnLWtYsSwWN06uUIyq79jh17Y1tjSXO
# 3RLv7Kdu5TcepPHz5e/I0d7U3rUp1ilcTZ2x9l5/e2cngrq7tsQxq5q6e/b4aRURsNvnXV5bXr5u
# T1f/tlh/fFNTZ3tfn9CXLxc5y5tbzl3beGrzuetWRptFwfKVrc1NbWtaN5y7fM1Zp7esaVy+TjiX
# d5wW6+zB9LTL3U9UyDuV5+9r3w2wVvBpdXmfPw7/S9viff5N29r7lwrP8lHfRW7mRirc6n1lf2yH
# mKRe1GLVt6K3fesOLNkiuxlD6Ue+/d29e0R++ptC5JwUxpnMauYN0791W3dfv39H9+aYf0t3r5+W
# aQIzShSP9S0V2gphrFixQugrWoRx6soVIuvUZAoBN7e20047ZccOYT+tu5OWKDftKKvbu1CvXqGt
# FDpS6CtXCgNLJh5W4Yd8VrZQSEuLMHFpEctWnr6uubWN4GfN+POfQIRhjEFqjv/Mxpb1zeuCS+eq
# PxhGUnmsafWvPPX0Na3NMrfRKwNn1t83bjZzRMv4mUy0SpUytwXj5zYOLKdSzxHnpFK1Nq9taWxS
# ycZZW8YuJWPWi8x6zUH3wzMhRmEVBmAVLvoqDMeqllV0ge3cFqG3NOGHQcMQOVtWrli5gkbfaMHw
# mS00iHikWEjRsorCVnHYKgpDVi1IZsMFxZgtFJOuUQrnR/mVMuMbR8IXZ4v6RxGjyCkqipMbbHtP
# T0Xjpv747nj/njpRmhneGevtl5hK+bJdcQLBOjHlWFHqhG8sft4Z7wEG0V4nAuN+6+hu792sADo9
# 69H4vZh/IgyqfE0XMtx0PmExsS7KrfSESJeYPCbKSr7ViZPGfFm3rb03tnkt0MFYbwwLX185zfZu
# FFR24rjpbSOA7mjvi1U07erto/QLx37q29kZ749VrDujBbflKrRcJljRvonWnDoR/ITp0lszfsw1
# PbEuuczWiTkniLuuH7NyB3fSlGOefNLqNuqTqn35Gp5H6Bj/CWKmg1Uyxppd/cDH0LLZY7+txVGp
# v3wFhgJYeC9CVlJdJx8j4vhfuvnL1LFf2vb0xCTslI/9uLm3/QJa9iuWq4c6Mfc4sU7txaqCjkzF
# npWMzZgqEGZc0Yp2GopWak1sN41RfjIeod/re+Pprejuq2ju2h3v7e6So1SY/uU0PHdSFgXpoS3d
# 3Tz2gfTAtd0XxHrV3Cw/q/38WEv3pvPTB31UnDoxM/lJHiB62uW5USL2yRWkLDNaGy7rcQDpK2/r
# 3dW1CdDV2J8OQBypr38PegwHEZwpund1bW7q7uzupQLqRFEyJh9jlgFiFy6oE97M4JbMBYrDkL63
# L8YnpPRaJQ9TY9eU2eNGaunu2joqYsE4R7O0/kkGnorW9ACU9nTv6uejYl/6UGZGSx+f5Bc1E9aq
# U0z6/FVnJyAwO2KyhFEFTTtO1HQAH30IK1+3qb0zRlMhvUvHHNXSlh31rQU1be8dty7Tjxc3fSqq
# z+s29XZ3dspypoz+SCA1qssTWJw76SCPVaeCBixj8DOi9cZG7z9N3Tt62tO7ZFS0FfHOtBGoSsXq
# jW3aswnzjYdrnIYlp0718dO0qjDufYW21onaCSQitLSxK45zPG05iyeQclRNF04gKV0k6lonKpiy
# lZzU3d2dfRW9tQjo6saOQntBRQvTMhWhRYQ4AXo/Bgw8k5BAs7evYlecB1ot4yXHo5xV1okZx/s+
# /4QR5p8oQtWJIlSfKMKCE0WoOVGEhSeKsOgE/TT/BN+rTvC9+gTfF5zge80Jvi88wfcTta/2BN8X
# 0z5wnO/yfGvB5icj01ZOLPr8iUWvmlj06olFXzCx6DW0+H3y6JlLwycneouTP3kCWrBOHHn0YZ8G
# 7T9KN/8/TFf1H6ar/g/TYWAX/AfpaE8/Tqo08gT14NxPGnc+xT75E8eeUOT5E4lcNZHI1ROJfKKZ
# lBG5pnxCta6ZSOSFE4m8aCKRaycSefEEQGkinVE1EeComlDOEwGOqokAR9WCiUSeyGhXTWS0qyYy
# 2lUTGe2qiYz2RLq5eiKjXT2R0a6eUDWqJzC7qxecECUYFX3+xKJPqOYTAb3qiYBe9URAr3oioFc9
# EdCrngjoTWTSLpgI6C2YP4FaLJhA3JoJxF04gbiLJhC3dgJxF58A70iPe/qWznUxOrLTiTX4CVMR
# 3fc4MYmYdoLZdGb7rs7+5R3lFuepTlR+ougpHtInLMDiFhFd4cTRiVxz7FhEG9rc3rk7fn7aiRp0
# wE2d3X1xok6B6UZnm+PEsc7dJeNEWtkFqpbKpHSc76tjOzpUBCJ2l48bhbJnck+MeDNE8hkba118
# a1d7/65epiyN/dy2rbf7gj7Cef8jMQyMyydKt75rG25Ej/0k0Yl8dKx4fFvWS7SROpErhR3i3Uww
# IrpV+vvKrp5d/SCVxqiivoxPIPKlfStIk5lYBjpKjKigk9MCm8B+WBfbuYtYD0QsTvsih3DS6KCW
# 7nY+iRWlfWi2mF2ZWazo7JaksFTQShDWtrZ3NvZu3UWHhLSE6VUl1gpPUP+owN7eXTipbU5L5kmL
# QbS6zJDV7f3bMnM5vXvdrk3bVsRjnem5eNNj7CIAzQyzZJ0KxxN1yoipJFOsAUsLSxK1M6Lv6eNp
# nR4GyAUTMbOHGZplaaVpwWnw3ph8pAk+bpTmTubGSGqsf/w4YB0gCucy6wQx1naDkL2HJue48dra
# e0HlIzLTWAkpEEf6+nt3yfV6yjgReIBotoz9tLJrd/cmLiLJofCNK4Qlh9F3bAGtcUsGcTaxh1bQ
# 44g3WUMzSmxq/OAwWKip4KYkabnuGOFha86NCu8bnQCE5l5Jo/WkhVM1k7OJQ05r79tGYlxjAtfF
# UiDNgSuJgDomR9m20SFhC2YVz4To/Jk9gDLLm7v6iS+XN0qibHRAOCOA6zUqIAy6dyoA1PRNu3p7
# Fd1dPap2lkdie5BAEvWDnzARIWjjxkR37IhvqmjkW3IJnZIWmQUHaKXZtC3WO96nte396FekctDa
# iZUAfU5ib9v7MEtWrVtzuuIsFGWGWmvOnGPJyDXKDYQYYvH+OG2mJx0zak8P5ipPGmv9LjtWXGwJ
# /VbZx4xEW42V0zEruKKzfeu6GHoy1hpr75NLyjGiMiuoRc3rE8UD4yAW333coiU8Ek+GeD8UddYn
# EzUEHBw/Xl96psETRV6H2USSjkAMjxHz9O7++BY1Nhl5H7PvW8s3J9nApceO1Kd2omM2aF2sK0m1
# s/DXYxa6HrIuFkl79niSnfjYpRaqJH49rgioRF5ivZCwiF8Y2yxlPuaMF1PhMqOilo0XdXSk6eML
# n1qDPOPYsqkE/scoREZY3r1plyRwzj52JG4kskKfdBIGdPyIa2OSpX5CgdnjFanisCDAcVsoS/Mf
# J8K27v7u49aaY6xDdxMV///r7FvgrKzKvdd6957bnhnmAjgjom6JU+YnBAM6YnrOGRgQaAaIGeEL
# 60zD7D3MjrnsZu8tzq/LGYtyTNRNopGRopFiomIiDBcRFZVqNCxOUaGRB4tqSFIqKs7X9/8/a717
# v/vlUh0uaz3Pf13eZ613XZ/1rHefNVZzQ0ubqYKE2EXNNiZw5xSO7Yxq/jNESHYiTktrWxtO9ZPj
# U3HafNHEJIWR719zE+C8r07a4vTW3iw1PkM1RrtT87u5o5LDeC40/oH0pzXonGjTW9uWZ+0zzh0z
# Ec2qyaecI2ZzB1Zs4xdGEz2pXtgGZQ+OL/l7iU5rKogyI9qJs1A2cNtWCHAXd1pERMlaev2f08Ox
# 9ujphvSJD9TdiP7W67ViOUdkVlEq7jn2n3Su2CKFZM9hETtXFnzCOVPQ1AMbaz7AWHy8/1zRzXG1
# O/SdM2f3rJorHyP61HNFZ9cwA3u0N8dSInxaqoZWWLJ0ZGv7PafFwBTai/VMT6wtuqCztY+yluJu
# D+wXw+bIW9gJvdGrPezVH4pG43WdmCZVfuOc+vqGmaqIu/+JXJ2Q7F0eZp9UI90pwGolZuOIXAWp
# /FDFHrWKcub1qMJ5PWiL0DEoZ/6HVBFtwcKwn4yqixcsrLuusS6cZCNoiXW391zqNfjEnaAFC2fO
# alJBWBbXq4KF189rntM4UwVwRKcmNM1sgDEv7N2BXnrZ+8OzFs5vzDWHXzx7Jow2YWh57b+pq2x0
# lPFS2iub6DmWod744bp59eEuSTnNpqSFtSTyWXLaZKfZcl47WYWzScUW+zQRVbONQYNUsdm/PAxB
# zlkUWJbWw0h7+kcYs35m04xww5zGOc1hBMya1TQThKqxuRpDa8ktx2o1mwW2h5CrrmlGpph/9/GZ
# mrHPnazm2aQ5FrX/cAVnhaFlOkUJNs2cV6/ymmiprSo86w1jyqMCzXXXqaqzTBPqYn+AO2oZU5qE
# GosIrqV2SxdatLHavL5bGvYYbyiR+liiLZWQi3NhfxhUW719cWn+5uFV/hheqXIC/FKN9kZISJl5
# laTqdFiYXDklAB0sZhJV+8OwkJZxXl2UG5LMFKBZzI39UiQtzGTWpBzdFOMK5yAuUvs4qiXUeZ5w
# Fq7J0JLOzLhmmLBLG3fskNJ5wzMBIzMBnNnNdO0DrWilzVCNJdx1nAo2f2QBxiy6LfW4jqAKhWxs
# uk61X7+ASG7LZodhc0U7PHPrJHWpNOg5TebGBiy7JcX73pclb7CZfux971cF13cv7+5Z0a30IlWw
# aObCpjnz56kSS7TMmF8/UzmLYJO9iGb5i2B9vWgOOVpow5klLgys6c5R+eJJ+FyG0N57Ee2y4QgA
# o2tEbcAXmJxFMOpeRKNuOIw2V3jGpXG4s4hYA4PFgHuRmIOTaRCmQYKZJw3D8+gKIqE2hViEw5NY
# NqSBQjZkYgAtWGStwckvkUwo3RJVuGhJg1y+QhSjc1aBj+BtawQvgTH7Egi8hAVYwmc7TLqEz1oi
# puVLkEuAn5rSNyjnhun4j/q7AcluQJQb5uKuVD5d+GU3+PSglTecrqIceYaLirmgq5S74IZzKIOK
# Mq8eZHeP3En5mHI++mnlfCysdItyWlDK1qUJFcIaFO17/KRJkzL0ZNDFWdrD1HiZKV5mqpe5wstc
# 6WVqvcxVXmaaRwDvY2q8AtR4RavJieaVpsYrTY1XmhqvNDVeaWq80tRM8zBTvBJM8UowxSvBFK8E
# U7wSTPFKMMUrwZRaT6mneugrPPSVHtob/yoP7ZGXmoqEy7R0QCekClrbPpmK9UZVUau7slajDJmY
# nkom4ZlFHWLay0GB1khE5cOpwwWnQvgm23JQGLcjiztgE9+JaUTCFnIeV6NBNfeYRVbWhoZZRjgg
# q4rWduTMowOGsqEiu87OnhUwao1GuxPYhyUQiUhLwgONsacLE2MyK000gk9cNGfmYkgYx8Itoipb
# s1oha72q8oj1oYro1XXGO1qRO2lRqOCaQlcs1aWCUO0mkE+ijbed8qHfam1jjaUi6KiXqbLW3FU7
# vkmsKpdGca0pmluSpVEcTjX3tnYn7KxWuDTWHZne2bPUUFToG2peClUaImX6sipZanRxi1o7U1Fy
# eCNdWNUiQ+RLVbuxtI3eKNUoiMypLlQkEBageEFL++IYSVq6eyDiClwzQXyDyJTfwrtjSrep4jYU
# ExqGaOf8uCr1MM3Io621O2fqUxVAZpn8XKigrVVWlioIBVanCrS1LwNmplIVskRLDN+u5300d68h
# UZKERwiRjHVFxZJVlUGCtuVZzQueSUDkQCU1tyaWWwgcNo9dPXOwNEd+rLlUHI80RF0kofJI96JY
# 9KajopEkYVnuE7ljUEVt7k0cxseBFeqEntl8Ihi6q9Y4zrFUpbWhtrsKamXUhadjsyCU1ZFBLFFp
# R/FQQ0DBCUWuwbnLKLdE5l6JKmwzbQx916Wg/TUMwm3t2baNyPE+tgo1WlQIC6O4+BuN4B2ZdaPK
# RwdCzqgy8adLm2rqiEKGkQbiljSzG8djBJQNHoQWZl50hTyi0rBi/h4xt1RQ3YKJVMVWwcwc8UDD
# NOPNNsY6MUbkQLbJu0m4ZmI4GmdXi+l7LZEYellEFUfCjfgT7sMfFeSKRlzULV1WTSEJ2kIrJ7JU
# VUQiExsbJ/b1heX24NW4bpkfibbxSmHI+FKYAGZGZIT7xMgo2jkHNaryItINyuG1WFk6W5ciXgWR
# nnhrG7azLZ24hdKpSglhYFohq3STRlgbTZUQwSasJcas881ErEZEcteSlo+wOc5Bky3J2YFBMjMe
# 5ckyUo2MyFt1U0/vY5IiA7IzBbBAVGVcJU7i8g+LxGuunYTEslcs828eCyKxRFcM9VMMIo5tOLf4
# qE2rDERtxpFjfDkemzKjKnbn5rztaqwesJ7lzkkV03W3Ek60XRVFu+JJWXyrEA5hUePSVUZggM4Z
# GMEnFseSHaoUQzw06lB1pLrQ7i07z15YL+TRHA8DhOrlQYjKx/luayd2Wzg+VWW+Ta8qiN4UbcN9
# LeQEIpXE4TJwtHjLXu9Z1SOrmyC7JIpjbkwwu2QvZHRvHqGABpAmFmgfP4nOZDo1dKbQmUrnCjpX
# qrz2ObgkDxpjKJwVymlfrEracYeoJe7ONqJiKKLb0s2uVgayM6Pu7E4JwO1wS1ROZCNMggluBN06
# 6SDSjEvbZU5BlxS2gqzNZk63dMSqXCijjZesYomOzA3fUHusN5E04uS1yzSfL14N2RTXD5hL5jG4
# CkQbziywCcs5akACOU1E3uJTJmlUlYZf0Jnqbe20fb+wPXO32KWkzQDv6ZLSFJBiuy5sXxFpkVvB
# AZy/qjI4dUsTaC5JzBpoQCECdsVSQloqqB4DSAE4mXwZxZ51qUrQM2SqW4xlRryVF6wZUeqrzBKZ
# qSokACbWuqQqdekZHG1VIVm50T3CpcwJksmlM2aHtjkRdYEAmFcSyYymmY3f9DkGUtFnHkAKQzhS
# FQkrt8pHGNI9Y7ZpKEUFKTOO1hlNsjrfC9ml3uxobFlH0hTeBNkboRGTd3Zw9vIyMueBx36onF60
# rZPXO+U0Wx7tInYFVHkalJAqrMfNQarZRghtrIU4MhaTdwURxh1/hLFaWCkuTYYS6hJS1Ap2synJ
# PLsgtRTjE9636PX6RAa2IE7F7kKl2GIJNgvKnXtHUuTivOl9Onh5WVJyMKyM+b3STCQ6Z8k6U2V8
# pNxT8yx5+fZsrbMGUT7mk3PeJw3Fe7Ii7TdzgiJSYPRK8mMDKl+YpIlCRb+86yLLIfsyS7Ld0iRC
# mlP24FUy4IzJeA2tiWSm77P1y8qUCahTNXci5cW5FVi+zN/biwSRtx7KkAnJTIYJZmaimkGAbFtb
# Kt5ndCbcfjCdvcQnVboA6nQ8QJJXCG8tplichAvZZk3tspSFg0Bzj+n1UjuZLmY4LhNlDmIZcvct
# Iy3CRuZeGZbGZjYzlULJoooDwgK3JS2MfrIxwRZbYhi+LNtfrTqZsageFHwUGf+1aKkfY4gh5WVW
# qJne62LtSdN47Egp8dCyettkrGEhjJFNE2+f2YbR3GrGCDkqMTdE5U1k2gEuyqIJm4GVLDs2C2q2
# G6QWxyIYTFkji3tjyZwacZa1qyr5nkNLrEvm5WS0pae9XV5cyATIAF1m6O6elp5uCSzNAKLCHJ3D
# uoo5TzLzbQE8EIt6HBfIO9YdqhA7WNP/C0DN41RcDmL+im6ewJhrsSoExN3QjQXtbkLnubughNuY
# g7Ihzu8wX9co6YhFItHulmVS8yPA2eV/I4tUSL6lFS27sGP5DNkTWWrWCj5z+Qy7Q1FFHfTQMDB1
# diznVpkREQsQEbMu6FjeiOcvb4Qame5kJuNWhDmEMqSBYWSApRDTyqoAuWE1ztzKQfXYvVhDrAv5
# FggCiYTAQQ1Tzee4gcfgwrl4KVYbvWU93D8ZnTWEWVDP2Kw7PhYdpo3SFHcsXxi1D2GspjpxRfym
# hYzZJArcT1JurMvM7rpQaApZCooDMjsgBCi2bBM3n5AG6zBGhlffuYy5mYUZvo/Sga9+dJivfoRo
# jGhHmTzSUMnFlINVQTleC5eGfTR4mcE3VRDj2IrtemWsey4W8PWy3p/OtoGlday7SUxGZVwowKoo
# etP8dhXkF41UfswsEYvhI1t0w664KsSG1PSOUKz7RuhwZd+BqDdix68qYglzpTSKDTxOXCHr+adB
# memkAEG0iYHUiTpo7JfJso4TlBoZS7i7Udl8c2pURQDtjFAuZDLzuhN8tFlcyCjIx6HECXcG5wBQ
# z01JcSyRnRMhwEyuy0nIZ1ZUHgi0klLxMoLmx2g5EWG0xlQi1iZEn7RNEmg6pMti6IO4/d4MGWUb
# jWSi0ECmbGQl4rm9LSQc+irqCKXhYOkpDR5gZlmU2e29KGHOOI0S5hHqYs4L+UIX4TXh+C6WMGeJ
# LIahzDED8nLfSkWGzBQSyZpZ/0mKw5EwVxyTd5klvO9QjiyZY2bl2Nhj1B8xfvmmMGZnX6HE8EwV
# LsexJb8eYKhmSKJGkGqBFqqlVc4zi4Tvkc6KnR9WmhXwWoySpHt6aimXQGM9kJUWQxtmDtmYjPSE
# ckUi4HkZ0B6cEJ3TrarOhPO9VWYCoAa0zz3fizXJBh/jHV+PGuUNyjx1TAZNmAnKM0SP8odJilJB
# OWdhWoioS8jiW0GRnhXZpNIW8NWIPkkRlA3yBfYDkKdr4rBLO0fg5HMF1qjz3UCf0m187dmD8Nts
# 3iBq4bA1HGmxrJoEgs06AzgeiqAJWev2Cca6fcK5riOeKfPJZ8p88v8m81KbDwyeWZlu4ajtkOHA
# U00ZjBsatCKU2w3CziPWHcnehMdGecZZg/4BMd1d3lnzv+Ls+V/xT+Q/1maC1Rp0Ah1iZoI+1xiL
# QEGvRmVCWTt1ib7uNlTRv54J/SeeeaZcJ58RrTnjs2r+iWdV5aR3Xz4ynnuWgP9NE3JfEpXNZnFO
# mEdGmeczSAZsdiUcH6kyN4DaogaUv9oFoFwzAxJXQjiQOmvI5GzuuSHTshIhwErNZT1OtHJym5nV
# aOG9XuQJWYjOLl+n5cw9FwagaHDnDr9SFWPVnpxv18pFZIySxZAJHHCoUGe0PWlV/fmdME/EOryk
# k8s6no53cD3AWVD2ryrIN6MK6cpqfgSp6/jllVajWCP/IQ6PJaQwQCWisn0kh6zKOo3Nbnaf2omt
# n1noaCy8oNe9jloWdms2fHdJcqH0c5uozjWukTsM2IqroswwoMr9I4IqIMLpG4RYFasAGhOfdRMV
# PGX0cdQjxj4TocWt9gIfaM02HlXpDaFCrR36ha7WvqV20VbcZdbMMktkGG7R84w+MSSeORGrRJ4p
# VyvXAr0/5uYCOyWqEkuYdUuxZ6ZUpTm2I6ow83E9Nw0WXtggB7toc5TfZVavga6YOHjFXcuh3Ybi
# lRvL5p5ZVLxBLmFka1PRlVjWYlTBtIuh/i+fEM9R6LM2ZTsV7OJm47wu2leY3Q7toe2HePBK+A44
# aEHEPrunB8ilGcGgUfbRhTDd0RUZVUMRGGN0rkpAwkgLqwrZdJHjfYJM1BFAFnqsSZiP+8EkPEFe
# Q7fH6qocmzy7HWuRJq5CmVMqrL7c75QWW2JCe09CjermhqaP29EmUc/BgDfionbN6KIaG0d3Z5oP
# IsFVnn8yQBTzdSNVCMKckVS6lCcaErrDmY1V3JM1siST+fwRc7q+W/pmEag4Nq/YkgRR5+iSdKWz
# lruUq8KCJERQMfk9caPegZ+xoVellrM6F8Q2mpoiEFY/8H6Y1010zesmZgycJ3rtsGdE2U/VtJyo
# YoQ6MWPZGqPayXbwmbKzajTXYkQ98S9/N6moFKacK1qXN/Pp7G6fTPWgUif9A4lybZYuPGsKkeJM
# 5Ux2TvTkdtacJ+QkTcUmZjSoEz3HZ+4nAVWtP7q1L5rIXY6lzWejmDubjm1a7zlbwoztEkdJnsYX
# iUmn6BELQRo1jY6rIvmIkyi2CoVk6zGgOWbmga+s04NxqsmDcaoUC+nW8KQoxDNzNCAO2Xlx2YI5
# OO4Jxrk7LKaLEFhnoqeR8TYnaRF5cX4ADZ4MwgXu4UZp3M442HzyIMxlGzg8l2e5BDYT6BuZcNpe
# qjKXg4a0hwNCeRbokvtP2QSylRrl5TKTUSYbnDhh5xfJCtUiZzojsmy0Db0sy/OoZ7mbPqshPQ9f
# SqYlhW+toyotTrWMi422mKt+Ml/74zMEdrUcEIl6SVf3XeZhRWmHeuUryYunenn2Lh7MMLKHuwH0
# ObzEVOZgIx+0aQMpd2wocPvlqHhfXSSzGHCrpVLQXJVoyGLUf5bF+2bmnMaV4ryZh0Hut27jfdeZ
# gxW0K5J8/CihFvheZC7qEeA6v07WxSCAvWiUoFCzXQ0gHjrH6jCKSVoFLSq4b2HU7L5E+CryDPI/
# dLQbkPvc0izM5HhMU7ZsTaZsFaByF4+soybv0o/5NOMj251RIyPyWRyL89VRQpKZJVkhjjc+nIpi
# 1AnKdF5EF4+ZUqNKe3NkGyHqUhxx2tPTkl5vSQt6bSWXeDcsauQZti8qr9dNAg/HwKU55rfE0ZZo
# BuHZW/Dx3v2AClBTiJy6cMaZ3yuVRuHpSzkzpLHBLDP8DChNluIVJNR7DXDazqGnK+fyEEtw+nsq
# 7vW8JTCw5THmSyokzIdlTjF0c48oIi3d3BMX5WmvNXhwbx8wAk5jkq76qbiXo61dkAd56xolTnUj
# Wqp7fvf1c8x9VtTVTXKOqDSK6NvlqNKcvY0qJDsf+aoSUpkmUEBuEZcaJFq8S12JaJeBGK2FMwvO
# soQdWtwDv5EJn70VLQNGuKBdOxR7bXSFcXclhdYQN2JgaHSpEckwqWhCHpFhMuPsaIIevZs9VM+B
# 5XmSeb7Z1amAmHtkDGNhLSaXu4mjF8KpE6u1GLVPFwh75ksuqJPswW+F0DA9yxppjSJEQ7J5UaoQ
# zVcTs+h8vHLMb82xJLVrLsojGCyxkjA5MMpc2VMlXO32aFI0HzOLgQUwaxPjZMDUVZmGmDm2LLWw
# 1X5V5rDmkCYXk9NBKZc7qJ+X4Gl1d70x1pAz4xWxuMk6e5+ETYInznE5noYeGO3JAM097gFbFnGl
# ZrGMFCOE6kWtLcTmMGayy/CphIngOROWx4uRjOg7GcyVCAyczImT5Zukn7ElZU95S2jcDZOgeIKa
# +JCYehsjBz4VvUVGjXgP9PEMvM6eAlFYUz0jXMpaIbHy5+BLDikY9+Dlu6XLT5gxm7l6vwApFZHz
# YUeJ4v0kpzyC36psojkP59GEPSFlSRpbbzKLsFDCcz56mjWEIDhe6Ep12eUbm968aDSS+DDMV1Cm
# 5g7czJeWOw/fEIAuP2oHMjYK/1mpYLnfZJXGcYZPsIr4GGzwskz7lbS5H0sV8dgF8H2GqGkELI5b
# eRRqAT6N4RFKgu0IOUJo1GdvH5sURwfM2PZoUw6m7etciCUk85BTl6JE5nyUGYgi37avkgzPqiOX
# +c6qvHkcn9pm1QTxcFWXLZpvhbei5K00pZYmpSvzvfM4lKGyaS2xhCnlSMvNsQs06ajFFjT2GGQk
# q2pSWaMlfGiBA1N7qtMksN9DFrHYK2xf5IOlV5QKgYGM95lhHZXAeRPVRKgZqqY81ngjcnVVyD67
# yjCxc5YcPi2VARZ6ZsJiq42iDgr940yqKdQTJiaaOhhfbmNAZKnepFEjQFxWSB5dLB0SnTinAMau
# mQebMWzeixJUu3CFAYRDJ3KglzmgH2HYzFBqgutxCUH2yiY4e7WrXHi5MGKmL7REIu6ESS0GWiIh
# Yz2WnzA3MMoSbk+xnQ0P6olnM640lq/moDluTiQLEqmlUswiEOZ2sQqKZWU+f0MByj/jT1Yh+q4Z
# RZLdFjN80p54mMGgiKyHlIpReaZNFid7pvclzRd3yTRw6zRDjA2SPa41ZNK7aoTwdgEyUvYkLagx
# sxiQlVFB0i5mg0jcpRysCpxUTOWn5Ost9KV6y1LdOJfHiko+RU3TOdxSmR3pVWF7t0UGZalJ/9bm
# 0r8XI7MKKDExZ0fF9qnccJkvzePVGCT7LioMYBfupo96b7+qgpTdJJ1Hk76ZXT2fiJkQs3nCaog4
# d6E4wi0gTd1YPg5px7djYXqjKDMLxMNhmiEQM0CTmwIOfzTqxu/2hla0JlxLhaBMq3krYPuAFd1N
# SsNonFanLY2NLbCCdz71GRX4dORaOF10EtfiVzk/+9n6qz41jmta9J1xV4/DmfK4y8cZo15p7hNo
# FSEBS1PLEAS14wQxX06kuhLjrm6HFWP08nFQCk5ojcfGXT358nGJjtYJk5Fi0lVTW6+aNrnmytbo
# pCumXdl2xbRpk6dOibRfUdt65RVTr5x6ZWt7zVVTJk9BpvYn05Bo2sSaiVMnRKI3jvuM0m/236oc
# OPTBPAjnnq1w1t2qiuGEw1yOxvgCQ2Qz3Kg3190ZDp/hJz6CEnCjCtC/EXmvuwv/lHNs/a5juzdY
# /5vWf8z6T9N/5thTK61/h/W3G3/rLlVKf9vqcDj7Wyr6Ba0Hii94VT8n/vf19MD9BbWX6W2FtX8K
# fK2o6hIPUBoEMNYDlBA4zwOc8icJMcYID5BPYIwHyCPwLx7gd8yj2gP8gcAI5+sUsHqdc792XsFP
# Gb9YeJFzH6Gr39KeyEXMbrQHKPADhQTGeYC3mf97PUCQMd7jAf4oxXIe5OPGBms7dO1sXTtLV33Q
# ClP0YmHVXIlfdQzR/4fRy7L838hfkOVPkr8wy/+F/EVZ/jj587O8ojwj7bMK8aw2WxktWax2q7by
# FVSV10Z07fsgXi1+4Xe46lobEDCsK3QIGV3vrJcgp/o/qj9mHvguHvh7ClDlPMB4Y3V9eb96qXCs
# tvU95i/ZiE729Qr/LhOOyvLflXBPLS321OqwCTQvcUBb4laXuMslvuwSo1wi7RL3uMRXXOJrLrHK
# Jda6xN0u8SWXeMAl1rjEape43zaqUZT0GIt1sQf4LYGwBxgWwNaYwxobX16OSnMuclvpfW7O613i
# dpe41yXusA8dzyyLWT3jbchX3Sh3usTXXWKdS9ym3Tdmno0X5mmx/65rP5VtsQ5azGPaNqN7nG9I
# PF39hE1RiDa0hGmqrrFpZzOt244+cTr/71leHjfdPi7b9Gqn+Pie3A4UsW1RV9+dbaG1m7SnuX7Y
# Cvw1T7JFHnqJR/yPGpmy4rvdIFhV6fYPi+Qh+hmiuVWQRUqJTPeU1CCzvUgFM7/UVzvTfUWfnVP0
# 2pBjyx6oXlv91ep7M5mVnCaCi/zraW/qg9lqvzbzQnf73gmijnV8WI5wBrrAFSi/+ivVX6teVH1P
# dbS6hT81er++uVC5/5XWumJ1+EF98/rglqf0N3TF/qc0wcrV4Q0Eh5/S39QVA1s0fqNUj1wd1vLv
# IQZt2KIf1hX7tkj881aHN0r8LfoRxH+aP5qqvyXRntaP6opdBtlE5PjT+jFdcQBIUOsqN0/8e5yh
# A1v1E7pi81atHK0vEHwz8YNb9ZO64uRWyefbRNZu008h5jZBtuj+/uC+bfpprQ8bZCuRge16m9br
# twsySGR4u96u9codkv+Fkv8O5rZnh96pKw4b/GLBdxHv36mf0RXrdwp+ieC7Jf5O/SziAy/Q+j2e
# UmT+7ZHkuzAxV2zZJRV4mRv0PIOO7sLcXbHqGanACavDewlufka/qCuGnuHjCifiB2FfotBHn9Uv
# a92/GxHX74FzApQ6QPboM3D2PQvnFAN27ZFaneQR4/vMdtdzer+uOPKcPGvq6vBrBFc9r3+gKzY+
# L7Jd4cb/IYP2Pq8P6IqjJugqN+i/pJ28oH8EsV+QKv0xkVMv6IO64uAL8ugPeh79E4au2at/igaw
# V35H92cszRHQeRq/cqzt30NET+3VTl6wuH6MHjOLP4SLnxPWla8zZM13tHb2fUfnDYSPv6yDA+H+
# V/TPtRpAleiNdPbvYyW8rJ3+8IYh1FP46Et09wwRWfcKMlr1KmKseRG1tv6Q1MGHVoePUbrDh/Tv
# dMUpAzakw2/zgWtf18fRpF4XsHF1+PfSAF/X7+iKE69LQ5gn5XuXePoNfQLN8A2JvGB1+A8E97+h
# /6grhgEW5eUvdHTO34DzJ31reOBXWt/cH1wPD+X6pS50Nv1aO87+3+i/APm5DvSHB3/FYhwRd+9R
# ZH/oLfzG8uBvpCpXOreGh4flLXzBwSMHj+kvOhX7j4kc/5EO3+Kw4RzTA45e+TsBW9LhWwnu/53+
# kqOHDfjxdPg2ghvf1qscvfdtAVvT4dsJ9h/Xdzh63XEBl6bDdxI8eVynHb3m9wK2pcOrCQ7+Xn/Z
# 0QcMGEmH7yKYfkevcfSmdwSMpsN3Ezz1rr7H0WtPCNieDn+F4Po/6rWO3vVHAZelw18luPnP+l5H
# D/1ZwI50+GsE9/1Vr3P0kb8SdGLp8NcJDn/Ruc/RA7fgzQwOOIz+iXR4PUNW3uE84Oj1dwi4PB1+
# UKLf4XwD0e8UsDMd3kBwXdr5pqMH0wJ2pcMPERxa7Tzs6KOrBexOhzdKBXzZeQQVcJeAPenwtwhu
# WOM86ug9awSMp8ObpKbvdh5DTd8t4CfT4celrPc4T6Cs9wjYmw5vJnj8HudJNNOvCJhIh79N8NBX
# nKccfVJAJ5kObyG4Za3ztKP3o7r0lnUSPZUOb5OQB5xBhDwg4I3p8HYp1oPODhTrQQFXpMM7CR7c
# 4Oxy9IkNAt6UDj8j1f1NZzeq+5sOG3ifNPBn2bBWPeTscSo2PST4pwR/jvjQQ87zTsWwwT8j+AsS
# /2FnL+I/DDyo/zMd1unwi1KXDzvOQHhoI2o+vO4RuvsfcV5C9TwsYtyMeN9lvLWPOt9z9JZHBf0c
# 0CF5Z5ucV5DmMUE/D/RVaWGPO99HC3vcYY/Yjx6xf7OQr4E8tVnirkTcH0j5nnR+iPI9KREOIMLh
# rSL6F+zgRtkPbXN+5FSc2iYpb0HKH0sdDjoHUYeDgg4A/QnRIzudnzq6f5egtwL9mTS4IecQGtyQ
# oF8C+rq8ySHnDbxJg94G9Oci0SvOYUj0iqCrgP5CeuKrzpvoia9COCdwO8bBdPi/nc8h9mt4GkR8
# zXkL7w6MHv6Ro9XGg05pcHrgTj3G/g3hr3Hx15nrjLlXVzqVDgaeo85Af/Dwrx2OO+nf8g2ceIvu
# gV/SXSXuBnF3iTt0yJGxie6WX9Dd/LrEl7R7D9I9JO7Kn0kOv6I7/FNBJP6wxFwHOrRyGDPaATjO
# EFhn7xFg65EstBGpQhuOwRlC7s7+X1FSPNnZdBTOrt/BOfg2nH3H4Wz4PZyBd+CseRfOcTpr/wBn
# 1Z/gnKRz6iSc4T/TOQXnyN+YaX8Aj/wcnI2fh3NoJZyTt8BZeSucTV+Cc+Q2OPtXwTlwO5zjd8BZ
# n4aza3XACZ08SEl/QqmkeJvuCujQ3vvgbLw/EHAOPkj2GwF4x78cKNBH1mPePPBAQOU5ar3OTHSP
# BfAe048E+B73PYKfhdfOEwG88sOPBL4N8FtItP9bAf4ePFZd/LslgFd11EAbDfQ0oVWP4lfr80c/
# qs1MWbmVuewdDARD/YMUeXugyNmzA3If2BooDA09GdgW0JsfRfabN8FZ9e1AQO95ClQ/nfV01j0N
# Z+9jFPtxOKfobHoigCFtF5wju8Fu2cOArXCOPAHn4GY4h5+Dc/R5RBneC+rQS4FqdaN6QhogG+P5
# +BvK/B2j51p8TMaXv68F0DJXrgyyZW7+QpAt8K0A2+fLdPcJslfo43+je1iQk+Ie7qe78Q/E018k
# fcogQu8V/PBRusOS52FBVu3DG9uDKKGNpDb/AM7JW8CeGoCz922wpxAxdOTWoC4cuA3YwT+DHfw1
# nLU3g933OTgbfhvQRQeQbdFJPKHoEEQM7ToW0IXD74Dd+As4B+n0v8lnHr2NEvWvojt8O921d9A9
# Ku7QnXT3rZGS3U330LtMtfKrpNeJu/ZeukfEPbSO7gm4RUObKPjjkPQ4khTt+TawwaeArf88nCNb
# 4AxtgzPwS4iGmkZRd7CUOxFveDecjW9SUjobngV7is76/wab/jWxU3CO/j84A6j9ol109jH0JJ01
# R+Cs34MUe8Uhe4LOcbJDz8E5TmfgeWZKZ5DOITprX4CzBU5o5V44w3QGXoTT/xKco3CKdn2Xab+H
# sh0eArbpVb6P/Szva0z2Qzj7fwznBJ21B+Ec+gkz+BmcXXCK1h1CioOH+LTXgR2ns+GvAR3U+eMH
# Rl5U+/5g7UO66spgreIfzA7qGuwypm5lh1Wq3vrXW7/d+iusP2D9r1r/W9bfaf0h7mTgvw7/LUsf
# tzTD/2z90DbjX2D9y61/rfXnWb9lWza/Xot91vp3WP8+6z9u/d3Wf9Wm/Q38Nzz0Ozb8b9avHsS6
# 3z7jfaCnDBr836w/f9Ck/fhgthwdNuwm6w8MZuVca7ENFnt6MFsXuz30yx56v4f+iYd+00P/1kOf
# 8ND/46Hzt2fpMg9d7aEv8dCXeegpHvoaDz1ze7bc87bb92L9uPVv3m7i3uNJd7+H3uihn/TQOzx5
# v2DzOOAJf91D/9JDv+2hT3ry0DuMX7HDhE2GP95i06w/w/oft37c+v3Wv8v639iRbRdPgX52R/Yd
# f8fSb8L/kYf+jYf+o83H2Wn8i60/2fqzrP9/rf8J63/a+ndY/76dWTkeB73T4t+x/s+sf8z6oV3G
# H2X9S63/Qes3wF/5Ha28f9I+fq2PX+/jN/r4zT5+0Mfv8fH7fPx+H3/Qxx/x8cd9/Ekfr76byxf6
# +HIfX+3jL/2uqSv3z0T8DwOb6ot3jY+v9/ENPr7Zx3/Ux0d8fNzHr7Jyuehc/F8J7A22G/jrfPE3
# Wd5F3+a4BGzQF2+Pj9/n4/f7+EM+/ojvOQXaPOe4Dy+y+CkfXnwWvNTihd/LxcssPsqHV5wl/siz
# 4KMtHvbhVRa/3Iefb/GrfPgFFq/34RdafIEPD1v8oz583Fnw8WfB33sW/NKz4JdZvMOHX34WfOJZ
# 8EkWT/rwGov3+/CpFl/lw2stvtaHT7P4Bh/+wbPg11p88/d845CP3+Pj9/vymQliH7BDPnyuzf+o
# D2+w+AkfPs/iaigXX2jxkiHfeOWLt9ht3z78I2579eE3uO3Vh3/Mba8+vMVtrz681eIfH8odZ27n
# PAWsDcAC+FA8qkIq2G34nRx/gKWtv9r6X7b+3Ryn4N8Lfw38r3Msg38f/HXwP4t8k/D/0/q3wO+E
# v8VXT3t8/JCPP+jjL3/FzJ0F7niI/2OBPcY+Bf9xy2NKVKPgQxOtgvB3wy+B/yz8YeT5Ivwj8F+G
# fwr+9+GfgJ9EHOYdsvk/Y/Pbbf3nOKbDf976L8DvhL+XYz/8l2w8B4uGZvgl8K+BXwq/Hv5Yx8hx
# oWPkusj6F1v8Azb+ZMfIVQO/AXyFlUd7/MfyMJ/YNf9YAOOrzPubDnrNBcjXRtx0mVKIqvoQuKkG
# a0rQS/A/OQ/BYcUdvtryEaQNm/4+6QNoK+VKbWWZ6lCWsHlWaDZU5GHzTO4vVNjk83FUdl7YyNGF
# eTuAtDGuv0DnA7+ebQZlUOVG7o/+l1b/H4Ushmg=
# __DEX_END__
